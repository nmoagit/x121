import { PlaceholderPage } from "@/app/pages/PlaceholderPage";

export function AnnotationsPage() {
  return (
    <PlaceholderPage
      title="Annotations"
      description="Select a scene segment to view and create frame annotations."
    />
  );
}
