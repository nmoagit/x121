import { PlaceholderPage } from "@/app/pages/PlaceholderPage";

export function PromptsPage() {
  return (
    <PlaceholderPage
      title="Prompt Editor"
      description="Select a scene type to edit its prompt templates and view version history."
    />
  );
}
