import { PlaceholderPage } from "@/app/pages/PlaceholderPage";

export function StoryboardPage() {
  return (
    <PlaceholderPage
      title="Storyboard"
      description="Select a scene to view its storyboard timeline and keyframes."
    />
  );
}
