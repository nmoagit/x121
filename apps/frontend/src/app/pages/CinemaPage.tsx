import { PlaceholderPage } from "@/app/pages/PlaceholderPage";

export function CinemaPage() {
  return (
    <PlaceholderPage
      title="Cinema Mode"
      description="Select a segment to enter full-screen cinema review mode."
    />
  );
}
