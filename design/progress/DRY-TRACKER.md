# DRY Tracker — Components & Functions Under Watch

Ongoing log of components, functions, patterns, and utilities that the DRY-GUY agent has flagged or that developers have identified as candidates for deduplication, extraction, or sharing.

**Purpose:** Prevent duplication before it happens. When implementing a new PRD, check this list first — if something similar already exists, reuse it instead of building from scratch.

**Process:**
1. After every significant implementation, run the `dry-guy` agent
2. Record any flagged patterns, near-duplicates, or extraction candidates here
3. When a shared utility is created from a DRY finding, move the entry to the "Resolved" section
4. Reference this log in PRs that touch flagged areas

---

## Status Legend

| Status | Meaning |
|--------|---------|
| `watch` | Identified as potential duplication risk — monitor as more PRDs are implemented |
| `flagged` | DRY-GUY has flagged active duplication — extraction needed |
| `in-progress` | Shared utility/component is being extracted |
| `resolved` | Shared utility created and all consumers refactored to use it |

---

## Active Watch List

### Database & Backend Patterns

| ID | Pattern / Component | PRDs Involved | Status | Notes |
|----|---------------------|---------------|--------|-------|
| DRY-001 | `DbId` type alias (`i64`) usage consistency | All PRDs | `watch` | Defined in PRD-000. Every module must use `DbId`, not raw `i64` for IDs |
| DRY-002 | Status lookup table query patterns | All PRDs with status FKs | `watch` | Expect repeated `JOIN {domain}_statuses` patterns — candidate for shared query helpers |
| DRY-003 | CRUD handler boilerplate (list/get/create/update/delete) | PRD-01, PRD-02, and all entity PRDs | `watch` | Axum handlers will repeat the same patterns — candidate for macro or generic handler |
| DRY-004 | Pagination query pattern | PRD-20, PRD-42, PRD-73, all list endpoints | `watch` | `LIMIT/OFFSET` or cursor-based pagination will appear in many handlers |
| DRY-005 | `updated_at` trigger creation in migrations | All PRDs with tables | `watch` | Trigger function defined once in PRD-000; each table just adds the trigger. Watch for copy-paste divergence |
| DRY-006 | Error response formatting | PRD-02 and all API PRDs | `watch` | Constraint violation → user-friendly error translation will be needed everywhere |
| DRY-007 | FK index creation pattern in migrations | All PRDs with tables | `watch` | Every FK needs `CREATE INDEX idx_{table}_{col}` — easy to forget or name inconsistently |
| DRY-008 | Soft delete `WHERE deleted_at IS NULL` filter | PRD-109, all entity repos | `watch` | Every repo query (find_by_id, list_*) must include this filter. Missing it = showing deleted records. |
| DRY-009 | `soft_delete` / `restore` / `hard_delete` method pattern | PRD-109, all entity repos | `watch` | All 9+ repos need identical method signatures. Candidate for trait or macro if boilerplate grows. |
| DRY-010 | Version-as-final transactional pattern | PRD-109, PRD-069 | `watch` | Unmark old + mark new in one transaction. Could be reused if other entities get versioning. |
| DRY-011 | Trash entity-type parallel lists (5 locations) | PRD-109 | `watch` | 9 entity type strings appear in 5 parallel match/list sites: `KNOWN_ENTITY_TYPES`, `PURGE_ORDER`, `table_and_name_expr`, `check_parent_trashed` (all in `trash_repo.rs`), and `dispatch_restore` (in `handlers/trash.rs`). Adding a new entity type requires updating all 5. Consider a single `EntityMeta` data array if the codebase grows beyond 12 entity types. |
| DRY-012 | `resolve_role_name` logic duplicated across handlers | PRD-03 | `resolved` | Extracted to `RoleRepo::resolve_name()` in `db/src/repositories/role_repo.rs`. Both `auth.rs` and `admin.rs` now call it. |
| DRY-013 | Role name magic strings (`"admin"`, `"creator"`, `"reviewer"`) | PRD-03 | `resolved` | Constants defined in `core/src/roles.rs` (`ROLE_ADMIN`, `ROLE_CREATOR`, `ROLE_REVIEWER`). `rbac.rs` updated to use them. |
| DRY-014 | Validate-then-hash password 2-step pattern | PRD-03 | `watch` | `validate_password_strength(...)` + `hash_password(...)` with identical `map_err` wrapping appears twice in `admin.rs:64-69` and `admin.rs:177-182`. Extract to `validate_and_hash_password(password, min_len) -> AppResult<String>` if a third occurrence appears (e.g., self-service password change endpoint). |
| DRY-015 | RBAC extractor boilerplate (`RequireAdmin`, `RequireCreator`, `RequireAuth`) | PRD-03 | `watch` | Three `FromRequestParts` impls that differ only in role check logic. If a 4th extractor (e.g., `RequireReviewer`) is added, extract a `require_role!` macro. |
| DRY-016 | `find_by_prompt_id` + event emit pattern in processor.rs | PRD-05 | `watch` | `ComfyUIExecutionRepo::find_by_prompt_id` + `event_tx.send(...)` appears 2x in `processor.rs` (handle_executing completion + handle_execution_error). Different event variants carry different data. Extract to shared helper if a 3rd occurrence appears. |
| DRY-017 | Middleware/router construction duplicated in main.rs and tests/common/mod.rs | PRD-02, PRD-05, PRD-10, PRD-09 | `resolved` | Fully resolved: `build_app_router()` extracted to `x121_api::router` module. Both `main.rs` and `tests/common/mod.rs` now call the same shared function. Zero middleware duplication. |
| DRY-018 | Broadcast receiver loop pattern (recv + Lagged + Closed) | PRD-10 | `watch` | `persistence.rs` and `notifications/router.rs` share structurally identical `loop { match receiver.recv().await { Ok => handle, Lagged => warn, Closed => break } }`. Two instances with different handler logic. Extract a `run_subscriber(receiver, handler_fn)` utility if a 3rd broadcast subscriber is added. |
| DRY-019 | Channel name magic strings (`"in_app"`, `"digest"`, `"webhook"`, `"email"`) | PRD-10 | `resolved` | Constants defined in `core/src/channels.rs` (`CHANNEL_IN_APP`, `CHANNEL_DIGEST`, `CHANNEL_WEBHOOK`, `CHANNEL_EMAIL`). Router, digest scheduler, and notification handler updated. |

| DRY-074 | Rule type name strings in validation evaluator (`"required"`, `"type_check"`, etc.) | PRD-14 | `watch` | 8 rule type strings matched against in `core/src/validation/evaluator.rs`. They match the seeded `validation_rule_types` names. Currently only one consumer. Extract to `core/src/validation_rule_types.rs` constants if a second consumer (e.g., frontend, seed verification) appears. |
| DRY-075 | Import status magic strings + status_id magic number | PRD-14 | `resolved` | Hardcoded `"preview"`, `"committed"` strings and `status_id != 1` in handler. Extracted to `core/src/import_status.rs` constants (following DRY-013/DRY-019/DRY-072 pattern). |
| DRY-076 | Source type `"api"` magic string in `persist_preview_report` | PRD-14 | `watch` | Single-use string literal in `handlers/validation.rs`. Extract to a constant if a second import source type (e.g., `"csv_upload"`, `"bulk_file"`) is added. |
| DRY-077 | Tracing init boilerplate (dotenvy + registry + EnvFilter + fmt) | All binaries (api, worker, agent) | `flagged` | 3 binaries now share identical 6-line tracing init differing only in default filter string. Extract to `x121_core::telemetry::init_tracing(default_filter: &str)`. Threshold of 3 met with PRD-06. |
| DRY-078 | Restart status magic `status_id` numbers (e.g., `DEFAULT 1` = initiated) | PRD-06 | `watch` | `restart_logs.status_id DEFAULT 1` relies on seed ordering. Same fragile pattern as DRY-075 (import status). `core/src/restart_status.rs` constants exist but are unused. Extract numeric IDs or resolve by name when status transitions are implemented. |
| DRY-079 | Metric name strings crossing SQL/Rust boundary | PRD-06 | `watch` | Metric names (`temperature_celsius`, `vram_used_percent`, `utilization_percent`) appear in migration seed SQL and `core/src/metric_names.rs`. Same cross-boundary pattern as DRY-070 (QA check types). Acceptable -- SQL migrations can't reference Rust constants. |
| DRY-080 | `DataResponse<T>` typed response envelope | PRD-06+ | `resolved` | Extracted to `api/src/response.rs`. Hardware handlers use it; other handlers still use `serde_json::json!({ "data": ... })`. Existing handlers should migrate to `DataResponse<T>` when touched. |
| DRY-081 | Agent WS reconnect (fixed delay) vs ComfyUI reconnect (exponential backoff) | PRD-05, PRD-06 | `watch` | Agent `sender.rs` uses fixed 5s reconnect delay. ComfyUI `reconnect.rs` uses configurable exponential backoff. Intentionally different -- agent reconnect is simpler. If a 3rd WebSocket client needs reconnect, consider extracting a shared `ReconnectStrategy` enum. |
| DRY-082 | `getCssVar()` runtime CSS custom property resolver | PRD-06 | `watch` | `MetricsChart.tsx:64-67` defines a local `getCssVar(name, fallback)` helper that reads CSS custom properties via `getComputedStyle`. Only 1 consumer now. Extract to `src/lib/css-vars.ts` when a 2nd charting or canvas component needs it. |
| DRY-083 | `DataResponse<T>` adoption in existing handlers | PRD-06+ | `flagged` | `notification.rs` (4 instances) and `validation.rs` (7 instances) still use ad-hoc `serde_json::json!({ "data": ... })` instead of the shared `DataResponse<T>` from `api/src/response.rs`. `reclamation.rs` (11 instances) migrated in PRD-15 audit. Migrate remaining when touched. |
| DRY-084 | Script type name magic strings (`"shell"`, `"python"`, `"binary"`) | PRD-09 | `resolved` | Constants defined in `core/src/script_types.rs` (`SCRIPT_TYPE_SHELL`, `SCRIPT_TYPE_PYTHON`, `SCRIPT_TYPE_BINARY`). `orchestrator.rs` updated to use them. Same pattern as DRY-013, DRY-019, DRY-072. |
| DRY-085 | QA handler `run_python_script` vs scripting subprocess module | PRD-09, PRD-22 | `watch` | `image_qa.rs:232-262` has its own `run_python_script()` (CLI args, no stdin, no timeout, no output cap) that is behaviorally duplicated by the new `core/scripting/subprocess.rs` (stdin-based, timeout, output cap, kill-on-drop). Different interfaces (args vs stdin) prevent direct replacement now. When QA scripts are refactored to accept stdin-based input, migrate to the scripting module's `run_command`. |
| DRY-086 | `VENV_DIR` env var name string | PRD-09 | `watch` | `"VENV_DIR"` literal appears 2x: `orchestrator.rs:101` (sets it) and `python.rs:108` (reads it). Different crates, tightly coupled contract. Extract to `core/src/script_types.rs` constant if a 3rd consumer appears. |
| DRY-090 | `ADMIN_ROLE` constant redefined in `handlers/jobs.rs` | PRD-07 | `resolved` | Hardcoded `const ADMIN_ROLE: &str = "admin"` instead of using `ROLE_ADMIN` from `core/src/roles.rs` (DRY-013). Replaced with import of existing constant. |
| DRY-092 | `list_by_user` / `list_all` structural duplicate in `job_repo.rs` | PRD-07 | `resolved` | Two methods shared identical pagination clamping, status filter logic, and query assembly differing only by `WHERE submitted_by = $1`. Extracted shared `list_jobs(pool, user_id: Option, params)` private method. |
| DRY-093 | `MAX_LIMIT`/`DEFAULT_LIMIT` pagination clamping pattern duplicated | PRD-07, PRD-10, PRD-12, PRD-17, PRD-39, PRD-42, PRD-44, PRD-47 | `flagged` | **9 consumers**: `tag_repo.rs` (100/500), `job_repo.rs` (50/100), `notification.rs` (50/100), `script_execution_repo` (~), `asset_repo.rs` (50/100), `dashboard.rs` handler (`FEED_MAX_LIMIT`/`FEED_DEFAULT_LIMIT`), `webhooks.rs` (50/200, now using `clamp_limit`/`clamp_offset` from core), `bug_reports.rs` (50/200, inline clamping), `delivery.rs` (50/200, fixed -- now uses `clamp_limit`/`clamp_offset`). Also 2x bare `unwrap_or(50)` in `reclamation.rs` handler without MAX_LIMIT cap. Shared `clamp_limit`/`clamp_offset` exists in `core/src/search.rs` -- remaining consumers should migrate. |
| DRY-098 | `validate_entity_type` function duplicated in `tags.rs` and `trash.rs` | PRD-47, PRD-109 | `watch` | Same function name and pattern but different entity registries (singular taggable types vs plural soft-deletable types). If a 3rd handler needs entity type validation, extract shared `validate_entity_type(value, valid_list, kind_label) -> AppResult<()>`. |
| DRY-326 | `can_undo_operation` structural duplicate between `batch_metadata.rs` and `maintenance.rs` core | PRD-88, PRD-18 | `watch` | `batch_metadata::can_undo_operation(&BatchOperationStatus)` and `maintenance::can_undo_operation(&BulkOperationStatus)` are structurally identical (check if status == Completed). Different enum types (BatchOperationStatus vs BulkOperationStatus). 2 consumers. Extract `CanUndo` trait or shared function at 3rd consumer. |
| DRY-327 | `STATUS_PREVIEW`/`STATUS_COMPLETED`/`STATUS_FAILED`/`STATUS_UNDONE` constants duplicated across 2 core modules | PRD-88, PRD-18 | `watch` | `batch_metadata.rs` defines `STATUS_PREVIEW`/`APPLYING`/`COMPLETED`/`FAILED`/`UNDONE` and `maintenance.rs` defines identical status name constants. Different domain contexts, same names. Not actionable yet (different enums). Watch for a 3rd module defining same lifecycle. |
| DRY-328 | `MissingItemType` (readiness.rs) vs `MissingItemCategory` (character_dashboard.rs) conceptual overlap | PRD-107, PRD-108 | `watch` | `readiness::MissingItemType` (6 variants) and `character_dashboard::MissingItemCategory` (5 variants) overlap on images, metadata, embeddings, quality_scores. Different granularity (readiness = per-asset, dashboard = aggregate). 2 consumers. Acceptable conceptual overlap -- different semantics. |
| DRY-329 | Local pagination structs in 6 Phase 8 handler files instead of shared `PaginationParams` | PRD-67, PRD-86, PRD-88, PRD-107, PRD-108, PRD-18 | `watch` | `ReadinessStateFilterParams`, `ListOperationsParams`, `ListRunsParams`, `ListEntityLogParams`, `BulkOperationListParams`, `CriteriaSearchParams` all contain `limit: Option<i64>, offset: Option<i64>` mixed with domain-specific filters. Extends DRY-276 (now 20+ handler files). Shared `PaginationParams` from `api/src/query.rs` covers pure-pagination cases but these mix domain fields. |
| DRY-094 | Magic status_id numbers `1, 2` in `dispatcher.rs` | PRD-07 | `resolved` | Hardcoded `status_id IN (1, 2)` instead of `JobStatus::Pending.id()` / `JobStatus::Running.id()`. Replaced with parameterized binds using the enum. |
| DRY-095 | Job WS message type magic strings in `progress.rs` | PRD-07 | `resolved` | `"job_progress"`, `"job_completed"`, `"job_failed"`, `"job_cancelled"` were inline strings. Extracted to `core/src/job_events.rs` constants (following DRY-079 / metric_names pattern). |
| DRY-096 | `serde_json::json!` + `Message::Text` + `broadcast` repeated 4x in `progress.rs` | PRD-07 | `resolved` | Extracted `broadcast_json(ws_manager, payload)` private helper to deduplicate the serialization + WS wrapping + broadcast pipeline. |
| DRY-097 | Ownership guard pattern (find + authorize) repeated 3x in `jobs.rs` | PRD-07 | `resolved` | `find_by_id` + `NotFound` + ownership check + `Forbidden` was copy-pasted in `get_job`, `cancel_job`, `retry_job`. Extracted `find_and_authorize(pool, job_id, auth, action)` helper. |
| DRY-167 | SHA-256 hex digest implemented 3x in core crate | PRD-13, PRD-12, PRD-45 | `flagged` | Three independent implementations: `metadata.rs:sha256_hex()` (Sha256+hex::encode), `api_keys.rs:hash_api_key()` (Sha256+hex::encode), `audit.rs:compute_integrity_hash()` (chained SHA-256). First two are structurally identical (both `Sha256::digest` + `hex::encode`). Extract shared `sha256_hex(data: &[u8]) -> String` to `core/src/hashing.rs`; keep `compute_integrity_hash` separate (chained/different purpose). 3 consumers, threshold met. |
| DRY-168 | Metadata regeneration logic duplicated within `metadata.rs` handler | PRD-13 | `flagged` | Lines 197-215 and 267-279 in `api/src/handlers/metadata.rs` repeat the same 5-step pipeline: `serialize_metadata` -> `sha256_hex` -> `CreateMetadataGeneration` construction -> `MetadataGenerationRepo::upsert`. Extract `upsert_generation(pool, entity_type, entity_id, project_id, metadata) -> Result<MetadataGeneration>` private helper. 2 call sites in same file. |
| DRY-169 | `find_stale_characters` / `find_stale_scenes` structural duplicates | PRD-13 | `flagged` | `metadata_repo.rs` has two 20-line methods that differ only in table name (`characters` vs `scenes`), join column, and entity type string. Same SELECT, same WHERE logic, same COALESCE pattern. Extract shared `find_stale_entities(pool, project_id, table, join_col, entity_type) -> Vec<StaleMetadata>` or use a generic approach with `&str` table name parameter. |
| DRY-170 | Magic strings `'character'`/`'scene'` in metadata SQL instead of constants | PRD-13 | `flagged` | `metadata_repo.rs` hardcodes `'character'` and `'scene'` in 4 SQL queries instead of using `ENTITY_TYPE_CHARACTER`/`ENTITY_TYPE_SCENE` from `core/src/metadata.rs`. Use `$N` bind params with the existing constants. |
| DRY-174 | `IMAGE_EXTENSIONS` in `importer.rs` overlaps with `VALID_IMAGE_FORMATS` in `images.rs` | PRD-16, PRD-21 | `watch` | `core/src/importer.rs` defines `IMAGE_EXTENSIONS = [".jpg", ".jpeg", ".png", ".webp", ".tiff", ".bmp"]` (with dots) while `core/src/images.rs` defines `VALID_IMAGE_FORMATS = ["jpg", "jpeg", "png", "webp", "tiff", "bmp"]` (without dots). Same set, different format. Consolidate by making `importer.rs` derive from `VALID_IMAGE_FORMATS` with dot prefix, or add a `VALID_IMAGE_EXTENSIONS` to `images.rs`. 2 consumers. |
| DRY-175 | Hardcoded `'uploading'` string in `import_session_repo.rs` SQL | PRD-16 | `watch` | Line 42 uses `'uploading'` string literal instead of `SESSION_STATUS_UPLOADING` constant from `core/src/importer.rs`. 1 SQL consumer. Fix when file is next touched. |
| DRY-176 | `serde_json::json!` responses in `metadata.rs` and `importer.rs` handlers | PRD-13, PRD-16 | `flagged` | `metadata.rs` handler uses `serde_json::json!` at 2 endpoints (regeneration report, staleness preview) and `importer.rs` at 1 endpoint (upload response) instead of typed `DataResponse<T>` structs. Same pattern as DRY-083. Migrate to `DataResponse<T>` with typed response structs. |

| DRY-179 | Duplicate sensitive-key redaction functions in `audit.rs` vs `config_export.rs` | PRD-44, PRD-45 | `flagged` | Two recursive JSON redaction functions with overlapping key lists (6 shared keys), different placeholders (`"[REDACTED]"` vs `"***REDACTED***"`), and different APIs (clone vs mutate-in-place). `audit.rs:SENSITIVE_FIELDS` (10 entries) and `config_export.rs:SENSITIVE_KEYS` (10 entries). Extract shared `core/src/redaction.rs` with unified key list, shared `is_sensitive_key()`, and both mutate/clone variants. |
| DRY-180 | `ensure_segment_exists` pattern repeated 3x in `approval.rs` handler | PRD-35, PRD-38 | `resolved` | Resolved by DRY-227. Extracted `ensure_segment_exists(pool, segment_id)` to `api/src/handlers/segment.rs` as pub function. Both `approval.rs` (3x) and `review_notes.rs` (2x) now import and call it. |
| DRY-182 | ComfyUI workflow parsing duplicated between backend handler and frontend parser | PRD-33 | `watch` | `handlers/workflow_canvas.rs:import_comfyui` (~85 lines) and `comfyui-parser.ts:parseComfyUIWorkflow` (~53 lines) implement the same node/edge extraction from ComfyUI JSON. Cross-boundary behavioral duplicate. Frontend also has `exportToComfyUI` (no backend equivalent). Designate frontend as client-side parser for offline use; if backend handler is the primary import path, consider moving parsing logic to `core/src/workflow_canvas.rs` for testability. 2 consumers (1 Rust, 1 TS). |
| DRY-183 | Inline pagination clamping in `bug_reports.rs` instead of `clamp_limit`/`clamp_offset` | PRD-44 | `flagged` | `params.limit.unwrap_or(50).min(200)` and `params.offset.unwrap_or(0)` inline in handler instead of using shared `clamp_limit`/`clamp_offset` from `core/src/search.rs`. DRY-093 8th consumer. |
| DRY-184 | Hardcoded `"admin"` string in `bug_reports.rs` instead of `ROLE_ADMIN` | PRD-44 | `flagged` | 2x `"admin"` string at lines 73 and 111 instead of `ROLE_ADMIN` from `core/src/roles.rs`. Same violation as DRY-090 (CRITICAL in PRD-07). |
| DRY-189 | State machine pattern structural duplicate (`bug_report.rs` vs `scheduling.rs`) | PRD-44, PRD-08 | `watch` | Both have `valid_transitions(from) -> &[targets]` + `validate_transition(from, to) -> Result<()>` with identical logic but different types (`&str` vs `i16`) and error types (`CoreError` vs `String`). 2 consumers. Extract generic `StateMachine` trait at 3rd consumer. |
| DRY-190 | Auth extractor inconsistency (`RequireAuth` vs `AuthUser`) across handler files | PRD-33, PRD-35, PRD-44 | `watch` | `workflow_canvas.rs` uses `RequireAuth` while `approval.rs` and `bug_reports.rs` use `AuthUser`. Both provide same functionality. Style inconsistency, not duplication. Standardize when files are next touched. |
| DRY-199 | Role name strings `admin`/`creator`/`reviewer` in `tourPaths.ts` cross-boundary | PRD-53 | `watch` | Tour paths keyed by role name strings matching backend `core/src/roles.rs` constants. Same cross-boundary pattern as DRY-114 (preset names). Sync comment added. No frontend role constants exist. Extract at 2nd frontend consumer. |
| DRY-201 | Regex compiled on every call in `scene_type_config.rs` | PRD-23 | `resolved` | `regex::Regex::new(r"\{(\w+)\}")` was compiled on every call to `resolve_prompt_template()` and `extract_placeholders()`. Extracted to `static PLACEHOLDER_RE: LazyLock<regex::Regex>` -- compiled once, reused forever. |
| DRY-202 | `BadgeVariant` type redefined in `SceneMatrixView.tsx` | PRD-23 | `resolved` | `type BadgeVariant` was locally redefined instead of importing from `Badge.tsx`. Exported `BadgeVariant` from `Badge.tsx` + primitives barrel. SceneMatrixView now imports it. StatusBadge.tsx and ActiveTasksWidget.tsx also redefine it (DRY-020 related) -- fix when touched. |
| DRY-203 | Textarea className string duplicated 3x in scene-types feature | PRD-23 | `resolved` | Identical 150-char textarea className string appeared in `PromptTemplateEditor.tsx` (2x) and `SceneTypeEditor.tsx` (1x). Extracted `TEXTAREA_CLASSES` constant to `PromptTemplateEditor.tsx`, imported in `SceneTypeEditor.tsx`. |
| DRY-204 | Magic `status_id` numbers in `generate_matrix` handler | PRD-23 | `resolved` | `match status_id { 1 => "pending", 2 => "generating", ... }` used raw numbers instead of `SceneStatus` enum. Extracted `scene_status_label()` function using `SceneStatus::*.id()` pattern (same as DRY-094 fix in `dispatcher.rs`). |
| DRY-205 | `validate_variant_applicability` structurally duplicates `validate_known_key` | PRD-23, PRD-53 | `watch` | Same contains-check + error-format pattern as `onboarding.rs:validate_known_key()`. 2 consumers with different error types and contexts. Extract generic validator at 3rd consumer. |
| DRY-206 | CRUD handlers in `scene_type.rs` return raw `Json<T>` instead of `DataResponse<T>` | PRD-23, PRD-01 | `watch` | `list_by_project`, `get_by_id`, `update`, `delete`, `create` handlers return `Json(entity)` directly without `DataResponse` envelope. Same pattern as other pre-DataResponse handlers (scenes, segments, source_images, derived_images). Low priority -- matches existing convention for original entity CRUD. Migrate when handlers are next touched. |
| DRY-207 | Variant applicability constants duplicated between Rust and TypeScript | PRD-23 | `watch` | `core/src/scene_type_config.rs` defines `VARIANT_CLOTHED/TOPLESS/BOTH/CLOTHES_OFF` and `VALID_VARIANT_TYPES`. Frontend `types.ts` defines `VARIANT_OPTIONS` with same values. Same cross-boundary pattern as DRY-114 (preset names), DRY-079 (metric names). Acceptable -- different runtimes. |
| DRY-208 | `CLIP_POSITIONS` and `CLIP_TABS` duplicate clip position labels | PRD-23 | `watch` | `types.ts:CLIP_POSITIONS = ["full_clip", "start_clip", "continuation_clip"]` and `PromptTemplateEditor.tsx:CLIP_TABS` duplicate the same 3 position identifiers. `CLIP_TABS` adds human labels. Same file-pair, acceptable -- `CLIP_POSITIONS` is for API params, `CLIP_TABS` is for UI tabs. |
| DRY-209 | `EmbeddingStatus` enum duplicated in `core/src/embedding.rs` and `db/src/models/status.rs` | PRD-76 | `resolved` | Hand-rolled enum in core (with `from_id`, `label`, `id`) and macro-generated enum in db (with only `id`). Core version is canonical because domain logic (`classify_extraction_result`) depends on it. Removed db `define_status_enum!` version, added comment pointing to core as canonical source. Tests moved to core. |
| DRY-212 | `find_by_id + ok_or(NotFound)` repeated 9x in `workers.rs` handler without helper | PRD-46 | `resolved` | Extracted `ensure_worker_exists(pool, id) -> AppResult<Worker>` helper and `validate_create_input(input) -> AppResult<()>` helper, following DRY-097/DRY-121 pattern. Reduced from 9 inline instances to 5 calls to helper + 2 special cases (update returns `Option`, approve returns `Option`). |
| DRY-213 | Hardcoded `status_id` numbers in `worker_repo.rs` SQL | PRD-46 | `resolved` | `status_id = 3` (Offline), `status_id = 1` (Idle) hardcoded in `register`, `list_available`, `approve`, `decommission` methods. Replaced with bind parameters using `WorkerStatus::Idle.id()` / `WorkerStatus::Offline.id()`. Fleet stats FILTER clauses left as-is (cannot easily parameterize SQL aggregate filters). |
| DRY-215 | Redundant `.map_err(\|e\| AppError::Core(e))` in `embedding.rs` handler | PRD-76 | `resolved` | `#[from] CoreError` on `AppError` provides auto-conversion via `?`. Removed explicit `map_err` closure. |
| DRY-216 | Worker validation block duplicated in `register_worker` and `self_register_worker` | PRD-46 | `resolved` | Identical 4-line validation block (validate_worker_name + validate_tags) extracted to `validate_create_input(&input)` helper in same file. |
| DRY-218 | `formatHeartbeat` relative time formatter in `WorkerCard.tsx` | PRD-46 | `watch` | Custom relative time formatter (`Xs ago`, `Xm ago`, `Xh ago`) in `WorkerCard.tsx:28-37`. 1 consumer. Extract to `@/lib/format.ts` as `formatRelativeTime(iso)` if a 2nd consumer appears. |
| DRY-226 | `makeAction()` test helper duplicated between undo test files | PRD-51 | `watch` | `UndoTree.test.ts` defines `makeAction(overrides: Partial<UndoAction>)` (full override object) and `HistoryBrowser.test.tsx` defines `makeAction(label: string)` (shortcut). Same concept, different signatures. 2 consumers in same feature. Extract shared `makeTestAction()` to `features/undo/__tests__/helpers.ts` if a 3rd undo test file appears. |
| DRY-227 | `ensure_segment_exists` duplicated in `approval.rs` and `review_notes.rs` | PRD-35, PRD-38 | `resolved` | Identical 8-line pattern (SegmentRepo::find_by_id + ok_or_else + CoreError::NotFound) duplicated between `approval.rs` (3x inline) and `review_notes.rs` (2x inline). Extracted to public `ensure_segment_exists(pool, segment_id) -> AppResult<()>` in `api/src/handlers/segment.rs`. Both handlers now import and call it. Also resolves DRY-180. |
| DRY-231 | Fully-qualified `x121_core::error::CoreError::NotFound` in `review_notes.rs` and `approval.rs` | PRD-38, PRD-35 | `resolved` | `review_notes.rs` used fully-qualified `x121_core::error::CoreError::NotFound` (3x) and `approval.rs` similarly (1x) instead of a `use CoreError;` import alias used by all other handler files. Added `use x121_core::error::CoreError;` import to both files. |
| DRY-232 | `validate_control_action` called with constant strings always succeeds | PRD-34 | `watch` | `job_debug.rs` handler calls `validate_control_action("pause")`, `validate_control_action("resume")`, `validate_control_action("abort")` -- all hardcoded valid strings, so validation always returns `Ok`. Validation is useful for dynamic input but unnecessary for compile-time constants. 3 call sites. Remove when handler is next touched, or keep as defensive-in-depth. |
| DRY-233 | `validate_recent_limit` reimplements `clamp_limit` pattern | PRD-31 | `watch` | `core/src/command_palette.rs:validate_recent_limit()` implements `limit.min(MAX_RECENT_ITEMS).max(1)` which is a specialized version of `clamp_limit()` from `core/src/search.rs` (uses `unwrap_or(default).min(max)`). Different API (no Option, no default, has min=1 floor). 1 consumer. Consider aligning at 2nd consumer. |
| DRY-264 | `serde_json::json!` in 3 `production_run.rs` handler endpoints instead of `DataResponse<T>` | PRD-57 | `resolved` | Created `SubmitCellsResponse`, `ResubmitResponse`, `DeliverResponse` typed structs in `db/src/models/production_run.rs`. All 3 handler endpoints now use `DataResponse<T>`. |
| DRY-265 | Magic `status_id` numbers in `production_run_repo.rs` and `production_run.rs` handler | PRD-57 | `resolved` | Repo: `mark_completed` and `list_failed_cells` now use `$2` bind param with `RUN_STATUS_ID_COMPLETED`/`RUN_STATUS_ID_FAILED`. Handler: all magic numbers replaced with `RUN_STATUS_ID_*` constants from `core/src/batch_production.rs`. |
| DRY-266 | `validate_unit_range` pattern (`!(0.0..=1.0).contains(&threshold)`) duplicated 5x across core modules | PRD-25, PRD-26, PRD-22 | `resolved` | Extracted `validate_unit_range(value, name)` to `core/src/threshold_validation.rs`. 5 consumers refactored to delegate: `restitching.rs`, `temporal_continuity.rs` (2x), `embedding.rs`, `quality_gate.rs`. |
| DRY-267 | Face detection boilerplate duplicated across Python scripts | PRD-26, PRD-22 | `watch` | Haar cascade init + `detectMultiScale` + largest face selection repeated in `temporal_drift_analysis.py`, `temporal_centering.py`, and `qa_face_detection.py`. Also `GaussianBlur((21,21),0)` grain extraction repeated between `temporal_grain_analysis.py` and `temporal_grain_normalize.py`. Extract `common/face_utils.py` with `detect_largest_face()` and `common/grain_utils.py` with `extract_grain()` at 4th consumer. |
| DRY-268 | `increment_completed`/`increment_failed` structural duplicate in `production_run_repo.rs` | PRD-57 | `watch` | Lines 144-175: two methods differ only in column name (`completed_cells` vs `failed_cells`). Same UPDATE + SET + WHERE + RETURNING pattern. 2 consumers. Extract `increment_counter(pool, run_id, column_name)` if a 3rd counter column is added. |
| DRY-291 | `Regex::new(PLACEHOLDER_PATTERN)` compiled per-call in `prompt_editor.rs` | PRD-63 | `resolved` | `extract_placeholders()` compiled regex on every invocation instead of using `static LazyLock<Regex>` pattern established in `scene_type_config.rs`. Extracted `static PLACEHOLDER_RE: LazyLock<Regex>` module-level constant. Same violation as DRY-201 (scene_type_config.rs, previously fixed). |
| DRY-292 | `serde_json::json!` for diff response in `workflow_import.rs` handler | PRD-75 | `resolved` | `diff_versions` handler built response with `serde_json::json!({...})` (6 fields) instead of typed struct + `DataResponse<T>`. Created `WorkflowDiffResponse` in `db/src/models/workflow_version.rs`. Handler now uses typed struct. Same violation pattern as DRY-083/DRY-264. |
| DRY-293 | Inline pagination clamping in `hook_repo.rs` | PRD-77 | `resolved` | `filter.limit.unwrap_or(100).min(500)` and `filter.offset.unwrap_or(0).max(0)` instead of `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Replaced with `clamp_limit(filter.limit, 100, 500)` and `clamp_offset(filter.offset)`. Extends DRY-093 pattern. |
| DRY-298 | `VersionListParams`/`ListVersionsParams`/`PaginationParams` -- 3 new identical pagination structs | PRD-63, PRD-75, PRD-77 | `watch` | `VersionListParams` in `prompt_editor.rs` handler, `ListVersionsParams` in `workflow_import.rs` handler, and `PaginationParams` in `pipeline_hooks.rs` handler are all `{ limit: Option<i64>, offset: Option<i64> }`. Extends DRY-276 (now 16+ handler files). |
| DRY-299 | Version auto-increment SQL pattern duplicated across 2 repos | PRD-63, PRD-75 | `watch` | `COALESCE((SELECT MAX(version) FROM {table} WHERE {fk} = $1), 0) + 1` structural duplicate in `prompt_version_repo.rs` (line 27) and `workflow_version_repo.rs` (line 30). 2 consumers. Acceptable as repo-specific SQL. Extract generic `next_version_subquery(table, fk_column)` SQL fragment builder at 3rd consumer. |
| DRY-300 | Hook point strings hardcoded in `get_effective_hooks` handler | PRD-77 | `watch` | Lines 366-372: `["post_variant", "pre_segment", "post_segment", "pre_concatenation", "post_delivery"]` as hardcoded string array instead of deriving from `HookPoint` enum variants. The `HookPoint` enum in `core/src/pipeline_hooks.rs` has `as_str()` on each variant but no `all_variants()` method. 1 consumer. Add `HookPoint::all()` method at 2nd consumer. |
| DRY-301 | Enum boilerplate (as_str + from_str + Display) repeated 4x in `pipeline_hooks.rs` | PRD-77 | `watch` | `HookType`, `HookPoint`, `ScopeType`, `FailureMode` each have identical boilerplate: `as_str() -> &str`, `from_str(&str) -> Result`, `Display` impl. 4 enums in 1 module. Currently contained. Extract macro `impl_str_enum!(EnumName, [("variant", "string")]...)` if a 2nd module with 3+ similar enums appears. |
| DRY-334 | `EffectiveProjectSceneSetting` / `EffectiveCharacterSceneSetting` structural duplicate | PRD-111, PRD-123 | `resolved` | Both had identical 5 fields (`scene_type_id`, `name`, `slug`, `is_enabled`, `source`). Extracted shared `EffectiveSceneSetting` in `db/src/models/scene_type.rs` (moved from deleted `scene_catalog.rs` during PRD-123). Both original types are now type aliases. |
| DRY-335 | `ProjectSceneSettingUpdate` / `CharacterSceneOverrideUpdate` exact duplicate | PRD-111, PRD-123 | `resolved` | Both had identical 2 fields (`scene_type_id: DbId`, `is_enabled: bool`). Extracted shared `SceneSettingUpdate` in `db/src/models/project_scene_setting.rs`. Original types are now type aliases. FK renamed from `scene_catalog_id` to `scene_type_id` during PRD-123. |
| DRY-336 | `ListParams { include_inactive: bool }` duplicated in `track.rs` and `scene_catalog.rs` handlers | PRD-111 | `resolved` | Extracted `IncludeInactiveParams` to `api/src/query.rs` alongside existing `PaginationParams`. Both handlers now import the shared type. |
| DRY-337 | `toSlug()` duplicated in `SceneCatalogForm.tsx` and `TrackManager.tsx` | PRD-111 | `resolved` | Identical snake_case slug generation function in 2 files. Existing `generateSlug` in `@/lib/format.ts` uses hyphens (different convention). Extracted `generateSnakeSlug` to `@/lib/format.ts`. Both components now import from shared location. |
| DRY-338 | `new_project()`/`new_character()` test fixture helpers duplicated 6x across test files | PRD-111, PRD-109 | `flagged` | Identical factory functions appear in: `db/tests/scene_catalog.rs`, `db/tests/scene_video_version.rs`, `db/tests/entity_crud.rs`, `db/tests/soft_delete.rs`, `api/tests/scene_catalog_api.rs`, `api/tests/trash_api.rs`. Extract to `db/tests/common/mod.rs` shared test fixtures module. 6 consumers, threshold long passed. |
| DRY-339 | Loading spinner pattern repeated 17+ times across features | PRD-111, PRD-110 | `flagged` | `<div className="flex items-center justify-center py-{N}"><Spinner size="lg" /></div>` now at 17 consumers (PRD-110 `SettingsPanel.tsx` is latest). Exceeded 15th consumer threshold. Extract `<CenteredSpinner />` or `<LoadingPlaceholder />` to `components/primitives/`. |
| DRY-354 | Health status magic strings (`"healthy"`, `"degraded"`, `"down"`) in `health_aggregator.rs` | PRD-117 | `watch` | 1 consumer. Extract to `core/src/health_status.rs` constants when a 2nd consumer appears. Same pattern as DRY-013/DRY-019/DRY-072. |
| DRY-357 | `mask_value()` in `platform_settings.rs` handler vs `redact_sensitive()` in `core/src/config_export.rs` and `redact_sensitive_fields()` in `core/src/audit.rs` | PRD-110 | `watch` | Three different masking/redaction implementations: `mask_value` (shows last 4 chars), `redact_sensitive` (replaces with `"[REDACTED]"`, recursive, in-place), `redact_sensitive_fields` (replaces with `"[REDACTED]"`, recursive, returns clone). All serve different use cases. DRY-179 already tracks audit vs config_export. `mask_value` is intentionally different (partial reveal vs full redaction). Watch for a 2nd partial-reveal consumer. |
| DRY-358 | Audit action_type/entity_type magic strings (`"setting_updated"`, `"setting_reset"`, `"platform_setting"`) in `platform_settings.rs` | PRD-110 | `watch` | 2 consumers within same file. Not yet worth extracting to constants (single domain, 2 uses). Extract to `core/src/settings.rs` if a 3rd consumer appears (e.g., settings import/export handler). Same pattern as DRY-170/DRY-265. |
| DRY-646 | `SceneCatalogEntry` type in `scene-catalog/types.ts` vs `SceneType` in `scene-types/types.ts` -- parallel types for same entity | PRD-123, PRD-111 | `flagged` | After PRD-123 unification, both types represent the same `scene_types` table. `SceneCatalogEntry` has `{name, slug, description, has_clothes_off_transition, sort_order, is_active, tracks}` while `SceneType` has the full config schema. Should unify to one canonical `SceneType` + a `SceneTypeWithTracks` wrapper. Deferred: touches many import sites across features. |
| DRY-647 | `use-scene-catalog.ts` hooks vs `use-scene-types.ts` hooks -- dual hook sets for same `/scene-types` API | PRD-123, PRD-111 | `flagged` | Both hook files hit the same `/scene-types` endpoint but use different query keys (`["scene-catalog"]` vs `["scene-types"]`). `use-scene-catalog.ts` manages the catalog view (with tracks), `use-scene-types.ts` manages the config view. Cache invalidation across features is disconnected. Should merge query key factories. Deferred: requires coordinated refactor of both feature modules. |
| DRY-648 | `SettingRow` component structurally duplicated in `ProjectSceneSettings.tsx` and `CharacterSceneOverrides.tsx` | PRD-111, PRD-123 | `watch` | Both have a `SettingRow` with identical name/slug/toggle/source-badge columns. Character version adds a 5th "reset" column. 2 consumers. Extract shared `SceneSettingRow` at 3rd consumer. |
| DRY-649 | `"not_started"` magic string in `scene_type.rs` handler line 306 | PRD-123 | `watch` | Should use `STATE_NOT_STARTED` from `core/src/readiness.rs`. 1 consumer. Fix at next handler touch. |

### Frontend Patterns

| ID | Pattern / Component | PRDs Involved | Status | Notes |
|----|---------------------|---------------|--------|-------|
| DRY-020 | Status badge/chip component | PRD-29, PRD-35, PRD-42, PRD-54 | `watch` | Status display (colored badge with lookup name) will appear in many views |
| DRY-021 | Data table with sort/filter/pagination | PRD-29, PRD-20, PRD-42, PRD-73 | `watch` | Reusable table component needed early — many PRDs will need it |
| DRY-022 | Confirmation dialog (destructive actions) | PRD-29, PRD-15, PRD-72 | `watch` | CASCADE deletes require confirmation — shared dialog needed |
| DRY-023 | Image thumbnail/preview component | PRD-21, PRD-62, PRD-83, PRD-96 | `watch` | Multiple PRDs display image/video thumbnails — one component |
| DRY-024 | Progress indicator (job/generation) | PRD-24, PRD-54, PRD-57, PRD-90 | `watch` | Generation progress bars/strips appear across many views |
| DRY-025 | Side-by-side comparison layout | PRD-22, PRD-68, PRD-101 | `watch` | Image and video comparison views share the same layout pattern |
| DRY-026 | Form validation patterns | PRD-14, PRD-23, PRD-66 | `watch` | Input validation with error display will repeat across all forms |
| DRY-099 | `TagSuggestion` type duplicated `TagWithCount` in `TagInput.tsx` | PRD-47 | `resolved` | `TagSuggestion` was structurally identical to `TagWithCount` (`TagInfo + usage_count`). Unified to single `TagWithCount` type in `TagChip.tsx`. |
| DRY-102 | Click-outside pattern duplicated in `TagInput.tsx`, `Dropdown.tsx`, `JobTrayPanel.tsx` | PRD-47, PRD-29, PRD-54 | `resolved` | 3 consumers reached threshold. Extracted `useClickOutside(ref, callback, enabled?)` to `src/hooks/useClickOutside.ts`. All 3 consumers refactored. |
| DRY-103 | Raw `api.*()` calls in tag components instead of TanStack Query hooks | PRD-47 | `watch` | `TagInput.tsx` and `TagFilter.tsx` use `api.get/post/delete` directly. Established pattern is TanStack Query hooks (see `use-hardware.ts`). Extract `useTags`, `useApplyTag`, `useRemoveTag`, `useTagSuggestions` hooks when feature matures. |
| DRY-104 | Taggable entity type string constants | PRD-47 | `watch` | `VALID_ENTITY_TYPES` in `tags.rs:292` -- 1 consumer. Extract to `core/src/entity_types.rs` if a 2nd consumer needs the list. |
| DRY-105 | Inline SVG close icon in `TagChip.tsx` | PRD-47 | `watch` | Hardcoded X icon SVG. 1 consumer. Extract to design system icon token at 2nd inline close icon. |
| DRY-106 | Theme status name `'active'` magic string in `theme_repo.rs` SQL | PRD-29 | `watch` | `list_custom_themes()` uses `WHERE name = 'active'` subquery. 1 consumer. Extract to `core/src/theme_status.rs` constant if a 2nd consumer appears. Same cross-boundary pattern as DRY-079. |
| DRY-107 | `STATUS_OPTIONS` hardcoded status_id numbers in `TokenEditor.tsx` | PRD-29 | `watch` | `{ value: "1", label: "Draft" }` etc. relies on migration seed ordering. 1 consumer. If a 2nd frontend component needs theme status list, fetch from API or define shared constants. |
| DRY-108 | `TokenEditor.tsx` exceeded 500-line threshold (670 lines) | PRD-29 | `resolved` | Extracted 7 section components (Surface/Text/Action/Border/Font/Spacing + Preview) and shared types/constants to `token-editor/TokenSections.tsx` (404 lines). Main component now 298 lines. 6x structurally identical `updateX` functions replaced with generic `updateTokenSection<K>()`. |
| DRY-109 | `TagWithCount` redundantly re-exported from `TagInput.tsx` and `TagFilter.tsx` | PRD-47 | `resolved` | Both files re-exported `TagWithCount` even though they import it from the canonical source `TagChip.tsx`. Removed redundant re-exports to prevent import confusion. |
| DRY-110 | `CATEGORY_LABELS` and `CATEGORY_ORDER` duplicated in `CheatSheet.tsx` and `KeymapEditor.tsx` | PRD-52 | `resolved` | Identical constant declarations in two files. Extracted to `ShortcutRegistry.ts` alongside `groupBindingsByCategory()` utility. |
| DRY-111 | `snapValue()` duplicated in `useSnapGrid.ts` and `usePanelResize.ts` | PRD-30 | `resolved` | Identical function `Math.round(value / gridSize) * gridSize`. Exported from `useSnapGrid.ts`, imported in `usePanelResize.ts`. |
| DRY-112 | Debounce timer pattern across 3 persistence hooks | PRD-29, PRD-30, PRD-32 | `watch` | `useThemePersistence`, `useLayoutPersistence`, and `useProficiencyTracker` all use `useRef<setTimeout>` + `clearTimeout` + `setTimeout` debounce pattern with different delays (500ms, 2000ms, 2000ms). Structurally similar but different callback logic. Extract `useDebouncedCallback(fn, delay)` if a 4th consumer appears. |
| DRY-113 | Proficiency level magic strings in SQL | PRD-32 | `watch` | `'beginner'`, `'intermediate'`, `'expert'` appear in `proficiency_repo.rs` SQL and migration DDL. Same cross-boundary pattern as DRY-079 (metric names in SQL). Acceptable for now. Extract to `core/src/proficiency_levels.rs` if a second Rust consumer appears. |
| DRY-114 | Preset name list duplicated between backend and frontend | PRD-52 | `watch` | Backend `PRESET_NAMES` in `keymaps.rs` and frontend `presets` object in `presets/index.ts` both enumerate `[default, premiere, resolve, avid]`. Cross-boundary duplication. The `GET /keymaps/presets` endpoint serves as single source of truth; frontend presets map is local for offline key resolution. No action needed unless lists diverge. |
| DRY-115 | Raw `api.*()` calls in layout persistence hooks | PRD-30 | `watch` | `useLayoutPersistence.ts` and `PresetSwitcher.tsx` both make raw `api.get/put/post/delete` calls to `/user/layouts`. Same pattern as DRY-103 (tags). Extract TanStack Query hooks when layout feature matures. |
| DRY-127 | `formatTime()` duplicated in `CinemaMode.tsx` and `GridControls.tsx` | PRD-36 | `resolved` | Identical function (seconds to `M:SS`) in 2 cinema files, behaviorally duplicating `formatDuration()` from `video-player/frame-utils.ts`. Removed both locals, replaced with `formatDuration` import from `@/features/video-player`. |
| DRY-128 | Recharts Tooltip `contentStyle` object duplicated 7x across 3 chart components | PRD-41 | `resolved` | Identical 4-property style object in QualityCharts (4x), WorkflowComparison (2x), WorkerBenchmark (1x). Extracted `TOOLTIP_CONTENT_STYLE`, `AXIS_TICK_STYLE`, `GRID_STROKE` to `performance/chartStyles.ts`. |
| DRY-129 | `formatElapsed()` reimplements duration formatting in `ActiveTasksWidget.tsx` | PRD-42 | `resolved` | Behavioral duplicate of `formatDuration()` from `@/lib/format.ts`. Refactored to delegate to `formatDuration(seconds * 1000)` with null guard. |
| DRY-130 | Click-outside pattern at 3 consumers (threshold met) | PRD-47, PRD-29, PRD-54 | `resolved` | See DRY-102 (updated to resolved). Extracted `useClickOutside` hook to `src/hooks/useClickOutside.ts`. Refactored TagInput, Dropdown, JobTrayPanel. |
| DRY-116 | `export_keymap` handler duplicate JSON branches | PRD-52 | `resolved` | Two `serde_json::json!` branches (Some/None) produced identical structure with different values. Extracted shared destructuring with fallback defaults. |
| DRY-117 | `formatFileSize()` duplicated in `AssetBrowser.tsx` and `AssetDetail.tsx` | PRD-17 | `resolved` | Identical function behaviorally duplicating `formatBytes()` from `@/lib/format.ts`. Removed both, replaced with `formatBytes` import. |
| DRY-118 | `formatDate()` inline helper in `AssetDetail.tsx` | PRD-17 | `resolved` | Short-date formatter was local to one file. Extracted to shared `formatDate()` in `@/lib/format.ts` alongside existing `formatDateTime` and `formatCountdown`. |
| DRY-119 | `reclamation.rs` handler uses `serde_json::json!` (11x) instead of `DataResponse<T>` | PRD-15 | `resolved` | Handler imported `DataResponse` but only used it for 2/13 endpoints. Migrated all 11 remaining endpoints to `DataResponse<T>` with `impl IntoResponse` return type. DRY-083 partially improved (reclamation cleared; notification.rs + validation.rs remain). |
| DRY-120 | Column list repeated 15x across `reclamation_repo.rs` (no COLUMNS constants) | PRD-15 | `resolved` | Trash queue columns 5x, protection rule columns 4x, policy columns 3x, run columns 3x. Extracted `RULE_COLUMNS`, `POLICY_COLUMNS`, `TRASH_COLUMNS`, `RUN_COLUMNS` constants following `asset_repo.rs` COLUMNS pattern. |
| DRY-121 | `verify_exists + NotFound` guard repeated 4x in `assets.rs` handler | PRD-17 | `resolved` | Identical 5-line pattern (verify_exists + AppError::Core(CoreError::NotFound)) in add_dependency, get_impact, add_note, rate_asset. Extracted `ensure_asset_exists(pool, id)` private helper (DRY-097 pattern). |
| DRY-122 | Trash queue status_id magic numbers in `reclamation_repo.rs` | PRD-15 | `watch` | `TRASH_STATUS_PENDING = 1`, `TRASH_STATUS_DELETED = 3`, `TRASH_STATUS_RESTORED = 4` are repo-local constants matching seed ordering. Same fragile pattern as DRY-078 (restart status). Extract to `core/src/trash_status.rs` if a second consumer needs these values. |
| DRY-123 | `STATUS_OPTIONS` hardcoded asset status_id numbers in `AssetBrowser.tsx` | PRD-17 | `watch` | `{ value: "1", label: "Active" }` etc. relies on migration seed ordering. Same pattern as DRY-107 (theme STATUS_OPTIONS). 1 consumer. Fetch from API endpoint or share constants at 2nd consumer. |
| DRY-124 | `ASSET_TYPE_OPTIONS` hardcoded asset type_id numbers in `AssetBrowser.tsx` | PRD-17 | `watch` | `{ value: "1", label: "Model" }` etc. relies on migration seed ordering. 1 consumer. `AssetRepo::list_types()` endpoint exists but is not being called. Fetch dynamically if a 2nd consumer needs type list. |
| DRY-125 | `"required"` and `"info"` magic strings as default values in `asset_repo.rs` | PRD-17 | `watch` | `dependency_role.unwrap_or("required")` and `severity.unwrap_or("info")` -- 1 consumer each. Extract to constants if a 2nd consumer appears. |
| DRY-126 | `"manual"` run_type magic string in reclamation handler | PRD-15 | `watch` | `run_type: "manual".to_string()` in handler. 1 consumer. Extract to `core/src/reclamation_types.rs` constant if a 2nd run_type source (e.g., "scheduled") appears. |
| DRY-131 | Workflow aggregation SELECT columns duplicated in `performance_metric_repo.rs` | PRD-41 | `watch` | 9 identical column expressions in `aggregate_by_workflow` vs `aggregate_for_workflows`. Same pattern as DRY-120 (column constants). Extract `WORKFLOW_AGG_COLUMNS` constant if a 3rd consumer appears. |
| DRY-132 | Scope type magic strings (`"global"`, `"workflow"`, `"worker"`) in `performance.rs` handler | PRD-41 | `watch` | 3 magic strings used in threshold create/update handlers + SQL `scope_type` CHECK. 1 Rust consumer. Extract to `core/src/scope_types.rs` if a 2nd consumer appears. |
| DRY-133 | Worker aggregation SELECT columns duplicated in `performance_metric_repo.rs` | PRD-41 | `watch` | 8 identical column expressions in `aggregate_by_worker` vs `aggregate_single_worker`. Same as DRY-131. Extract `WORKER_AGG_COLUMNS` constant if a 3rd consumer appears. |
| DRY-134 | `parse_from`/`parse_to` date parsing helpers in `performance.rs` handler | PRD-41 | `watch` | Extracts optional `from`/`to` query params, parses with `chrono::NaiveDate::parse_from_str`. 1 consumer. Extract to `api/src/util.rs` if a 2nd handler needs date range parsing. |
| DRY-135 | `renderWithProviders` test helper duplicated across 6 test files | PRD-85, PRD-41, PRD-42, PRD-15, PRD-21, PRD-08 | `resolved` | Extracted to `src/lib/test-utils.tsx`. All 6 consumers (ExtensionManager, PerformanceDashboard, StudioPulse, ReclamationDashboard, VariantGallery, QueueStatusView) now import from shared location. |
| DRY-136 | `parseError` + JSON parse try/catch pattern in 2 sub-components of `ExtensionManager.tsx` | PRD-85 | `watch` | `SettingsEditor` and `InstallForm` both use `useState<string \| null>(null)`, `try { JSON.parse } catch { setParseError }`, and identical error `<p>` display. Same file, 2 instances. Extract shared `useJsonParse` hook if a 3rd JSON-editing component appears. |
| DRY-137 | `collectDesignTokens` in `ExtensionSandbox.tsx` vs `getCssVar` in `MetricsChart.tsx` | PRD-85, PRD-06 | `watch` | Both read CSS custom properties via `getComputedStyle(document.documentElement)`. Different purposes (batch collection vs single lookup). Related to DRY-082. Extract shared `src/lib/css-vars.ts` when a 3rd consumer needs CSS var access. |
| DRY-161 | Confirmation dialog pattern (AlertTriangle + description + Cancel/Action buttons) | PRD-12 | `watch` | `ApiKeyManager.tsx` (rotate/revoke key confirmations) and `WebhookManager.tsx` (delete webhook confirmation) share identical dialog structure: AlertTriangle icon, warning description paragraph, Cancel + destructive Action button pair. 2 consumers. Design system `ConfirmDialog` component candidate at 3rd consumer. Related to DRY-022. |
| DRY-162 | Admin table styling pattern (Card + overflow-x-auto + table + th/td classes) | PRD-12, PRD-85 | `watch` | `ApiKeyManager.tsx`, `WebhookManager.tsx`, `DeliveryLog.tsx`, `ExtensionManager.tsx` all repeat identical Card > overflow-x-auto > table > thead > th class pattern. 4 consumers. Design system `AdminTable` component candidate (threshold met but deferred to design system work). |
| DRY-146 | `formatWait()` + `formatSeconds()` seconds-to-human formatters in queue components | PRD-08 | `watch` | `QueueStatusView.tsx:formatWait(secs)` and `QuotaStatusBadge.tsx:formatSeconds(secs)` are structurally similar to `formatDuration(ms)` from `@/lib/format.ts`. Different input units (seconds vs ms) and slightly different output format. 2 consumers. Extract `formatDurationSeconds()` to `@/lib/format.ts` at 3rd consumer. |
| DRY-147 | `UpdateImageVariant` struct with all `None` fields repeated in handler | PRD-21 | `watch` | `reject_variant` and `export_for_editing` both construct `UpdateImageVariant` with 11 `None` fields. Could add `Default` impl. 2 consumers. Extract at 3rd occurrence. |
| DRY-148 | Multipart upload + validate format + store file pattern in image_variant handler | PRD-21 | `watch` | `reimport_variant` and `upload_manual_variant` share ~30 lines of near-identical multipart parsing, format validation, and file storage logic. 2 consumers. Extract `parse_image_upload()` helper at 3rd upload handler (related to DRY-042). |
| DRY-149 | `state_machine::status_name` duplicates `JobStatus` enum variant names | PRD-08 | `watch` | `core/src/scheduling.rs:status_name()` maps status_id to string names ("Pending", "Running", etc.) which are logically the same as `db/models/status.rs:JobStatus` variant names. Intentional: `core` has zero internal deps so cannot reference `db` types. No action needed unless a 3rd mapping appears. |
| DRY-150 | `off_peak` policy_type magic string in `scheduling_repo.rs` SQL | PRD-08 | `watch` | `'off_peak'` string appears in `find_active_off_peak` query. 1 Rust consumer. Extract to `core/src/scheduling.rs` constant at 2nd consumer (e.g., scheduler engine). |
| DRY-154 | `FailureDiagnostics` (db model) vs `FailureDiagnosticData` (core) near-duplicate | PRD-28 | `watch` | Both structs have same 9 fields; differ in `stage_index` type (`i32` vs `u32`) and `timestamp` type (`Timestamp` vs `String`). Intentional layer separation (db types vs domain types). Merge if types ever align or a 3rd struct appears. |
| DRY-159 | `parse_timestamp` in audit handler vs `parse_from`/`parse_to` in performance handler | PRD-45, PRD-42 | `watch` | Both parse `Option<String>` to `chrono::DateTime<Utc>` with fallback. 2 consumers. Extract to shared `api/src/util.rs` at 3rd consumer (DRY-134 escalated). |
| DRY-160 | Debounce timer pattern (useRef+setTimeout) in persistence hooks | PRD-04, PRD-30, PRD-32, PRD-51, PRD-52, PRD-82 | `flagged` | Now 7 consumers: `useThemePersistence`, `useLayoutPersistence`, `useProficiencyTracker`, `useAutoSave`, `TagInput`, `use-entity-undo.ts`, `SensitivityProvider.tsx` (inline). DRY-112 threshold exceeded. Extract `useDebouncedCallback(callback, delayMs)` hook to `src/hooks/`. |
| DRY-171 | Raw `fetch` with `localStorage.getItem("access_token")` bypassing shared `api` client | PRD-66, PRD-16 | `flagged` | `use-metadata-editor.ts` (lines 120, 151) manually reads auth token from localStorage for CSV export/import `fetch` calls. `use-importer.ts` (line 74) uses raw `fetch` with NO auth header at all (potential bug). Shared `api.ts` client already handles auth via `useAuthStore`. These should use `api.post()`/`api.get()` or at minimum import from `useAuthStore` instead of reading localStorage directly. 3 instances across 2 files. |
| DRY-172 | `formatValue()` in `CsvImport.tsx` and `formatCellValue()` in `MetadataSpreadsheet.tsx` | PRD-66 | `watch` | Both functions handle null/undefined/boolean/array/object -> string formatting. `CsvImport.tsx:222-226` and `MetadataSpreadsheet.tsx:301-305` have near-identical logic with minor presentation differences (CsvImport uses `JSON.stringify` for objects, Spreadsheet returns `[Array]`/`[Object]`). 2 consumers in same feature. Extract shared `formatMetadataValue()` to `features/metadata-editor/utils.ts` if a 3rd display context appears. |
| DRY-228 | `statusBadgeVariant()` duplicated in `NoteTimeline.tsx` and `ReviewThread.tsx` | PRD-38 | `resolved` | Identical function mapping note status to Badge variant string ("success"/"warning"/"default") duplicated in both components. Extracted to `features/review-notes/types.ts` alongside existing `noteStatusLabel()`/`noteStatusColor()`. Both consumers now import. |
| DRY-229 | Card panel styling repeated 5x in debugger feature | PRD-34 | `resolved` | Identical 4-class `cn()` block (`bg-surface-primary`, `border-default`, `rounded-lg`, `p-4`) appeared in `JobControls.tsx`, `MidRunParamEditor.tsx`, `LatentPreview.tsx` (2x), `AbortDialog.tsx`. Extracted `DEBUGGER_CARD_CLASSES` array constant to `features/debugger/types.ts`. All 4 component files updated. |
| DRY-230 | Textarea styling duplicated in `MidRunParamEditor.tsx` and `AbortDialog.tsx` | PRD-34 | `resolved` | Identical 8-class textarea `cn()` block (`bg-surface-secondary`, `border-default`, `rounded-md`, `px-3 py-2`, `text-sm`, `text-primary`, `placeholder-muted`, `focus-ring`) duplicated in both components. Extracted `DEBUGGER_TEXTAREA_BASE` array constant to `features/debugger/types.ts`. Both consumers now spread it with `...DEBUGGER_TEXTAREA_BASE`. |
| DRY-234 | `review_note_repo.rs` repeats `rn.` column prefix in cross-table queries | PRD-38 | `watch` | `NOTE_COLUMNS` uses unprefixed column names but cross-table queries with tag joins need `rn.` prefix. Handler writes `rn.id, rn.segment_id, ...` inline instead of using `NOTE_COLUMNS`. 2 query sites. Acceptable: prefixed columns needed for disambiguated JOINs, and creating a second `PREFIXED_NOTE_COLUMNS` constant adds minimal value. |
| DRY-235 | `AbortDialog.tsx` hand-rolls modal overlay instead of `Modal` composite | PRD-34 | `watch` | Custom `fixed inset-0 z-50 bg-black/50` overlay with manual `role="dialog" aria-modal`. Missing Escape key handling, focus trapping. Same pattern as DRY-186 (RejectionDialog). 3rd consumer of hand-rolled modal overlay. Design system `Modal` or `ConfirmDialog` component candidate. Related to DRY-022, DRY-161. |
| DRY-236 | `CommandPalette.tsx` hand-rolls overlay with portal | PRD-31 | `watch` | Custom `fixed inset-0 z-50 bg-black/25` overlay rendered via `createPortal`. Includes Escape handling and click-outside via custom keyboard hook. 4th consumer of hand-rolled overlay. Same pattern as DRY-186/DRY-235. Specialized behavior (command palette UX) may not fit standard `Modal` component. |
| DRY-173 | Inline `new Date().toLocaleString()` instead of `formatDateTime` from `@/lib/format.ts` | PRD-13 | `watch` | `StalenessIndicator.tsx:58` uses `new Date(generatedAt).toLocaleString()` instead of the shared `formatDateTime()` utility. 1 consumer. Fix when file is next touched. |
| DRY-177 | Import status constants overlap between `importer.rs` and `import_status.rs` | PRD-16, PRD-14 | `watch` | `core/src/importer.rs` defines `SESSION_STATUS_UPLOADING/MAPPING/PREVIEWING/COMMITTED/CANCELLED/FAILED` while `core/src/import_status.rs` defines `IMPORT_STATUS_PREVIEW/COMMITTED/PARTIAL/FAILED/CANCELLED`. Different lifecycle stages (session vs import batch) but conceptually related. Watch for confusion. No action needed unless a handler needs both sets. |
| DRY-178 | `PhysicalAttributes` struct in `metadata_editor.rs` vs field definitions | PRD-66, PRD-13 | `watch` | `core/src/metadata_editor.rs` defines `PhysicalAttributes` struct with typed fields and also has `FIELD_DEFINITIONS` array describing the same fields as metadata. Intentional dual representation (typed struct for validation vs field defs for dynamic UI). Watch for divergence if fields are added to one but not the other. |
| DRY-181 | Identical `onSuccess` invalidation in 3 review mutation hooks | PRD-35 | `flagged` | `useApproveSegment`, `useRejectSegment`, `useFlagSegment` in `use-review.ts` have identical `onSuccess` callbacks (invalidate approvals + all review keys) and same `mutationFn` shape `({ segmentId, input }) => api.post(...)`. Extract `useDecisionMutation<TInput>(endpoint)` factory. ~40 lines of duplication. |
| DRY-185 | Magic timing thresholds 1000/5000 in `WorkflowCanvas.tsx` duplicate backend constants | PRD-33 | `flagged` | Inline `executionMs < 1000 ? green : < 5000 ? yellow : red` duplicates `TIMING_FAST_MS`/`TIMING_MODERATE_MS` from `core/src/workflow_canvas.rs`. Extract frontend constants to `types.ts` with sync comment + `timingColorClass()` utility. |
| DRY-186 | `RejectionDialog.tsx` hand-rolls modal overlay instead of `Modal` composite | PRD-35 | `flagged` | Custom `fixed inset-0 z-50 bg-black/50` overlay with manual positioning, missing keyboard handling (Escape), focus trapping. Shared `Modal` component at `@/components/composite/Modal` used by 11+ components. Refactor to use `<Modal>`. |
| DRY-187 | Inline loading spinner in `WorkflowCanvas.tsx` instead of `Spinner` primitive | PRD-33 | `flagged` | `animate-spin rounded-full border-2 border-current border-t-transparent` hand-rolled spinner. Shared `Spinner` component at `@/components/primitives/Spinner` used by 30+ components. Replace with `<Spinner />` import. |
| DRY-188 | Inline `toLocaleDateString()` in `BugReportList.tsx` instead of `formatDate` | PRD-44 | `flagged` | `new Date(report.created_at).toLocaleDateString()` at line 115. Shared `formatDate(iso)` exists in `@/lib/format.ts`. Same violation as DRY-157 and DRY-173. Replace with `formatDate(report.created_at)`. |
| DRY-200 | TourEngine overlay lacks Escape key handler and focus trapping | PRD-53 | `watch` | `TourEngine.tsx` hand-rolls `fixed inset-0 z-50` overlay without keyboard handling (Escape key) or focus trapping, unlike shared `Modal` composite. Tour has specialized behavior (spotlight, step navigation) making `Modal` a poor fit. Add Escape handler when interactive features mature. |
| DRY-210 | Local `formatDate()` in `WorkerDetailPanel.tsx` instead of shared `formatDateTime` | PRD-46 | `resolved` | Custom `formatDate(iso)` function returning `new Date(iso).toLocaleString()` -- behavioral duplicate of `formatDateTime()` from `@/lib/format.ts`. Replaced with `formatDateOrDash()` wrapper using shared `formatDateTime` import. |
| DRY-211 | Inline `BadgeVariant` union type in `workers/types.ts` | PRD-46 | `resolved` | `"success" \| "warning" \| "danger" \| "default"` inline union type in `WORKER_STATUS_VARIANT` Record instead of importing `BadgeVariant` from `@/components/primitives/Badge`. Replaced with import. Also added `WORKER_STATUS` named constants object to eliminate magic status_id numbers in the same file. |
| DRY-214 | Inline SVG close icon in `LowConfidenceWarning.tsx` | PRD-76 | `resolved` | Hand-coded 7-line `<svg>` element for close/dismiss button. Replaced with `<X size={16} />` from `@/tokens/icons`. Same pattern as DRY-105 (TagChip inline SVG). |
| DRY-217 | Magic number `4` for draining status in `WorkerDetailPanel.tsx` | PRD-46 | `resolved` | Hardcoded `worker.status_id !== 4` in conditional rendering. Replaced with `worker.status_id !== WORKER_STATUS.DRAINING` using named constant from `workers/types.ts`. |
| DRY-269 | `badgeVariant` mapping functions proliferating across frontend features | PRD-57, PRD-25, PRD-26, PRD-38, PRD-49, PRD-46, PRD-76 | `watch` | Seven feature modules define their own `*BadgeVariant()` mapper functions: `cellStatusVariant` (production/types.ts), `qualityBadgeVariant` (restitching/types.ts), `driftBadgeVariant` (temporal/types.ts), `grainBadgeVariant` (temporal/types.ts), `statusBadgeVariant` (quality-gates/types.ts), `statusBadgeVariant` (review-notes/types.ts), `embeddingBadgeVariant` (embedding/types.ts). All map domain enum to `BadgeVariant` union. Extract generic `createBadgeVariantMapper<T>(mapping: Record<T, BadgeVariant>)` factory to `@/components/primitives/Badge.tsx` if pattern reaches 10 consumers. |
| DRY-270 | SSIM threshold constants duplicated cross-boundary (Rust and TypeScript) | PRD-25 | `watch` | `DEFAULT_SSIM_THRESHOLD = 0.85` and `SSIM_WARNING_THRESHOLD = 0.92` manually duplicated between `core/src/restitching.rs` and `features/restitching/types.ts`. Same cross-boundary pattern as DRY-114/DRY-207. Different runtimes. Sync comments added pointing to canonical Rust source. |
| DRY-271 | Temporal continuity threshold constants duplicated cross-boundary (Rust and TypeScript) | PRD-26 | `watch` | `DEFAULT_DRIFT_THRESHOLD`, `DEFAULT_GRAIN_THRESHOLD`, `DEFAULT_CENTERING_THRESHOLD` manually duplicated between `core/src/temporal_continuity.rs` and `features/temporal/types.ts`. Same cross-boundary pattern as DRY-270. Sync comments added pointing to canonical Rust source. |
| DRY-272 | `ensure_run_exists` helper pattern correctly applied in `production_run.rs` | PRD-57 | `resolved` | Handler extracted `ensure_run_exists(pool, run_id)` helper following established DRY-097/DRY-121 pattern. No duplication. |
| DRY-273 | `qualityColor` function at 2 consumers in restitching feature | PRD-25 | `watch` | `qualityColor()` in `restitching/types.ts` maps boundary quality to Tailwind color classes. 1 consumer (BoundaryQualityIndicator.tsx). 2nd usage would justify extraction to shared design utilities. Acceptable for now. |
| DRY-274 | Fully-qualified `CoreError::NotFound` in `resolution.rs` handler | PRD-59 | `resolved` | Used 5x without import. Added `use CoreError;`, extracted `ensure_scene_exists` and `ensure_tier_exists` helpers. |
| DRY-275 | Redundant `.map_err(AppError::Core)?` in resolution.rs and estimation.rs | PRD-59, PRD-61 | `resolved` | `AppError` has `#[from] CoreError`, so plain `?` auto-converts. Replaced 5 instances. |
| DRY-276 | `PaginationQuery` struct duplicated across 16+ handler files | PRD-58, PRD-61, PRD-63, PRD-75, PRD-77 | `watch` | `VersionListParams` (PRD-63), `ListVersionsParams` (PRD-75), `PaginationParams` (PRD-77) are 3 new additions. Many have extra domain-specific fields (`project_id`, `scene_type_id`, etc.). Extract generic `PaginationParams` to `api/src/response.rs` when pattern reaches 20+. |
| DRY-277 | `validate_batch_size` / `validate_estimate_count` structural duplicates | PRD-58, PRD-61 | `resolved` | Extracted `validate_count_range(count, max, label)` to `core/src/threshold_validation.rs`. Both callers delegate. |
| DRY-278 | `confidenceBadgeVariant` returned inline union type | PRD-61 | `resolved` | Changed to return `BadgeVariant` imported from primitives for type consistency. |
| DRY-279 | `TestShotButton.tsx` hand-rolled modal overlay (5th consumer) | PRD-58 | `resolved` | Replaced with `Modal` from `@/components/composite`. |
| DRY-280 | Magic `3600` and `1024` in `EstimationCard.tsx` | PRD-61 | `resolved` | Added `SECS_PER_HOUR` / `MB_PER_GB` constants with sync comments pointing to canonical `core/src/estimation.rs`. |
| DRY-281 | `list_by_scene_type`/`list_by_character` structural similarity in test_shot_repo | PRD-58 | `watch` | 2 consumers, differ only in WHERE column. Extract generic `list_by_fk(column)` at 3rd consumer. |
| DRY-282 | Badge variant mapper count at 15 features | PRD-58, PRD-59, PRD-61, PRD-75, PRD-77, PRD-70, PRD-95 | `watch` | +2 new in Phase 7 remainder: `annotationStatusVariant` (PRD-70, annotations), `categoryBadgeVariant` (PRD-95, production-notes). Now 15 total. Domain-specific by design. Approaching extraction threshold. See DRY-302, DRY-308, DRY-319. |
| DRY-283 | `frameToTimecode(frame, fps)` duplicated in `TrimTimeline.tsx` | PRD-78 | `resolved` | Exact duplicate of `frameToTimecode()` from `features/video-player/frame-utils.ts`. Same algorithm (frame -> HH:MM:SS:FF). Removed local copy, replaced with import from shared module. |
| DRY-284 | `clamp(value, min, max)` duplicated in `TrimTimeline.tsx` and `usePanelResize.ts` | PRD-78, PRD-30 | `watch` | Identical 1-line `Math.max(min, Math.min(max, value))` function at 2 consumers. Extract to `@/lib/math-utils.ts` at 3rd consumer. |
| DRY-285 | Inline `toLocaleDateString()` in `VersionHistory.tsx` instead of `formatDate` | PRD-69 | `resolved` | Used inline `new Date(entry.created_at).toLocaleDateString(undefined, { year, month, day })` which is behaviorally identical to `formatDate()` from `@/lib/format.ts`. Replaced with `formatDate` import. Same violation pattern as DRY-157/DRY-173/DRY-188/DRY-223. |
| DRY-286 | `StoryboardParams` pagination struct in storyboard handler duplicates `PaginationQuery` | PRD-62 | `watch` | `StoryboardParams { limit: Option<i64>, offset: Option<i64> }` is structurally identical to `PaginationQuery` in estimation.rs. Extends DRY-276. Now 13+ handler files with identical pagination struct. |
| DRY-287 | `count_for_segment` method structurally duplicated across 3 repos | PRD-62, PRD-69, PRD-78 | `watch` | `KeyframeRepo::count_for_segment`, `GenerationReceiptRepo::count_for_segment`, `SegmentTrimRepo::count_for_segment` are structurally identical (differ only in table name). Same `SELECT COUNT(*) FROM {table} WHERE segment_id = $1` pattern. 3 consumers. Acceptable as thin-adapter repo methods. Watch if more segment-child tables appear. |
| DRY-288 | `formatTimecode(secs)` in storyboard types.ts is a distinct timecode format | PRD-62 | `watch` | `formatTimecode(secs) -> "MM:SS.f"` in `features/storyboard/types.ts` vs `frameToTimecode(frame, fps) -> "HH:MM:SS:FF"` in `features/video-player/frame-utils.ts`. Different input types and output formats (seconds-based with fractional vs frame-based SMPTE). NOT a duplicate -- different purpose. Watch if a 3rd timecode formatter appears; consider unifying under `@/lib/timecodes.ts`. |
| DRY-289 | `validate_receipt_inputs` has 4 sequential positive-value checks | PRD-69 | `watch` | `resolution_width`, `resolution_height`, `steps`, and `cfg_scale` each validated as `> 0` with near-identical code (4x same pattern). Could extract `validate_positive_i32(value, name)` or `validate_positive_f64(value, name)` to `core/src/threshold_validation.rs`. Currently 1 consumer. Extract at 2nd consumer (related to DRY-266 extraction pattern). |
| DRY-290 | Loading/error/empty state triptych repeated across 3 provenance components | PRD-69 | `watch` | `ReceiptPanel.tsx`, `StalenessReport.tsx`, `VersionHistory.tsx` all share identical loading (`Spinner` centered in p-6), error (danger text p-4), and empty state (muted text centered p-4) patterns. 3 consumers in same feature, ~15 lines per component. Extract `QueryStateWrapper<T>({ query, emptyMessage, children })` component if pattern reaches 5+ feature modules. |
| DRY-294 | Inline `new Date().toLocaleString()` in `VersionTimeline.tsx` and `ExecutionLogViewer.tsx` | PRD-63, PRD-77 | `resolved` | `VersionTimeline.tsx` (line 120) and `ExecutionLogViewer.tsx` (line 74) both used `new Date(x).toLocaleString()` instead of `formatDateTime()` from `@/lib/format.ts`. Replaced both with `formatDateTime` import. Same violation pattern as DRY-157/DRY-173/DRY-188/DRY-223/DRY-285. |
| DRY-295 | Cross-boundary constants (MAX_PROMPT_LENGTH, MAX_NEGATIVE_PROMPT_LENGTH, PLACEHOLDER_REGEX) without sync comments | PRD-63 | `resolved` | `features/prompt-editor/types.ts` mirrored 3 constants from `core/src/provenance.rs` and `core/src/prompt_editor.rs` without sync comments. Added JSDoc sync comments pointing to canonical Rust sources. Same cross-boundary pattern as DRY-114/DRY-207/DRY-270/DRY-271. |
| DRY-296 | Cross-boundary `TOKEN_ESTIMATE_MULTIPLIER` without sync comment | PRD-63 | `resolved` | `PromptEditor.tsx` (line 17) mirrored `TOKEN_ESTIMATE_MULTIPLIER = 1.3` from `core/src/prompt_editor.rs` without sync comment. Added JSDoc sync comment. Behavioral duplicate across runtimes is acceptable. |
| DRY-297 | Cross-boundary `WORKFLOW_STATUS` constants without sync comments | PRD-75 | `resolved` | `features/workflow-import/types.ts` mirrored `WORKFLOW_STATUS_ID_*` constants from `core/src/workflow_import.rs` without sync comments. Added JSDoc sync comment. Same pattern as DRY-295. |
| DRY-302 | Badge variant mappers: `workflowStatusVariant`, `failureModeVariant`, `hookTypeVariant` (3 new) | PRD-75, PRD-77 | `watch` | 3 new badge variant mapper functions in Phase 7 Track A: `workflowStatusVariant` (workflow-import/types.ts), `failureModeVariant` (pipeline-hooks/types.ts), `hookTypeVariant` (pipeline-hooks/types.ts). Extends DRY-269/DRY-282 count to 13 feature-level mappers total. All map domain enum to `BadgeVariant`. Domain-specific by design. Approaching extraction threshold. |
| DRY-316 | Shared `useAnnotationInvalidation` callback in annotation mutation hooks | PRD-70 | `resolved` | 3 mutation hooks (`useCreateAnnotation`, `useUpdateAnnotation`, `useDeleteAnnotation`) had identical `onSuccess` callbacks (invalidate bySegment + summary queries). Extracted `useAnnotationInvalidation(segmentId)` private hook returning shared callback. Same pattern as DRY-181 (review mutation hooks). |
| DRY-318 | Color swatch button pattern in `DrawingCanvas.tsx` and `TextLabel.tsx` | PRD-70 | `watch` | Both components render a palette of color swatch buttons with identical structure: `button` with `w-6 h-6 rounded-full border-2` + `ring-2 ring-offset-2` for active state. 2 consumers in same feature. Extract `ColorSwatchPicker` sub-component if a 3rd annotation component needs color selection. |
| DRY-319 | `categoryBadgeVariant` badge variant mapper in `NotesPanel.tsx` | PRD-95 | `watch` | Local `categoryBadgeVariant()` maps note category ("blocker"/"instruction"/"fyi"/"custom") to Badge variant. 15th consumer of badge variant mapper pattern. Extends DRY-269/DRY-282/DRY-302/DRY-308. Domain-specific by design. |
| DRY-303 | Placeholder parsing logic structurally similar in `PromptEditor.tsx` and `LivePreview.tsx` | PRD-63 | `watch` | Both components use same regex (`PLACEHOLDER_REGEX`) to split prompt text into segments for rendering with placeholder highlighting. `highlightPlaceholders()` in PromptEditor (lines 53-73) returns `{ text, isPlaceholder }[]`. `LivePreview` (lines 35-67) returns `{ type, text }[]` with 3 segment types (text/placeholder/resolved). Same split-on-regex pattern, different output shapes. 2 consumers in same feature. Acceptable divergence. |
| DRY-304 | `PaginationParams` struct duplicated across 4 handler files | PRD-74, PRD-43, PRD-10, PRD-77 | `resolved` | Identical `PaginationParams { limit: Option<i64>, offset: Option<i64> }` defined in `project_config.rs`, `integrity.rs`, `webhooks.rs`, `pipeline_hooks.rs`. Extracted to shared `api/src/query.rs`. All 4 consumers refactored to import. Extends DRY-276/DRY-004. |
| DRY-305 | `serde_json::json!` in `project_config_repo.rs` export function | PRD-74 | `watch` | `export_config` method in `project_config_repo.rs` uses `serde_json::json!` to build an export representation. This is data transformation in the repo layer (not an API response envelope), so it's a different use case from DRY-083. Watch for a typed `ExportConfigResponse` DTO if a 2nd consumer appears. |
| DRY-306 | Inline SQL `SELECT COUNT(*)` for segments duplicated 2x in `branching.rs` handler | PRD-50 | `resolved` | `compare_branches` handler had inline `SELECT COUNT(*) FROM segments WHERE branch_id = $1` executed twice with raw `sqlx::query_as`. Extracted to `BranchRepo::count_segments()` repository method. Both calls in handler replaced with repo method. |
| DRY-307 | `validate_config_name` vs `validate_branch_name` structurally similar | PRD-74, PRD-50 | `watch` | Both validate a name string: empty check + length check + regex pattern check. Different patterns and max lengths (config names vs branch names). 2 consumers. Same structural pattern as DRY-242 (`validate_template_name`/`validate_preset_name`). Extract generic `validate_entity_name(name, label, max_len, pattern)` at 3rd consumer. |
| DRY-308 | `effectivenessBadgeVariant` pattern in `PatternDetail.tsx` | PRD-64 | `watch` | Local `effectivenessBadgeVariant()` maps fix effectiveness ("effective"/"partial"/"ineffective") to Badge variant. Extends DRY-269/DRY-282 pattern (now 14 feature-level badge variant mappers). Domain-specific by design. |
| DRY-309 | Frontend `DiffStatus` types not shared between config-templates and branching | PRD-74, PRD-50 | `watch` | `config-templates/types.ts` defines `DiffStatus = "added" \| "changed" \| "unchanged"`. `branching/types.ts` defines `ParameterDiff.status = "added" \| "removed" \| "changed" \| "unchanged"`. Same concept with overlapping values. Backend unified to shared `core/src/diff.rs`. Frontend could share a `DiffStatus` union type from `@/lib/types.ts`. 2 consumers, extract at 3rd. |
| DRY-310 | Diff style/badge mapping structural similarity | PRD-74, PRD-50 | `watch` | `ConfigDiffView.tsx` has `DIFF_STATUS_COLORS` and `DIFF_STATUS_LABELS` maps. `BranchComparison.tsx` has `DIFF_STATUS_COLORS`, `DIFF_STATUS_LABELS`, and `DIFF_STATUS_BADGE_VARIANTS` maps. Same pattern, slightly different keys (branching has "removed"). 2 consumers. If a 3rd diff-display component appears, extract `DIFF_DISPLAY_CONFIG` to shared location. |
| DRY-311 | `validate_frame_number` duplicated in `annotation.rs` and `storyboard.rs` | PRD-70, PRD-62 | `resolved` | Exact copy-paste of `validate_frame_number(frame)` function (8 lines) from `core/src/storyboard.rs` into `core/src/annotation.rs`. Including duplicate unit tests. Resolved by removing duplicate, adding `pub use crate::storyboard::validate_frame_number;` re-export in annotation.rs. |
| DRY-312 | `validate_color_hex` in `annotation.rs` vs `validate_tag_color` in `review.rs` | PRD-70, PRD-38 | `watch` | Both validate hex color strings but with different constraints: `annotation::validate_color_hex` accepts `#RRGGBB` or `#RRGGBBAA` (6 or 8 hex digits after `#`), while `review::validate_tag_color` accepts only `#RRGGBB` (exactly 6). Different error types (`CoreError` vs `String`). 2 consumers. Extract shared `validate_hex_color(color, allow_alpha)` to `core/src/color.rs` at 3rd consumer. Cross-reference comment added to `annotation.rs`. |
| DRY-313 | Fully-qualified `CoreError::NotFound` in `production_notes.rs` handler | PRD-95 | `resolved` | 8 occurrences of `x121_core::error::CoreError::NotFound` without import alias. Added `use x121_core::error::CoreError;`, replaced all fully-qualified paths. Same violation as DRY-231/DRY-274. |
| DRY-314 | Hardcoded `"admin"` and `"reviewer"` role strings in `production_notes.rs` core | PRD-95 | `resolved` | `can_view_note()` used hardcoded `"admin"` and `"reviewer"` string literals instead of `ROLE_ADMIN`/`ROLE_REVIEWER` from `core/src/roles.rs`. Same violation as DRY-013/DRY-090/DRY-184. Added `use crate::roles::{ROLE_ADMIN, ROLE_REVIEWER};` import, replaced literals. |
| DRY-315 | `validate_entity_type` function at 3 core modules with different entity lists | PRD-95, PRD-51, PRD-31 | `watch` | `production_notes::validate_entity_type` (project/character/scene/segment/scene_type/workflow), `undo::validate_entity_type` (scene/segment/character/scene_type), and `command_palette::validate_entity_type` (similar). Same function signature, same validation pattern (`VALID_ENTITY_TYPES.contains()`), but genuinely different allowed entity lists. 3 consumers. Extract generic `validate_entity_type_in(value, valid_list, kind_label) -> Result<(), String>` at 4th consumer. Same structural pattern as DRY-098. |
| DRY-317 | Verbose `.map_err(\|e\| AppError::BadRequest(e))` in `production_notes.rs` handler | PRD-95 | `resolved` | 7 occurrences of `.map_err(\|e\| AppError::BadRequest(e))` instead of method reference `.map_err(AppError::BadRequest)`. Simplified all 7 to use method reference syntax. |
| DRY-320 | `DrawingToolType` enum boilerplate (as_str/from_str/Display/as_db) in `annotation.rs` | PRD-70 | `watch` | Same pattern as DRY-301 (`HookType`, `HookPoint`, `ScopeType`, `FailureMode` in `pipeline_hooks.rs`). 5th enum with identical boilerplate structure. 2 modules. Extract `impl_str_enum!` macro at 3rd module. |
| DRY-355 | `MiniProgressBar` (FooterSegment.tsx) and `ProgressBar` (JobTrayPanel.tsx) structural similarity | PRD-117 | `watch` | 2 consumers. Both render a styled progress bar from a percentage value. Extract shared `ProgressBar` to `components/primitives/` at 3rd consumer. Related to DRY-024. |
| DRY-356 | `formatCost(cents)` in `CloudGpuSegment.tsx` | PRD-117 | `watch` | 1 consumer. Formats cent value to dollar string. Extract to `@/lib/format.ts` when PRD-114 (cloud GPU integration) arrives and needs cost formatting. |

### API & Data Fetching

| ID | Pattern / Component | PRDs Involved | Status | Notes |
|----|---------------------|---------------|--------|-------|
| DRY-040 | React Query / data fetching hooks | All frontend PRDs | `watch` | `useQuery`/`useMutation` patterns will repeat — establish hook conventions early |
| DRY-041 | WebSocket event subscription pattern | PRD-05, PRD-10, PRD-11 | `watch` | Real-time updates from event bus will be consumed by many components |
| DRY-042 | File upload handling | PRD-16, PRD-21, PRD-86 | `watch` | Multiple PRDs accept file uploads — shared upload component + backend handler |
| DRY-043 | CSV/report export pattern | PRD-22, PRD-73, PRD-94 | `watch` | Several PRDs export data as CSV — shared export utility |

### Pipeline & Generation

| ID | Pattern / Component | PRDs Involved | Status | Notes |
|----|---------------------|---------------|--------|-------|
| DRY-060 | FFmpeg command builder | PRD-24, PRD-25, PRD-39, PRD-83 | `resolved` | Implemented in `core/src/ffmpeg.rs`. PRD-24/25/39 will reuse `probe_video`, `extract_frame_thumbnail`, `extract_thumbnails_at_interval`. |
| DRY-091 | 10x `.map_err(\|e\| AppError::InternalError(e.to_string()))` in `handlers/video.rs` | PRD-83 | `watch` | Add `From<FfmpegError>` impl to `AppError` to eliminate 6 of 10. Watch for `From<io::Error>` as more file-serving handlers appear. |
| DRY-061 | Image quality scoring | PRD-22, PRD-49, PRD-76 | `watch` | First consumer (PRD-22) now live. `run_python_script` helper, `determine_status`, `compute_overall_status` in `api/src/handlers/image_qa.rs`. Python scripts in `scripts/python/qa/`. Shared runner in `qa/__init__.py`. When PRD-49/PRD-76 arrive, consider extracting `run_python_script` to a shared module. |
| DRY-070 | QA check type name strings across Rust + Python + SQL | PRD-22 | `watch` | Check type names (`"resolution"`, `"format"`, `"face_detection"`, etc.) appear in migration seed data, Rust handler constants (`CHECKS_*`), and Python script output keys. Currently 3 consumers. If a 4th consumer appears (e.g., frontend), extract to `core/src/qa_checks.rs`. |
| DRY-071 | Python QA script status classification pattern | PRD-22 | `watch` | `"pass" if X else ("warn" if Y else "fail")` ternary appears 7x across 3 scripts. Shared `classify_score()` in `qa/__init__.py` is available but scripts not yet refactored to use it (different threshold logic per check). Refactor when adding new QA scripts. |
| DRY-062 | ComfyUI workflow submission pattern | PRD-05, PRD-24, PRD-58 | `watch` | Workflow dispatch to ComfyUI will be called from multiple orchestrators |
| DRY-340 | `load_env()` function duplicated 3x across `comfyui_generate.py`, `comfyui_batch.py`, `comfyui_serverless.py` | PRD-120 | `flagged` | Byte-identical .env loader. Extracted to `scripts/python/comfyui_common.py`. **Scripts not yet migrated to import from common.** Migrate when next touching these files. |
| DRY-341 | `IMAGE_EXTENSIONS` / `POLL_INTERVAL` / `GENERATION_TIMEOUT` / `COMFYUI_DIR` / `STARTUP_SCRIPT` constants duplicated across 2-3 scripts | PRD-120 | `flagged` | All identical. Extracted to `scripts/python/comfyui_common.py`. Scripts not yet migrated. |
| DRY-342 | SSH helpers (`ssh_run`/`ssh_cmd`, `scp_upload`, `scp_download`) structurally duplicated in `comfyui_generate.py` and `comfyui_batch.py` | PRD-120 | `flagged` | Same SSH opts, same logic. Batch uses globals, generate uses params. Unified parameterized version in `comfyui_common.py`. Scripts not yet migrated. |
| DRY-343 | ComfyUI API functions (upload, queue, history, wait, download) structurally duplicated in `comfyui_generate.py` and `comfyui_batch.py` | PRD-120 | `flagged` | ~200 lines duplicated. Batch uses global `COMFYUI_BASE_URL`, generate passes `base_url`. Unified in `comfyui_common.py` with `base_url` param. Scripts not yet migrated. |
| DRY-344 | `start_comfyui()` function structurally duplicated in `comfyui_generate.py` and `comfyui_batch.py` | PRD-120 | `flagged` | ~50 lines each. Same startup sequence (check running, try startup script, fallback direct start, wait for API). Extracted to `comfyui_common.py`. |
| DRY-345 | Workflow resolution and loading (`resolve_workflow`, `load_workflow`) duplicated in `comfyui_generate.py` and `comfyui_batch.py` | PRD-120 | `flagged` | Same search-dirs + find fallback logic. Extracted to `comfyui_common.py`. |
| DRY-346 | `set_load_image` / `set_input_image_name` duplicated 3x across all three scripts | PRD-120 | `flagged` | Identical LoadImage node search. Extracted to `comfyui_common.py`. |
| DRY-347 | `collect_input_files()` byte-identical in `comfyui_batch.py` and `comfyui_serverless.py` | PRD-120 | `flagged` | Extracted to `comfyui_common.py`. |
| DRY-348 | RunPod GraphQL client + pod lifecycle (get/resume/stop/wait) structurally duplicated in `comfyui_generate.py` and `comfyui_batch.py` | PRD-120 | `flagged` | Batch uses globals, generate passes API key. Unified in `comfyui_common.py` with API key param. |
| DRY-349 | `resolve_scenes()` internally re-implements `parse_scene_spec()` + `apply_scene_spec()` | PRD-120 | `resolved` | Refactored `resolve_scenes()` to delegate to `parse_scene_spec()` + `apply_scene_spec()`, eliminating ~15 lines of duplicated include/exclude parsing. |
| DRY-350 | Preview + dry-run + confirmation block duplicated 2x in `main()` (config mode and batch-dir mode) | PRD-120 | `resolved` | Extracted `_preview_and_confirm()` helper. Both call sites now use 2-line call. |
| DRY-351 | ProgressTracker `start_job`/`complete_job`/`fail_job` repeat 7 identical job metadata fields | PRD-120 | `resolved` | Extracted `_job_fields()` static method. 3 methods now use `**self._job_fields(job)` spread. |
| DRY-352 | `wf_short_name()` computation (`Path(wf).stem.replace("-api", "")`) at 5 call sites | PRD-120 | `resolved` | Extracted `wf_short_name()` helper function. All 5 call sites refactored. |
| DRY-353 | `comfyui_generate.py` at 1797 lines exceeds 500-line threshold by 3.6x | PRD-120 | `watch` | Should be split into modules: scene registry, pod orchestration, job processing, CLI. Defer until next major feature addition. |
| DRY-398 | `scene_type_to_field_map()` in handler layer instead of core/db | PRD-100 | `watch` | Converts `SceneType` DB model to `HashMap<String, serde_json::Value>` for inheritance resolution. Currently in `api/src/handlers/scene_type_inheritance.rs` but should live near the inheritance engine in `core` or as a method on `SceneType` in `db`. Move when `core` gains a dependency on `db` models, or convert to a trait method. |
| DRY-399 | `Mixin` -> `MixinEntry` conversion in handler | PRD-100 | `watch` | `effective_config` handler manually converts `Mixin` (db model) to `MixinEntry` (core type) by deserializing `parameters: serde_json::Value` to `HashMap`. 1 consumer. Add `impl From<Mixin> for MixinEntry` when a 2nd consumer appears. |
| DRY-406 | `auto_retry.rs` handler skips `DataResponse<T>` envelope | PRD-71 | `watch` | Returns raw `Json<RetryAttempt>` and `Json<RetryPolicyResponse>` without `DataResponse<T>` wrapper. `scene_type_inheritance.rs` also skips it. Both are sub-resource handlers. Adopt `DataResponse<T>` when frontend expects `{ data: ... }` envelope. 2 handler modules skip it. |
| DRY-407 | `RetryAttemptScore.status` is `String` not `RetryAttemptStatus` enum | PRD-71 | `watch` | `select_best_attempt` compares `a.status == RetryAttemptStatus::Passed.as_str()` -- string comparison. Could be typed as `RetryAttemptStatus` instead. 1 consumer. |
| DRY-408 | `rand::random::<u32>() as i64` seed generation pattern repeated 2x in `auto_retry.rs` | PRD-71 | `watch` | Lines 145 and 235 of `core/src/auto_retry.rs`. Extract `fn new_seed() -> i64` at 3rd consumer. |
| DRY-434 | `ensure_scene_type_exists` redefined locally in `comparison.rs` instead of importing shared version from `scene_type_inheritance.rs` | PRD-68 | `resolved` | Deleted local, imported from `scene_type_inheritance.rs`. |
| DRY-435 | `ensure_project_exists` in `comparison.rs` uses raw SQL instead of `ProjectRepo::find_by_id` | PRD-68 | `resolved` | Rewritten to use `ProjectRepo::find_by_id`. |
| DRY-436 | `ensure_character_in_project` in `comparison.rs` uses raw SQL instead of `CharacterRepo::find_by_id` | PRD-68 | `resolved` | Rewritten to use `CharacterRepo::find_by_id` + project_id check. |
| DRY-437 | `SceneGallery` and `CharacterAllScenes` are 85% identical -- ~120 lines duplicated | PRD-68 | `resolved` | Extracted shared `GalleryLayout` component. Both components now ~30 lines each (down from ~180). |
| DRY-438 | `APPROVE_ALL_QA_THRESHOLD` and `DEFAULT_SEGMENT_VERSION` duplicated across 2 files | PRD-68 | `resolved` | Moved `APPROVE_ALL_QA_THRESHOLD`, `DEFAULT_SEGMENT_VERSION`, `QA_THRESHOLD_GOOD`, `QA_THRESHOLD_FAIR` to `types.ts`. All consumers import from shared source. |
| DRY-439 | `handleApprove/Reject/Flag` mutation boilerplate duplicated in both gallery components | PRD-68 | `resolved` | Extracted `useGalleryActions()` hook. Both components now use shared hook via `GalleryLayout`. |
| DRY-440 | `APPROVAL_BADGE_VARIANT` partially overlaps `review/types.ts` decision mapping | PRD-68 | `watch` | Same 3 approval statuses mapped to badge variants (comparison) and colors/labels (review). 2 consumers. Consolidate at 3rd consumer. |
| DRY-441 | SQL queries 90% duplicated within `fetch_comparison_rows` (variant-filtered vs unfiltered branches) | PRD-68 | `resolved` | Consolidated into single query with conditional `format!` clause. Eliminated 35 duplicate lines. |
| DRY-442 | `fetch_character_scenes` shares 80% SQL with `fetch_comparison_rows` (same LATERAL join, approval subquery, columns) | PRD-68 | `watch` | Lines 379-416 vs 286-327. Extract shared base SELECT + LATERAL join as constant. 3 total queries share the same structure. |
| DRY-443 | Loading spinner pattern: 1 consumer via `GalleryLayout` (DRY-339 now at 31 total) | PRD-68 | `watch` | GalleryLayout has single spinner. Original 2 consumers merged by DRY-437 extraction. |
| DRY-444 | 3 action buttons in `GalleryCell` differ only by color/icon/handler | PRD-68 | `watch` | Lines 108-132. Data-drive with an array of `{ handler, color, icon, title }` objects. Internal to one file. |
| DRY-445 | `BORDER_BY_STATUS` and `APPROVAL_BADGE_VARIANT` both map same 3 approval statuses | PRD-68 | `watch` | Different visual domains (border class vs badge variant). 2 consumers. Extract `APPROVAL_STATUS_CONFIG` at 3rd visual mapping. |
| DRY-446 | `useDualSync` (PRD-101) vs `useSyncPlay` (PRD-036): two video sync hooks with different paradigms | PRD-101, PRD-036 | `watch` | `useSyncPlay` uses raw refs + drift correction for N-player cinema grids. `useDualSync` uses `useVideoPlayer` + `onFrameChange` for 2-player frame-accurate comparison. Different use cases. Extract shared `useSyncedPlayers` at 3rd sync pattern. |
| DRY-447 | Loading spinner pattern: PRD-101 adds 2 more consumers (DRY-339 now at 34+ total) | PRD-101 | `watch` | `RegenerationComparison.tsx` (py-12, md) + `VersionFilmstrip.tsx` (py-4, sm). `<CenteredSpinner />` extraction overdue. |
| DRY-448 | `formatScore`/`formatDiff` local helpers in `QAScoreComparison.tsx` | PRD-101 | `watch` | `formatDiff` does sign-prefix formatting (`+0.07`). 1 consumer. Extract to `@/lib/format.ts` at 2nd consumer. |
| DRY-449 | `computeOverallTrend` (frontend) vs `overall_quality_improved` (backend): same concept, different granularity | PRD-101 | `watch` | Frontend: ternary (improved/degraded/mixed). Backend: binary (improved/not). Acceptable divergence. |
| DRY-450 | Keyboard shortcut setup with input guard in `ComparisonActions.tsx` | PRD-101 | `watch` | Raw `window.addEventListener('keydown')` with `INPUT/TEXTAREA` guard. 1 consumer. Extract `useKeyboardShortcut(key, handler, opts?)` at 3rd consumer. |
| DRY-453 | `SensitivityProvider` inlines API persistence pattern (load-from-API + debounced save + lastSavedRef dirty check) structurally identical to `useThemePersistence` | PRD-82, PRD-29 | `watch` | `SensitivityProvider.tsx` lines 147-218 duplicate the same 70-line pattern from `hooks/useThemePersistence.ts`: `lastSavedRef`, cancelled load effect, 500ms debounce save effect, JSON.stringify dirty check. ThemeProvider extracted this to a hook; SensitivityProvider inlines it. 2 consumers. Extract shared `useApiPersistence<T>(endpoint, mapToPayload, mapFromResponse, currentState, onLoad)` at 3rd provider. Also adds 7th consumer to DRY-160 debounce timer pattern. |
| DRY-454 | `BlurPreviewCard` (SensitivityDefaults) duplicates placeholder/blur rendering logic from `BlurredMedia` | PRD-82 | `watch` | Both components implement identical conditional: `isPlaceholder ? <User icon in centered flex> : <img with filter: BLUR_CSS[level]>`. `BlurPreviewCard` is static admin preview (no viewContext/video/sensitivity context). 2 consumers. Extract shared presentational `BlurPreview` component at 3rd consumer. |
| DRY-455 | `match option { Some => Ok(Json(DataResponse).into_response()), None => Ok(StatusCode::NO_CONTENT.into_response()) }` repeated 6x in 4 handler files | PRD-82, PRD-29, PRD-52, PRD-32 | `flagged` | Identical 4-line block in: `sensitivity.rs` (2x), `themes.rs` (1x), `keymaps.rs` (1x), `proficiency.rs` (1x). PRD-82 added 2. 6 consumers exceeds 3-instance threshold. Extract `optional_data_response<T: Serialize>(value: Option<T>) -> impl IntoResponse` to `api/src/response.rs`. |
| DRY-456 | `SensitivityDefaults.tsx` uses raw `Loading...` text instead of `<Spinner>` component | PRD-82 | `watch` | Line 62 renders `<p>Loading...</p>` instead of `<Spinner />` from `components/primitives/Spinner.tsx`. 35th consumer of loading pattern (DRY-339). Should use Spinner for visual consistency. |
| DRY-457 | `POSITION_CLASSES` constant name appears in 4 files (Tooltip, Drawer, WatermarkOverlay, QAToolbar) | PRD-82 | `watch` | All 4 map completely different position semantics to Tailwind classes. Module-scoped, no actual duplication. Name collision is harmless. No action needed. |
| DRY-458 | Blur level values `["full", "soft_blur", "heavy_blur", "placeholder"]` defined in 3 tiers (SQL CHECK, Rust BLUR_LEVELS, TS BlurLevel type) | PRD-82 | `watch` | Cross-boundary pattern (same as DRY-079 metric names, DRY-207 variant applicability). Values consistent across all 3 tiers. Add sync comments referencing each other. No action needed. |
| DRY-459 | `maxBlurLevel` (frontend SensitivityProvider) mirrors `enforce_minimum_level` (backend core/sensitivity.rs) | PRD-82 | `watch` | Both implement "return more restrictive of two blur levels" via array index comparison. Intentional cross-boundary behavioral duplication -- frontend for instant UI, backend for authoritative enforcement. No action needed. |
| DRY-460 | `formatScore` / `formatDiff` exact duplicates in `ScoreDiffDisplay.tsx` and `QAScoreComparison.tsx` | PRD-65, PRD-101 | `resolved` | Extracted to `@/lib/qa-constants.ts`. Both consumers updated to import shared versions. |
| DRY-461 | `ScoreDiffDisplay` structural duplicate of `QAScoreComparison` (both iterate QA metrics with colored diffs) | PRD-65, PRD-101 | `watch` | Different visual layout (table vs flex rows). `QAScoreComparison` includes overall trend badge. Shared `formatScore`/`formatDiff` extracted (DRY-460). Components kept separate due to layout differences. Monitor for convergence -- if a third consumer appears, extract shared data layer. |
| DRY-462 | `classify_from_diffs` (regression.rs) duplicates `overall_quality_improved` (segment_comparison.rs) fold/average pattern | PRD-65, PRD-101 | `resolved` | Extracted `average_score_diffs(diffs: &Value) -> Option<f64>` to `segment_comparison.rs`. Both consumers now call it. |
| DRY-463 | `RunReportSummary` (db model) duplicates `RunSummary` (core) with different integer types | PRD-65 | `resolved` | Unified into single `RunSummary` in `core/src/regression.rs` with `i32` fields + `Serialize`. `RunReportSummary` deleted. Handler boilerplate (6 lines of `as i32` casts) eliminated. |
| DRY-464 | `RUN_STATUS_COMPLETED` / `RUN_STATUS_FAILED` name collision between `regression.rs` and `batch_production.rs` | PRD-65, PRD-57 | `watch` | Same constant names and values in different domain modules. Intentional duplication -- different domains. Risk: name collision if both imported in same scope. No action needed. |
| DRY-465 | `VERDICT_BADGE_VARIANTS` typed as `Record<Verdict, string>` instead of `Record<Verdict, BadgeVariant>` | PRD-65 | `resolved` | Fixed to `Record<Verdict, BadgeVariant>`. Removed `as BadgeVariant` cast from `VerdictBadge.tsx`. |
| DRY-466 | `STATUS_BADGE_VARIANT` and `STATUS_LABELS` defined inline in `RunHistoryPanel.tsx` instead of `types.ts` | PRD-65 | `resolved` | Moved to `types.ts` as `RUN_STATUS_BADGE_VARIANT` and `RUN_STATUS_LABELS`. Exported from `index.ts`. |
| DRY-467 | `VERDICT_COLORS` dead code -- defined and exported but never imported | PRD-65 | `resolved` | Removed from `types.ts` and `index.ts`. Was never used by any component. |
| DRY-468 | Loading state text pattern adds 4 more instances (DRY-339 continuation) | PRD-65 | `watch` | `ReferenceManager`, `RunHistoryPanel`, `RegressionReport` (x2) all use `<Card><CardBody><p>Loading X...</p></CardBody></Card>`. Total DRY-339 count now 39+. Extract `<CenteredSpinner />` at next opportunity. |
| DRY-540 | String-list validators (`validate_power_state`/`validate_scope`/`validate_wake_method`) follow identical pattern | PRD-87 | `watch` | Structurally identical to `validate_known_key` in `onboarding.rs`. 50+ validators across codebase use same pattern — project convention. Not actionable. |
| DRY-541 | `update_fleet_settings` handler does not persist settings to DB | PRD-87 | `watch` | Functional concern — handler validates and returns but never writes. Fix when fleet settings storage is designed. |
| DRY-542 | `fmtPct()` in UptimeBar duplicates percentage formatting | PRD-80 | `watch` | `UptimeBar.tsx` formats values already in 0-100 range; shared `formatPercent` expects 0-1 range. Different semantics. Acceptable. |
| DRY-543 | `notification_channels_json` type mismatch (JSONB vs string[]) | PRD-80 | `watch` | Backend stores arbitrary JSONB; frontend assumes string array. Document contract or add typed wrapper. |
| DRY-544 | Magic number `50` for recent check history limit | PRD-80 | `watch` | `handlers/system_health.rs:70` uses `50` inline. Extract to `DEFAULT_RECENT_CHECKS_LIMIT` constant when handler is next touched. |
| DRY-545 | `StatBadge`/`StatItem` duplicated 4+ times across features | PRD-87, PRD-46, PRD-84, PRD-112 | `resolved` | Extracted `StatBadge` to `components/primitives/StatBadge.tsx`. PowerDashboard, ConsumptionSummary, WorkerDashboard updated. |
| DRY-546 | `daysAgo`/`daysAgoIso` duplicated in 2 features | PRD-87, PRD-42 | `resolved` | Extracted `daysAgoDate()` and `daysAgoIso()` to `@/lib/format.ts`. Both consumers updated. |
| DRY-547 | `parse_date` local function duplicates `parse_timestamp` pattern | PRD-87 | `resolved` | Moved to `api/src/query.rs` as shared `parse_date()` alongside `parse_timestamp()`. |
| DRY-548 | `StartupCheckResponse`/`StartupCheckItem` duplicated core `StartupCheck`/`StartupCheckResult` | PRD-80 | `resolved` | Added `Serialize` to core structs. Removed handler duplicates. Handler returns `DataResponse<StartupCheckResult>` directly. |
| DRY-549 | `health_aggregator.rs` uses `"healthy"`/`"down"`/`"degraded"` string literals instead of constants | PRD-80, PRD-117 | `resolved` | Replaced all 8 occurrences with `STATUS_HEALTHY`/`STATUS_DEGRADED`/`STATUS_DOWN` from `x121_core::system_health`. |
| DRY-550 | Frontend `UpdateOffPeakConfig.windows` vs backend `UpdateOffPeakConfigBulk.entries` field name mismatch | PRD-119 | `resolved` | Renamed frontend field from `windows` to `entries` to match backend serde key. Would have caused runtime 422 on every PUT. |
| DRY-551 | Frontend `Schedule` type missing `updated_at` field | PRD-119 | `resolved` | Added `updated_at: string` to match backend `Schedule` Serialize struct. |
| DRY-552 | Frontend `ScheduleHistory` type missing `created_at` field | PRD-119 | `resolved` | Added `created_at: string` to match backend `ScheduleHistory` Serialize struct. |
| DRY-553 | Frontend `OffPeakConfig` type missing `created_at` and `updated_at` fields | PRD-119 | `resolved` | Added both fields to match backend `OffPeakConfig` Serialize struct. |
| DRY-554 | `useSchedules` hook accepts `params` but never passes them to the API URL | PRD-119 | `resolved` | Built query string from params and appended to URL. Filters now reach the backend. |
| DRY-555 | `ScheduleListParams` and `ScheduleHistoryParams` duplicate `PaginationParams` from `api/src/query.rs` | PRD-119 | `watch` | Both structs define `limit: Option<i64>` and `offset: Option<i64>` inline. Should use `#[serde(flatten)] pub pagination: PaginationParams`. Left as watch since functional -- not blocking. |
| DRY-556 | Repeated `find_by_id + ok_or(NotFound) + owner_id check` (6x) in job_scheduling handler | PRD-119 | `resolved` | Extracted `ensure_schedule_owned(pool, id, user_id)` helper. 6 call sites refactored. |
| DRY-557 | `TIMEZONE_SELECT_OPTIONS` duplicated in `ScheduleForm.tsx` and `OffPeakConfigEditor.tsx` | PRD-119 | `resolved` | Moved to single definition in `types.ts`, both consumers import it. |
| DRY-558 | `CRON_DAY_NAMES` in `cron-utils.ts` duplicates `DAY_NAMES` in `types.ts` | PRD-119 | `resolved` | Removed `CRON_DAY_NAMES`, imported `DAY_NAMES` from `types.ts` instead. |
| DRY-559 | Inline JSON validation in `ScheduleForm.tsx` instead of `isValidJson` from `@/lib/validation.ts` | PRD-119 | `watch` | Inline pattern is acceptable here since form needs the parsed result, not just a boolean check. |
| DRY-560 | Verbose `AppError::Core(CoreError::Validation(...))` wrapping in handler | PRD-119 | `watch` | Stylistic -- `CoreError` has `#[from]` so `?` works, but direct construction is also valid. |
| DRY-561 | Off-peak hour validation inline in handler duplicates DB CHECK constraints | PRD-119 | `watch` | Defense-in-depth is acceptable. Could use `validate_count_range` from `core/src/threshold_validation.rs` if a 3rd validator appears. |
| DRY-562 | Magic strings `"recurring"` and `"one_time"` in `compute_next_run_for_schedule` | PRD-119 | `resolved` | Replaced with `SCHEDULE_RECURRING` and `SCHEDULE_ONE_TIME` constants from `core/src/job_scheduling.rs`. |

---

## Resolved

| ID | Original Pattern | Resolution | Shared Location | Date |
|----|-----------------|------------|-----------------|------|
| DRY-509 | `serde_json::json!({ "verified": true })` in `verify_password` handler | Created typed `PasswordVerifiedResponse` struct | `api/src/handlers/shared_link.rs` | 2026-03-01 |
| DRY-510 | Token lookup + validity check duplicated 3x in public handlers | Extracted `ensure_valid_link(pool, token)` helper | `api/src/handlers/shared_link.rs` | 2026-03-01 |
| DRY-511 | Frontend API route mismatch (`/public/shared-links/` vs `/review/`) | Corrected all 3 public hook endpoints to `/review/` | `features/shared-links/hooks/use-shared-links.ts` | 2026-03-01 |
| DRY-512 | Frontend `SharedLink` interface included `skip_serializing` fields (`token_hash`, `password_hash`) | Removed phantom fields from TS type, fixed broken Copy button | `features/shared-links/types.ts` | 2026-03-01 |
| DRY-513 | `useRevokeLink` used POST instead of DELETE (method mismatch) | Changed to `api.delete`, updated test | `features/shared-links/hooks/use-shared-links.ts` | 2026-03-01 |
| DRY-514 | Copy-to-clipboard pattern duplicated 4x across 3 features | Extracted `useCopyToClipboard(resetMs?)` hook, updated 3 consumers | `@/hooks/useCopyToClipboard.ts` | 2026-03-01 |
| DRY-460 | `formatScore`/`formatDiff` duplicated in `ScoreDiffDisplay.tsx` and `QAScoreComparison.tsx` | Extracted to shared `qa-constants.ts`, both consumers updated | `@/lib/qa-constants.ts` | 2026-03-01 |
| DRY-462 | `classify_from_diffs` / `overall_quality_improved` duplicated fold/average pattern | Extracted `average_score_diffs()` shared helper, both consumers refactored | `core/src/segment_comparison.rs` | 2026-03-01 |
| DRY-463 | `RunReportSummary` (db) duplicated `RunSummary` (core) with different int types | Unified into `RunSummary` with `i32` + `Serialize`, deleted `RunReportSummary` | `core/src/regression.rs` | 2026-03-01 |
| DRY-465 | `VERDICT_BADGE_VARIANTS` typed as `string` instead of `BadgeVariant` | Fixed type to `Record<Verdict, BadgeVariant>`, removed cast | `features/regression/types.ts` | 2026-03-01 |
| DRY-466 | `STATUS_BADGE_VARIANT`/`STATUS_LABELS` inline in `RunHistoryPanel.tsx` | Moved to `types.ts` as `RUN_STATUS_BADGE_VARIANT`/`RUN_STATUS_LABELS` | `features/regression/types.ts` | 2026-03-01 |
| DRY-467 | `VERDICT_COLORS` dead code (defined, exported, never used) | Removed from `types.ts` and `index.ts` | `features/regression/types.ts` | 2026-03-01 |
| DRY-545 | `StatBadge`/`StatItem` duplicated 4+ times across features | Extracted shared `StatBadge` component to primitives | `components/primitives/StatBadge.tsx` | 2026-03-01 |
| DRY-546 | `daysAgo`/`daysAgoIso` date utility duplicated in 2 features | Extracted `daysAgoDate()` and `daysAgoIso()` to shared format library | `@/lib/format.ts` | 2026-03-01 |
| DRY-547 | `parse_date` local function in gpu_power handler | Moved to shared `api/src/query.rs` alongside existing `parse_timestamp()` | `api/src/query.rs` | 2026-03-01 |
| DRY-548 | `StartupCheckResponse`/`StartupCheckItem` duplicated core structs in handler | Added `Serialize` to core structs, handler returns core types directly | `core/src/system_health.rs` | 2026-03-01 |
| DRY-549 | Health aggregator string literals instead of core constants | Replaced 8 raw strings with `STATUS_HEALTHY`/`STATUS_DEGRADED`/`STATUS_DOWN` imports | `api/src/engine/health_aggregator.rs` | 2026-03-01 |
| DRY-550 | `UpdateOffPeakConfig.windows` vs `UpdateOffPeakConfigBulk.entries` field name mismatch | Renamed frontend field from `windows` to `entries` to match backend serde key | `features/job-scheduling/types.ts` | 2026-03-01 |
| DRY-551 | Frontend `Schedule` type missing `updated_at` | Added `updated_at: string` to match backend Serialize struct | `features/job-scheduling/types.ts` | 2026-03-01 |
| DRY-552 | Frontend `ScheduleHistory` type missing `created_at` | Added `created_at: string` to match backend Serialize struct | `features/job-scheduling/types.ts` | 2026-03-01 |
| DRY-553 | Frontend `OffPeakConfig` missing `created_at` and `updated_at` | Added both timestamp fields | `features/job-scheduling/types.ts` | 2026-03-01 |
| DRY-554 | `useSchedules` hook ignores `params` in API call | Built URLSearchParams from params, appended to URL | `features/job-scheduling/hooks/use-job-scheduling.ts` | 2026-03-01 |
| DRY-556 | 6x repeated `find_by_id + NotFound + owner_id check` | Extracted `ensure_schedule_owned(pool, id, user_id)` helper | `api/src/handlers/job_scheduling.rs` | 2026-03-01 |
| DRY-557 | `TIMEZONE_SELECT_OPTIONS` duplicated in 2 files | Single definition in `types.ts`, both consumers import | `features/job-scheduling/types.ts` | 2026-03-01 |
| DRY-558 | `CRON_DAY_NAMES` duplicates `DAY_NAMES` | Removed duplicate, imported from `types.ts` | `features/job-scheduling/cron-utils.ts` | 2026-03-01 |
| DRY-562 | Magic strings `"recurring"`/`"one_time"` in handler helper | Replaced with `SCHEDULE_RECURRING`/`SCHEDULE_ONE_TIME` constants | `api/src/handlers/job_scheduling.rs` | 2026-03-01 |
| DRY-451 | Diff overlay JSX duplicated in `RegenerationComparison.tsx` (inline) AND `DiffOverlay.tsx` (component) | Split `DiffOverlay` into `DiffOverlayToggle` + `DiffOverlayPanel`; removed inline duplicate from `RegenerationComparison.tsx` | `features/segment-comparison/DiffOverlay.tsx` | 2026-02-28 |
| DRY-012 | `resolve_role_name` duplicated in auth.rs + admin.rs | Extracted to `RoleRepo::resolve_name()` | `db/src/repositories/role_repo.rs` | 2026-02-20 |
| DRY-013 | Role magic strings hardcoded in rbac.rs | Created `ROLE_ADMIN/CREATOR/REVIEWER` constants | `core/src/roles.rs` | 2026-02-20 |
| DRY-019 | Channel name magic strings in router, digest, handlers | Created `CHANNEL_IN_APP/DIGEST/WEBHOOK/EMAIL` constants | `core/src/channels.rs` | 2026-02-20 |
| DRY-072 | QA status magic strings (`"pass"`, `"warn"`, `"fail"`) in Rust handler | Created `QA_PASS/QA_WARN/QA_FAIL` constants + Python `QA_PASS/QA_WARN/QA_FAIL` | `core/src/qa_status.rs`, `scripts/python/qa/__init__.py` | 2026-02-20 |
| DRY-073 | Python QA script `__main__` boilerplate (13 lines x 3 scripts) | Extracted `run_check_cli()` shared runner | `scripts/python/qa/__init__.py` | 2026-02-20 |
| DRY-075 | Import status magic strings + `status_id != 1` magic number | Created `IMPORT_STATUS_*` constants + `IMPORT_STATUS_PREVIEW_ID` | `core/src/import_status.rs` | 2026-02-20 |
| DRY-080 | `DataResponse<T>` typed response envelope local to `hardware.rs` | Extracted to shared `api/src/response.rs` module | `api/src/response.rs` | 2026-02-21 |
| DRY-084 | Script type name magic strings in orchestrator.rs | Created `SCRIPT_TYPE_SHELL/PYTHON/BINARY` constants | `core/src/script_types.rs` | 2026-02-21 |
| DRY-060 | FFmpeg command builder duplicated across PRDs | Shared `probe_video`, `extract_frame_thumbnail`, `extract_thumbnails_at_interval` | `core/src/ffmpeg.rs` | 2026-02-21 |
| DRY-087 | `AudioTrackInfo` struct duplicated in core + db | Removed duplicate from `db/models/video.rs`, re-export from `core::ffmpeg` | `core/src/ffmpeg.rs` | 2026-02-21 |
| DRY-088 | `"storage/thumbnails"` path literal duplicated 2x in handler | Extracted `THUMBNAIL_STORAGE_DIR` constant + `thumbnail_dir()` helper | `api/src/handlers/video.rs` | 2026-02-21 |
| DRY-089 | "Find first video stream" iteration pattern 5x in ffmpeg.rs | Extracted `first_video_stream()` private helper | `core/src/ffmpeg.rs` | 2026-02-21 |
| DRY-090 | `ADMIN_ROLE` constant redefined in jobs.rs (duplicate of `ROLE_ADMIN`) | Replaced local constant with import of `ROLE_ADMIN` from `core/src/roles.rs` | `core/src/roles.rs` (existing) | 2026-02-21 |
| DRY-092 | `list_by_user` / `list_all` structural duplicate in job_repo.rs | Extracted shared `list_jobs()` private method | `db/src/repositories/job_repo.rs` | 2026-02-21 |
| DRY-094 | Magic status_id numbers `1, 2` in dispatcher.rs SQL | Replaced with `JobStatus::Pending.id()` / `JobStatus::Running.id()` binds | `api/src/engine/dispatcher.rs` | 2026-02-21 |
| DRY-095 | Job WS message type magic strings in progress.rs | Created `MSG_TYPE_JOB_*` constants | `core/src/job_events.rs` | 2026-02-21 |
| DRY-096 | serde_json + Message::Text + broadcast repeated 4x | Extracted `broadcast_json()` helper | `api/src/engine/progress.rs` | 2026-02-21 |
| DRY-097 | Find + authorize ownership pattern 3x in jobs.rs | Extracted `find_and_authorize()` helper | `api/src/handlers/jobs.rs` | 2026-02-21 |
| DRY-099 | `TagSuggestion` structurally identical to `TagWithCount` | Unified to single `TagWithCount` type exported from `TagChip.tsx` | `src/components/domain/TagChip.tsx` | 2026-02-21 |
| DRY-101 | Inline `prefix.trim().to_lowercase()` in `suggest()` | Replaced with call to existing `normalize_tag_name()` | `db/src/repositories/tag_repo.rs` | 2026-02-21 |
| DRY-017 | Middleware/router construction duplicated in main.rs and tests | Extracted `build_app_router()` to `x121_api::router` module | `api/src/router.rs` | 2026-02-21 |
| DRY-108 | `TokenEditor.tsx` 670-line god component with 7 section components + 6x duplicate updaters | Split sections to `token-editor/TokenSections.tsx`, replaced 6 updaters with generic `updateTokenSection<K>()` | `features/admin/token-editor/TokenSections.tsx` | 2026-02-21 |
| DRY-109 | `TagWithCount` redundantly re-exported from `TagInput.tsx` and `TagFilter.tsx` | Removed re-exports; canonical source is `TagChip.tsx` | `components/domain/TagChip.tsx` (existing) | 2026-02-21 |
| DRY-110 | `CATEGORY_LABELS` + `CATEGORY_ORDER` duplicated in CheatSheet + KeymapEditor | Extracted to `ShortcutRegistry.ts` with `groupBindingsByCategory()` utility | `features/shortcuts/ShortcutRegistry.ts` | 2026-02-21 |
| DRY-111 | `snapValue()` duplicated in `useSnapGrid.ts` + `usePanelResize.ts` | Exported from `useSnapGrid.ts`, imported in `usePanelResize.ts` | `features/layout/useSnapGrid.ts` | 2026-02-21 |
| DRY-116 | `export_keymap` handler duplicate JSON branches | Extracted shared destructuring with fallback defaults | `api/src/handlers/keymaps.rs` | 2026-02-21 |
| DRY-117 | `formatFileSize()` duplicated in AssetBrowser + AssetDetail | Removed duplicate, replaced with `formatBytes` from shared `@/lib/format.ts` | `src/lib/format.ts` (existing) | 2026-02-21 |
| DRY-118 | `formatDate()` inline helper in AssetDetail.tsx | Extracted to shared `formatDate()` in `@/lib/format.ts` | `src/lib/format.ts` | 2026-02-21 |
| DRY-119 | `serde_json::json!` (11x) in reclamation.rs instead of `DataResponse<T>` | Migrated all 11 endpoints to `DataResponse<T>` with `impl IntoResponse` return | `api/src/handlers/reclamation.rs` | 2026-02-21 |
| DRY-120 | Column list repeated 15x in reclamation_repo.rs | Extracted `RULE_COLUMNS`, `POLICY_COLUMNS`, `TRASH_COLUMNS`, `RUN_COLUMNS` constants | `db/src/repositories/reclamation_repo.rs` | 2026-02-21 |
| DRY-121 | `verify_exists + NotFound` guard 4x in assets.rs handler | Extracted `ensure_asset_exists(pool, id)` private helper | `api/src/handlers/assets.rs` | 2026-02-21 |
| DRY-102 | Click-outside pattern 3x (TagInput, Dropdown, JobTrayPanel) | Extracted `useClickOutside(ref, callback, enabled?)` hook | `src/hooks/useClickOutside.ts` | 2026-02-21 |
| DRY-127 | `formatTime()` duplicated in CinemaMode + GridControls | Removed locals, replaced with `formatDuration` from `video-player/frame-utils` | `features/video-player/frame-utils.ts` (existing) | 2026-02-21 |
| DRY-128 | Recharts Tooltip contentStyle 7x across 3 chart components | Extracted `TOOLTIP_CONTENT_STYLE`, `AXIS_TICK_STYLE`, `GRID_STROKE` | `features/dashboard/performance/chartStyles.ts` | 2026-02-21 |
| DRY-129 | `formatElapsed()` reimplements formatDuration logic | Refactored to delegate to shared `formatDuration(seconds*1000)` | `src/lib/format.ts` (existing) | 2026-02-21 |
| DRY-138 | `enable_extension`/`disable_extension` structural duplicate (22 identical lines) | Extracted shared `toggle_extension(state, id, user_id, enabled)` helper | `api/src/handlers/extensions.rs` | 2026-02-21 |
| DRY-139 | Textarea styling 9-line `cn()` block duplicated in SettingsEditor + InstallForm | Extracted `TEXTAREA_BASE_CLASSES` + `textareaBorderClass()` | `features/extensions/ExtensionManager.tsx` | 2026-02-21 |
| DRY-140 | Frontend `methodToAccess` mapped POST/PUT/DELETE to "create"/"update"/"delete" but backend `KNOWN_ACCESS_LEVELS` only defines `["read", "write"]` | Aligned frontend to use "write" for all mutating methods | `features/extensions/ExtensionApiBridge.ts` | 2026-02-21 |
| DRY-141 | `formatFileSize()` in `SourceImageUpload.tsx` duplicated `formatBytes()` from `@/lib/format.ts` | Removed local, replaced with `formatBytes` import | `src/lib/format.ts` (existing) | 2026-02-21 |
| DRY-142 | `formatDate()` local in `VariantHistory.tsx` duplicated `formatDateTime()` from `@/lib/format.ts` | Removed local, replaced with `formatDateTime` import | `src/lib/format.ts` (existing) | 2026-02-21 |
| DRY-143 | `"storage/variants"` path literal duplicated 2x in `image_variant.rs` handler | Extracted `VARIANT_STORAGE_DIR` constant + `ensure_variant_dir()` helper | `api/src/handlers/image_variant.rs` | 2026-02-21 |
| DRY-135 | `renderWithProviders` test helper duplicated across 6 test files | Extracted to shared `src/lib/test-utils.tsx`, all 6 consumers refactored | `src/lib/test-utils.tsx` | 2026-02-21 |
| DRY-145 | `image_variant.rs` handler used raw `Json<T>` instead of `DataResponse<T>` (10 endpoints) | Migrated all endpoints to `DataResponse<T>` with `impl IntoResponse` | `api/src/handlers/image_variant.rs` | 2026-02-21 |
| DRY-151 | `formatBytes()` duplicated in `checkpoints/types.ts` (behavioral duplicate of `@/lib/format.ts`) | Removed local, re-exported from `@/lib/format` | `src/lib/format.ts` (existing) | 2026-02-21 |
| DRY-152 | `find_and_authorize_job` in `checkpoints.rs` -- line-for-line copy of `find_and_authorize` in `jobs.rs` | Made `jobs::find_and_authorize` pub, imported in checkpoints.rs | `api/src/handlers/jobs.rs` (existing) | 2026-02-21 |
| DRY-153 | Raw `INSERT INTO jobs` SQL with inline COLUMNS in `checkpoints.rs` handler | Extracted `JobRepo::resume_from_checkpoint()` method, handler now delegates to repo | `db/src/repositories/job_repo.rs` | 2026-02-21 |
| DRY-155 | Query/count filter duplication (45+ identical lines) in `audit_repo.rs` | Extracted `build_audit_filter()` + `bind_audit_values()` shared helpers | `db/src/repositories/audit_repo.rs` | 2026-02-21 |
| DRY-156 | `serde_json::json!` in `checkpoints.rs` diagnostics + `audit.rs` query response | Replaced with typed `JobDiagnosticsResponse` struct and `AuditLogPage` struct | `api/src/handlers/checkpoints.rs`, `db/src/models/audit.rs` | 2026-02-21 |
| DRY-163 | SearchBar.tsx inline click-outside (addEventListener/removeEventListener) | Replaced with shared `useClickOutside` hook import | `src/hooks/useClickOutside.ts` (existing) | 2026-02-21 |
| DRY-164 | Duplicate search execution logic in `unified_search` + `execute_saved_search` | Extracted `run_search(pool, params)` private async helper | `api/src/handlers/search.rs` | 2026-02-21 |
| DRY-165 | Duplicate term sanitization in `build_tsquery` + `build_prefix_tsquery` | Extracted `sanitize_terms(query)` private helper | `core/src/search.rs` | 2026-02-21 |
| DRY-166 | Webhook handler inline pagination clamping (`unwrap_or(50).min(200)`) | Replaced with `clamp_limit`/`clamp_offset` from core | `core/src/search.rs` (existing) | 2026-02-21 |
| DRY-191 | `handleTourComplete` and `handleTourSkip` identical callbacks in `OnboardingGate.tsx` | Merged into single `handleTourEnd` callback | `features/onboarding/OnboardingGate.tsx` | 2026-02-21 |
| DRY-192 | `PLACEMENT_CLASSES` duplicated in `ContextualHint.tsx` (identical to Tooltip.tsx `POSITION_CLASSES`) | Exported `Placement` type and `PLACEMENT_CLASSES` from `Tooltip.tsx`, imported in ContextualHint | `components/primitives/Tooltip.tsx` | 2026-02-21 |
| DRY-193 | Placement union type `"top"\|"bottom"\|"left"\|"right"` repeated 4x in onboarding | Extracted shared `Placement` type from Tooltip, used in `types.ts`, `ContextualHint.tsx`, `hintDefinitions.ts` | `components/primitives/Tooltip.tsx`, `features/onboarding/types.ts` | 2026-02-21 |
| DRY-194 | Redundant `idx_user_onboarding_user_id` index duplicates unique index on same column | Removed redundant plain index (unique index provides same coverage) | `migrations/20260221000052_create_user_onboarding.sql` | 2026-02-21 |
| DRY-195 | Dismiss-button styling class repeated 3x across onboarding components | Extracted `DISMISS_LINK_CLASSES` constant to `types.ts`, used in TourEngine, OnboardingChecklist, ContextualHint | `features/onboarding/types.ts` | 2026-02-21 |
| DRY-196 | `HintDefinition` interface defined in `hintDefinitions.ts` instead of canonical `types.ts` | Moved type to `types.ts`, re-exported from `hintDefinitions.ts` | `features/onboarding/types.ts` | 2026-02-21 |
| DRY-197 | `validate_checklist_item`/`validate_feature_key` structurally identical validation patterns | Extracted generic `validate_known_key`/`validate_known_keys` private helpers | `core/src/onboarding.rs` | 2026-02-21 |
| DRY-198 | `get_or_create` used 2-query pattern (INSERT DO NOTHING + SELECT fallback) | Refactored to single-query `DO UPDATE SET user_id = user_onboarding.user_id` pattern (matches `workspace_repo.rs`) | `db/src/repositories/onboarding_repo.rs` | 2026-02-21 |
| DRY-219 | `formatBytes()` duplicated locally in `BackendConfigPanel.tsx` and `MigrationProgressView.tsx` | Removed both local copies, replaced with import from `@/lib/format.ts` | `src/lib/format.ts` (existing) | 2026-02-22 |
| DRY-220 | `StorageBackendStatus` and `StorageMigrationStatus` enums duplicated in `core/src/storage.rs` | Removed dead code from core (canonical source is `db/src/models/status.rs` via `define_status_enum!` macro). Added sync comment. | `db/src/models/status.rs` (existing) | 2026-02-22 |
| DRY-221 | 4x `.ok_or(NotFound{entity:"LibraryCharacter"})` inline in `library.rs` handler | Extracted `ensure_library_character_exists(pool, id)` private helper (DRY-097/DRY-121 pattern) | `api/src/handlers/library.rs` | 2026-02-22 |
| DRY-222 | 3x `.ok_or(NotFound{entity:"StorageMigration"})` inline in `storage.rs` handler | Extracted `ensure_migration_exists(pool, id)` private helper (DRY-097/DRY-121 pattern) | `api/src/handlers/storage.rs` | 2026-02-22 |
| DRY-223 | Inline `toLocaleDateString()` in `LibraryUsagePanel.tsx` instead of `formatDate` | Replaced with `formatDate` import from `@/lib/format.ts` | `src/lib/format.ts` (existing) | 2026-02-22 |
| DRY-224 | Inline `toLocaleString()` in `MigrationProgressView.tsx` and `StatePreview.tsx` | Replaced with `formatDateTime` import from `@/lib/format.ts`. StatePreview converts epoch ms to ISO string before passing. | `src/lib/format.ts` (existing) | 2026-02-22 |
| DRY-227 | `ensure_segment_exists` duplicated in `approval.rs` and `review_notes.rs` | Extracted to public function in `segment.rs`, imported by both handlers. Also resolves DRY-180. | `api/src/handlers/segment.rs` | 2026-02-22 |
| DRY-228 | `statusBadgeVariant()` duplicated in `NoteTimeline.tsx` and `ReviewThread.tsx` | Extracted to `review-notes/types.ts`, both consumers import | `features/review-notes/types.ts` | 2026-02-22 |
| DRY-229 | Card panel styling repeated 5x in debugger feature | Extracted `DEBUGGER_CARD_CLASSES` to `debugger/types.ts`, all 4 component files updated | `features/debugger/types.ts` | 2026-02-22 |
| DRY-230 | Textarea styling duplicated in 2 debugger components | Extracted `DEBUGGER_TEXTAREA_BASE` to `debugger/types.ts`, both consumers updated | `features/debugger/types.ts` | 2026-02-22 |
| DRY-231 | Fully-qualified `CoreError::NotFound` in `review_notes.rs` and `approval.rs` | Added `use CoreError` import, replaced fully-qualified paths | `api/src/handlers/review_notes.rs`, `api/src/handlers/approval.rs` | 2026-02-22 |
| DRY-246 | `formatFileSize()` local in `ExportHistory.tsx` duplicated `formatBytes()` from `@/lib/format.ts` | Removed local, replaced with `formatBytes` import | `src/lib/format.ts` (existing) | 2026-02-22 |
| DRY-247 | `formatDate()` local in `ExportHistory.tsx` duplicated `formatDateTime()` from `@/lib/format.ts` | Removed local, replaced with `formatDateTime` import | `src/lib/format.ts` (existing) | 2026-02-22 |
| DRY-248 | Inline pagination clamping in `delivery.rs` handler (`unwrap_or(50).min(200)`) | Replaced with `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Aligned types to i64. | `core/src/search.rs` (existing) | 2026-02-22 |
| DRY-249 | Magic `status_id` numbers 6 and 7 in `delivery_export_repo.rs` SQL | Added `EXPORT_STATUS_ID_COMPLETED`/`EXPORT_STATUS_ID_FAILED` constants to `core/src/assembly.rs`, imported in repo | `core/src/assembly.rs`, `db/src/repositories/delivery_export_repo.rs` | 2026-02-22 |
| DRY-250 | Dead code `QaScoreSummary` in `db/src/models/quality_score.rs` -- identical to `core::quality_gate::QaSummary` | Removed unused struct, added reference comment pointing to canonical `core::quality_gate::QaSummary` | `core/src/quality_gate.rs` (existing) | 2026-02-22 |
| DRY-251 | Inline pagination in `integrity.rs` handler -- local `MAX_LIMIT`/`DEFAULT_LIMIT` instead of `clamp_limit`/`clamp_offset` | Removed local constants, replaced with `clamp_limit`/`clamp_offset` from `core/src/search.rs` | `core/src/search.rs` (existing) | 2026-02-23 |
| DRY-252 | Inline pagination in `duplicates.rs` handler -- no max clamping at all | Replaced with `clamp_limit`/`clamp_offset` from `core/src/search.rs` | `core/src/search.rs` (existing) | 2026-02-23 |
| DRY-253 | Inline pagination in `downloads.rs` handler -- wrong max (200 instead of 100) | Replaced with `clamp_limit`/`clamp_offset` from `core/src/search.rs` | `core/src/search.rs` (existing) | 2026-02-23 |
| DRY-254 | Magic `status_id` numbers 6 and 7 in `model_download_repo.rs` SQL (`mark_completed`, `mark_failed`, `check_duplicate_hash`) | Parameterized SQL, bound `DownloadStatus::Completed.id()` and `DownloadStatus::Failed.id()` | `db/src/models/status.rs` (existing) | 2026-02-23 |
| DRY-255 | Magic `status_id = 2` in `duplicate_check_repo.rs` `list_pending` SQL | Parameterized SQL, bound `duplicate_detection::STATUS_MATCH_FOUND_ID` | `core/src/duplicate_detection.rs` (existing) | 2026-02-23 |
| DRY-256 | Inline byte formatting in `ModelChecksumManager.tsx` (`/ (1024*1024*1024)).toFixed(2) GB`) | Replaced with `formatBytes()` from `@/lib/format.ts` | `src/lib/format.ts` (existing) | 2026-02-23 |
| DRY-257 | Inline `toLocaleString()` in `ScanHistory.tsx` instead of `formatDateTime` | Replaced with `formatDateTime` from `@/lib/format.ts` | `src/lib/format.ts` (existing) | 2026-02-23 |
| DRY-258 | `serde_json::json!` in `get_worker_report` integrity handler | Created typed `WorkerReportResponse` struct, handler returns `DataResponse<Option<WorkerReportResponse>>` | `api/src/handlers/integrity.rs` | 2026-02-23 |
| DRY-259 | 3 repair handlers (`repair_worker`, `sync_models`, `install_nodes`) structurally identical | Extracted shared `trigger_scan(state, auth, worker_id, scan_type)` helper, all 5 scan endpoints use it | `api/src/handlers/integrity.rs` | 2026-02-23 |
| DRY-260 | `start_scan` and `start_worker_scan` near-identical (differ only in worker_id source) | Both now delegate to shared `trigger_scan` helper (DRY-259) | `api/src/handlers/integrity.rs` | 2026-02-23 |

| DRY-261 | `generateSlug` local in `WikiArticleEditor.tsx` duplicated slug generation logic | Extracted to `@/lib/format.ts` as shared `generateSlug()` with sync comment noting backend canonical version in `core/src/wiki.rs` | `src/lib/format.ts` | 2026-02-23 |
| DRY-262 | `DEFAULT_SIMILARITY_THRESHOLD` name collision between `search.rs` (0.5) and `duplicate_detection.rs` (0.90) | Renamed to domain-specific: `DEFAULT_SEARCH_SIMILARITY` (search.rs) and `DEFAULT_DUPLICATE_SIMILARITY` (duplicate_detection.rs). Updated consumer in `search_repo.rs` | `core/src/search.rs`, `core/src/duplicate_detection.rs` | 2026-02-23 |
| DRY-263 | `formatSpeed`/`estimateEta` local helpers in `DownloadItem.tsx` | Extracted to `@/lib/format.ts` as shared `formatSpeed()` and `estimateEta()`. `DownloadItem.tsx` now imports from shared. | `src/lib/format.ts` | 2026-02-23 |
| DRY-264 | `serde_json::json!` in 3 production_run handler endpoints | Created `SubmitCellsResponse`, `ResubmitResponse`, `DeliverResponse` typed structs. All 3 endpoints now use `DataResponse<T>`. | `db/src/models/production_run.rs` | 2026-02-23 |
| DRY-265 | Magic `status_id` numbers in production_run repo and handler | Parameterized SQL and replaced all magic numbers with `RUN_STATUS_ID_*` constants from `core/src/batch_production.rs`. | `core/src/batch_production.rs` | 2026-02-23 |
| DRY-266 | `validate_unit_range` pattern duplicated 5x across core modules | Extracted `validate_unit_range(value, name)` to `core/src/threshold_validation.rs`. 5 consumers refactored to delegate. | `core/src/threshold_validation.rs` | 2026-02-23 |
| DRY-274 | Fully-qualified `CoreError::NotFound` in resolution.rs handler | Added import, extracted `ensure_scene_exists` and `ensure_tier_exists` helpers. | `api/src/handlers/resolution.rs` | 2026-02-23 |
| DRY-275 | Redundant `.map_err(AppError::Core)?` in resolution.rs/estimation.rs | Replaced 5 instances with plain `?` (AppError has `#[from] CoreError`). | `api/src/handlers/resolution.rs`, `estimation.rs` | 2026-02-23 |
| DRY-277 | `validate_batch_size`/`validate_estimate_count` structural duplicates | Extracted `validate_count_range(count, max, label)` to shared threshold_validation.rs. | `core/src/threshold_validation.rs` | 2026-02-23 |
| DRY-278 | `confidenceBadgeVariant` inline union return type | Changed to `BadgeVariant` import from primitives. | `features/estimation/types.ts` | 2026-02-23 |
| DRY-279 | `TestShotButton.tsx` hand-rolled modal overlay | Replaced with `Modal` from `@/components/composite`. | `features/test-shots/TestShotButton.tsx` | 2026-02-23 |
| DRY-280 | Magic `3600`/`1024` in EstimationCard.tsx | Added `SECS_PER_HOUR`/`MB_PER_GB` constants with sync comments to canonical Rust source. | `features/estimation/EstimationCard.tsx` | 2026-02-23 |
| DRY-283 | `frameToTimecode(frame, fps)` exact duplicate in `TrimTimeline.tsx` | Removed local 8-line copy, replaced with import from `@/features/video-player/frame-utils.ts`. | `features/video-player/frame-utils.ts` (existing) | 2026-02-23 |
| DRY-285 | Inline `toLocaleDateString()` in `VersionHistory.tsx` | Replaced with `formatDate(entry.created_at)` from `@/lib/format.ts`. Same pattern as DRY-157/DRY-173/DRY-188/DRY-223. | `src/lib/format.ts` (existing) | 2026-02-23 |
| DRY-302/PaginationParams | `PaginationParams` struct duplicated in 4 handler files | Extracted to shared `api/src/query.rs` module. All 4 consumers (`project_config.rs`, `integrity.rs`, `webhooks.rs`, `pipeline_hooks.rs`) refactored to import. | `api/src/query.rs` | 2026-02-23 |
| DRY-303/DiffStatus | `DiffStatus` enum duplicated in `project_config.rs` and `branching.rs` | Created shared `DiffStatus` enum in `core/src/diff.rs` with 4 variants (Added, Removed, Changed, Unchanged). Both modules import. | `core/src/diff.rs` | 2026-02-23 |
| DRY-304/TrendPointResponse | `serde_json::json!` for trend response in `failure_analytics.rs` | Created typed `TrendPointResponse` DTO in `db/src/models/failure_pattern.rs`. Handler uses struct instead of JSON macro. | `db/src/models/failure_pattern.rs` | 2026-02-23 |
| DRY-306/BranchSegmentCount | Inline `SELECT COUNT(*)` for branch segments 2x in `branching.rs` handler | Added `BranchRepo::count_segments()` repository method. Handler calls repo method. | `db/src/repositories/branch_repo.rs` | 2026-02-23 |
| DRY-311 | `validate_frame_number` exact duplicate in `annotation.rs` (copy-paste from `storyboard.rs`) | Removed duplicate function + 4 duplicate tests. Added `pub use crate::storyboard::validate_frame_number;` re-export. | `core/src/annotation.rs` (re-export from `core/src/storyboard.rs`) | 2026-02-23 |
| DRY-313 | Fully-qualified `CoreError::NotFound` 8x in `production_notes.rs` handler | Added `use x121_core::error::CoreError;` import, replaced all 8 fully-qualified paths. | `api/src/handlers/production_notes.rs` | 2026-02-23 |
| DRY-314 | Hardcoded `"admin"`/`"reviewer"` in `can_view_note()` instead of role constants | Added `use crate::roles::{ROLE_ADMIN, ROLE_REVIEWER};`, replaced 3 string literals. | `core/src/production_notes.rs` | 2026-02-23 |
| DRY-316 | Identical `onSuccess` invalidation in 3 annotation mutation hooks | Extracted `useAnnotationInvalidation(segmentId)` private hook returning shared callback. | `features/annotations/hooks/use-annotations.ts` | 2026-02-23 |
| DRY-317 | Verbose `.map_err(\|e\| AppError::BadRequest(e))` 7x in `production_notes.rs` handler | Simplified all 7 to method reference `.map_err(AppError::BadRequest)`. | `api/src/handlers/production_notes.rs` | 2026-02-23 |
| DRY-321 | 7x inline `find_by_id + NotFound` in `legacy_import.rs` handler without helper | Extracted `ensure_run_exists(pool, id)` private helper. All 7 handler functions refactored. | `api/src/handlers/legacy_import.rs` | 2026-02-23 |
| DRY-322 | Raw `params.limit`/`params.offset` without clamping in `list_runs`/`list_entity_logs` | Added `clamp_limit`/`clamp_offset` from `core/src/search.rs`. | `api/src/handlers/legacy_import.rs` | 2026-02-23 |
| DRY-323 | 6x verbose `.map_err(\|e\| AppError::BadRequest(e))` in `batch_metadata.rs` handler | Simplified all 6 to method reference `.map_err(AppError::BadRequest)`. | `api/src/handlers/batch_metadata.rs` | 2026-02-23 |
| DRY-324 | `serde_json::json!` for run report response in `legacy_import.rs` handler | Created typed `RunReportResponse`/`ActionCountEntry` DTOs in handler. Uses `DataResponse<T>`. | `api/src/handlers/legacy_import.rs` | 2026-02-23 |
| DRY-325 | `serde_json::json!` for readiness summary in `readiness.rs` handler | Replaced with existing `readiness::ReadinessSummary` typed struct from core. | `api/src/handlers/readiness.rs` | 2026-02-23 |
| DRY-349 | `resolve_scenes()` re-implemented `parse_scene_spec()` + `apply_scene_spec()` inline | Refactored to delegate to existing functions, eliminating ~15 lines. | `scripts/python/comfyui_generate.py` | 2026-02-27 |
| DRY-350 | Preview + dry-run + confirmation block duplicated in 2 CLI modes | Extracted `_preview_and_confirm()` helper. Both sites reduced to 2 lines. | `scripts/python/comfyui_generate.py` | 2026-02-27 |
| DRY-351 | 7 job metadata fields repeated 3x in ProgressTracker methods | Extracted `_job_fields()` static method. 21 duplicated lines reduced to 7 + helper. | `scripts/python/comfyui_generate.py` | 2026-02-27 |
| DRY-352 | `wf_short_name()` computation inline at 5 call sites | Extracted `wf_short_name()` helper function. All 5 sites refactored. | `scripts/python/comfyui_generate.py` | 2026-02-27 |
| DRY-354 | Inline `SELECT 1` DB probe in `health_aggregator.rs` | Replaced with `x121_db::health_check()` call | `db/src/lib.rs` (health_check function) | 2026-02-27 |
| DRY-359 | `SettingResponse` 11-field struct construction duplicated 4x in handler | Extracted `build_setting_response()` helper. 44 lines reduced to 4 calls + 1 helper. | `api/src/handlers/platform_settings.rs` | 2026-02-27 |
| DRY-360 | `find_definition` + Validation error block duplicated 4x in handler | Extracted `ensure_setting_definition()` helper. 12 lines reduced to 4 one-liners. | `api/src/handlers/platform_settings.rs` | 2026-02-27 |
| DRY-392 | Inline 27-column `SELECT` in `list_children` handler duplicating repo `COLUMNS` | Extracted `SceneTypeRepo::list_children()` method. Handler reduced from 16-line inline query to 1-line repo call. | `db/src/repositories/scene_type_repo.rs` | 2026-02-28 |
| DRY-393 | 2 inline SQL queries in `cascade_preview` handler (child IDs + override fields) | Extracted `SceneTypeRepo::list_children_ids()` and `SceneTypeOverrideRepo::list_field_names()`. Handler now uses repo methods. | `db/src/repositories/scene_type_repo.rs`, `scene_type_override_repo.rs` | 2026-02-28 |
| DRY-394 | `SceneTypeRepo::find_by_id + ok_or(NotFound)` repeated 5x across 2 handler files | Extracted `ensure_scene_type_exists(pool, id)` to `handlers/scene_type_inheritance.rs`. Both `scene_type.rs` and `scene_type_inheritance.rs` use it. ~25 lines eliminated. | `api/src/handlers/scene_type_inheritance.rs` | 2026-02-28 |
| DRY-397 | Inline `UPDATE scene_types SET depth = $1` raw SQL in handler | Extracted `SceneTypeRepo::update_depth(pool, id, depth)`. Handler now calls repo method. | `db/src/repositories/scene_type_repo.rs` | 2026-02-28 |
| DRY-409 | `SceneTypeRepo::find_by_id + ok_or(NotFound)` 2x in `auto_retry.rs` handler | `get_retry_policy` now calls `ensure_scene_type_exists()` from `scene_type_inheritance.rs`. Eliminated 6 duplicate lines. | `api/src/handlers/auto_retry.rs` | 2026-02-28 |
| DRY-410 | `RetryPolicyResponse { ... }` construction duplicated 2x in handler | Extracted `build_retry_policy_response(st)` helper. Both handlers now call single helper. | `api/src/handlers/auto_retry.rs` | 2026-02-28 |
| DRY-411 | `RetryAttemptRepo::find_by_id + ok_or(NotFound)` 3x in handler | Extracted `ensure_retry_attempt_exists(pool, attempt_id)` helper. `get_retry_attempt` now uses helper. | `api/src/handlers/auto_retry.rs` | 2026-02-28 |
| DRY-412 | `x121_db::models::retry_attempt::RetryAttempt` fully-qualified 5x | Added `RetryAttempt` to import statement. All 5 occurrences now use short name. | `api/src/handlers/auto_retry.rs` | 2026-02-28 |
| DRY-413 | Magic numbers `1` and `10` for max_attempts in handler | Extracted `MIN_RETRY_ATTEMPTS`/`MAX_RETRY_ATTEMPTS` to `core/src/auto_retry.rs`. Frontend has sync comment. | `core/src/auto_retry.rs` | 2026-02-28 |
| DRY-414 | 25-field `None` boilerplate in `UpdateSceneType` construction | Added `#[derive(Default)]` to `UpdateSceneType`. Handler now uses `..Default::default()`. | `db/src/models/scene_type.rs` | 2026-02-28 |
| DRY-415 | `"passed"` magic string in `select_best_attempt` comparison | Replaced with `RetryAttemptStatus::Passed.as_str()`. | `core/src/auto_retry.rs` | 2026-02-28 |
| DRY-416 | 4x `!draft.enabled && "opacity-50 pointer-events-none"` in RetryPolicyEditor | Extracted to local `disabledClass` const. 4 `cn()` calls now use shared variable. | `features/auto-retry/RetryPolicyEditor.tsx` | 2026-02-28 |
| DRY-420 | `classify_score()` duplicates `evaluate_score()` + uses magic strings instead of `QA_PASS`/`QA_WARN`/`QA_FAIL` | Refactored `classify_score()` to delegate to `evaluate_score()`. Replaced 12 magic string occurrences in `run_ab_test()` with `QA_PASS`/`QA_WARN` constants. | `core/src/qa_ruleset.rs` | 2026-02-28 |
| DRY-421 | `QA_METRIC_LABELS` in qa-rulesets duplicates `CHECK_TYPE_LABELS` in quality-gates (overlapping keys, divergent labels) | Created shared `@/lib/qa-constants.ts` with canonical `QA_CHECK_TYPE_LABELS` + `qaMetricLabel()`. Both features now re-export from shared source. | `lib/qa-constants.ts`, `features/quality-gates/types.ts`, `features/qa-rulesets/types.ts` | 2026-02-28 |
| DRY-422 | `QaProfileRepo::find_by_id + ok_or(NotFound)` 4x in handler | Extracted `ensure_qa_profile_exists(pool, id)` helper. 3 of 4 consumers refactored (get, delete, upsert_override). `update_profile` retains inline since `update()` returns `Option`. | `api/src/handlers/qa_rulesets.rs` | 2026-02-28 |
| DRY-423 | 8 fully-qualified type paths (`QaProfile`, `SceneTypeQaOverride`, `QaThreshold`) in handler | Added imports for all 3 entity types. All 8 occurrences now use short names. | `api/src/handlers/qa_rulesets.rs` | 2026-02-28 |
| DRY-424 | Threshold resolution logic (~20 lines) duplicated between `resolve_effective_thresholds` and `ab_test_thresholds` | Extracted `load_effective_thresholds(state, scene_type_id, project_id)` helper. Both handlers now delegate to it. | `api/src/handlers/qa_rulesets.rs` | 2026-02-28 |
| DRY-427 | `{ warn: 0, fail: 0 }` magic threshold fallback in 2 components | Extracted `EMPTY_THRESHOLD` constant to `types.ts`. Both `ThresholdEditor` and `AbTestPanel` import it. | `features/qa-rulesets/types.ts` | 2026-02-28 |
| DRY-428 | Inline `{ warn: number; fail: number }` type instead of `MetricThreshold` in hook | Replaced with `MetricThreshold` import in `useEffectiveThresholds` query function. | `features/qa-rulesets/hooks/use-qa-rulesets.ts` | 2026-02-28 |
| DRY-430 | `handleWarnChange`/`handleFailChange` structural duplicates in ThresholdSlider | Extracted `handleFieldChange("warn" | "fail")` factory function. | `features/qa-rulesets/ThresholdSlider.tsx` | 2026-02-28 |
| DRY-431 | Section heading class string duplicated 3x across QA ruleset panels | Extracted `SECTION_HEADING_CLASSES` constant to `types.ts`. All 3 consumers import it. | `features/qa-rulesets/types.ts` | 2026-02-28 |
| DRY-434 | Local `ensure_scene_type_exists` in `comparison.rs` (raw SQL) | Deleted local function, imported shared `ensure_scene_type_exists()` from `scene_type_inheritance.rs`. | `api/src/handlers/scene_type_inheritance.rs` | 2026-02-28 |
| DRY-435 | `ensure_project_exists` uses raw SQL | Rewritten to delegate to `ProjectRepo::find_by_id`. | `api/src/handlers/comparison.rs` | 2026-02-28 |
| DRY-436 | `ensure_character_in_project` uses raw SQL | Rewritten to delegate to `CharacterRepo::find_by_id` + project_id check. | `api/src/handlers/comparison.rs` | 2026-02-28 |
| DRY-437 | `SceneGallery` and `CharacterAllScenes` 85% identical (~120 lines duplicated) | Extracted shared `GalleryLayout` component. Both components now ~30 lines each. ~150 lines eliminated. | `features/comparison/GalleryLayout.tsx` | 2026-02-28 |
| DRY-438 | `APPROVE_ALL_QA_THRESHOLD`, `DEFAULT_SEGMENT_VERSION`, `QA_THRESHOLD_GOOD/FAIR` scattered across 3 files | Consolidated all constants to `types.ts`. All consumers import from shared source. | `features/comparison/types.ts` | 2026-02-28 |
| DRY-439 | `handleApprove/Reject/Flag` mutation boilerplate 24 lines x 2 | Extracted `useGalleryActions()` hook. `GalleryLayout` uses it internally. | `features/comparison/hooks/useGalleryActions.ts` | 2026-02-28 |
| DRY-441 | `fetch_comparison_rows` two 35-line SQL queries differing by one WHERE clause | Consolidated into single query with conditional `format!` clause + dynamic bind. | `api/src/handlers/comparison.rs` | 2026-02-28 |
| DRY-442 | `ENTITY_TYPE_CHARACTER`/`ENTITY_TYPE_SCENE` duplicated in `poster_frame.rs` and `metadata.rs` | Removed local definitions, added `pub use crate::metadata::{ENTITY_TYPE_CHARACTER, ENTITY_TYPE_SCENE}` re-export. Single source of truth in `metadata.rs`. | `core/src/poster_frame.rs` | 2026-02-28 |
| DRY-443 | `ensure_scene_exists`/`ensure_character_exists`/`ensure_project_exists` redefined locally in poster_frame handler | Changed signatures from `&AppState` to `&PgPool` matching codebase convention (embedding.rs, comparison.rs, resolution.rs). | `api/src/handlers/poster_frame.rs` | 2026-02-28 |
| DRY-444 | `CharacterPoster` and `ScenePoster` 85% identical (~90 lines duplicated) | Extracted shared `EntityPoster` component. Both components now ~40 lines each (thin wrappers). ~70 lines eliminated. | `features/poster-frame/EntityPoster.tsx` | 2026-02-28 |
| DRY-445 | `"face_confidence"` magic string in auto_select_posters handler | Replaced with `CHECK_FACE_CONFIDENCE` constant from `x121_core::quality_gate`. | `api/src/handlers/poster_frame.rs` | 2026-02-28 |
| DRY-447 | `AutoSelectResult` DTO mismatch between backend and frontend | Aligned frontend type with backend: removed `character_name`/`poster_frame` fields, added `segment_id: number \| null`. | `features/poster-frame/types.ts` | 2026-02-28 |
| DRY-449 | set/get scene/character poster handlers structurally identical | Extracted `set_entity_poster()` and `get_entity_poster()` shared helpers. 4 public handlers now 3 lines each. | `api/src/handlers/poster_frame.rs` | 2026-02-28 |
| DRY-450 | `segment_id: 0` sentinel in `AutoSelectResult` instead of `Option<DbId>` | Changed `segment_id` to `Option<DbId>`. Success: `Some(id)`, failure: `None`. | `api/src/handlers/poster_frame.rs` | 2026-02-28 |
| DRY-470 | `resolveStatus()` exact duplicate in `ReportList.tsx` and `ReportViewer.tsx` | PRD-73 | `flagged` | Identical 8-line function with `STATUS_MAP` record copy-pasted between two components. Extract to `types.ts`. |
| DRY-471 | `ListReportsParams` duplicates existing `PaginationParams` from `api/src/query.rs` | PRD-73 | `flagged` | Structurally identical `{ limit: Option<i64>, offset: Option<i64> }`. `PaginationParams` used by 4+ other handlers. Delete local, import shared. |
| DRY-472 | `BASE_URL` constant duplicated in `ReportList.tsx` and `ReportViewer.tsx` | PRD-73 | `flagged` | Same `import.meta.env.BASE_URL + api/v1` string. Also duplicated in `lib/api.ts` and `auth-store.ts` (4 total). Export from `lib/api.ts`. |
| DRY-473 | Inline `new Date().toLocaleString()` in 3 PRD-73 components instead of `formatDateTime` | PRD-73 | `flagged` | `ReportList.tsx`, `ReportViewer.tsx`, `ScheduleManager.tsx` all use raw `toLocaleString()`. Shared `formatDateTime` from `@/lib/format.ts` used by 20+ other components. Same pattern as DRY-224/DRY-257. |
| DRY-474 | Inline raw SQL `SELECT id FROM report_types WHERE id = $1` in handler | PRD-73 | `flagged` | Handler layer should not contain raw SQL. Add `get_report_type_by_id` repo method + `ensure_report_type_exists` helper. Same pattern as DRY-321/DRY-394/DRY-411. |
| DRY-475 | Validators return `&'static str` instead of convention `String` | PRD-73 | `flagged` | `validate_format`/`validate_schedule` return `Result<(), &'static str>` but 15+ other validators use `Result<(), String>`. Forces verbose `.map_err(\|e\| AppError::BadRequest(e.to_string()))` closure (5x in handler). |
| DRY-476 | Magic `1_i64` for pending status_id in repo | PRD-73 | `flagged` | `production_report_repo.rs` line 74 hardcodes `bind(1_i64)`. Need `REPORT_STATUS_ID_PENDING`/`RUNNING`/`COMPLETED`/`FAILED` constants in `core/src/production_report.rs`. Same pattern as DRY-249/DRY-265. |
| DRY-477 | Report type select options mapping duplicated in `ReportGenerator.tsx` and `CreateScheduleForm.tsx` | PRD-73 | `watch` | Identical `(reportTypes ?? []).map(rt => ({ value: String(rt.id), label: rt.name }))`. Only 2 consumers. Extract if 3rd appears. |
| DRY-478 | Date From / Date To state + grid layout duplicated between `ReportGenerator.tsx` and `CreateScheduleForm.tsx` | PRD-73 | `watch` | Same `useState` pair + `grid grid-cols-2 gap-3` + `<Input type="date">` pair. Only 2 consumers within same feature. Extract `DateRangeInputs` if 3rd consumer appears. |
| DRY-479 | Duplicate icon imports from same module (2 import lines instead of 1) | PRD-73 | `flagged` | `ReportList.tsx`, `ReportViewer.tsx`, `ScheduleManager.tsx` import `{ Download }` and `{ iconSizes }` separately from `@/tokens/icons`. Combine to single import. |
| DRY-480 | Download URL + link rendering structural duplicate in `ReportList.tsx` and `ReportViewer.tsx` | PRD-73 | `watch` | Different visual contexts (table cell vs header button). Only 2 instances. Monitor for 3rd consumer. |
| DRY-481 | Loading/empty state message styling `px-3 py-4 text-sm text-muted text-center` 4x in PRD-73 | PRD-73 | `watch` | Feeds into DRY-468 (39+ codebase-wide). Part of broader `<EmptyState>` component extraction. |
| DRY-482 | `ensure_character_exists` duplicated in 3 handler files | PRD-94, PRD-76, PRD-96 | `resolved` | Was duplicated in `embedding.rs`, `poster_frame.rs`, and inlined in `consistency_report.rs`. PRD-94 handler now has `pub(crate) ensure_character_exists()`. Note: still 3 separate definitions across handler files (embedding, poster_frame, consistency_report). Future: consolidate into shared `handlers/helpers.rs` at next cleanup pass. |
| DRY-483 | Report-building logic (compute + outlier + DTO assembly) duplicated in `generate_report` and `batch_generate` | PRD-94 | `resolved` | Extracted `build_report_input(character_id, project_id, scores, report_type)` helper in `consistency_report.rs`. Both handlers now call shared function. ~20 lines eliminated. |
| DRY-484 | `cellBg` in `ConsistencyHeatmap.tsx` structural duplicate of `consistencyBg` in `types.ts` | PRD-94 | `resolved` | Extracted `consistencyCellBg()` to `features/consistency/types.ts`. Co-located with other threshold-based helpers. `ConsistencyHeatmap.tsx` imports from types. |
| DRY-485 | `(score * 100).toFixed(N)%` percentage formatting repeated 15+ times across frontend | PRD-94, PRD-76, PRD-68, PRD-65, PRD-82, others | `resolved` | Added `formatPercent(value, decimals?)` to `@/lib/format.ts`. PRD-94 components (`ConsistencyHeatmap.tsx`, `ConsistencyReportCard.tsx`) updated to use it. Other features' instances flagged for future migration. |
| DRY-486 | `validate_report_type` returns `Result<(), String>` -- minority error type | PRD-94 | `watch` | 5 validators return `Result<(), String>`, 14 return `Result<(), CoreError>`. PRD-94 follows the minority pattern. Not unique to PRD-94. Defer to future unification pass. |
| DRY-487 | Score-to-color threshold pattern (if x>=high green; elif x>=mid yellow; else red) across 3 features | PRD-94, PRD-68, PRD-83 | `watch` | Consistency, comparison, and workflow-canvas each define threshold-based color selectors. Different threshold values and semantics (similarity vs QA vs timing). Consistency feature is well-structured (shared `CONSISTENCY_THRESHOLDS` object). No extraction needed unless a 4th domain appears. |
| DRY-530 | `validate_qa_threshold` duplicated `validate_unit_range` from `core/src/threshold_validation.rs` | PRD-92 | `resolved` | Replaced inline [0.0,1.0] range check in `core/src/batch_review.rs` with delegation to shared `validate_unit_range(threshold, "QA threshold")`. Return type changed from `Result<(), String>` to `Result<(), CoreError>`. Handler updated to `.map_err(AppError::Core)`. |
| DRY-531 | `parse_deadline` in batch_review handler structurally similar to `parse_timestamp` in `api/src/query.rs` | PRD-92 | `watch` | Both parse ISO 8601 strings via `.parse::<chrono::DateTime<chrono::Utc>>()`. Different signatures: `parse_deadline(Option<&str>) -> Option<DateTime>` vs `parse_timestamp(Option<String>, fallback) -> DateTime`. Not merged due to different return semantics. Monitor for 3rd deadline parser. |
| DRY-532 | `AssignmentListParams` duplicated `limit`/`offset` fields from `PaginationParams` | PRD-92 | `resolved` | Replaced inline fields with `#[serde(flatten)] pub pagination: PaginationParams`. Handler updated to `params.pagination.limit`/`.offset`. |
| DRY-533 | `ASSIGNMENT_STATUS_BADGE_VARIANT` typed as `Record<..., string>` instead of `Record<..., BadgeVariant>` | PRD-92 | `resolved` | Changed to `Record<AssignmentStatus, BadgeVariant>` with `BadgeVariant` import from `@/components/primitives/Badge`. Removed 2x `as BadgeVariant` casts in `AssignmentManager.tsx` and `DeadlineTracker.tsx`. |
| DRY-534 | `"segment_ids must not be empty"` validation duplicated in `batch_approve` and `batch_reject` | PRD-92 | `resolved` | Extracted `require_non_empty_segment_ids(ids)` helper in `api/src/handlers/batch_review.rs`. Both handlers now call shared helper. |
| DRY-535 | `count_approved_segments` and `count_rejected_segments` were structurally identical SQL (only differ by decision string) | PRD-92 | `resolved` | Extracted private `count_segments_by_decision(pool, project_id, decision)` method in `batch_review_repo.rs`. Both public methods now delegate to shared implementation. |
| DRY-536 | `formatEstimatedTime` in `batch-review/types.ts` overlaps with `formatDuration` from `@/lib/format.ts` | PRD-92 | `watch` | Both format seconds/ms into human-readable time strings, but `formatEstimatedTime` appends " remaining" suffix and takes seconds (not ms). Legitimate domain-specific thin wrapper. No extraction needed. |
| DRY-537 | `createWrapper()` in `use-batch-review.test.ts` duplicates `renderWithProviders` concept from `test-utils.tsx` | PRD-92 | `watch` | Hook tests need a raw wrapper function (not render), and also need `mutations: { retry: false }`. `renderWithProviders` only disables query retries and calls `render()`. Different purposes prevent unification. If a 3rd hook test file defines `createWrapper`, extract to shared `createHookTestWrapper()`. |
| DRY-538 | Hardcoded `'approved'`/`'rejected'` strings in batch_review_repo.rs SQL | PRD-92 | `watch` | Used in `batch_approve_segments` INSERT, `batch_reject_segments` INSERT, and `count_segments_by_decision` WHERE clause. Only 3 instances, all internal to repo. If decision strings spread to a 2nd file, extract to `core/src/batch_review.rs` constants. |
| DRY-539 | Emergency stop terminate-and-record loop duplicated 3x | PRD-114 | `resolved` | Extracted `terminate_and_record()` in `cloud/src/services/mod.rs`. Emergency stop handlers + scaling service now call shared function. |
| DRY-540 | `CloudProviderError` -> `AppError` conversion via 13 `.map_err()` calls | PRD-114 | `resolved` | Added `From<CloudProviderError> for AppError` + `From<CryptoError> for AppError` in `api/src/error.rs`. All 13+2 `.map_err()` calls eliminated, replaced with `?`. |
| DRY-541 | Background service spawn boilerplate duplicated 3x | PRD-114 | `resolved` | Extracted `spawn_periodic_service()` in `cloud/src/services/mod.rs`. Scaling, monitoring, and reconciliation services now delegate to shared spawner. |
| DRY-542 | `InstanceStatus` -> DB status_id mapping duplicated | PRD-114 | `resolved` | Added `InstanceStatus::to_db_status_id()` method in `core/src/cloud.rs`. Monitoring service now calls it instead of manual 8-arm match. |
| DRY-543 | `formatCents` duplicated 5x across frontend components | PRD-114 | `resolved` | Exported `formatCents()` from `@/lib/format.ts`. DashboardStats, CloudInstanceList, CloudProviderDetail all import from shared location. |
| DRY-544 | `serde_json::json!` response in `submit_serverless_job` | PRD-114 | `resolved` | Created typed `ServerlessJobResponse` struct in handler file. |
| DRY-545 | Page header duplicated 2x in `CloudGpuDashboard.tsx` | PRD-114 | `resolved` | Extracted `PageHeader` local component with children slot. |
| DRY-546 | Presigned URL generation duplicated 2x in `storage.rs` | PRD-114 | `resolved` | Extracted shared `generate_presigned_url()` private function. Upload/download delegate to it. |
| DRY-547 | Serverless REST API boilerplate duplicated 3x | PRD-114 | `resolved` | Extracted `serverless_request()` helper in `cloud/src/runpod/serverless.rs`. All 3 endpoints delegate to shared helper. |
| DRY-548 | `cloud_providers.rs` handler at 720 lines exceeds 500-line threshold | PRD-114 | `watch` | Reduced to ~620 lines after DRY fixes. Still above 500 but all handlers are cohesive (single admin resource). Split into sub-modules if a 2nd resource is added to the file or if it grows beyond 750. |
| DRY-549 | Magic number status IDs in frontend components | PRD-114 | `resolved` | Created named `INSTANCE_STATUS` and `PROVIDER_STATUS` constants in CloudInstanceList.tsx and CloudProviderList.tsx with sync comments referencing Rust source. |
| DRY-550 | Provider iterator pattern duplicated 3x across services | PRD-114 | `watch` | `registry.provider_ids()` + `get()` + `list_active_by_provider()` loop appears in monitoring, reconciliation, and emergency_stop_all. Three instances with different per-instance logic. Extract `for_each_active_instance()` if a 4th consumer appears. |
| DRY-551 | Redundant Content-Type header with `.json()` | PRD-114 | `resolved` | Removed manual `Content-Type: application/json` headers from `graphql.rs` and `serverless.rs`. reqwest `.json(&body)` sets it automatically. |
| DRY-552 | `ensure_provider` / `ensure_instance` naming inconsistency | PRD-114 | `resolved` | Renamed to `ensure_provider_exists` and `ensure_instance_exists` to match codebase convention (`ensure_X_exists`). |
| DRY-553 | `new Date().toLocaleString()` instead of shared `formatDateTime` | PRD-114 | `resolved` | Replaced with `formatDateTime()` import from `@/lib/format.ts` in CloudProviderDetail.tsx. |
| DRY-554 | `"Encryption failed: {e}"` error string duplicated 2x | PRD-114 | `resolved` | Eliminated by `From<CryptoError>` impl — both create and update now use `?` operator. |
| DRY-638 | `ListExportsParams` exact duplicate of `PaginationParams` in `delivery.rs` | PRD-39 | `resolved` | `ListExportsParams { limit: Option<i64>, offset: Option<i64> }` was identical to `PaginationParams` from `api/src/query.rs`. Removed local struct, imported shared `PaginationParams`. |
| DRY-639 | `handleLoad` parse-integer function copy-pasted in 8 page wrappers | PRD-34, PRD-88, PRD-103, PRD-26, PRD-51, PRD-93, PRD-16, PRD-77 | `watch` | Same 3-line function (`parseInt` + `isNaN` + `> 0` guard + `setSomeId`) copied in BatchMetadataPage, BudgetPage, ContactSheetPage, DebuggerPage, ImporterPage, PipelineHooksPage, TemporalPage, UndoPage. 8 consumers. Candidate for `useEntityIdInput()` custom hook or `parsePositiveInt()` utility. |
| DRY-641 | Raw HTML `<input>` in `PipelineHooksPage.tsx` instead of design system `Input` | PRD-77 | `resolved` | `HookIdInput` sub-component used raw `<input>` with hand-rolled CSS classes. Replaced with design system `<Input>` component. |
| DRY-642 | Raw HTML `<select>` in `UndoPage.tsx` instead of design system `Select` | PRD-51 | `resolved` | Entity type selector used raw `<select>` with hand-rolled CSS. Replaced with design system `<Select>` component with `options` array. |
| DRY-643 | Inline `<label>` + `<Input>` pattern redundant with `Input` `label` prop | PRD-34, PRD-88, PRD-103, PRD-26, PRD-51, PRD-93, PRD-16, PRD-77 | `resolved` | 8 page wrappers (10 occurrences) manually rendered `<label className="block text-sm font-medium ...">` above `<Input>`. The `Input` component already has a built-in `label` prop. Removed all inline labels, replaced with `<Input label="...">`. |
| DRY-645 | Page header pattern duplicated in 43+ files | Cross-cutting | `watch` | `<h1 className="text-xl font-semibold text-[var(--color-text-primary)]">Title</h1><p className="mt-1 text-sm text-[var(--color-text-muted)]">Description</p>` appears in 43+ page/feature files. Local `PageHeader` component exists in CloudGpuDashboard but is not shared. Extract to `@/components/composite/PageHeader.tsx` as a larger refactor when convenient. |

---

## DRY-GUY Audit Log

Record of every DRY-GUY audit run against the codebase.

| Date | PRD(s) Touched | Files Audited | Findings | Action Taken |
|------|---------------|---------------|----------|--------------|
| 2026-02-20 | Phase -1 scaffold | 33 (11 Rust, 14 Frontend, 8 Infra) | 6 (0 critical, 3 medium, 3 low) | Fixed CI DATABASE_URL dedup, vitest config merge, Storybook color comment. Watch: tracing init, Docker anchors. |
| 2026-02-20 | PRD-00 Database Normalization | 12 (2 Rust lib, 2 Rust test, 1 API, 4 SQL, 2 docs, 1 DRY-TRACKER) | 3 (0 critical, 1 medium, 2 low) | All acceptable/deferred. Medium: tracing init (2 binaries, extract at 3). Low: SQL filter clause in tests (readable as-is), lookup DDL repetition (mitigated by template migration 000003). No code changes needed. |
| 2026-02-20 | PRD-01 Phase 4 (API Endpoints) | 17 (8 handlers, 5 routes, 1 error, 1 lib, 1 test helper, 1 main) | 2 low, rest acceptable | No changes. NotFound construction (19x) and delete-if pattern (8x) are thin-adapter repetition. DRY-003 stays `watch`. scene_type.rs correctly deduplicates dual-scope via inner helpers. |
| 2026-02-20 | PRD-01 Test Files (entity_crud, entity_api, schema_conventions) | 8 test files across db+api crates | 2 HIGH (extracted), 1 MEDIUM (watch), 2 LOW (acceptable) | Extracted `body_json`, `post_json`, `put_json`, `get`, `delete`, `send_json` to `api/tests/common/mod.rs`. Unified `post_json`/`put_json` via `send_json` base. Refactored health.rs + entity_api.rs to use shared helpers. Watch: entity_crud.rs fixtures if second api test needs them. |
| 2026-02-20 | PRD-109 Phases 5-6 (Trash API + Delivery) | 12 (1 trash_repo, 5 entity repos modified, 1 trash handler, 1 trash route, 1 delivery.rs, 1 handlers/mod, 1 routes/mod, 1 repos/mod) | 1 HIGH (dead code), 2 MEDIUM (watch), 2 LOW, 4 CLEAN | HIGH: `find_by_id_include_deleted` on 5 individual repos is dead code (uncalled) — recommend removal. MEDIUM: DRY-009 confirmed (27 identical soft_delete/restore/hard_delete methods across 9 repos), DRY-011 added (5 parallel entity-type lists). LOW: fallback in `table_and_name_expr`, dispatch_restore inherent to architecture. CLEAN: delivery.rs, route file, handler pattern. |
| 2026-02-20 | PRD-03 Phases 1-6 (User Identity & RBAC) | 19 new + 9 modified (3 SQL migrations, 3 db models, 3 db repos, 3 auth modules, 2 middleware modules, 2 handler modules, 2 route modules, 1 config) | 2 flagged (DRY-012, DRY-013), 2 watch (DRY-014, DRY-015), 3 LOW, rest CLEAN | FLAGGED: `resolve_role_name` duplicated between auth.rs and admin.rs (DRY-012); role name magic strings in rbac.rs (DRY-013). WATCH: validate+hash pattern 2x in admin.rs (DRY-014), RBAC extractor boilerplate at 3 impls (DRY-015). LOW: `"Account is deactivated"` 2x in auth.rs (same file, different paths), `UserInfo` vs `UserResponse` (intentionally different scopes), `"unknown"` fallback (resolves with DRY-012). CLEAN: password.rs, jwt.rs, AuthUser extractor, all repos, all routes, all migrations, test config. |
| 2026-02-20 | PRD-05 (ComfyUI WebSocket Bridge) | 8 new + 6 modified (8 comfyui crate files, 2 db model/repo files, 2 db repo files, 2 migration files, 2 Cargo.toml, 1 state.rs, 1 main.rs, 1 tests/common/mod.rs) | 1 FLAGGED (DRY-017), 1 MEDIUM fixed, 1 LOW fixed, 1 MEDIUM watch (DRY-016), 3 LOW acceptable, rest CLEAN | FIXED: `api.rs` internal duplication -- extracted `ensure_success()` helper to deduplicate `parse_response`/`check_status` (eliminated 7 duplicate lines). FIXED: `reqwest` and `tokio-util` not in workspace deps -- moved to `workspace.dependencies` with `workspace = true` in comfyui Cargo.toml. FLAGGED: DRY-017 middleware stack duplication between main.rs and tests/common/mod.rs (worsened by PRD-05 adding comfyui_manager). WATCH: DRY-016 find_by_prompt_id+event pattern (2x in processor.rs). LOW: UUID client_id generation 2x (different purposes), execution status SQL strings (encapsulated behind methods), instance status strings (migration-only). CLEAN: messages.rs, events.rs, reconnect.rs, client.rs, manager.rs structure, db models, db repos, migrations. |
| 2026-02-20 | PRD-10 (Event Bus & Notification System) | 22 files (8 events crate, 5 db models/repos, 4 api handlers/routes, 3 api notification module, 2 migrations) + 7 modified (state, main, test common, ws/manager, lib, routes/mod, handlers/mod) | 1 CRITICAL fixed, 3 HIGH (2 fixed, 1 worsened), 1 MEDIUM fixed, 2 LOW acceptable | FIXED (CRITICAL): `persistence.rs` duplicated `INSERT INTO events` SQL + `SELECT id FROM event_types` -- refactored to call `EventRepo::insert()` and `EventRepo::get_event_type_by_name()`, eliminating 18 lines of duplicate SQL. FIXED (HIGH): `digest.rs` duplicated "users due for digest" query with divergent SQL (hardcoded hourly/daily vs repo's `digest_interval::interval` cast) + raw SQL for marking notifications delivered + updating last-sent timestamp -- refactored to call `NotificationPreferenceRepo::list_users_due_for_digest()`, `NotificationRepo::pending_count_for_channel()`, `NotificationRepo::mark_channel_delivered()`, and `NotificationPreferenceRepo::mark_digest_sent()`. Added 2 new methods to NotificationRepo and 1 to NotificationPreferenceRepo. FIXED (MEDIUM): Channel name magic strings (`"in_app"`, `"digest"`) in 7 locations -- extracted to `core/src/channels.rs` constants (DRY-019 resolved). WORSENED: DRY-017 (middleware/router duplication) now includes `event_bus` in AppState. WATCH: DRY-018 added (broadcast recv loop pattern, 2 instances). LOW: `find_latest_event_id` called 2x in router.rs (internal, different paths), webhook retry vs comfyui reconnect (different purposes). CLEAN: bus.rs, delivery/mod.rs, email.rs, webhook.rs, all db models, all migrations, notification route, notification handler, WsManager.send_to_user. |
| 2026-02-20 | PRD-14 (Data Validation & Import Integrity) | 14 (3 SQL migrations, 5 core validation module files, 2 db repos, 2 db models, 1 handler, 1 route) + 3 modified (core lib.rs, handlers/mod.rs, routes/mod.rs) | 1 HIGH fixed, 2 MEDIUM (1 fixed, 1 watch), 1 LOW watch | FIXED (HIGH): Import status magic strings `"preview"`, `"committed"` and `status_id != 1` magic number in handler — extracted `IMPORT_STATUS_PREVIEW/COMMITTED/PARTIAL/FAILED/CANCELLED` and `IMPORT_STATUS_PREVIEW_ID` to `core/src/import_status.rs` (DRY-075, following DRY-013/DRY-019/DRY-072 pattern). FIXED (MEDIUM): Near-duplicate SQL in `validation_rule_repo.rs` — `load_rules` and `list_by_entity_type` differed only by `AND vr.is_active = true` — extracted shared `query_rules(active_only: bool)` private method. FIXED (MEDIUM): Fragile `ImportAction` serialization using `format!("{:?}", ...)` in handler — added `ImportAction::as_str()` returning stable serde-aligned strings. WATCH: DRY-074 (rule type name strings, 1 consumer), DRY-076 (source_type `"api"` literal, 1 consumer). CLEAN: evaluator.rs unit tests, conflict.rs logic, import_preview.rs types, all SQL migrations, integration test file, route registration. |
| 2026-02-20 | PRD-22 (Source Image Quality Assurance) | 16 (3 SQL migrations, 1 model, 3 repos, 1 handler, 1 route, 3 Python scripts, 1 Python __init__, 3 mod.rs) + cross-ref to core/src | 2 CRITICAL fixed, 1 HIGH fixed, 2 MEDIUM watch (DRY-070, DRY-071), 1 LOW acceptable | FIXED (CRITICAL BUG): Missing UNIQUE constraint on `(project_id, check_type_id)` in migration 000022 -- `ON CONFLICT` upsert would have failed at runtime. Added `COALESCE(project_id, 0)` expression index + matching upsert SQL. FIXED (CRITICAL BUG): Handler parsed Python output as bare array but scripts return `{"results": [...]}` dict -- would have failed with "Expected array". Updated to extract `"results"` key. FIXED (HIGH): QA status magic strings `"pass"/"warn"/"fail"` in 10 locations in `image_qa.rs` -- extracted `QA_PASS/QA_WARN/QA_FAIL` to `core/src/qa_status.rs`, following established pattern (DRY-013 roles, DRY-019 channels). Also added Python-side constants in `qa/__init__.py`. FIXED (HIGH): Python `__main__` boilerplate (13 lines x 3 scripts = 39 lines identical) -- extracted `run_check_cli()` to `qa/__init__.py`. WATCH: DRY-070 (check type name strings across 3 boundaries), DRY-071 (Python status classification ternary 7x). LOW: `list_by_character` vs `list_by_character_source` structural similarity (thin-adapter, acceptable). CLEAN: models, qa_check_type_repo, routes, mod.rs exports, migration 000020, migration 000021. |
| 2026-02-21 | PRD-06 (Hardware Monitoring) -- initial | 24 files (3 SQL migrations, 3 core modules, 1 core hardware submod, 1 core hardware/thresholds, 3 db repos, 1 db model, 1 api handler, 1 api route, 1 api background/mod, 1 api background/metrics_retention, 4 agent crate files, 1 api lib, 1 api response.rs new) + cross-ref to comfyui/reconnect.rs, db/image_qa_threshold_repo | 3 HIGH fixed, 1 FLAGGED (DRY-077), 4 WATCH (DRY-078, DRY-079, DRY-081), rest CLEAN | FIXED (HIGH): `UpdateWorkerThresholdsRequest` and `UpdateGlobalThresholdsRequest` were identical structs -- merged into single `UpdateThresholdsRequest`. FIXED (HIGH): `MetricThresholdRepo::update_global()` was a copy of `upsert()` with `worker_id=NULL` hardcoded (identical SQL, 14 duplicate lines) -- removed, handler now calls `upsert()` with `worker_id: None`. FIXED (HIGH): `DataResponse<T>` response envelope struct was local to `hardware.rs` -- extracted to shared `api/src/response.rs` (DRY-080). FIXED (MEDIUM): INSERT column list duplicated between `GpuMetricRepo::insert()` and `insert_batch()` -- extracted `INSERT_COLUMNS` constant. FIXED (MEDIUM): `"gpu_metrics"` WS message type string duplicated between agent `sender.rs` and API `hardware.rs` -- added `MSG_TYPE_GPU_METRICS` constant to `core/src/metric_names.rs`, API handler now uses it, agent has sync comment. FIXED (LOW): Missing `Default` impl for `MetricsCollector` (clippy warning). FLAGGED: DRY-077 -- tracing init now at 3 binaries (agent is 3rd), extraction threshold met. WATCH: DRY-078 (restart status_id magic numbers), DRY-079 (metric name strings SQL/Rust boundary), DRY-081 (agent vs ComfyUI reconnect strategies). CLEAN: collector.rs, restart.rs, thresholds.rs evaluator, alert.rs, all migrations, routes, background/metrics_retention. |
| 2026-02-21 | PRD-06 (Hardware Monitoring) -- re-audit | 29 PRD-06 files + 9 comparison files (notification.rs, validation.rs, digest.rs, main.rs, tests/common, StatusBadge.tsx, api.ts, worker/main.rs, ThumbnailCard.tsx) | 3 HIGH (2 fixed, 1 flag-only), 4 MEDIUM (1 fixed, 2 watch, 1 pre-existing), 2 LOW acceptable | FIXED (HIGH): Agent `sender.rs` used hardcoded `"gpu_metrics"` + `"restart_result"` strings instead of `MSG_TYPE_GPU_METRICS` constant -- added `x121-core` dep to agent crate, imported constant, added new `MSG_TYPE_RESTART_RESULT` constant. FIXED (HIGH): `update_worker_thresholds` and `update_global_thresholds` were 19-line structural duplicates differing only in `worker_id` -- extracted shared `upsert_thresholds()` helper. FLAGGED (HIGH): DRY-083 -- `notification.rs` (7x) and `validation.rs` (7x) still use `serde_json::json!` instead of `DataResponse<T>`. FIXED (MEDIUM): WorkerCard.tsx missing `ease-[var(--ease-default)]` in transition class. WATCH: DRY-082 (`getCssVar` utility, 1 consumer). MEDIUM: Test threshold fixture duplication (acceptable -- test isolation). LOW: Power/fan display rows in WorkerCard (2 instances, same component), centering divs in MetricsChart (2 instances, same component). CLEAN: all repos, all migrations, routes, background tasks, all other frontend components. |
| 2026-02-21 | PRD-09 (Multi-Runtime Script Orchestrator) | 16 new + 2 migrations + 2 modified (main.rs, state.rs) + cross-ref to image_qa.rs, tests/common/mod.rs, response.rs | 1 HIGH fixed (DRY-084), 1 MEDIUM fixed (test helper), 2 WATCH (DRY-085, DRY-086), rest CLEAN | FIXED (HIGH): Script type name magic strings `"shell"`, `"python"`, `"binary"` in 4 locations in `orchestrator.rs` -- extracted `SCRIPT_TYPE_SHELL/PYTHON/BINARY` to `core/src/script_types.rs` (DRY-084, following DRY-013/DRY-019/DRY-072 pattern). FIXED (MEDIUM): `default_input()` test helper duplicated identically in `shell.rs` and `binary.rs` -- extracted to shared `scripting::test_helpers::default_input()` in `scripting/mod.rs`. WATCH: DRY-085 (QA handler `run_python_script` vs scripting subprocess -- behavioral duplicate, different interfaces prevent direct merge). WATCH: DRY-086 (`"VENV_DIR"` env var name literal in 2 files, 2 crates). CLEAN: executor.rs (well-factored trait + types), subprocess.rs (single `run_command`), python.rs (venv management), status.rs (numeric constants matching established pattern), all DB models, both repos (clean COLUMNS/JOIN pattern), all handlers (properly use `DataResponse<T>`), routes, migrations, orchestrator lifecycle logic, mod.rs exports. DRY-017 worsened (AppState now has `script_orchestrator` field requiring parallel update in tests/common). |
| 2026-02-21 | PRD-09 (Multi-Runtime Script Orchestrator) -- re-audit | 21 files (16 PRD-09 + cross-ref to auth_api.rs, hardware_api.rs, common/mod.rs, image_qa.rs, notification.rs) | 2 HIGH fixed, 1 MEDIUM fixed, 3 MEDIUM watch, rest CLEAN | FIXED (HIGH): `build_app_with_orchestrator()` in `script_api.rs` was 60-line copy of `build_test_app()` -- extracted `build_test_app_with()` to `common/mod.rs` with optional orchestrator parameter. Both `build_test_app()` and `build_test_app_with_orchestrator()` now delegate to single builder (DRY-017 partially resolved). FIXED (HIGH): `create_admin()` + `login()` test helpers duplicated across `script_api.rs`, `auth_api.rs`, `hardware_api.rs` (3 independent user creation helpers) -- extracted `create_test_user()`, `login_user()`, `login_for_token()` to `common/mod.rs`. `script_api.rs` refactored to use shared helpers. FIXED (MEDIUM): `scripts.rs` used fully-qualified `x121_core::error::CoreError::NotFound` (4 locations) instead of imported alias used by all other handlers -- added `use x121_core::error::CoreError;` import. WATCH: DRY-004 (2nd pagination query struct `ExecutionListQuery`), DRY-085 (QA run_python_script vs subprocess), DRY-086 (VENV_DIR string). |
| 2026-02-21 | PRD-83 (Video Playback Engine & Codec Support) | 27 files (7 backend new, 6 backend modified, 12 frontend new, 2 frontend tests) | 3 HIGH fixed, 2 MEDIUM (1 pre-existing DRY-083, 1 watch DRY-091), rest CLEAN | FIXED (HIGH): DRY-087 `AudioTrackInfo` struct duplicated in `core/src/ffmpeg.rs` and `db/src/models/video.rs` -- removed duplicate from db, re-export from core, eliminated 8-line field-by-field mapper in handler. FIXED (HIGH): DRY-088 `"storage/thumbnails"` path literal duplicated 2x in `handlers/video.rs` -- extracted `THUMBNAIL_STORAGE_DIR` constant + `thumbnail_dir()` helper. FIXED (MEDIUM): DRY-089 "find first video stream" iteration repeated 5x in `core/src/ffmpeg.rs` -- extracted `first_video_stream()` private helper. FIXED (LOW): Hardcoded `'segment' or 'version'` error message -- now uses `VALID_SOURCE_TYPES` constant. RESOLVED: DRY-060 (FFmpeg command builder) -- shared utility now lives in `core/src/ffmpeg.rs`. WORSENED: DRY-083 (DataResponse not used by `get_metadata` and `generate_thumbnails` -- 3rd handler module skipping envelope). WATCH: DRY-091 (10x `.map_err` in handler, add `From<FfmpegError>` when pattern reaches 4th handler). CLEAN: all frontend files (types, codec-detector, frame-utils, hooks, components, barrel export, tests), all repos, migration, routes, constants. |
| 2026-02-21 | PRD-07 (Parallel Task Execution Engine) | 14 files (8 new: migration, model, repo, engine/mod, dispatcher, progress, handler, route; 6 modified: db models/mod, db repos/mod, api lib, handlers/mod, routes/mod, comfyui manager) + cross-ref to core/roles.rs, notification.rs, scripts.rs, response.rs, metric_names.rs | 1 CRITICAL fixed, 2 HIGH fixed, 3 MEDIUM fixed, 1 MEDIUM watch (DRY-093), rest CLEAN | FIXED (CRITICAL DRY-090): `ADMIN_ROLE` constant redefined as `"admin"` in `jobs.rs` -- exact duplicate of `ROLE_ADMIN` from `core/src/roles.rs`. Replaced with import. FIXED (HIGH DRY-092): `list_by_user`/`list_all` in `job_repo.rs` were 30-line structural duplicates -- extracted shared `list_jobs(pool, user_id: Option, params)` private method with dynamic WHERE clause builder. FIXED (HIGH DRY-097): Ownership guard (find_by_id + NotFound + ownership check + Forbidden) repeated 3x in handlers -- extracted `find_and_authorize(pool, job_id, auth, action)` helper. FIXED (MEDIUM DRY-094): Magic status_id numbers `1, 2` in `dispatcher.rs` SQL -- replaced with `JobStatus::Pending.id()`/`JobStatus::Running.id()` parameterized binds. FIXED (MEDIUM DRY-095): Job WS message type magic strings 4x in `progress.rs` -- created `core/src/job_events.rs` with `MSG_TYPE_JOB_PROGRESS/COMPLETED/FAILED/CANCELLED` constants. FIXED (MEDIUM DRY-096): `serde_json::json! + Message::Text + broadcast` pipeline repeated 4x -- extracted `broadcast_json()` helper. WATCH: DRY-093 (`MAX_LIMIT`/`DEFAULT_LIMIT` pagination constants now at 3 consumers). CLEAN: migration (proper trigger, indexes, FK constraints), model (uses DbId/Timestamp), route file, engine/mod.rs, DataResponse used correctly throughout, JobStatus enum properly defined in status.rs. |
| 2026-02-21 | Group 1 Phase 2 (PRD-07, PRD-47, PRD-02 DRY fix, PRD-29 deferred) | 46 files (20 backend: 4 migrations, 3 models, 3 repos, 3 handlers, 3 routes, 1 router, 1 engine/mod, 1 dispatcher, 1 progress; 6 frontend: 3 domain components, 1 TokenEditor, 1 TokenEditor.test, 1 useThemePersistence; 2 modified: main.rs, tests/common/mod.rs) + cross-ref to DRY-TRACKER, notification.rs, trash.rs, response.rs, Dropdown.tsx, use-hardware.ts | 1 HIGH fixed (DRY-108), 2 MEDIUM fixed (DRY-109, 6x duplicate updaters), 1 RESOLVED (DRY-017), 3 WATCH new (DRY-106, DRY-107, DRY-093 status updated), rest CLEAN | **DRY-017 RESOLVED**: `build_app_router()` in `api/src/router.rs` now single source of truth; both `main.rs` and `tests/common/mod.rs` call it with zero duplication. **DRY-108 FIXED**: `TokenEditor.tsx` was 670 lines (over 500-line threshold) with 7 inlined section components and 6 structurally identical `updateX` functions; extracted sections to `token-editor/TokenSections.tsx` (404 lines), main component now 298 lines, 6 updaters replaced with single generic `updateTokenSection<K>()`. **DRY-109 FIXED**: `TagWithCount` redundantly re-exported from `TagInput.tsx` and `TagFilter.tsx`; removed, canonical source is `TagChip.tsx`. **DRY-093 CONFIRMED**: 4th consumer (`tag_repo.rs`) confirmed, extraction threshold met, status updated to flagged. **NEW WATCH**: DRY-106 (theme status `'active'` magic string, 1 consumer), DRY-107 (`STATUS_OPTIONS` hardcoded status_id numbers in TokenEditor, 1 consumer). **CLEAN**: All 3 handler files properly use `DataResponse<T>`, `AuthUser`, `ROLE_ADMIN` import, `CoreError::NotFound`. All 3 repo files use `COLUMNS` constant pattern, `DbId`/`Timestamp` types. All 3 route files are thin routers. `router.rs` cleanly extracts DRY-017. Job model uses `JobStatus` enum, no magic numbers. Tag model types well-structured with `TagInfo`/`TagWithCount` hierarchy. Theme model follows established CRUD patterns. `dispatcher.rs` uses `JobStatus::*.id()` (DRY-094 pattern). `progress.rs` uses `MSG_TYPE_JOB_*` constants and `broadcast_json` helper (DRY-095/096 resolved). `jobs.rs` uses `find_and_authorize` helper (DRY-097 resolved). Frontend `TagChip/TagInput/TagFilter` follow design token conventions. `useThemePersistence` properly isolated from `ThemeProvider`. |
| 2026-02-21 | PRD-47 (Tagging & Custom Labels) | 9 new (2 migrations, 1 model, 1 repo, 1 handler, 1 route, 3 frontend components) + 12 cross-ref (scripts.rs, hardware.rs, jobs.rs, trash.rs, trash_repo.rs, response.rs, job_repo.rs, notification.rs, StatusBadge.tsx, ThumbnailCard.tsx, Dropdown.tsx, use-hardware.ts) | 1 HIGH fixed (DRY-099), 1 MEDIUM fixed (DRY-101), 1 HIGH watch (DRY-098), 3 MEDIUM watch (DRY-093 worsened, DRY-102, DRY-103), 2 LOW watch (DRY-104, DRY-105), rest CLEAN | FIXED (HIGH DRY-099): `TagSuggestion` in `TagInput.tsx` was structurally identical to `TagWithCount` (`TagInfo + usage_count`) -- unified to single `TagWithCount` type exported from `TagChip.tsx`, eliminated 8-line duplicate interface. FIXED (MEDIUM DRY-101): Inline `prefix.trim().to_lowercase()` in `tag_repo.rs:suggest()` duplicated existing `normalize_tag_name()` helper -- replaced with function call. WORSENED: DRY-093 now at 4 consumers (tag_repo.rs is 4th), extraction threshold met. WATCH: DRY-098 (validate_entity_type function in 2 handlers with different registries), DRY-102 (click-outside pattern at 2 consumers), DRY-103 (raw API calls instead of TanStack Query hooks), DRY-104 (taggable entity type constants, 1 consumer), DRY-105 (inline SVG close icon, 1 consumer). CLEAN: both migrations (proper trigger, indexes, UNIQUE constraints), model DTOs (uses DbId/Timestamp, proper projection types), handler (correctly uses DataResponse<T>, AuthUser, RequireAdmin, CoreError::NotFound), route file, TagChip.tsx (focused single-responsibility), TagFilter.tsx (proper TagInfo extension). |
| 2026-02-21 | Group 3 Phase 2 (PRD-15, PRD-17) | 28 files (8 backend new: 2 models, 2 repos, 2 handlers, 2 routes; 4 backend core: 2 reclamation, 2 assets submodules; 8 SQL migrations; 10 frontend: 6 components, 2 hook files, 1 format utility, 1 CompatibilityWarning) | 5 HIGH fixed (DRY-117 to DRY-121), 1 FLAGGED worsened (DRY-093), 5 WATCH new (DRY-122 to DRY-126), rest CLEAN | **FIXED (HIGH DRY-117):** `formatFileSize()` identically defined in `AssetBrowser.tsx:38` and `AssetDetail.tsx:23` -- behaviorally duplicated `formatBytes()` from `@/lib/format.ts`. Removed both (18 lines), replaced with `formatBytes` import. **FIXED (HIGH DRY-118):** `formatDate()` local helper in `AssetDetail.tsx` -- extracted to shared `formatDate()` in `@/lib/format.ts`. **FIXED (HIGH DRY-119):** `reclamation.rs` handler imported `DataResponse` but used `serde_json::json!` for 11 of 13 endpoints -- migrated all to `DataResponse<T>` with `impl IntoResponse` return types. DRY-083 partially reduced (reclamation cleared). **FIXED (HIGH DRY-120):** Column lists repeated 15x across `reclamation_repo.rs` -- extracted 4 COLUMNS constants (`RULE_COLUMNS`, `POLICY_COLUMNS`, `TRASH_COLUMNS`, `RUN_COLUMNS`) following established `asset_repo.rs` pattern. Eliminated ~60 lines of duplicate column strings. **FIXED (HIGH DRY-121):** `verify_exists + NotFound` guard repeated 4x identically in `assets.rs` handler (add_dependency, get_impact, add_note, rate_asset) -- extracted `ensure_asset_exists(pool, id)` helper, following DRY-097 (find_and_authorize) pattern. Eliminated 20 lines. **WORSENED DRY-093:** `asset_repo.rs` (50/100) is 5th consumer of pagination clamping pattern; `reclamation.rs` has 2x bare `unwrap_or(50)` without MAX_LIMIT cap. **WATCH:** DRY-122 (trash queue status_id magic numbers), DRY-123 (asset STATUS_OPTIONS hardcoded ids), DRY-124 (ASSET_TYPE_OPTIONS hardcoded ids), DRY-125 (`"required"`/`"info"` default strings in repo), DRY-126 (`"manual"` run_type string). **CLEAN:** All 8 SQL migrations (proper triggers, indexes, UNIQUE constraints, FK indexes). Both models (use DbId/Timestamp, clean DTOs). Both route files (thin routers). Core modules (pure logic, no DB deps, proper unit tests). All frontend hooks (TanStack Query with proper query key factories). Frontend components all under 500 lines. `ProtectedBadge` is a thin intentional wrapper. `CompatibilityWarning` well-structured. |
| 2026-02-21 | Group 2 Phase 2 (PRD-30, PRD-32, PRD-52) | 53 files (15 backend: 3 migrations, 3 models, 3 repos, 3 handlers, 3 routes; 38 frontend: 14 layout, 10 progressive-disclosure, 13 shortcuts, 1 AdvancedDrawer) + 3 cross-ref (themes.rs, theme_repo.rs, useThemePersistence.ts) | 3 HIGH fixed (DRY-110, DRY-111, DRY-116), 5 WATCH new (DRY-112 to DRY-115), rest CLEAN | **FIXED (HIGH DRY-110):** `CATEGORY_LABELS` and `CATEGORY_ORDER` identically declared in `CheatSheet.tsx` and `KeymapEditor.tsx` -- extracted to `ShortcutRegistry.ts` alongside new `groupBindingsByCategory()` utility; both consumers refactored to import. Eliminated 20 duplicate lines. **FIXED (HIGH DRY-111):** `snapValue()` identically defined in `useSnapGrid.ts` and `usePanelResize.ts` (same `Math.round(value/gridSize)*gridSize` body) -- exported from `useSnapGrid.ts`, imported in `usePanelResize.ts`. Eliminated 3 duplicate lines. **FIXED (HIGH DRY-116):** `export_keymap` handler had two identical `serde_json::json!` branches (Some/None) producing same JSON shape -- extracted shared destructuring with fallback defaults. Eliminated 7 duplicate lines. **WATCH DRY-112:** Debounce timer pattern at 3 persistence hooks (theme, layout, proficiency). Extract `useDebouncedCallback` at 4th consumer. **WATCH DRY-113:** Proficiency level strings in SQL (beginner/intermediate/expert). Same cross-boundary pattern as DRY-079. **WATCH DRY-114:** Preset name list cross-boundary duplication (backend PRESET_NAMES vs frontend presets map). **WATCH DRY-115:** Raw `api.*()` calls in layout persistence (same pattern as DRY-103 for tags). **CLEAN:** All 3 migrations (proper triggers, indexes, UNIQUE constraints, FK indexes). All 3 models use `DbId`/`Timestamp`, proper `FromRow`/`Serialize`/`Deserialize` derives. All 3 repos use `COLUMNS` constant pattern, clean SQL. All 3 handlers use `DataResponse<T>`, `RequireAuth`/`RequireAdmin`, `CoreError::NotFound` pattern. All 3 route files are thin routers. Frontend: all components under 500 lines, design token CSS variables used consistently, proper Zustand store patterns, barrel exports. No cross-feature type duplication. DRY-003 (CRUD handler boilerplate) remains `watch` -- handlers are thin-adapter, no macro warranted yet. DRY-004 (pagination) not impacted -- no list endpoints use pagination in this batch. DRY-093 (pagination clamping) not impacted. |
| 2026-02-21 | Group 4 Phase 2 (PRD-41, PRD-42, PRD-54, PRD-36, PRD-37) | 52 files (9 backend: 2 models, 2 repos, 2 handlers, 2 routes, 1 migration; 43 frontend: 7 dashboard/performance, 5 dashboard/widgets, 3 dashboard hooks/base, 7 cinema, 10 qa-aids, 8 job-tray, 1 hooks/useEventBus, 1 lib/format, 1 video-player/index) | 4 HIGH fixed (DRY-127 to DRY-130), 4 MEDIUM watch (DRY-131 to DRY-134), 1 FLAGGED worsened (DRY-093 to 6 consumers), rest CLEAN | **FIXED (HIGH DRY-127):** `formatTime(seconds)` identically defined in `CinemaMode.tsx` and `GridControls.tsx` -- behavioral duplicate of `formatDuration()` from `video-player/frame-utils.ts`. Removed both locals (16 lines), added barrel export to `video-player/index.ts`, replaced with `formatDuration` import. **FIXED (HIGH DRY-128):** Recharts Tooltip `contentStyle` object duplicated 7x across `QualityCharts.tsx` (4x), `WorkflowComparison.tsx` (2x), `WorkerBenchmark.tsx` (1x). Also axis tick style duplicated 14x and grid stroke 7x. Created `performance/chartStyles.ts` with `TOOLTIP_CONTENT_STYLE`, `AXIS_TICK_STYLE`, `GRID_STROKE`. All 3 files refactored. Eliminated ~70 lines. **FIXED (HIGH DRY-129):** `formatElapsed(seconds)` in `ActiveTasksWidget.tsx` reimplemented `formatDuration` logic from `@/lib/format.ts`. Refactored to delegate: `formatDuration(seconds * 1000)` with null guard. **FIXED (HIGH DRY-130):** Click-outside pattern reached 3-consumer threshold (TagInput, Dropdown, JobTrayPanel). Extracted `useClickOutside(ref, callback, enabled?)` to `src/hooks/useClickOutside.ts`. All 3 consumers refactored. DRY-102 resolved. **WATCH DRY-131:** Workflow aggregation SELECT duplication in `performance_metric_repo.rs` (2 methods). **WATCH DRY-132:** Scope type magic strings (`"global"`, `"workflow"`, `"worker"`) in performance handler. 1 Rust consumer. **WATCH DRY-133:** Worker aggregation SELECT duplication in `performance_metric_repo.rs` (2 methods). **WATCH DRY-134:** `parse_from`/`parse_to` date parsing helpers. 1 consumer. **DRY-093 WORSENED:** Dashboard handler adds 6th consumer (`FEED_MAX_LIMIT`/`FEED_DEFAULT_LIMIT`). **CLEAN:** All backend handlers use `DataResponse<T>`, `RequireAuth`/`RequireAdmin`, `CoreError::NotFound`. Both repos use `COLUMNS`/`INSERT_COLUMNS` pattern. All SQL migrations have proper triggers, indexes, FK indexes. Job tray uses shared `formatDuration` from `@/lib/format.ts`. All dashboard widgets under 500 lines. QA aids are well-factored (pure geometry helpers, physics engine class, audio scrub class all cleanly separated). Cinema mode properly reuses `useVideoPlayer`, `useVideoMetadata`, `formatDuration` from shared video-player module. No cross-feature type duplication. No unused imports detected. |
| 2026-02-21 | PRD-85 (UI Plugin/Extension Architecture) | 16 files (6 backend: 1 migration, 1 model, 1 repo, 1 core module, 1 handler, 1 route; 10 frontend: 1 types, 1 hooks, 1 sandbox, 1 api-bridge, 1 panel-integration, 1 context-menu, 1 metadata-renderer, 1 manager, 1 barrel, 1 test) + cross-ref (router.rs, response.rs, MetricsChart.tsx, 4 test files) | 3 HIGH fixed (DRY-138 to DRY-140), 1 MEDIUM flagged (DRY-135), 2 MEDIUM watch (DRY-136, DRY-137), rest CLEAN | **FIXED (HIGH DRY-138):** `enable_extension`/`disable_extension` handlers were 44-line structural duplicates differing only in boolean arg and log message -- extracted shared `toggle_extension(state, id, user_id, enabled)` helper. Eliminated 22 lines. **FIXED (HIGH DRY-139):** Textarea styling 9-line `cn()` block duplicated in `SettingsEditor` and `InstallForm` (same file) -- extracted `TEXTAREA_BASE_CLASSES` array constant + `textareaBorderClass(hasError)` helper. Both textareas now use shared constants. Eliminated 12 duplicate lines. **FIXED (HIGH DRY-140):** Frontend `methodToAccess()` in `ExtensionApiBridge.ts` mapped POST->"create", PUT->"update", DELETE->"delete" but backend `KNOWN_ACCESS_LEVELS` in `core/src/extensions.rs` only defines `["read", "write"]`. Client-side defense-in-depth check would wrongly deny extensions with `write` permission trying to POST/PUT/DELETE. Aligned frontend to use "read"/"write" matching backend. **FLAGGED DRY-135:** `renderWithProviders` test helper identical across 4+ test files (threshold met). **WATCH DRY-136:** `parseError` + JSON try/catch pattern in 2 sub-components (same file, 2 instances). **WATCH DRY-137:** `collectDesignTokens` vs `getCssVar` CSS property access (related to DRY-082). **CLEAN:** Migration (proper trigger, indexes, UNIQUE constraint). Model uses `DbId`/`Timestamp`, clean DTOs with `FromRow`/`Serialize`/`Deserialize`. Repo uses `COLUMNS` + `REGISTRY_COLUMNS` constant pattern. Core module has proper validation with unit tests, constants in leaf crate. Handler correctly uses `DataResponse<T>`, `RequireAdmin`, `AuthUser`, `CoreError::NotFound/Forbidden`. Route file is a thin router with 3 sub-routers. Routes properly registered in `router.rs`. Frontend types mirror backend manifest schema. TanStack Query hooks follow established pattern with query key factory. Barrel export is clean. Test uses proper vitest/react-testing-library patterns. No `serde_json::json!` in handler (uses `DataResponse<T>` throughout). No magic strings (constants in core). Frontend components all under 500 lines. |
| 2026-02-21 | PRD-28, PRD-04, PRD-45 (Pipeline Checkpointing, Workspace Persistence, Audit Logging) | 38 files (12 PRD-28: 2 migrations, 1 core, 1 model, 1 repo, 1 handler, 1 route, 3 frontend, 1 test, 1 hooks; 14 PRD-04: 2 migrations, 1 core, 1 model, 1 repo, 1 handler, 1 route, 5 frontend, 1 test; 12 PRD-45: 2 migrations, 1 core, 1 model, 1 repo, 1 handler, 1 route, 4 frontend, 1 test) | 6 FIXED (DRY-151 to DRY-156), 3 WATCH new (DRY-154, DRY-159, DRY-160), 2 MEDIUM unfixed (DRY-157 inline dates, DRY-158 raw fetch) | **FIXED (CRITICAL DRY-151):** `formatBytes()` duplicated in `checkpoints/types.ts` -- removed local, re-exported from `@/lib/format.ts`. **FIXED (CRITICAL DRY-152):** `find_and_authorize_job()` in `checkpoints.rs` was line-for-line duplicate of `find_and_authorize()` in `jobs.rs` -- made jobs helper `pub`, imported in checkpoints. **FIXED (HIGH DRY-153):** `resume_from_checkpoint` handler hand-rolled `INSERT INTO jobs` with inline 14-column COLUMNS list duplicating `job_repo.rs` -- extracted `JobRepo::resume_from_checkpoint()` method. **FIXED (HIGH DRY-155):** `query()` and `count()` in `audit_repo.rs` had 45+ identical filter-building lines -- extracted `build_audit_filter()` + `bind_audit_values()`/`bind_audit_values_scalar()` helpers. **FIXED (MEDIUM DRY-156):** Ad-hoc `serde_json::json!` in `checkpoints.rs` diagnostics and `audit.rs` query response -- replaced with typed `JobDiagnosticsResponse` struct and `AuditLogPage` struct. DRY-083 reduced. **WATCH DRY-154:** `FailureDiagnostics` (db) vs `FailureDiagnosticData` (core) near-duplicate -- intentional layer separation. **WATCH DRY-159:** `parse_timestamp` in audit vs `parse_from/parse_to` in performance -- 2 consumers, extract at 3rd (DRY-134). **FLAGGED DRY-160:** Debounce timer pattern now at 5 consumers (DRY-112 escalated). **UNFIXED DRY-157:** Inline `new Date().toLocale*` in AuditLogViewer and ResumeDialog -- should use `formatDateTime` from `@/lib/format.ts`. **UNFIXED DRY-158:** `exportAuditLogs` uses raw `fetch` with `localStorage.getItem("access_token")` bypassing shared API client. **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean, `npx biome check` clean, all core unit tests pass. CLEAN: All test files use `renderWithProviders` from `@/lib/test-utils.tsx`. All handlers use `DataResponse<T>`. All repos use COLUMNS constant pattern. All core modules have unit tests. No dead imports. |
| 2026-02-21 | Group 5 Phase 2 (PRD-20, PRD-11, PRD-12) | 45 files (15 backend PRD-20: 3 migrations, 1 core search.rs, 1 model, 1 repo, 1 handler, 1 route, 7 frontend; 13 backend PRD-11: 2 migrations, 1 core, 1 model, 1 repo, 1 handler, 1 route, 6 frontend; 17 backend PRD-12: 3 migrations, 1 core api_keys.rs, 2 models, 2 repos, 2 handlers, 1 route, 6 frontend) | 4 FIXED (DRY-163 to DRY-166), 2 WATCH new (DRY-161, DRY-162), 1 FLAGGED updated (DRY-093 to 7 consumers), rest CLEAN | **FIXED (HIGH DRY-163):** `SearchBar.tsx` used inline click-outside implementation (11-line useEffect with addEventListener/removeEventListener on mousedown) instead of shared `useClickOutside` hook from `src/hooks/useClickOutside.ts` (DRY-102/130 pattern) -- replaced with single `useClickOutside(containerRef, () => setIsOpen(false))` call. **FIXED (HIGH DRY-164):** `execute_saved_search` in `handlers/search.rs` duplicated the search+facets+timing+response assembly pattern from `unified_search` (~15 identical lines) -- extracted `run_search(pool, params)` private async helper. Both handlers now delegate to it. **FIXED (HIGH DRY-165):** `build_tsquery` and `build_prefix_tsquery` in `core/src/search.rs` both had identical 5-line term sanitization logic (split_whitespace + trim_matches + filter) -- extracted `sanitize_terms(query) -> Option<Vec<&str>>` private helper. **FIXED (MEDIUM DRY-166):** `webhooks.rs:list_deliveries` used inline `params.limit.unwrap_or(50).min(200)` + `params.offset.unwrap_or(0)` instead of shared `clamp_limit`/`clamp_offset` from `core/src/search.rs` -- replaced with shared imports. DRY-093 now at 7 consumers (webhook is 7th, now fixed). **WATCH DRY-161:** Confirmation dialog UI pattern duplicated across ApiKeyManager (rotate/revoke) and WebhookManager (delete). Same AlertTriangle + description + Cancel/Action button structure. Design system candidate at 3rd consumer. **WATCH DRY-162:** Admin table styling pattern (Card > overflow-x-auto > table > th classes) repeated across 4 components (ApiKeyManager, WebhookManager, DeliveryLog, ExtensionManager). Threshold met but deferred to design system work. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core -- search` 19 tests + 2 doc tests passed, `npx tsc --noEmit` clean, `npx vitest run SearchBar.test.tsx` 8/8 passed. **NOT DUPLICATED:** SHA-256 in `api_keys.rs:hash_api_key()` vs `audit.rs:compute_integrity_hash()` -- different purposes (simple hash vs chained integrity hash). `collaboration.rs` vs `search.rs` entity type validation -- different registries by design. **CLEAN:** All 8 SQL migrations. All frontend test files use `renderWithProviders` from `@/lib/test-utils.tsx`. All handlers use `DataResponse<T>`. All repos use COLUMNS constant pattern. All frontend components use `formatDateTime` from `@/lib/format.ts`. Core modules have proper unit tests. TanStack Query hooks follow established key factory pattern. No magic strings detected. |
| 2026-02-21 | Group 4 Phase 2b (PRD-13, PRD-16, PRD-66) | 34 files (10 backend PRD-13: 1 core metadata.rs, 1 model, 1 repo, 1 handler, 1 route, 3 frontend, 1 hooks, 1 test; 10 backend PRD-16: 1 core importer.rs, 1 model, 1 repo, 1 handler, 1 route, 3 frontend, 1 hooks, 1 test; 8 backend PRD-66: 1 core metadata_editor.rs, 1 handler, 1 route, 3 frontend, 1 hooks, 1 test; 6 shared: search.rs, api_keys.rs, audit.rs, images.rs, import_status.rs, api.ts, format.ts) | 5 HIGH (DRY-167 to DRY-170, DRY-176), 3 MEDIUM (DRY-171, DRY-172, DRY-173), 2 WATCH (DRY-174, DRY-175), 2 LOW (DRY-177, DRY-178), 0 fixed (read-only audit) | **HIGH DRY-167:** SHA-256 hex digest implemented 3x in core (`metadata.rs:sha256_hex`, `api_keys.rs:hash_api_key`, `audit.rs:compute_integrity_hash`). First two are structurally identical. Extract shared `sha256_hex()` to `core/src/hashing.rs`. **HIGH DRY-168:** Metadata regeneration logic (serialize -> hash -> create -> upsert) duplicated 2x within `metadata.rs` handler at lines 197-215 and 267-279. Extract `upsert_generation()` private helper. **HIGH DRY-169:** `find_stale_characters`/`find_stale_scenes` are 20-line structural duplicates in `metadata_repo.rs` differing only by table name and entity type. Extract generic `find_stale_entities()`. **HIGH DRY-170:** Magic strings `'character'`/`'scene'` hardcoded in 4 SQL queries in `metadata_repo.rs` instead of using constants from `core/src/metadata.rs`. **HIGH DRY-176:** `serde_json::json!` at 3 handler endpoints (`metadata.rs` x2, `importer.rs` x1) instead of typed `DataResponse<T>` (DRY-083 pattern). **FLAGGED DRY-171:** Raw `fetch` with `localStorage.getItem("access_token")` in 2 frontend files (3 instances), bypassing shared `api.ts` client auth. One instance in `use-importer.ts` has NO auth header at all (potential bug). **MEDIUM DRY-172:** `formatValue()`/`formatCellValue()` near-identical in 2 metadata-editor components. **MEDIUM DRY-173:** Inline `toLocaleString()` in `StalenessIndicator.tsx` instead of `formatDateTime`. **WATCH DRY-174:** `IMAGE_EXTENSIONS` in `importer.rs` overlaps `VALID_IMAGE_FORMATS` in `images.rs` (same set, different format). **WATCH DRY-175:** Hardcoded `'uploading'` in SQL instead of `SESSION_STATUS_UPLOADING`. **LOW DRY-177:** Session status vs import status constant overlap (different lifecycles, acceptable). **LOW DRY-178:** `PhysicalAttributes` struct vs field definitions dual representation (intentional). **CLEAN:** All frontend test files use `renderWithProviders` from `@/lib/test-utils.tsx`. All repos use COLUMNS constant pattern. TanStack Query hooks follow established key factory pattern. `character_metadata.rs` handler properly uses `DataResponse<T>`. Core modules (`metadata_editor.rs`, `importer.rs`, `metadata.rs`) are pure logic with zero DB deps. All route files are thin routers. |
| 2026-02-21 | Group 1 Phase 3 (PRD-21, PRD-08) | ~40 files (10 backend new: 2 core modules, 2 models, 2 repos, 2 handlers, 2 routes; 6 backend modified: status.rs, image.rs, image_variant_repo.rs, image_variant.rs, character routes, job routes; 8 SQL migrations; 14 frontend: types, hooks, components, barrel exports for images + queue; 6 test files refactored) | 5 HIGH fixed (DRY-141 to DRY-145), 1 FLAGGED resolved (DRY-135), 5 MEDIUM watch (DRY-146 to DRY-150), rest CLEAN | **RESOLVED (DRY-135):** `renderWithProviders` test helper duplicated across 6 test files -- extracted to shared `src/lib/test-utils.tsx`. All 6 consumers refactored: VariantGallery.test, QueueStatusView.test, ExtensionManager.test, PerformanceDashboard.test, StudioPulse.test, ReclamationDashboard.test. **FIXED (HIGH DRY-141):** `formatFileSize()` in `SourceImageUpload.tsx` was behavioral duplicate of `formatBytes()` from `@/lib/format.ts` -- removed local, replaced with shared import. **FIXED (HIGH DRY-142):** `formatDate()` in `VariantHistory.tsx` was identical to `formatDateTime()` from `@/lib/format.ts` -- removed local, replaced with shared import. **FIXED (HIGH DRY-143):** `"storage/variants"` path literal duplicated 2x in `image_variant.rs` -- extracted `VARIANT_STORAGE_DIR` constant + `ensure_variant_dir()` async helper (following DRY-088 `THUMBNAIL_STORAGE_DIR` pattern). **FIXED (HIGH DRY-145):** All 10 endpoints in `image_variant.rs` returned raw `Json<T>` instead of `DataResponse<T>` -- migrated all to `DataResponse<T>` + `impl IntoResponse` return types. Removed unused `ImageVariant` import. **WATCH DRY-146:** `formatWait()`/`formatSeconds()` in queue components -- similar to `formatDuration` but specialized for queue display. 1 consumer each. **WATCH DRY-147:** `UpdateImageVariant` struct all-None construction repeated 2x in image_variant.rs (reject/export) -- extract `UpdateImageVariant::status_only(status_id)` builder at 3rd consumer. **WATCH DRY-148:** Multipart upload parsing pattern duplicated in `reimport_variant`/`upload_manual_variant` -- extract `parse_image_upload(multipart)` helper at 3rd consumer. **WATCH DRY-149:** `state_machine::status_name()` in `core/src/scheduling.rs` maps same IDs as `JobStatus` enum -- intentional separation (core vs db), no action needed unless they diverge. **WATCH DRY-150:** `"off_peak"` magic string in scheduling model -- 1 consumer, extract at 2nd. **VERIFIED:** `cargo check` clean (0 warnings), `npx tsc --noEmit` clean, `cargo test --lib` all passed, `npx vitest run` 292/292 tests passed across 26 files. |

| 2026-02-21 | Group 5 Phase 2b (PRD-33, PRD-35, PRD-44) | 34 files (11 PRD-33: 1 core, 1 model, 1 repo, 1 handler, 1 route, 6 frontend; 12 PRD-35: 1 core, 1 model, 1 repo, 1 handler, 1 route, 7 frontend; 12 PRD-44: 2 core, 1 model, 1 repo, 2 handlers, 2 routes, 6 frontend) + cross-ref to hashing.rs, audit.rs, search.rs, format.ts, roles.rs, Modal, Spinner | 4 HIGH (DRY-179 to DRY-182), 6 MEDIUM (DRY-183 to DRY-188), 2 LOW (DRY-189, DRY-190), 0 fixed (read-only audit) | **HIGH DRY-179:** Duplicate sensitive-key redaction functions in `audit.rs::redact_sensitive_fields` and `config_export.rs::redact_sensitive` -- overlapping key lists (6 shared keys out of 10 each), different placeholders, different APIs (clone vs mutate). Extract shared `core/src/redaction.rs`. **HIGH DRY-180:** `ensure_segment_exists` pattern (SegmentRepo::find_by_id + NotFound) repeated 3x identically in `approval.rs` (approve/reject/flag). Extract private helper (DRY-097/DRY-121 pattern). **HIGH DRY-181:** Identical `onSuccess` invalidation callbacks in 3 review mutation hooks (`useApproveSegment`/`useRejectSegment`/`useFlagSegment`). Same `mutationFn` shape. Extract `useDecisionMutation<T>(endpoint)` factory. ~40 lines duplicate. **HIGH DRY-182:** ComfyUI workflow parsing duplicated between backend handler (~85 lines) and frontend `comfyui-parser.ts` (~53 lines). Same node/edge extraction logic. Cross-boundary behavioral duplicate. Watch only (2 consumers, different runtimes). **MEDIUM DRY-183:** Inline pagination clamping in `bug_reports.rs` (`unwrap_or(50).min(200)`) instead of `clamp_limit`/`clamp_offset` from `core/src/search.rs`. DRY-093 now at 8 consumers. **MEDIUM DRY-184:** Hardcoded `"admin"` string 2x in `bug_reports.rs` instead of `ROLE_ADMIN` from `core/src/roles.rs`. Same violation as DRY-090 (CRITICAL in PRD-07). **MEDIUM DRY-185:** Magic timing thresholds 1000/5000 in `WorkflowCanvas.tsx` duplicating `TIMING_FAST_MS`/`TIMING_MODERATE_MS` from backend. Extract frontend constants to `types.ts`. **MEDIUM DRY-186:** `RejectionDialog.tsx` hand-rolls modal overlay instead of using `Modal` composite (used by 11+ components). Missing Escape key, focus trapping. **MEDIUM DRY-187:** `WorkflowCanvas.tsx` inline loading spinner instead of `Spinner` primitive (used by 30+ components). **MEDIUM DRY-188:** `BugReportList.tsx` uses `toLocaleDateString()` instead of `formatDate` from `@/lib/format.ts` (DRY-157/DRY-173 pattern). **LOW DRY-189:** State machine pattern structural duplicate in `bug_report.rs` and `scheduling.rs` (same `valid_transitions`/`validate_transition` structure, different types `&str` vs `i16`). 2 consumers, watch. **LOW DRY-190:** Auth extractor inconsistency (`RequireAuth` in workflow_canvas vs `AuthUser` in approval/bug_reports). Style issue, not duplication. **CLEAN:** All test files use `renderWithProviders` from `@/lib/test-utils`. All repos use COLUMNS constant pattern. All handlers use `DataResponse<T>`. All route files are thin routers. All TanStack Query hooks follow key factory pattern. All frontend hooks use shared `api` client. All DB models use `DbId`/`Timestamp`. Core modules have unit tests. No dead imports. No cross-feature type duplication. |

| 2026-02-21 | PRD-53 (First-Run Experience & Onboarding) | 16 files (6 backend: 1 migration, 1 core, 1 model, 1 repo, 1 handler, 1 route; 10 frontend: 1 types, 2 hooks, 4 components, 1 tourPaths, 1 hintDefinitions, 1 barrel) + 2 cross-ref (Tooltip.tsx, workspace_repo.rs) | 5 HIGH fixed (DRY-191/192/194/197/198), 3 MEDIUM fixed (DRY-193/195/196), 2 LOW watch (DRY-199/200), 1 LOW quality (DRY-201) | **FIXED (HIGH DRY-191):** `handleTourComplete`/`handleTourSkip` identical callbacks in OnboardingGate -- merged to single `handleTourEnd`. **FIXED (HIGH DRY-192):** `PLACEMENT_CLASSES` in ContextualHint identical to Tooltip.tsx `POSITION_CLASSES` -- exported `Placement` type + `PLACEMENT_CLASSES` from Tooltip, imported in ContextualHint. **FIXED (HIGH DRY-194):** Redundant plain index duplicating unique index on `user_onboarding(user_id)` -- removed. **FIXED (HIGH DRY-197):** `validate_checklist_item`/`validate_feature_key` structurally identical -- extracted generic `validate_known_key`/`validate_known_keys` private helpers. **FIXED (HIGH DRY-198):** `get_or_create` 2-query pattern (DO NOTHING + SELECT) -- refactored to single-query no-op UPDATE pattern from `workspace_repo.rs`. **FIXED (MEDIUM DRY-193):** Placement union type 4x -- unified to `Placement` from Tooltip. **FIXED (MEDIUM DRY-195):** Dismiss-button styling 3x -- extracted `DISMISS_LINK_CLASSES`. **FIXED (MEDIUM DRY-196):** `HintDefinition` in wrong file -- moved to `types.ts`. **WATCH DRY-199:** Role name strings in tourPaths cross-boundary. **WATCH DRY-200:** TourEngine overlay lacks Escape/focus trap. **LOW DRY-201:** `console.log` stub in OnboardingChecklist. **VERIFIED:** `cargo check` clean, 315 core tests passed, `tsc --noEmit` clean. |

| 2026-02-22 | PRD-23 (Scene Type Configuration) | 16 files (5 backend: 1 core, 1 model, 1 repo, 1 handler, 1 route; 7 frontend: 1 types, 1 hooks, 3 components, 1 barrel, 1 index; 3 tests; 1 migration) + 5 cross-ref (Badge.tsx, StatusBadge.tsx, ActiveTasksWidget.tsx, onboarding.rs, dashboard.rs) | 4 HIGH fixed (DRY-201 to DRY-204), 4 WATCH new (DRY-205 to DRY-208), rest CLEAN | **FIXED (CRITICAL DRY-201):** `regex::Regex::new(r"\{(\w+)\}")` compiled on every call to `resolve_prompt_template()` (line 94) and `extract_placeholders()` (line 182) in `core/src/scene_type_config.rs` -- extracted to `static PLACEHOLDER_RE: LazyLock<regex::Regex>` module-level constant. Compiles once, reused forever. **FIXED (HIGH DRY-202):** `type BadgeVariant` locally redefined in `SceneMatrixView.tsx` (line 31) instead of importing from `Badge.tsx` -- exported `BadgeVariant` from `Badge.tsx`, added to primitives barrel, SceneMatrixView now imports it. Same redefinition exists in `StatusBadge.tsx` (line 3) and `ActiveTasksWidget.tsx` (line 13) -- fix when files are next touched. **FIXED (HIGH DRY-203):** 150-character textarea className string duplicated 3x across `PromptTemplateEditor.tsx` (lines 117, 138) and `SceneTypeEditor.tsx` (line 129) -- extracted `TEXTAREA_CLASSES` constant to `PromptTemplateEditor.tsx`, exported via barrel, imported in `SceneTypeEditor.tsx`. **FIXED (HIGH DRY-204):** Magic `status_id` numbers `1..5` mapped to display strings in `generate_matrix` handler (line 326) -- extracted `scene_status_label()` function using `SceneStatus::*.id()` pattern (follows DRY-094 fix in `dispatcher.rs`). **WATCH DRY-205:** `validate_variant_applicability` in `scene_type_config.rs` structurally duplicates `validate_known_key` in `onboarding.rs`. 2 consumers, different error types. Extract generic at 3rd. **WATCH DRY-206:** Pre-DataResponse CRUD handlers return raw `Json<T>` without envelope. Same pattern as other Phase 1 handlers. **WATCH DRY-207:** Variant constants cross-boundary Rust/TS. Same pattern as DRY-079/DRY-114. **WATCH DRY-208:** `CLIP_POSITIONS` vs `CLIP_TABS` -- same IDs, different purposes. **CLEAN:** Migration (proper UNIQUE index, correct `WHERE` clause for partial index). Model uses `DbId`/`Timestamp`, clean DTOs with `FromRow`/`Serialize`/`Deserialize`. Repo uses `COLUMNS` constant, consistent `format!` query pattern. Handler properly uses `DataResponse<T>` + `impl IntoResponse` for PRD-23 endpoints. Route file is thin router. Frontend hooks follow TanStack Query key factory pattern with `api` client. Tests use `renderWithProviders` from `@/lib/test-utils.tsx`. Core module has comprehensive unit tests (21 tests). No `serde_json::json!` in PRD-23 handlers. No raw `fetch` calls. All imports resolve correctly. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core -- scene_type_config` 21/21 passed, `npx tsc --noEmit` clean, `npx vitest run src/features/scene-types` 11/11 passed. |

| 2026-02-22 | PRD-46, PRD-76 (Worker Pool Management, Character Identity Embedding) | 40 files (14 backend: 2 core modules, 4 db models, 3 db repos, 2 handlers, 2 routes, 1 error.rs; 19 frontend: 2 types, 2 hooks, 5 components, 2 barrel exports, 6 test files; 7 SQL migrations) + cross-ref (Badge.tsx, format.ts, db lib.rs, models/mod.rs, repos/mod.rs, core lib.rs) | 1 CRITICAL fixed (DRY-209), 5 HIGH fixed (DRY-210 to DRY-213, DRY-216), 3 MEDIUM fixed (DRY-214, DRY-215, DRY-217), 1 WATCH new (DRY-218), rest CLEAN | **FIXED (CRITICAL DRY-209):** `EmbeddingStatus` enum duplicated in `core/src/embedding.rs` (hand-rolled with `from_id`, `label`, `id` + domain logic dependency) and `db/src/models/status.rs` (macro-generated via `define_status_enum!`). Core version is canonical (domain logic `classify_extraction_result` depends on it). Removed db macro version, added explanatory comment. **FIXED (HIGH DRY-210):** Local `formatDate()` in `WorkerDetailPanel.tsx` -- behavioral duplicate of `formatDateTime` from `@/lib/format.ts`. Replaced with `formatDateOrDash()` wrapper using shared import. **FIXED (HIGH DRY-211):** Inline `BadgeVariant` union type in `workers/types.ts` instead of importing from `@/components/primitives/Badge`. Added `WORKER_STATUS` named constants object (IDLE=1, BUSY=2, OFFLINE=3, DRAINING=4) to eliminate magic numbers. **FIXED (HIGH DRY-212):** `find_by_id + ok_or(NotFound)` pattern repeated 9x in `workers.rs` handler. Extracted `ensure_worker_exists(pool, id) -> AppResult<Worker>` + `validate_create_input(input) -> AppResult<()>` helpers. **FIXED (HIGH DRY-213):** Hardcoded `status_id` numbers (1, 3) in `worker_repo.rs` SQL for `register`, `list_available`, `approve`, `decommission`. Replaced with bind parameters using `WorkerStatus::Idle.id()` / `WorkerStatus::Offline.id()`. Fleet stats FILTER clauses left as-is (cannot parameterize SQL aggregate filters). **FIXED (MEDIUM DRY-214):** Inline 7-line SVG close icon in `LowConfidenceWarning.tsx`. Replaced with `<X size={16} />` from `@/tokens/icons`. **FIXED (MEDIUM DRY-215):** Redundant `.map_err(\|e\| AppError::Core(e))` in `embedding.rs` handler -- `#[from] CoreError` on `AppError` provides auto-conversion via `?`. Removed explicit map_err. **FIXED (HIGH DRY-216):** Worker validation block (validate_worker_name + validate_tags) duplicated in `register_worker` and `self_register_worker` -- extracted to `validate_create_input()` helper. **FIXED (MEDIUM DRY-217):** Magic number `4` for draining status in `WorkerDetailPanel.tsx` conditional -- replaced with `WORKER_STATUS.DRAINING` named constant. **WATCH DRY-218:** `formatHeartbeat` relative time formatter in `WorkerCard.tsx` (1 consumer). Extract to `@/lib/format.ts` as `formatRelativeTime(iso)` if a 2nd consumer appears. **CLEAN:** All 6 test files use `renderWithProviders` from `@/lib/test-utils.tsx`. All handlers use `DataResponse<T>`. All repos use COLUMNS constant pattern. All route files are thin routers. TanStack Query hooks follow established key factory pattern with `api` client. `embedding.rs` handler correctly uses `ensure_character_exists` pattern. Core modules (`worker_pool.rs`, `embedding.rs`) are pure validation logic with comprehensive unit tests. Frontend `EmbeddingStatusBadge` correctly imports `BadgeVariant` from primitives. `embedding/types.ts` has proper `EMBEDDING_STATUS` named constants (clean). No `serde_json::json!` in any handler. No raw `fetch` calls. All imports resolve. **VERIFIED:** `cargo check` clean (0 warnings), `cargo test -p x121-core --lib` 366 passed, `cargo test -p x121-db --lib` 5 passed, `npx tsc --noEmit` clean, `npx vitest run` 469/469 tests passed across 56 files. |

| 2026-02-22 | PRD-48, PRD-51, PRD-60 (External & Tiered Storage, Undo/Redo Architecture, Character Library) | 45 files (9 backend core/db/handler/route per PRD: 3 core modules, 3 db models, 3 db repos, 3 handlers, 3 routes; 24 frontend: 6 storage, 8 undo, 8 library + barrel exports; 9 test files) + cross-ref (@/lib/format.ts, db/src/models/status.rs, Badge.tsx, ensure_*_exists patterns) | 2 CRITICAL fixed (DRY-219, DRY-220), 4 HIGH fixed (DRY-221 to DRY-224), 1 MEDIUM updated (DRY-160 to 6 consumers), 1 LOW watch new (DRY-226), rest CLEAN | **FIXED (CRITICAL DRY-219):** `formatBytes()` duplicated locally in `BackendConfigPanel.tsx` (line 9, 8 lines) and `MigrationProgressView.tsx` (line 10, 8 lines) -- behavioral duplicate of `formatBytes()` from `@/lib/format.ts`. Removed both locals (16 lines total), replaced with shared import. **FIXED (CRITICAL DRY-220):** `StorageBackendStatus` and `StorageMigrationStatus` enums duplicated in `core/src/storage.rs` (hand-rolled with `id()`, `label()`, `from_id()`, `from_name()`) and `db/src/models/status.rs` (canonical source via `define_status_enum!` macro). The core versions were dead code -- only used by internal unit tests, never imported by handlers (handlers correctly import from `db::models::status`). Removed enums + 16 unit tests from core. Added comment: `// NOTE: StorageBackendStatus and StorageMigrationStatus live in x121_db::models::status (DRY-220).` **FIXED (HIGH DRY-221):** 4x `.ok_or_else(\|\| AppError::Core(CoreError::NotFound { entity: "LibraryCharacter", id }))` scattered across `library.rs` (get, usage, import, update). Extracted `ensure_library_character_exists(pool, id) -> AppResult<LibraryCharacter>` private helper, following DRY-097/DRY-121 pattern. **FIXED (HIGH DRY-222):** 3x `.ok_or_else(\|\| AppError::Core(CoreError::NotFound { entity: "StorageMigration", id }))` in `storage.rs` (get_migration, rollback_migration x2). Extracted `ensure_migration_exists(pool, id) -> AppResult<StorageMigration>` private helper. `ensure_backend_exists` already existed for StorageBackend -- now both entity types have helpers. **FIXED (HIGH DRY-223):** `LibraryUsagePanel.tsx` used inline `new Date(entry.imported_at).toLocaleDateString()` instead of shared `formatDate()` from `@/lib/format.ts`. Replaced with `formatDate(entry.imported_at)`. Same violation pattern as DRY-157/DRY-173/DRY-188. **FIXED (HIGH DRY-224):** `MigrationProgressView.tsx` used `new Date(migration.started_at).toLocaleString()` and `StatePreview.tsx` used `new Date(node.timestamp).toLocaleString()` (where `node.timestamp` is epoch ms). Both replaced with `formatDateTime()` from `@/lib/format.ts`. StatePreview converts epoch ms to ISO string first: `new Date(node.timestamp).toISOString()`. **UPDATED DRY-160 (MEDIUM):** `use-entity-undo.ts` is the 6th consumer of the debounce timer pattern (useRef+setTimeout). DRY-160 status remains `flagged`. **NEW WATCH DRY-226 (LOW):** `makeAction()` test helper duplicated between `UndoTree.test.ts` (full override object) and `HistoryBrowser.test.tsx` (label-only shortcut). Same concept, different signatures. 2 consumers in same feature. **NOT DUPLICATED:** `UndoTree.ts` class (199 lines) is unique -- no similar tree data structure exists in the codebase. `StorageBackendType` enum in `core/src/storage.rs` is NOT a duplicate of `define_status_enum!` -- it maps type names/labels/IDs, not status lifecycle. `validate_tier()`, `validate_backend_config()`, `validate_entity_type()`, `validate_linked_fields()` are all domain-specific validators with different logic. All 3 frontend hook files properly use TanStack Query with key factories. All test files use `renderWithProviders` from `@/lib/test-utils.tsx`. **CLEAN:** All 3 handler files properly use `DataResponse<T>` + `impl IntoResponse`. All route files are thin routers. All core modules have unit tests. All repos use COLUMNS constant pattern. No `serde_json::json!` in any handler. No raw `fetch` calls. No magic status_id numbers. No dead imports. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core --lib` 401 passed, `npx tsc --noEmit` clean, `npx vitest run` 504/504 tests passed across 65 files. |

| 2026-02-22 | PRD-31, PRD-34, PRD-38 (Command Palette, Interactive Debugger, Collaborative Review) | 42 files (15 backend: 3 core modules, 3 models, 3 repos, 3 handlers, 3 routes; 21 frontend: 3 types, 3 hooks, 10 components, 3 barrel exports, 2 keyboard hooks; 6 test files) + cross-ref (segment.rs, approval.rs, format.ts, search.rs, Badge.tsx) | 1 CRITICAL fixed (DRY-227), 4 HIGH fixed (DRY-228 to DRY-231), 2 MEDIUM watch (DRY-232, DRY-233), 1 MEDIUM watch (DRY-234), 2 LOW watch (DRY-235, DRY-236), rest CLEAN | **FIXED (CRITICAL DRY-227):** `ensure_segment_exists` pattern (SegmentRepo::find_by_id + ok_or_else + CoreError::NotFound) duplicated identically in `approval.rs` (3 inline instances) and `review_notes.rs` (2 inline instances). Extracted to public `ensure_segment_exists(pool, segment_id) -> AppResult<()>` in `api/src/handlers/segment.rs`. Both handlers now import and call it. Also resolves DRY-180 (previously flagged). **FIXED (HIGH DRY-228):** `statusBadgeVariant()` function identically defined in `NoteTimeline.tsx` and `ReviewThread.tsx` -- maps note status to Badge variant ("success"/"warning"/"default"). Extracted to `features/review-notes/types.ts` alongside existing `noteStatusLabel()`/`noteStatusColor()`. Both consumers import. **FIXED (HIGH DRY-229):** Card panel styling (4 CSS classes: surface-primary bg, border-default, rounded-lg, p-4) repeated 5x across 4 debugger components (`JobControls.tsx`, `MidRunParamEditor.tsx`, `LatentPreview.tsx` 2x, `AbortDialog.tsx`). Extracted `DEBUGGER_CARD_CLASSES` array constant to `features/debugger/types.ts`. All 4 component files updated to spread `...DEBUGGER_CARD_CLASSES`. **FIXED (HIGH DRY-230):** Textarea styling (8 CSS classes: surface-secondary bg, border, rounded-md, padding, text-sm, text color, placeholder color, focus ring) duplicated in `MidRunParamEditor.tsx` and `AbortDialog.tsx`. Extracted `DEBUGGER_TEXTAREA_BASE` array constant to `features/debugger/types.ts`. Both consumers updated. **FIXED (HIGH DRY-231):** Fully-qualified `x121_core::error::CoreError::NotFound` used 3x in `review_notes.rs` and 1x in `approval.rs` instead of imported alias. All other handler files use `use x121_core::error::CoreError;`. Added import to both files, replaced all fully-qualified paths. **WATCH DRY-232:** `validate_control_action("pause"/"resume"/"abort")` called with hardcoded valid strings in `job_debug.rs` handler -- validation always succeeds. Defensive-in-depth, acceptable. **WATCH DRY-233:** `validate_recent_limit` in `command_palette.rs` is a specialized `clamp_limit` (no Option, has min=1 floor). 1 consumer. **WATCH DRY-234:** `review_note_repo.rs` repeats `rn.`-prefixed columns in JOIN queries vs `NOTE_COLUMNS` (unprefixed). Acceptable for disambiguated JOINs. **WATCH DRY-235:** `AbortDialog.tsx` hand-rolls modal overlay (3rd consumer with DRY-186/DRY-161). Design system candidate. **WATCH DRY-236:** `CommandPalette.tsx` hand-rolls overlay with portal (4th consumer). Specialized behavior (command palette UX). **NOT DUPLICATED:** All `formatDateTime` usages correctly import from `@/lib/format.ts`. All TanStack Query hooks use proper key factories and `api` client. All test files use `renderWithProviders` from `@/lib/test-utils.tsx`. All handlers use `DataResponse<T>`. All repos use COLUMNS constant pattern. `frecencyScorer.ts` mirrors backend frecency logic intentionally (cross-boundary, different runtimes). `CommandRegistry` is unique. `VoiceMemoRecorder` is unique. **CLEAN:** All 3 core modules have unit tests (command_palette: 6, job_debug: 9, review: 14). All 3 repos use COLUMNS constant pattern. All 3 handlers use `DataResponse<T>` + `impl IntoResponse`. All 3 route files are thin routers. All frontend hooks follow TanStack Query key factory pattern. All test files (6) use `renderWithProviders`. No `serde_json::json!` in any handler. No raw `fetch` calls. No magic status_id numbers. No dead imports. No inline date formatting. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core --lib` 439/439 passed, `npx tsc --noEmit` clean, `npx vitest run` 537/537 passed across 74 files. |

| 2026-02-23 | PRD-43, PRD-56, PRD-79, PRD-104 (System Integrity & Repair Tools, Studio Wiki & Contextual Help, Character Duplicate Detection, Model & LoRA Download Manager) | 43 files (4 core modules, 9 db models, 9 db repos, 4 handlers, 4 routes; 4 frontend features: integrity, wiki, duplicates, downloads; 12 test files; 9 SQL migrations) | 6 HIGH fixed (DRY-251 to DRY-256), 4 MEDIUM fixed (DRY-257 to DRY-260), 3 LOW watch (DRY-261 to DRY-263), rest CLEAN | **FIXED (HIGH DRY-251/252/253):** Inline pagination in `integrity.rs` (local `MAX_LIMIT`/`DEFAULT_LIMIT` constants), `duplicates.rs` (no clamping at all), and `downloads.rs` (wrong max 200). All replaced with `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Extends DRY-093 pattern. **FIXED (HIGH DRY-254):** Magic `status_id = 6` (Completed) and `status_id = 7` (Failed) in `model_download_repo.rs` SQL (`mark_completed`, `mark_failed`, `check_duplicate_hash`). Parameterized SQL with `DownloadStatus::Completed.id()` / `DownloadStatus::Failed.id()`. **FIXED (HIGH DRY-255):** Magic `status_id = 2` in `duplicate_check_repo.rs` `list_pending`. Parameterized with `duplicate_detection::STATUS_MATCH_FOUND_ID`. **FIXED (HIGH DRY-256):** Inline `(bytes / (1024*1024*1024)).toFixed(2) GB` in `ModelChecksumManager.tsx`. Replaced with `formatBytes()` from `@/lib/format.ts`. **FIXED (MEDIUM DRY-257):** Inline `toLocaleString()` in `ScanHistory.tsx`. Replaced with `formatDateTime` from `@/lib/format.ts`. **FIXED (MEDIUM DRY-258):** `serde_json::json!` in `get_worker_report`. Created typed `WorkerReportResponse` struct. **FIXED (MEDIUM DRY-259/260):** 5 scan/repair handlers (`start_scan`, `start_worker_scan`, `repair_worker`, `sync_models`, `install_nodes`) shared identical body. Extracted `trigger_scan(state, auth, worker_id, scan_type)` helper. All 5 now delegate. **WATCH DRY-261:** `generateSlug` cross-boundary (frontend preview vs backend canonical). Different runtimes. **WATCH DRY-262:** `DEFAULT_SIMILARITY_THRESHOLD` name collision (search 0.5 vs duplicate 0.90). Different domains. **WATCH DRY-263:** `formatSpeed`/`estimateEta` local helpers (1 consumer). **CLEAN:** Wiki handler correctly uses `clamp_limit`/`clamp_offset`. All repos use COLUMNS constant. All handlers use `DataResponse<T>`. Frontend hooks follow TanStack Query key factory pattern. `download_manager.rs` re-exports `MODEL_TYPE_*` from `integrity.rs` (no duplication). Constants properly centralized in `core/src/`. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core --lib` 591/591 passed, `npx tsc --noEmit` clean, `npx vitest run` 612/612 passed across 97 files. |
| 2026-02-22 | PRD-39, PRD-49 (Scene Assembler & Delivery Packaging, Automated Quality Gates) | 32 files (14 PRD-39: 1 core assembly.rs, 3 db models, 3 db repos, 1 handler, 1 route, 6 frontend; 12 PRD-49: 1 core quality_gate.rs, 2 db models, 2 db repos, 1 handler, 1 route, 5 frontend; 6 cross-ref: qa_status.rs, ffmpeg.rs, response.rs, format.ts, image_qa.rs, review-notes/types.ts) | 2 CRITICAL fixed (DRY-246, DRY-247), 2 HIGH fixed (DRY-248, DRY-249), 1 MEDIUM fixed (DRY-250), rest CLEAN | **FIXED (CRITICAL DRY-246):** Local `formatFileSize()` in `ExportHistory.tsx` (11 lines) -- behavioral duplicate of `formatBytes()` from `@/lib/format.ts`. Same algorithm, different null handling. Removed local, replaced with `formatBytes` import + null guard. **FIXED (CRITICAL DRY-247):** Local `formatDate()` in `ExportHistory.tsx` (5 lines) -- uses bare `toLocaleString()` instead of standardized `formatDateTime()` from `@/lib/format.ts`. Removed local, replaced with `formatDateTime` import + null guard. **FIXED (HIGH DRY-248):** Delivery handler `list_exports` used inline `params.limit.unwrap_or(50).min(200)` and `params.offset.unwrap_or(0)` -- extends DRY-093 pattern. Replaced with `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Also aligned `ListExportsParams` and `delivery_export_repo::list_by_project` from `i32` to `i64` for consistency with shared pagination utilities. **FIXED (HIGH DRY-249):** `delivery_export_repo.rs` used magic `status_id = 6` (completed) and `status_id = 7` (failed) in SQL. Added `EXPORT_STATUS_ID_COMPLETED`/`EXPORT_STATUS_ID_FAILED` (and all 7 status IDs) to `core/src/assembly.rs`. Repo now imports and uses named constants. **FIXED (MEDIUM DRY-250):** `QaScoreSummary` struct in `db/src/models/quality_score.rs` had identical fields to `QaSummary` in `core/src/quality_gate.rs` (total_checks, passed, warned, failed -- all usize) and was never used anywhere (dead code). Removed struct, added comment pointing to canonical `core::quality_gate::QaSummary`. **NOT DUPLICATED:** `statusBadgeVariant()` exists in both `quality-gates/types.ts` and `review-notes/types.ts` -- different domains (QA pass/warn/fail vs note open/resolved/wont_fix), different return types. `parse_resolution_str` (assembly.rs, parses string) vs `parse_resolution` (ffmpeg.rs, parses ffprobe JSON) -- different inputs and purposes. `ValidationResult`/`ValidationIssue` (assembly.rs) vs QA scoring types -- different domain concepts (pre-export validation vs QA scoring). `ensure_*_exists` helpers (3 in delivery.rs) follow established project pattern (DRY-097/DRY-121), per-entity by design. `QaScoreSummary` (frontend) is a legitimate TS interface mirroring API response, not a duplicate of backend struct. Image QA `determine_status` (image_qa.rs) structurally similar to core `evaluate_score` -- pre-existing code, different signatures (Option wrapping), noted for future DRY-093-style migration. SQL status strings `'pass'`/`'warn'`/`'fail'` in `quality_score_repo.rs` -- cannot parameterize SQL aggregate CASE expressions, acceptable. **CLEAN:** All PRD-39 and PRD-49 handlers use `DataResponse<T>` + `impl IntoResponse`. All repos use COLUMNS constant pattern. Core modules (`assembly.rs`, `quality_gate.rs`) have comprehensive unit tests. `quality_gate.rs` correctly imports from `qa_status.rs`. Frontend hooks follow TanStack Query key factory pattern with `api` client. No `serde_json::json!` in any handler (delete_threshold uses `DataResponse { data: serde_json::Value::Null }` -- valid for DELETE). No raw `fetch` calls. No inline date formatting (after fix). No magic strings in new code. All route files are thin routers. **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean, `npx @biomejs/biome lint` clean. |
| 2026-02-22 | PRD-24, PRD-27 (Recursive Video Generation Loop, Template & Preset System) | 26 files (8 backend: 2 core modules, 4 db models, 4 db repos, 2 handlers, 2 routes; 14 frontend: 2 types, 2 hooks, 5 components, 2 barrel exports, 5 test files) + cross-ref (response.rs, format.ts, scene_type_config.rs, search.rs, CsvImport.tsx) | 1 CRITICAL fixed (DRY-237), 3 HIGH fixed (DRY-238, DRY-239, DRY-240), 1 HIGH fixed (DRY-241 via DRY-237 refactor), 3 MEDIUM watch (DRY-242, DRY-243, DRY-244), 1 LOW watch (DRY-245), rest CLEAN | **FIXED (CRITICAL DRY-237):** Generation handler `generation.rs` used `serde_json::json!` with ad-hoc JSON 3x (`start_generation`, `select_boundary_frame`, `batch_generate`) instead of `DataResponse<T>`. Created 4 typed response structs (`StartGenerationResponse`, `SelectBoundaryFrameResponse`, `BatchGenerateResponse`, `BatchGenerateError`) in `db/src/models/generation.rs`. Migrated all 4 handler functions to `DataResponse<T>` + `impl IntoResponse`. Removed unused `StatusCode` import. **FIXED (HIGH DRY-238):** Local `formatDuration(secs)` in `GenerationProgressBar.tsx` (line 34, 4 lines) -- behavioral duplicate of `formatDuration(ms)` from `@/lib/format.ts`. Replaced with `formatSecs()` thin wrapper that converts seconds to ms and delegates to shared `formatDuration`. Eliminated 4 lines of duplicate formatting logic. **FIXED (HIGH DRY-239):** `SegmentRepo::list_by_scene_for_generation()` (14 lines) was an EXACT duplicate of `SegmentRepo::list_by_scene()` -- identical SQL query, identical parameters, identical return type. Confirmed zero callers. Removed dead code. **FIXED (HIGH DRY-240):** Local `formatValue(value)` in `OverridePreviewDialog.tsx` (9 lines) was a near-duplicate of the same function in `CsvImport.tsx`. Extracted shared `formatValue()` to `@/lib/format.ts` with unified null/array/object/primitive handling. `OverridePreviewDialog` now imports from shared. CsvImport uses slightly different null text ("(empty)"), will migrate when next touched. Resolves DRY-172 partially. **FIXED (HIGH DRY-241):** `batch_generate` handler body (70+ lines) duplicated the entire init-scene-for-generation sequence from `start_generation` -- load scene, load scene type, validate, estimate segments, create UpdateSceneGeneration, update_generation_state. Extracted shared `init_scene_generation(state, scene_id, boundary_mode) -> AppResult<(u32, String)>` private async helper. Both `start_generation` and `batch_generate` now call it. Batch handler went from ~70 lines to ~20 lines. **WATCH DRY-242 (MEDIUM):** `validate_template_name` and `validate_preset_name` in `core/src/preset.rs` are structural duplicates -- same empty check + length check pattern, different entity label and max constant. 2 consumers. Extract generic `validate_entity_name(name, label, max_len)` at 3rd consumer. **WATCH DRY-243 (MEDIUM):** Scope-visibility WHERE clause `(scope = 'personal' AND owner_id = $1) OR (scope = 'project' AND project_id = $2) OR scope = 'studio'` duplicated in `template_repo::list_for_user` and `preset_repo::list_for_user`. 2 consumers. If a 3rd scope-aware repo appears, extract SQL fragment builder. **WATCH DRY-244 (MEDIUM):** Marketplace handler `presets.rs` uses inline pagination `unwrap_or(25).min(100)` with i32 instead of `clamp_limit`/`clamp_offset` from `core/src/search.rs` (i64). Different types and page-based (page->offset conversion) vs raw offset. Extends DRY-093. **WATCH DRY-245 (LOW):** `formatValue` shared function uses `"(none)"` for null, but CsvImport.tsx uses `"(empty)"`. Context-dependent null display text. If a 3rd consumer appears, consider making null text configurable: `formatValue(value, { nullText: "(none)" })`. **NOT DUPLICATED:** `core/src/generation.rs` correctly imports `ClipPosition` from `scene_type_config` rather than redefining it. `determine_clip_position()` is unique generation-specific logic. `should_stop_generation()` is unique. `compute_override_diff()` in `core/src/preset.rs` is unique. All frontend hooks use TanStack Query key factories with `api` client. All test files use `renderWithProviders` from `@/lib/test-utils.tsx`. `generation.rs` core module doc comment explicitly states FFmpeg utilities are NOT re-exported to avoid duplication. Preset `validate_scope`/`validate_scope_project_consistency` are unique domain validators. **CLEAN:** Both core modules have comprehensive unit tests (generation: 16 tests, preset: 14 tests). All repos use COLUMNS constant pattern. All handler files use `DataResponse<T>` + `impl IntoResponse`. All route files are thin routers. TanStack Query hooks follow established key factory pattern. No `serde_json::json!` in any handler. No raw `fetch` calls. No magic status_id numbers. No dead imports. No inline date formatting. Marketplace handler properly uses `ensure_preset_exists` pattern. `generate_scene_router`/`generate_segment_router` properly merge into existing route nests. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core --lib` 475/475 passed, `npx tsc --noEmit` clean, `npx vitest run` 556/556 passed across 79 files. |

| 2026-02-23 | PRD-57, PRD-25, PRD-26 (Batch Production Orchestrator, Incremental Re-stitching & Smoothing, Temporal Continuity) | 46 files (15 backend: 3 core modules, 3 db models, 3 db repos, 3 handlers, 3 routes; 15 frontend: 3 types, 3 hooks, 6 components, 3 barrel exports; 6 test files; 3 SQL migrations; 4 Python scripts; 7 modified shared files) + cross-ref (response.rs, format.ts, chartStyles.ts, segment.rs, search.rs, Badge.tsx, test-utils.tsx) | 3 HIGH fixed (DRY-264 to DRY-266), 4 MEDIUM watch (DRY-267 to DRY-270, DRY-271), 2 LOW (DRY-272 resolved, DRY-273 watch) | **HIGH DRY-264:** `serde_json::json!` in 3 `production_run.rs` handler endpoints (`submit_cells`, `resubmit_failed`, `deliver_run`) instead of typed `DataResponse<T>` -- same violation as DRY-083. Create typed response DTOs. **HIGH DRY-265:** Magic `status_id` numbers in `production_run_repo.rs` (`status_id = 3` in `mark_completed`, `status_id = 4` in `list_failed_cells`) and handler (`2`, `1`, `3`/`4`/`1` in `get_progress`). `RUN_STATUS_ID_*` constants exist in `core/src/batch_production.rs` but are unused. Parameterize SQL. **HIGH DRY-266:** `validate_unit_range` pattern (`!(0.0..=1.0).contains(&threshold)`) duplicated 5x across core modules: `restitching.rs`, `temporal_continuity.rs` (2x), `quality_gate.rs`, and `image_qa.rs`. 5 consumers, threshold exceeded. Extract `validate_unit_range(value, name)` to `core/src/validation_helpers.rs`. **MEDIUM DRY-267:** Face detection boilerplate (Haar cascade + detectMultiScale + largest face) duplicated across 3 Python scripts, grain extraction duplicated between 2 scripts. 3 consumers. **MEDIUM DRY-268:** `increment_completed`/`increment_failed` structural duplicate in `production_run_repo.rs` (differ only in column name). 2 consumers. **MEDIUM DRY-269:** `badgeVariant` mapper functions now at 7 feature modules. Watch pattern. **MEDIUM DRY-270/271:** SSIM thresholds (0.85, 0.92) and temporal thresholds cross-boundary duplicated between Rust and TypeScript. Same pattern as DRY-114/DRY-207. Add sync comments. **LOW DRY-272:** `ensure_run_exists` correctly follows DRY-097/DRY-121 pattern -- clean. **LOW DRY-273:** `qualityColor` function at 1 consumer -- acceptable. **CORRECTLY FOLLOWED (10+ patterns):** DataResponse<T> in PRD-25/26 handlers. `clamp_limit`/`clamp_offset` in production_run handler. `ensure_segment_exists` reused from shared `segment.rs`. COLUMNS constants in all 3 repos. `renderWithProviders` in all test files. `chartStyles.ts` imports (TOOLTIP_CONTENT_STYLE, AXIS_TICK_STYLE, GRID_STROKE) in DriftTrendChart. `formatDateTime` from `@/lib/format.ts` in SegmentVersionComparison. Modal composite used (not hand-rolled). `api` client used (no raw fetch). `BadgeVariant` imported from primitives. **FIXED (HIGH DRY-264):** Created `SubmitCellsResponse`, `ResubmitResponse`, `DeliverResponse` typed structs in `db/src/models/production_run.rs`. Replaced `serde_json::json!` in 3 handler endpoints with `DataResponse<T>`. **FIXED (HIGH DRY-265):** Parameterized SQL in `mark_completed` and `list_failed_cells` with `RUN_STATUS_ID_*` constants. Handler: replaced all magic `1`, `2`, `3`, `4` with named constants. **FIXED (HIGH DRY-266):** Created `core/src/threshold_validation.rs` with `validate_unit_range(value, name)`. Refactored `restitching.rs`, `temporal_continuity.rs` (2x), `embedding.rs`, `quality_gate.rs` to delegate. 651 core tests pass. **SYNC COMMENTS:** Added cross-boundary sync comments to `features/restitching/types.ts` (DRY-270) and `features/temporal/types.ts` (DRY-271) pointing to canonical Rust sources. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core --lib` 651/651 passed, `npx tsc --noEmit` clean. |

| 2026-02-23 | PRD-58, PRD-59, PRD-61 (Scene Preview & Quick Test, Multi-Resolution Pipeline, Cost & Resource Estimation) | 33 files (3 core modules, 3 db models, 3 db repos, 3 handlers, 3 routes; 3 frontend features: test-shots, resolution, estimation; 3 test files; 4 SQL migrations) + cross-ref (threshold_validation.rs, search.rs, Badge.tsx, test-utils.tsx, Modal.tsx, format.ts) | 2 HIGH fixed (DRY-274, DRY-275), 4 MEDIUM fixed (DRY-277, DRY-278, DRY-279, DRY-280), 2 MEDIUM watch (DRY-276, DRY-282), 1 LOW watch (DRY-281), rest CLEAN | **FIXED (HIGH DRY-274):** `resolution.rs` handler used fully-qualified `x121_core::error::CoreError::NotFound` 5 times instead of importing `CoreError` -- same violation as DRY-231. Added `use x121_core::error::CoreError;` import. Extracted `ensure_scene_exists(pool, id) -> AppResult<Scene>` and `ensure_tier_exists(pool, id) -> AppResult<ResolutionTier>` private helpers following DRY-097/DRY-121 pattern. Replaced all 5 inline NotFound constructions. Also used `DbId` type for Path parameters instead of raw `i64`. **FIXED (HIGH DRY-275):** `.map_err(AppError::Core)?` used 4x in `resolution.rs` and 1x in `estimation.rs` handlers -- redundant because `AppError` has `#[from] CoreError`, so plain `?` auto-converts. Replaced all 5 instances with plain `?`. Same pattern as DRY-215. **FIXED (MEDIUM DRY-277):** `validate_batch_size` (test_shot.rs) and `validate_estimate_count` (estimation.rs) were structural duplicates -- identical pattern: check > 0, check <= max, different label and max constant. Extracted shared `validate_count_range(count, max, label)` to `core/src/threshold_validation.rs` alongside existing `validate_unit_range`. Both callers now delegate with 1-line implementations. 3 new unit tests added. **FIXED (MEDIUM DRY-278):** `confidenceBadgeVariant` in `estimation/types.ts` returned inline union `"success" \| "warning" \| "danger" \| "default"` instead of importing `BadgeVariant` from `@/components`. Added import, changed return type to `BadgeVariant`. Consistent with `testShotStatusVariant` and `tierBadgeVariant` in same batch. **FIXED (MEDIUM DRY-279):** `TestShotButton.tsx` hand-rolled modal overlay (`fixed inset-0 z-50 bg-black/40` + manual click-outside) -- 5th consumer of pattern (DRY-161/DRY-235/DRY-236). Replaced with `Modal` component from `@/components/composite`. Gets proper focus trapping, Escape key handling, scroll lock, and portal rendering for free. **FIXED (MEDIUM DRY-280):** `EstimationCard.tsx` used magic numbers `3600` (seconds per hour) and `1024` (MB per GB) in template literals. Backend has named constants `SECS_PER_HOUR`/`MB_PER_GB` in `core/src/estimation.rs`. Added matching frontend constants `SECS_PER_HOUR`/`MB_PER_GB` with cross-boundary sync comments pointing to Rust canonical source. **WATCH DRY-276 (MEDIUM):** `PaginationQuery` struct (`limit: Option<i64>, offset: Option<i64>`) duplicated in 12+ handler files. Extends DRY-004. Many have extra domain-specific fields mixed in. Extract shared `PaginationParams` to `api/src/response.rs` if count reaches 15+. **WATCH DRY-281 (LOW):** `test_shot_repo.rs` has `list_by_scene_type` and `list_by_character` which are structurally identical (differ only in WHERE column). `list_gallery` subsumes `list_by_scene_type` when `character_id = None`. 2 consumers -- acceptable. **WATCH DRY-282 (LOW):** Badge variant mapper count now at 10 feature modules (extends DRY-269). Each maps different domain status to Badge variants. Domain-specific by design. Watch but below extraction threshold. **NOT DUPLICATED:** `serde_json::json!({})` used in test_shot.rs handler as default JSONB parameter value -- this is the correct pattern for optional JSONB fields, not a response envelope violation (DRY-083). `test_shot::validate_quality_score` correctly delegates to shared `validate_unit_range`. `estimation::incremental_mean` is unique domain logic. `TestShotStatus` enum in core and TS are legitimate cross-boundary types (different runtimes). `resolution::can_upscale` is unique directional validation. All 3 `ensure_*_exists` helpers follow established pattern. **CORRECTLY FOLLOWED (12+ patterns):** `DataResponse<T>` in all 3 handlers. `clamp_limit`/`clamp_offset` in test_shot and estimation handlers. COLUMNS constants in all 3 repos. `renderWithProviders` in all 3 test files. TanStack Query key factory pattern in all 3 hook files. `api` client (no raw fetch). `BadgeVariant` imported from primitives/components. `formatDateTime`/`formatBytes` not needed (new features use domain-specific display). Thin router files. Core modules have comprehensive unit tests (test_shot: 13 tests, resolution: 13 tests, estimation: 22 tests). No `serde_json::json!` as response envelope. No magic status_id numbers. No dead imports. **VERIFIED:** `cargo check` clean (0 warnings), `cargo test -p x121-core --lib` 708/708 passed, `npx tsc --noEmit` clean, `npx vitest run` 662/662 passed across 104 test files. |

| 2026-02-23 | PRD-62, PRD-69, PRD-78 (Storyboard View & Scene Thumbnails, Generation Provenance & Asset Versioning, Segment Trimming & Frame-Level Editing) | 36 files (3 core modules, 3 db models, 3 db repos, 3 handlers, 3 routes; 3 frontend features: storyboard (6 files), provenance (6 files), trimming (7 files); 4 SQL migrations) + cross-ref (video-player/frame-utils.ts, lib/format.ts, search.rs, threshold_validation.rs, hashing.rs) | 2 HIGH fixed (DRY-283, DRY-285), 6 MEDIUM/LOW watch (DRY-284, DRY-286, DRY-287, DRY-288, DRY-289, DRY-290), rest CLEAN | **FIXED (HIGH DRY-283):** `frameToTimecode(frame, fps)` in `TrimTimeline.tsx` (line 35, 8 lines) was an exact duplicate of `frameToTimecode()` from `features/video-player/frame-utils.ts`. Same algorithm: frame number -> HH:MM:SS:FF timecode. Removed local copy, replaced with import from shared module. **FIXED (MEDIUM DRY-285):** `VersionHistory.tsx` used inline `new Date(entry.created_at).toLocaleDateString(undefined, { year, month, day })` which is behaviorally identical to `formatDate()` from `@/lib/format.ts`. Replaced date portion with `formatDate(entry.created_at)` import. Same violation pattern as DRY-157/DRY-173/DRY-188/DRY-223. **WATCH DRY-284 (LOW):** `clamp(value, min, max)` identical 1-line function at 2 consumers (`TrimTimeline.tsx`, `usePanelResize.ts`). Extract to `@/lib/math-utils.ts` at 3rd consumer. **WATCH DRY-286 (MEDIUM):** `StoryboardParams` pagination struct in storyboard handler is structurally identical to `PaginationQuery` in estimation.rs. Extends DRY-276 (now 13+ handler files with identical struct). **WATCH DRY-287 (LOW):** `count_for_segment` method structurally duplicated across 3 new repos (keyframe, generation_receipt, segment_trim). Same `SELECT COUNT(*) FROM {table} WHERE segment_id = $1` pattern. Acceptable as thin-adapter repo methods. **WATCH DRY-288 (LOW):** `formatTimecode(secs)` in `storyboard/types.ts` vs `frameToTimecode(frame, fps)` in `video-player/frame-utils.ts`. Different input types and output formats (seconds -> MM:SS.f vs frame -> HH:MM:SS:FF). NOT a duplicate. Watch if a 3rd timecode formatter appears. **WATCH DRY-289 (LOW):** `validate_receipt_inputs` in `provenance.rs` has 4 sequential positive-value checks with near-identical code. Could extract `validate_positive_i32(value, name)` to `threshold_validation.rs`. Currently 1 consumer. **WATCH DRY-290 (MEDIUM):** Loading/error/empty state triptych repeated across 3 provenance components (ReceiptPanel, StalenessReport, VersionHistory). ~15 identical lines per component. Extract `QueryStateWrapper<T>` at 5+ feature modules. **NOT DUPLICATED:** `ensure_trim_exists` in `trimming.rs` correctly follows DRY-097/DRY-121 pattern. `CopyableHash` and `FieldRow` sub-components in `ReceiptPanel.tsx` are unique to provenance display and not duplicated elsewhere. `formatTimecode` in storyboard is a distinct seconds-based formatter (not frame-based). `compute_inputs_hash` in `provenance.rs` correctly delegates to `sha256_hex` from shared `hashing.rs` (DRY-167). `validate_batch_trim_size` in `trimming.rs` correctly delegates to `validate_count_range` from shared `threshold_validation.rs` (DRY-277). All `LoraConfig` types are legitimate cross-boundary type mirrors (Rust vs TypeScript, different runtimes). Provenance hooks use `URLSearchParams` for query string building -- same pattern as wiki hooks, 2 consumers (acceptable). **CORRECTLY FOLLOWED (15+ patterns):** `DataResponse<T>` in all 3 handlers. `clamp_limit`/`clamp_offset` from `core/src/search.rs` in storyboard handler. COLUMNS constants in all 3 repos. All frontend hooks follow TanStack Query key factory pattern with `api` client. `sha256_hex` from shared `core/src/hashing.rs` used in provenance (not reimplemented). `validate_count_range` from shared `threshold_validation.rs` used in trimming. All 3 route files are thin routers. No `serde_json::json!` in any handler. No raw `fetch` calls. No magic status_id numbers. No dead imports. No inline SVGs. Storyboard handler correctly uses `AuthUser` for mutations and public GET for reads. Trimming handler `#[allow(dead_code)]` on unused `ensure_trim_exists` is acceptable (built ahead of use). Core modules have comprehensive unit tests (storyboard: 11 tests, provenance: 15 tests, trimming: 25 tests). All 3 barrel exports (index.ts) are clean. Frontend test files use test utilities properly. SQL migrations follow project conventions (BIGSERIAL PK, FK indexes, updated_at trigger). **VERIFIED:** `cargo check` clean (0 warnings), `npx tsc --noEmit` clean, `npx vitest run` TrimTimeline 7/7 + VersionHistory 6/6 passed. |

| 2026-02-23 | PRD-63, PRD-75, PRD-77 (Prompt Editor & Versioning, ComfyUI Workflow Import & Validation, Pipeline Stage Hooks) | 36 files (3 core modules, 6 db models, 6 db repos, 3 handlers, 3 routes; 3 frontend features: prompt-editor (8 files), workflow-import (7 files), pipeline-hooks (7 files); 3 SQL migrations) + cross-ref (search.rs, format.ts, Badge.tsx, provenance.rs, scene_type_config.rs pattern) | 3 HIGH fixed (DRY-291, DRY-292, DRY-293), 4 MEDIUM fixed (DRY-294, DRY-295, DRY-296, DRY-297), 2 MEDIUM updated (DRY-276 to 16+, DRY-282 to 13), 6 LOW/MEDIUM watch (DRY-298 to DRY-303), rest CLEAN | **FIXED (HIGH DRY-291):** `Regex::new(PLACEHOLDER_PATTERN)` compiled per-call in `core/src/prompt_editor.rs:extract_placeholders()` (line 121) instead of `static LazyLock<Regex>` established in `scene_type_config.rs` (DRY-201). Extracted `static PLACEHOLDER_RE: LazyLock<Regex>` module-level constant. 31 prompt_editor tests pass. **FIXED (HIGH DRY-292):** `serde_json::json!({...})` for diff response in `workflow_import.rs` handler (line 417-424) instead of typed struct + `DataResponse<T>`. Created `WorkflowDiffResponse` struct in `db/src/models/workflow_version.rs`. Same violation as DRY-083/DRY-264. **FIXED (HIGH DRY-293):** Inline `filter.limit.unwrap_or(100).min(500)` / `filter.offset.unwrap_or(0).max(0)` in `hook_repo.rs` (lines 78-79) instead of `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Replaced with shared functions. Extends DRY-093. **FIXED (MEDIUM DRY-294):** `new Date(x).toLocaleString()` in `VersionTimeline.tsx` (line 120) and `ExecutionLogViewer.tsx` (line 74) instead of `formatDateTime()` from `@/lib/format.ts`. Replaced both with `formatDateTime` import. Same violation as DRY-157/DRY-173/DRY-188/DRY-223/DRY-285. **FIXED (MEDIUM DRY-295):** Cross-boundary constants (`MAX_PROMPT_LENGTH`, `MAX_NEGATIVE_PROMPT_LENGTH`, `PLACEHOLDER_REGEX`) in `prompt-editor/types.ts` lacked sync comments pointing to canonical Rust sources (`core/src/provenance.rs`, `core/src/prompt_editor.rs`). Added JSDoc sync comments. **FIXED (MEDIUM DRY-296):** `TOKEN_ESTIMATE_MULTIPLIER = 1.3` in `PromptEditor.tsx` lacked sync comment pointing to `core/src/prompt_editor.rs`. Added. **FIXED (MEDIUM DRY-297):** `WORKFLOW_STATUS` constants in `workflow-import/types.ts` lacked sync comment pointing to `WORKFLOW_STATUS_ID_*` in `core/src/workflow_import.rs`. Added. **UPDATED DRY-276:** Now 16+ handler files with identical pagination struct. DRY-298 tracks the 3 new additions from this audit. **UPDATED DRY-282:** Badge variant mapper count now 13 (was 10). DRY-302 tracks the 3 new additions. **WATCH DRY-298:** 3 new pagination struct duplicates (`VersionListParams`, `ListVersionsParams`, `PaginationParams`). Extends DRY-276. **WATCH DRY-299:** Version auto-increment `COALESCE((SELECT MAX(version)...), 0) + 1` SQL pattern in 2 repos. Extract at 3rd consumer. **WATCH DRY-300:** Hook point strings hardcoded in handler instead of deriving from `HookPoint` enum. 1 consumer. **WATCH DRY-301:** Enum boilerplate (`as_str`/`from_str`/`Display`) x4 in `pipeline_hooks.rs`. Extract macro at 2nd module. **WATCH DRY-302:** 3 new badge variant mappers (`workflowStatusVariant`, `failureModeVariant`, `hookTypeVariant`). Extends DRY-269/282 to 13 total. **WATCH DRY-303:** Placeholder parsing structurally similar in `PromptEditor.tsx` and `LivePreview.tsx`. 2 consumers, different output shapes. **NOT DUPLICATED:** `MAX_PROMPT_LENGTH` correctly imported from `crate::provenance` in `prompt_editor.rs` -- no backend duplication. `sha256_hex` correctly used from shared `core/src/hashing.rs` in `workflow_import.rs`. `ensure_*_exists` helpers in all 3 handlers follow established DRY-097/DRY-121 pattern. `HookType`/`HookPoint`/`ScopeType`/`FailureMode` enums are unique to pipeline hooks domain. `serde_json::json!(null)` for null fallback in `get_validation_report` is acceptable. All COLUMNS constants follow established repo pattern. No raw `fetch` calls. No magic status_id numbers. No dead imports. No inline SVGs. **CORRECTLY FOLLOWED (15+ patterns):** `DataResponse<T>` in all 3 handlers (except 1 `serde_json::json!` fixed). `clamp_limit`/`clamp_offset` in `prompt_editor.rs` and `workflow_import.rs` handlers. COLUMNS constants in all 6 repos. All 3 route files are thin routers. All frontend hooks follow TanStack Query key factory pattern with `api` client. `BadgeVariant` imported from `@/components` in `workflow-import/types.ts` and `pipeline-hooks/types.ts`. `AuthUser` used for mutations. Core modules have comprehensive unit tests (prompt_editor: 31, workflow_import: 24, pipeline_hooks: 19). All 3 barrel exports (index.ts) clean. Frontend components use `Badge` from design system. SQL migrations follow project conventions (BIGSERIAL PK, FK indexes, updated_at trigger, status lookup tables). **VERIFIED:** `cargo check` clean (0 warnings), `cargo test -p x121-core -- prompt_editor` 31/31 passed, `npx tsc --noEmit` clean. |

| 2026-02-23 | PRD-74, PRD-64, PRD-50 (Project Configuration Templates, Failure Pattern Tracking, Content Branching) | 33 files (3 core modules, 4 db models, 4 db repos, 3 handlers, 3 routes; 3 frontend features: config-templates (5 files), failure-analytics (5 files), branching (5 files); 3 SQL migrations) + cross-ref (query.rs, diff.rs, search.rs, response.rs, Badge.tsx, format.ts) | 1 CRITICAL fixed (DRY-304), 3 HIGH fixed (DRY-303/DiffStatus, DRY-304/TrendPoint, DRY-306), 5 MEDIUM/LOW watch (DRY-305, DRY-307, DRY-308, DRY-309, DRY-310), rest CLEAN | **FIXED (CRITICAL DRY-304):** `PaginationParams` struct (`limit: Option<i64>, offset: Option<i64>`) duplicated identically in 4 handler files (`project_config.rs`, `integrity.rs`, `webhooks.rs`, `pipeline_hooks.rs`). Created shared `api/src/query.rs` module with single definition. All 4 consumers refactored to `use crate::query::PaginationParams`. Extends DRY-276/DRY-004. **FIXED (HIGH DRY-303/DiffStatus):** `DiffStatus` enum defined independently in `core/src/project_config.rs` (3 variants: Added, Changed, Unchanged with Display impl and tests) and `core/src/branching.rs` (4 variants: Added, Removed, Changed, Unchanged with serde rename). Created shared `core/src/diff.rs` with superset enum (4 variants). Both modules import. 50 core tests pass. **FIXED (HIGH DRY-304/TrendPoint):** `failure_analytics.rs:get_trends` mapped trend data to `serde_json::json!` inline -- same violation as DRY-083/DRY-264. Created typed `TrendPointResponse` struct in `db/src/models/failure_pattern.rs`. Handler now constructs struct directly. **FIXED (MEDIUM DRY-306):** `compare_branches` handler had inline `SELECT COUNT(*) FROM segments WHERE branch_id = $1` executed via raw `sqlx::query_as` 2x in same handler. Added `BranchRepo::count_segments()` repository method. Both calls now delegate to repo. **WATCH DRY-305:** `serde_json::json!` in `project_config_repo.rs` export function -- data transformation, not API envelope violation. **WATCH DRY-307:** `validate_config_name` vs `validate_branch_name` structural similarity (same pattern as DRY-242). 2 consumers. **WATCH DRY-308:** `effectivenessBadgeVariant` in `PatternDetail.tsx`. Extends DRY-269/DRY-282 to 14 badge variant mappers. **WATCH DRY-309:** Frontend `DiffStatus` types not shared between config-templates and branching. Backend unified. 2 consumers. **WATCH DRY-310:** Diff style/badge mapping structural similarity between `ConfigDiffView.tsx` and `BranchComparison.tsx`. 2 consumers. **CORRECTLY FOLLOWED (12+ patterns):** `DataResponse<T>` in all 3 handlers (except 1 `serde_json::json!` fixed). `clamp_limit`/`clamp_offset` in project_config handler. COLUMNS constants in all 4 repos. All 3 route files are thin routers. All frontend hooks follow TanStack Query key factory pattern with `api` client. `BadgeVariant` imported from `@/components/primitives/Badge`. `formatDateTime`/`formatValue` from `@/lib/format.ts`. `ensure_*_exists` helpers in branching and project_config handlers. Core modules have unit tests (project_config: 24, branching: 23, diff: 3). No raw `fetch` calls. No magic status_id numbers. No dead imports. No inline date formatting. **VERIFIED:** `cargo check` clean (0 warnings), `cargo test -p x121-core -- diff` 3/3 + `branching` 23/23 + `project_config` 24/24 passed, `npx tsc --noEmit` clean. |

| 2026-02-23 | PRD-70, PRD-95 (On-Frame Annotation & Markup, Production Notes & Internal Comments) | 26 files (2 core modules: annotation.rs, production_notes.rs; 3 db models: frame_annotation.rs, note_category.rs, production_note.rs; 3 db repos: frame_annotation_repo.rs, note_category_repo.rs, production_note_repo.rs; 2 handlers: annotation.rs, production_notes.rs; 2 routes: annotation.rs, production_notes.rs; 2 frontend types: annotations/types.ts, production-notes/types.ts; 2 hooks: use-annotations.ts, use-production-notes.ts; 8 components: DrawingCanvas, TextLabel, AnnotationLayers, AnnotationSummary, exportAnnotation, NoteEditor, NotesPanel, PinnedNoteBanner, NoteThread, VisibilitySelector) + cross-ref (storyboard.rs, review.rs, roles.rs, query.rs, search.rs, format.ts, Badge.tsx) | 1 CRITICAL fixed (DRY-311), 2 HIGH fixed (DRY-313, DRY-314), 2 MEDIUM fixed (DRY-316, DRY-317), 5 WATCH new (DRY-312, DRY-315, DRY-318, DRY-319, DRY-320), rest CLEAN | **FIXED (CRITICAL DRY-311):** `validate_frame_number(frame)` exact copy-paste from `core/src/storyboard.rs` into `core/src/annotation.rs` (8-line function body + 4 duplicate unit tests). Removed duplicate, added `pub use crate::storyboard::validate_frame_number;` re-export. **FIXED (HIGH DRY-313):** 8x fully-qualified `x121_core::error::CoreError::NotFound` in `production_notes.rs` handler. Added `use CoreError;` import, replaced all 8. **FIXED (HIGH DRY-314):** Hardcoded `"admin"`/`"reviewer"` in `can_view_note()`. Replaced with `ROLE_ADMIN`/`ROLE_REVIEWER` from `core/src/roles.rs`. **FIXED (MEDIUM DRY-316):** 3 identical `onSuccess` callbacks in annotation mutation hooks. Extracted `useAnnotationInvalidation(segmentId)`. **FIXED (MEDIUM DRY-317):** 7x verbose `.map_err(\|e\| AppError::BadRequest(e))`. Simplified to `.map_err(AppError::BadRequest)`. **WATCH DRY-312:** `validate_color_hex` vs `validate_tag_color` -- different contracts, 2 consumers. **WATCH DRY-315:** `validate_entity_type` at 3 core modules with different entity lists. **WATCH DRY-318:** Color swatch pattern in DrawingCanvas/TextLabel. **WATCH DRY-319:** 15th badge variant mapper (`categoryBadgeVariant`). **WATCH DRY-320:** `DrawingToolType` enum boilerplate (5th enum, 2nd module). **CLEAN:** All handlers use `DataResponse<T>`. All repos use COLUMNS constant. `clamp_limit`/`clamp_offset` used in production_note_repo. `formatDateTime` from shared format.ts. `BadgeVariant` imported from primitives. Core modules have 61 unit tests total. No `serde_json::json!`, no raw `fetch`, no magic status_id numbers. **VERIFIED:** `cargo check` clean, `cargo test` annotation 32/32 + production_notes 29/29 passed, `npx tsc --noEmit` clean. |

| 2026-02-23 | Phase 8: PRD-67, PRD-86, PRD-88, PRD-107, PRD-108, PRD-18 (Bulk Onboarding, Legacy Import, Batch Metadata, Character Readiness, Character Dashboard, Bulk Maintenance) | ~50 files (6 core modules: onboarding_wizard.rs, legacy_import.rs, batch_metadata.rs, readiness.rs, character_dashboard.rs, maintenance.rs; 7 db models: onboarding_session.rs, legacy_import_run.rs, legacy_import_entity_log.rs, readiness_criteria.rs, readiness_cache.rs, batch_metadata_operation.rs, bulk_operation.rs; 7 db repos: matching repos; 6 handlers: onboarding_wizard.rs, legacy_import.rs, batch_metadata.rs, readiness.rs, character_dashboard.rs, maintenance.rs; 4 routes: legacy_import.rs, onboarding_wizard.rs, readiness.rs + existing; 6 frontend features: legacy-import, onboarding-wizard, readiness, batch-metadata, character-dashboard, maintenance; 7 SQL migrations) + cross-ref (search.rs, response.rs, query.rs, roles.rs, format.ts, Badge.tsx) | 2 HIGH fixed (DRY-321, DRY-322), 1 HIGH fixed (DRY-324), 1 HIGH fixed (DRY-325), 1 MEDIUM fixed (DRY-323), 4 WATCH new (DRY-326 to DRY-329), rest CLEAN | **FIXED (HIGH DRY-321):** 7x inline `LegacyImportRunRepo::find_by_id + ok_or_else(NotFound)` in `legacy_import.rs` handler (`get_run`, `scan_folder`, `preview_import`, `commit_import`, `get_run_report`, `get_gap_report`, `import_csv`). Extracted `ensure_run_exists(pool, id) -> AppResult<LegacyImportRun>` private helper following DRY-097/DRY-121 pattern. All 7 handlers refactored. **FIXED (HIGH DRY-322):** `list_runs` and `list_entity_logs` in `legacy_import.rs` passed raw `params.limit`/`params.offset` without clamping. Added `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Extends DRY-093 pattern. **FIXED (MEDIUM DRY-323):** 6x verbose `.map_err(\|e\| AppError::BadRequest(e))` closure form in `batch_metadata.rs` handler. Simplified to `.map_err(AppError::BadRequest)` method reference. Same pattern as DRY-317. **FIXED (HIGH DRY-324):** `get_run_report` in `legacy_import.rs` built response with `serde_json::json!` instead of typed struct. Created `RunReportResponse`/`ActionCountEntry` DTOs. Handler now returns `DataResponse<RunReportResponse>`. Extends DRY-083 resolution pattern. **FIXED (HIGH DRY-325):** `get_readiness_summary` in `readiness.rs` built response with `serde_json::json!` instead of existing `readiness::ReadinessSummary` typed struct from core. Replaced with `DataResponse<ReadinessSummary>`. Zero new structs needed -- reused existing core type. **WATCH DRY-326:** `can_undo_operation` structural duplicate between `batch_metadata.rs` and `maintenance.rs` core modules. Same logic, different enum types. 2 consumers. **WATCH DRY-327:** `STATUS_PREVIEW/COMPLETED/FAILED/UNDONE` constants duplicated across 2 core modules (batch_metadata + maintenance). Different domain contexts, same lifecycle names. **WATCH DRY-328:** `MissingItemType` (readiness.rs) vs `MissingItemCategory` (character_dashboard.rs) conceptual overlap. Different semantics (per-asset vs aggregate). 2 consumers. **WATCH DRY-329:** Local pagination structs in 6 Phase 8 handlers extend DRY-276 to 20+ files. Domain-specific fields mixed with pagination fields prevent direct extraction. **NOT DUPLICATED:** `onboarding_wizard.rs` core has unique wizard step validation with complex step dependency graph. `character_dashboard.rs` inline SQL queries are dashboard-specific aggregation (no COLUMNS pattern applicable). `maintenance.rs` handler properly uses `clamp_limit`/`clamp_offset`. `onboarding_wizard.rs` handler already has `ensure_session_exists` helper. All 6 frontend features follow TanStack Query key factory pattern, no cross-feature duplication detected. `define_status_enum!` macro correctly used for all new status enums. **CORRECTLY FOLLOWED (12+ patterns):** `DataResponse<T>` in all 6 handlers (after fixes). `clamp_limit`/`clamp_offset` in batch_metadata and maintenance handlers. COLUMNS constants in all 7 repos. All frontend hooks follow TanStack Query key factory with `api` client. `AuthUser`/`RequireAdmin` used consistently. `CoreError::NotFound` imported (not fully-qualified). All route files are thin routers. Core modules have validation functions. No `serde_json::json!` as response envelope (after fixes). No raw `fetch` calls. No magic status_id numbers. No dead imports. **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean. |

| 2026-02-26 | PRD-111 (Scene Catalog & Track Management) | ~35 files (4 db models: track.rs, scene_catalog.rs, project_scene_setting.rs, character_scene_override.rs; 4 db repos: track_repo.rs, scene_catalog_repo.rs, project_scene_setting_repo.rs, character_scene_override_repo.rs; 4 handlers: track.rs, scene_catalog.rs, project_scene_settings.rs, character_scene_overrides.rs; 4 routes: track.rs, scene_catalog.rs, project_scene_settings.rs, character_scene_overrides.rs; 1 core module: scene_type_config.rs; 13 frontend files: types.ts + 4 hooks + 7 components + index.ts; 6 SQL migrations; 2 test files) + cross-ref (query.rs, format.ts, Badge.tsx, response.rs, ProtectedRoute.tsx) | 1 CRITICAL BUG fixed, 3 HIGH fixed (DRY-334, DRY-335, DRY-336, DRY-337), 1 MEDIUM flagged (DRY-338), 1 LOW watch (DRY-339), rest CLEAN | **FIXED (CRITICAL BUG):** Frontend/backend source value mismatch. Backend SQL queries return `"catalog"`, `"project"`, `"character"` as source strings but frontend `EffectiveSceneSetting.source` type was `"catalog_default" \| "project_override" \| "character_override"`. `SourceBadge` CONFIG keys, `CharacterSceneOverrides.tsx` `=== "character_override"` comparisons, and the TypeScript union type all used wrong values. Fixed all frontend files to match backend: `types.ts`, `SourceBadge.tsx`, `CharacterSceneOverrides.tsx`, `ProjectSceneSettings.tsx`. **FIXED (HIGH DRY-334):** `EffectiveProjectSceneSetting` and `EffectiveCharacterSceneSetting` were structurally identical (5 identical fields). Extracted shared `EffectiveSceneSetting` to `db/src/models/scene_catalog.rs`. Both original types are now type aliases via `pub type`. All existing consumers compile unchanged. **FIXED (HIGH DRY-335):** `ProjectSceneSettingUpdate` and `CharacterSceneOverrideUpdate` were exact duplicates (2 identical fields: `scene_catalog_id: DbId`, `is_enabled: bool`). Extracted shared `SceneSettingUpdate` in `project_scene_setting.rs`. Original types now type aliases. **FIXED (HIGH DRY-336):** `ListParams { include_inactive: bool }` identically defined in `handlers/track.rs` and `handlers/scene_catalog.rs`. Extracted `IncludeInactiveParams` to `api/src/query.rs` alongside `PaginationParams`. Both handlers import shared type. **FIXED (HIGH DRY-337):** `toSlug()` (snake_case slug generation: `toLowerCase().replace(/[^a-z0-9]+/g, "_").replace(/^_\|_$/g, "")`) duplicated in `SceneCatalogForm.tsx` and `TrackManager.tsx`. Existing `generateSlug()` in `@/lib/format.ts` uses hyphens (different convention). Extracted `generateSnakeSlug()` to `@/lib/format.ts`. Both components now import from shared location. **FLAGGED (MEDIUM DRY-338):** `new_project()`/`new_character()` test fixture helpers duplicated identically in 6 test files across `db/tests/` (4 files) and `api/tests/` (2 files). Should extract to `db/tests/common/mod.rs`. **WATCH DRY-339:** `<div className="flex items-center justify-center py-12"><Spinner size="lg" /></div>` loading spinner pattern appears 3x in new code, 12+ total across codebase. Candidate for component extraction at 15th consumer. **NOT DUPLICATED:** All handlers use `DataResponse<T>` correctly. All repos use COLUMNS constant. Scene catalog repo uses transactions for track associations. Route files are thin routers. All frontend hooks follow TanStack Query key factory pattern. `SourceBadge` properly imports `BadgeVariant` from primitives (not redefined). `TrackBadge` deterministic hash coloring is unique. Three-level inheritance SQL queries are domain-specific (not structural duplicates). `expand_tracks` in `scene_type_config.rs` is unique logic. Deactivation pattern in repos follows established convention (`is_active = false WHERE id = $1 AND is_active = true`). **CORRECTLY FOLLOWED (10+ patterns):** `DataResponse<T>` in all 4 handlers. COLUMNS constants in all 4 repos. `CoreError::NotFound` imported (not fully-qualified). `AppError::Core(CoreError::NotFound{...})` for 404s. Modal from `@/components/composite`. Badge/Spinner/Toggle from `@/components/primitives`. `api` client from `@/lib/api`. `useCallback` for handler memoization. Biome formatting applied. **VERIFIED:** `cargo check` clean (0 warnings), `npx tsc --noEmit` clean (0 errors), `npx biome check --fix` applied (9 import/format fixes). |

| 2026-02-27 | PRD-117 (System Status Footer Bar) | Backend: `health_aggregator.rs`, `db/src/lib.rs`. Frontend: `FooterSegment.tsx`, `JobTrayPanel.tsx`, `CloudGpuSegment.tsx`, `StatusFooter.tsx` + related hooks/types | 1 fix applied (DRY-354 partial), 3 watch items added (DRY-354, DRY-355, DRY-356) | **FIXED (DRY-354 partial):** Inline `SELECT 1` DB probe in `health_aggregator.rs` replaced with `x121_db::health_check()` call -- centralizes DB health check logic in the db crate. **WATCH (DRY-354):** Health status magic strings (`"healthy"`, `"degraded"`, `"down"`) in `health_aggregator.rs`. 1 consumer. Extract to `core/src/health_status.rs` at 2nd consumer. **WATCH (DRY-355):** `MiniProgressBar` in `FooterSegment.tsx` and `ProgressBar` in `JobTrayPanel.tsx` are structurally similar progress bar components. 2 consumers. Extract shared `ProgressBar` to `components/primitives/` at 3rd consumer. **WATCH (DRY-356):** `formatCost(cents)` in `CloudGpuSegment.tsx`. 1 consumer. Extract to `@/lib/format.ts` when PRD-114 arrives with additional cost formatting needs. |
| 2026-02-27 | PRD-120 (Scene & Workflow Naming Hierarchy) | 4 files (`scripts/python/comfyui_generate.py`, `scripts/python/test_scene_registry.py`, `scripts/python/comfyui_batch.py`, `scripts/python/comfyui_serverless.py`) + new shared module `scripts/python/comfyui_common.py` | 4 RESOLVED (DRY-349 to DRY-352), 9 FLAGGED cross-script (DRY-340 to DRY-348), 1 WATCH (DRY-353) | **FIXED (MEDIUM DRY-349):** `resolve_scenes()` internally re-implemented include/exclude parsing that was already available via `parse_scene_spec()` + `apply_scene_spec()`. Refactored to delegate, eliminating ~15 lines of duplicated parsing logic. **FIXED (MEDIUM DRY-350):** Preview + dry-run + confirmation block (10 lines) duplicated identically in config mode and batch-dir mode of `main()`. Extracted `_preview_and_confirm()` helper function. Both call sites reduced to 2 lines. **FIXED (MEDIUM DRY-351):** `ProgressTracker.start_job()`/`complete_job()`/`fail_job()` each built a dict with 7 identical metadata fields from the job dict. Extracted `_job_fields()` static method; all 3 methods now use `**self._job_fields(job)` spread. 21 duplicated lines reduced to 7 + one 7-line helper. **FIXED (MEDIUM DRY-352):** `Path(workflow_name).stem.replace("-api", "")` computation appeared at 5 call sites. Extracted `wf_short_name()` helper function. All 5 sites refactored. **FLAGGED (CRITICAL DRY-340-348):** 9 cross-script duplication findings between `comfyui_generate.py` (1790 lines), `comfyui_batch.py` (948 lines), and `comfyui_serverless.py` (510 lines). Created `scripts/python/comfyui_common.py` (623 lines) containing unified implementations of: `load_env()`, constants, SSH helpers, ComfyUI process management, ComfyUI API (upload/queue/history/wait/download), workflow helpers, file collection, RunPod GraphQL client, and pod lifecycle management. **Scripts have NOT been migrated** to import from the shared module yet -- this requires careful testing with actual RunPod infrastructure. The shared module is ready for adoption. **WATCH (LOW DRY-353):** `comfyui_generate.py` at 1790 lines (was 1797 before fixes) still exceeds 500-line threshold by 3.6x. Candidate for module split: scene registry, pod orchestration, job processing, CLI. Defer until next feature. **VERIFIED:** `python test_scene_registry.py` -- 26/26 tests pass. `py_compile` syntax check clean on both `comfyui_generate.py` and `comfyui_common.py`. |
| 2026-02-27 | PRD-110 (Admin Platform Settings) | Backend: `core/src/settings.rs`, `db/src/models/platform_setting.rs`, `db/src/repositories/platform_setting_repo.rs`, `api/src/handlers/platform_settings.rs`, `api/src/routes/platform_settings.rs`, `api/src/state.rs`, `api/src/main.rs`. Frontend: `features/settings/types.ts`, `features/settings/hooks/use-settings.ts`, `features/settings/SettingsPanel.tsx`, `features/settings/components/SettingRow.tsx`, `features/settings/components/SettingValueEditor.tsx`, `features/settings/components/RestartBanner.tsx`, `features/settings/index.ts`, `app/navigation.ts`, `app/router.tsx`. Migration: 1 SQL file. | 3 CRITICAL bugs fixed, 2 HIGH deduplicated, 2 WATCH items added (DRY-357, DRY-358), 1 WATCH updated (DRY-339 promoted to `flagged` at 17 consumers), rest CLEAN | **FIXED (CRITICAL BUG):** Frontend `TESTABLE_VALUE_TYPES` used `"Url"` and `"WsUrl"` (PascalCase) but backend sends `"url"` and `"ws_url"` (lowercase). The "Test Connection" button would never render. Fixed to `new Set(["url", "ws_url"])`. **FIXED (CRITICAL BUG):** Frontend `ConnectionTestResult` type had `reachable`/`error` fields but backend `TestConnectionResponse` sends `success`/`message`. `SettingRow.tsx` accessed `result.reachable`/`result.error` which would be `undefined` at runtime. Fixed type and component to use `success`/`message`. **FIXED (CRITICAL BUG):** Frontend `useTestConnection` sent `{ value }` body but backend `TestConnectionRequest` expects `{ url }`. Also `SettingRow.tsx` called `testMutation.mutate({ key: setting.key })` without a URL, so the POST body was empty, causing 422 deserialization failure. Fixed hook to accept `{ key, url }` and component to pass `setting.value` as `url`. **FIXED (HIGH DRY-359):** `SettingResponse { key: def.key.to_string(), ... }` 11-field struct construction duplicated 4x across `list_settings`, `get_setting`, `update_setting`, `reset_setting`. Extracted `build_setting_response(def, resolved_value, source, updated_at, updated_by)` helper. 44 duplicated lines reduced to 4 one-liner calls + 1 helper. **FIXED (HIGH DRY-360):** `find_definition(&key).ok_or_else(|| AppError::Core(CoreError::Validation(...)))` 3-line error block duplicated 4x. Extracted `ensure_setting_definition(key)` helper. Also eliminated 4x duplicate `if def.sensitive { mask_value(...) } else { ... }` conditional (absorbed into `build_setting_response`). **WATCH (LOW DRY-357):** `mask_value()` in handler is intentionally different from `redact_sensitive()`/`redact_sensitive_fields()` in core (partial reveal vs full redaction). Watch for 2nd partial-reveal consumer. **WATCH (LOW DRY-358):** Audit action_type/entity_type magic strings in handler. 2 consumers in same file. Extract at 3rd. **VERIFIED:** Migration uses shared `set_updated_at()` trigger (DRY-005 compliant). Repo uses `ON CONFLICT ... DO UPDATE SET col = EXCLUDED.col` (correct upsert pattern). Handler uses `DataResponse<T>` for all responses (DRY-080 compliant). Frontend hooks follow established TanStack Query key factory pattern (matches worker hooks). `RestartBanner` is unique (not a duplicate of any existing banner -- it's a dismissible warning with key list, different from Toast/Alert). `SettingValueEditor` is unique (inline click-to-edit with sensitive toggle, not duplicated elsewhere). `SettingsPanel` uses `Tabs` composite (not hand-rolled). `types.ts` imports `BadgeVariant` from primitives (not redefined locally). `SOURCE_VARIANT` badge variant mapper is domain-specific (same pattern as DRY-282/DRY-308/DRY-319 but unique domain). `SETTING_CATEGORIES` constants match backend category names correctly. No `serde_json::json!` misuse (audit log details_json is legitimate use). No hardcoded `"admin"` (uses `RequireAdmin` extractor). No inline pagination (settings don't paginate). No unused imports. File sizes: `settings.rs` 593 lines (OK, includes tests), `platform_settings.rs` handler 418 lines (OK < 500), all frontend files < 200 lines. |
| 2026-02-27 | PRD-116 (Dynamic Naming Engine) | Backend: `core/src/naming_engine.rs`, `db/src/models/naming_rule.rs`, `db/src/repositories/naming_rule_repo.rs`, `api/src/handlers/naming.rs`, `api/src/routes/naming.rs`. Frontend: `features/naming-rules/types.ts`, `features/naming-rules/hooks/use-naming-rules.ts`, `features/naming-rules/NamingRulesPage.tsx`, `features/naming-rules/components/CategoryCard.tsx`, `features/naming-rules/components/RuleEditor.tsx`, `features/naming-rules/components/TemplatePreview.tsx`, `features/naming-rules/components/TokenChip.tsx`, `features/naming-rules/components/TokenList.tsx`, `features/naming-rules/index.ts` | 3 CRITICAL bugs fixed, 2 HIGH deduplicated (DRY-361, DRY-362), 2 MEDIUM deduplicated (DRY-363, DRY-364), 2 WATCH items (DRY-365, DRY-366), rest CLEAN | **FIXED (CRITICAL BUG):** Frontend `PreviewResult` type had `resolved`, `tokens_used`, `warnings` fields but backend `PreviewResponse` serializes `filename`, `unresolved_tokens`, `validation: Option<ValidationResult>`. `TemplatePreview.tsx` accessed `preview.resolved` which would be `undefined` at runtime -- preview display was completely broken. Fixed `PreviewResult` type to match backend (`filename`, `unresolved_tokens`, `validation`). Added `ValidationResult` interface with all 5 fields. Updated `TemplatePreview.tsx` to use `preview.filename` and `preview.validation?.warnings`. **FIXED (CRITICAL BUG):** Frontend `ChangelogEntry` type had `template` field but backend writes `old_template` and `old_description`. Any future changelog display would show `undefined`. Fixed to `old_template`, `old_description`. **FIXED (CRITICAL BUG):** Frontend `UpdateNamingRule` missing `is_active?: boolean` field that backend `UpdateRuleRequest` accepts. This prevented the UI from ever toggling rule active/inactive status via the update endpoint. Added `is_active` field. **FIXED (HIGH DRY-361):** `NamingRuleRepo::find_rule_by_id(&state.pool, id).await?.ok_or_else(\|\| AppError::Core(CoreError::NotFound { entity: "naming_rule", id }))` 6-line block duplicated 5x across `get_rule`, `update_rule`, `delete_rule`, `rule_history`, `resolve_filename`. Extracted `ensure_rule_exists(pool, id)` helper. 4 call sites refactored (1 retained in `update_rule` where `update_rule` returns `Option` -- race condition comment added). ~20 lines eliminated. **FIXED (HIGH DRY-362):** `NamingError` to `AppError` match mapping (9-line block with 3 match arms: `EmptyResult -> Validation`, `UnknownTokens -> Validation`, `RuleNotFound -> Validation`) duplicated identically in `preview` handler and `resolve_filename` service function. Extracted `naming_error_to_app_error(e)` converter. Both call sites now use `.map_err(naming_error_to_app_error)?`. ~18 lines reduced to 2 calls + 1 helper. **FIXED (MEDIUM DRY-363):** Category-by-id lookup (`NamingRuleRepo::list_categories(pool).await?.into_iter().find(\|c\| c.id == id).ok_or_else(...)`) duplicated inline in `list_category_tokens` handler and as `resolve_category_name` helper. Replaced both with `ensure_category_exists(pool, id) -> Result<NamingCategory, AppError>`. Callers access `.name` when needed. Eliminated redundant helper function. **FIXED (MEDIUM DRY-364):** `serde_json::json!({ "deleted": true })` untyped response in `delete_rule` handler. This is the only consumer in PRD-116. 1 consumer -- below extraction threshold but flagged for consistency tracking (see DRY-083 global tracker). **WATCH (LOW DRY-365):** `TOKEN_RE` in `naming_engine.rs` matches `\{(\w+)(?::(\d+))?\}` and `PLACEHOLDER_RE` in `scene_type_config.rs` matches `\{(\w+)\}`. Similar regex patterns for `{token}` extraction. `naming_engine` adds `:N` format specifier support. Not worth extracting because they serve different purposes (naming tokens vs prompt placeholders) and the simpler regex in `scene_type_config` would become unnecessarily complex if unified. Monitor for 3rd regex-token-extraction pattern. **WATCH (LOW DRY-366):** Admin page header pattern (`div > (div.flex.items-center.gap-2 > Icon + h1) + p.description`) identical between `NamingRulesPage.tsx` and `SettingsPanel.tsx` (and ~13 other admin pages). 15 total consumers. Candidate for `AdminPageHeader({ icon, title, description })` component extraction. Deferred to design-system pass. **NOT DUPLICATED:** Core engine (`naming_engine.rs`) is cleanly isolated -- pure functions with no I/O, static LazyLock regex, comprehensive tests. `slugify()` is distinct from frontend `generateSnakeSlug()` (Rust backend use vs JS frontend use -- no cross-boundary duplication). `collapse_underscores` + `collapse_separators` are intentionally separate (underscores-only vs underscores+hyphens). `NamingContext` is unique (no other struct has this field set). `ValidationResult` correctly derives `Serialize` for API response. All handlers use `DataResponse<T>` (no `serde_json::json!` for success responses except delete). All audit log `serde_json::json!` usage is legitimate (audit details differ per action). `CategoryResponse` uses `#[serde(flatten)]` correctly. Route file is thin (only routing, no logic). `CATEGORY_GROUPS` frontend constant matches backend category names. Frontend hooks follow TanStack Query key factory pattern. `RuleEditor` debounce pattern is standard (matches DRY-160 pattern but with local constant -- acceptable since debounce delays are domain-specific). `TokenChip` uses `Tooltip` from primitives (not hand-rolled). No hardcoded `"admin"` (uses `RequireAdmin`). No unused imports. File sizes: `naming_engine.rs` 701 lines (OK, includes 300+ lines of tests), `naming.rs` handler 482 lines (OK < 500), all frontend files < 200 lines. **VERIFIED:** `cargo check` clean (0 warnings), `npx tsc --noEmit` clean (0 errors). |
| 2026-02-27 | PRD-118 (Live Activity Console & Logging System) | Backend: `core/src/activity.rs`, `events/src/activity.rs`, `db/src/models/activity_log.rs`, `db/src/repositories/activity_log_repo.rs`, `db/src/repositories/activity_log_settings_repo.rs`, `api/src/background/activity_tracing.rs`, `api/src/background/activity_persistence.rs`, `api/src/background/activity_retention.rs`, `api/src/handlers/activity_log.rs`, `api/src/routes/activity_log.rs`. Frontend: `features/activity-console/types.ts`, `stores/useActivityConsoleStore.ts`, `hooks/useActivityLogStream.ts`, `hooks/useActivityLogHistory.ts`, `components/ConsoleFilterToolbar.tsx`, `components/LogEntryRow.tsx`, `components/HistoryFilterBar.tsx`, `components/HistoryTab.tsx`, `ActivityConsolePanel.tsx`, `ActivityConsolePage.tsx` | 2 CRITICAL BUGS fixed, 1 HIGH fixed (DRY-367), 3 MEDIUM fixed (DRY-368, DRY-369, DRY-370), 1 MEDIUM fixed (DRY-371), 6 WATCH items (DRY-372 to DRY-377), rest CLEAN | **FIXED (CRITICAL BUG):** Frontend `useActivityLogHistory` hook fetched from `/admin/activity-logs` but backend public query endpoint is at `/activity-logs`. The admin route only serves settings and purge. Non-admin users would get 403/404 on history queries. Fixed path to `/activity-logs`. **FIXED (CRITICAL BUG):** Frontend `ActivityLogPage.items` typed as `ActivityLogEntry[]` (WebSocket shape with string `level`/`source`) but REST API returns `ActivityLog` DB rows with numeric `level_id`/`source_id` plus `id` and `created_at` fields. History tab would display `undefined` for all level/source badges. Added `ActivityLogRow` type matching backend DB model, `LEVEL_ID_MAP`/`SOURCE_ID_MAP` lookup constants, and `rowToEntry()` conversion function in `HistoryTab.tsx`. **FIXED (CRITICAL BUG):** Frontend `useUpdateActivityLogSettings` sent `api.patch()` but backend route uses `put()` (`PUT /admin/activity-logs/settings`). Settings updates would fail with 405 Method Not Allowed. Fixed to `api.put()`. **FIXED (HIGH DRY-367):** `parse_timestamp(s: &Option<String>, fallback)` duplicated identically in `handlers/audit.rs` (PRD-45) and `handlers/activity_log.rs` (PRD-118). Same function signature, same error message, same logic. Extracted to `api/src/query.rs` as `pub fn parse_timestamp()`. Both handlers now import from shared location. ~22 lines eliminated. **FIXED (MEDIUM DRY-368):** Hardcoded `"admin"` magic string in 3 places in `activity_log.rs` handler (`auth.role != "admin"` x2, `auth.role == "admin"` x1). Replaced with `ROLE_ADMIN` from `core/src/roles.rs`. Same violation as DRY-013/DRY-090. **FIXED (MEDIUM DRY-369):** `ActivityLogCategory` enum in `core/src/activity.rs` had `as_str()` but lacked `from_str()` (unlike `ActivityLogLevel` and `ActivityLogSource`). The WS handler had inline `match m { "curated" => Some(Curated), ... }` instead. Added `ActivityLogCategory::from_str()` method with test. WsFilter refactored to use `.and_then(ActivityLogCategory::from_str)`. **FIXED (MEDIUM DRY-370):** Inline pagination clamping `params.limit.unwrap_or(25).min(100)` and `params.offset.unwrap_or(0)` in `activity_log_repo.rs` instead of `clamp_limit`/`clamp_offset` from `core/src/search.rs`. Replaced with shared functions. Extends DRY-093. **FIXED (MEDIUM DRY-371):** `WsSubscribeAction` and `WsUpdateFilterAction` in frontend `types.ts` were structurally identical interfaces (same 6 optional fields) differing only in the `action` discriminant. Merged into single `WsClientAction` with `action: "subscribe" \| "update_filter"`. ~14 lines eliminated. **WATCH (HIGH DRY-372):** Dynamic query building pattern (`BindValue` enum + `build_*_filter()` + `bind_*_values()` + `bind_*_values_scalar()`) duplicated between `audit_repo.rs` and `activity_log_repo.rs`. Audit version has `BindValue { BigInt, Text, Timestamp }` + `RetentionBindValue { Int, Bool }`. Activity log version has `BindValue { BigInt, SmallInt, Text, Timestamp }` + `SettingsBindValue { Int }`. The `bind_*_values` and `bind_*_values_scalar` functions are structurally identical (only differ by which variants they match). 2 consumers. Extract shared `DynamicQuery<T>` builder to `db/src/query_builder.rs` at 3rd consumer. **WATCH (MEDIUM DRY-373):** `serde_json::json!` used for WebSocket outbound messages in `activity_log.rs` (entry payload line 475, lagged message line 497). These are WS messages not REST responses, so `DataResponse<T>` doesn't apply. Still, typed `WsEntryPayload`/`WsLaggedPayload` structs would improve type safety. 2 consumers in same handler. Extract typed WS message structs when WS message types reach 4+. **WATCH (LOW DRY-374):** Hardcoded level IDs `(1, 2, 3, 4)` in `activity_retention.rs` (line 73-78) and `activity_log.rs` handler manual purge (line 363 `for level_id in 1..=4i16`). These match seed data but are fragile. 2 consumers. Extract `LEVEL_ID_*` constants to `core/src/activity.rs` at 3rd consumer or when seed data changes. **WATCH (LOW DRY-375):** `SOURCE_ACCENT_CLASSES` in frontend `types.ts` is dead code -- defined and re-exported from `index.ts` but never imported by any component. `LogEntryRow.tsx` uses level-based border colors (not source-based). Remove if still unused after PRD-118 UI iteration. **WATCH (LOW DRY-376):** Admin page header pattern in `ActivityConsolePage.tsx` (Icon + h1 + description). 16th consumer. Updates DRY-366 count from 15 to 16. **WATCH (LOW DRY-377):** Loading spinner pattern in `HistoryTab.tsx` (`flex items-center justify-center py-{N} > Spinner`). 18th consumer. Updates DRY-339 count. **WATCH (LOW DRY-378):** Enum boilerplate (`as_str`/`from_str`) x3 in `activity.rs` (`ActivityLogLevel`, `ActivityLogSource`, `ActivityLogCategory`). Updates DRY-301 count from 5 to 8 enum as_str/from_str implementations. **NOT DUPLICATED:** `ActivityLogBroadcaster` in events crate is intentionally separate from `EventBus` -- different message types, different buffer sizes, different subscribers. The WS handler in `activity_log.rs` is NOT a duplicate of `ws/handler.rs` -- they serve completely different purposes (activity_log handles filter subscriptions + broadcast-channel entries, ws/handler.rs manages general WS connections via WsManager). `ActivityTracingLayer` is unique infrastructure (tracing subscriber that captures events to activity log -- no other tracing layer exists). `ActivityLogSettings` singleton repo pattern is unique (different from `platform_setting_repo.rs` which manages key-value settings). The batch persistence loop in `activity_persistence.rs` matches DRY-018 broadcast receiver pattern (now 3rd consumer: EventBus persistence, notification router, activity persistence) but has unique backpressure logic (drop verbose entries). Frontend `useActivityLogStream` WebSocket hook is unique (different protocol from `ws/handler.rs` general WS -- has filter subscriptions, typed messages). `useActivityConsoleStore` Zustand store is unique -- ring buffer with pause/resume has no equivalent. `ConsoleFilterToolbar` and `HistoryFilterBar` are NOT duplicates -- different filter models (toggle sets vs single select), different interaction patterns (live stream vs paginated query). All REST handlers use `DataResponse<T>`. Route files are thin routers. Frontend hooks follow TanStack Query key factory pattern. Core module has comprehensive tests (10 tests). **VERIFIED:** `cargo check` clean (0 warnings), `cargo test -p x121-core -- activity` 10/10 passed, `npx tsc --noEmit` clean (0 errors). |
| 2026-02-27 | PRD-112 (Project Hub & Management) | Frontend: `features/projects/types.ts`, `features/projects/hooks/use-projects.ts`, `features/projects/hooks/use-character-groups.ts`, `features/projects/hooks/use-project-characters.ts`, `features/projects/components/ProjectCard.tsx`, `features/projects/components/CharacterCard.tsx`, `features/projects/ProjectListPage.tsx`, `features/projects/ProjectDetailPage.tsx`, `features/projects/tabs/ProjectOverviewTab.tsx`, `features/projects/tabs/ProjectCharactersTab.tsx`, `features/projects/tabs/ProjectSceneSettingsTab.tsx`, `features/projects/tabs/ProjectProductionTab.tsx`, `features/projects/tabs/ProjectDeliveryTab.tsx`, `features/projects/tabs/ProjectConfigTab.tsx`, `features/projects/index.ts`, `features/characters/types.ts`, `features/characters/hooks/use-character-detail.ts`, `features/characters/CharacterDetailPage.tsx`, `features/characters/tabs/CharacterOverviewTab.tsx`, `features/characters/tabs/CharacterImagesTab.tsx`, `features/characters/tabs/CharacterScenesTab.tsx`, `features/characters/tabs/CharacterAssetsTab.tsx`, `features/characters/tabs/CharacterMetadataTab.tsx`, `features/characters/tabs/CharacterSettingsTab.tsx`, `features/characters/index.ts`, `app/router.tsx`, `app/navigation.ts` | 2 CRITICAL fixed (DRY-379, DRY-380), 1 HIGH fixed (DRY-381), 3 WATCH items (DRY-382, DRY-383, DRY-384), rest CLEAN | **FIXED (CRITICAL DRY-379):** `STATUS_BADGE_VARIANT` map (`{ active: "success", archived: "default", draft: "warning" }`) duplicated identically in `ProjectCard.tsx` (line 18) and `ProjectDetailPage.tsx` (line 32). Extracted to `types.ts` as `PROJECT_STATUS_BADGE_VARIANT`. Both consumers now import from single source. ~10 lines eliminated. **FIXED (CRITICAL DRY-380):** `COLOR_TO_VARIANT` map (`{ gray: "default", yellow: "warning", blue: "info", purple: "info", green: "success" }`) duplicated identically in `CharacterCard.tsx` (line 17) and `CharacterDetailPage.tsx` (line 37). The character status derivation logic (status_id -> STATUS_COLORS lookup -> COLOR_TO_VARIANT lookup) was duplicated in 3 files: `CharacterCard.tsx` (lines 36-44), `CharacterDetailPage.tsx` (lines 80-88), and `CharacterOverviewTab.tsx` (lines 20-22, label only). Extracted `characterStatusLabel(statusId)` and `characterStatusBadgeVariant(statusId)` helper functions to `types.ts`. All 3 consumers refactored to use shared helpers. ~30 lines of duplicated logic eliminated. **FIXED (HIGH DRY-381):** `formatSettingValue(value)` in `CharacterSettingsTab.tsx` (lines 81-84) was a behavioral duplicate of `formatValue(value)` in `@/lib/format.ts` (line 77). Both: `null/undefined -> "(none)"`, `object -> JSON.stringify`, `else -> String`. `formatValue` is a superset (also handles arrays with `.join(", ")`). Replaced local `formatSettingValue` with import of `formatValue` from `@/lib/format.ts`. 4 lines of duplicate code eliminated. **ALSO FIXED:** Removed redundant JS `truncate()` helper from `ProjectCard.tsx` -- the description `<p>` already uses CSS `line-clamp-2` class which handles visual truncation. The JS truncation at 120 chars was unnecessary double-truncation. **WATCH (LOW DRY-382):** `StatItem` component in `ProjectOverviewTab.tsx` is structurally similar to `StatCard` in `ProductionProgress.tsx` and `ImportProgress.tsx`. All show label + value in a card. `StatCard` adds a `variant` prop for color-coding. 3 consumers. Approaching extraction threshold. Extract `StatCard` to `components/domain/` if a 4th consumer appears. **WATCH (LOW DRY-383):** Loading spinner pattern (`flex items-center justify-center py-[var(--spacing-8)] > Spinner size="lg"`) appears 6 more times in PRD-112 files (`ProjectListPage.tsx`, `ProjectDetailPage.tsx`, `ProjectCharactersTab.tsx`, `CharacterDetailPage.tsx`, `CharacterSettingsTab.tsx`, and implicitly in loading text of `ProjectOverviewTab.tsx`). Updates DRY-339/377 count from 18 to **24 consumers**. This is well past extraction threshold. **WATCH (LOW DRY-384):** Page header pattern (Icon + h1 + description) in `ProjectListPage.tsx` adds another consumer to DRY-366. Updates count from 16 to **17 consumers**. **NOT DUPLICATED:** Placeholder tabs (`ProjectSceneSettingsTab`, `ProjectProductionTab`, `ProjectDeliveryTab`, `ProjectConfigTab`, `CharacterImagesTab`, `CharacterScenesTab`, `CharacterAssetsTab`, `CharacterMetadataTab`) all use `EmptyState` from `@/components/domain` (not `PlaceholderPage`). This is correct -- `PlaceholderPage` is for top-level route stubs with `p-6 > h1 + p`, while `EmptyState` is for inline empty states within tabs/panels with icon+title+description. These are different components serving different purposes. `CharacterSettings` tab correctly renders settings when data exists (not just a stub). `CharacterOverview` tab correctly renders readiness summary (not just a stub). TanStack Query hooks follow key factory pattern consistently across all 3 hook files. Query keys are correctly scoped (`["projects"]` for top-level, `["projects", projectId, "characters"]` for nested). `useCharacter` in `use-project-characters.ts` and `useProject` in `use-projects.ts` are NOT duplicates -- they fetch different entities from different endpoints. `Character` type in `projects/types.ts` and `CharacterSettings`/`CharacterMetadata` types in `characters/types.ts` are NOT duplicates -- they represent different API shapes (entity vs sub-resource). `ProjectCard.tsx` and `CharacterCard.tsx` share similar Card+button+Badge structure but are NOT exact duplicates -- different data shapes, different layouts, different metadata shown (project has description+date; character has group). Card hover pattern (`cursor-pointer transition-shadow hover:shadow-[var(--shadow-md)]`) is consistent with `ThumbnailCard`, `WorkerCard`, `BackendConfigPanel` -- this is an established codebase convention, not duplication. Breadcrumb in `ProjectDetailPage.tsx` (2-segment) and `CharacterDetailPage.tsx` (3-segment) have similar structure but different depths -- breadcrumb extraction (DRY-366 related) deferred to design-system pass. `router.tsx` and `navigation.ts` changes are purely additive (new routes + nav item). No logic duplicated. All imports use barrel exports from `@/features/projects` and `@/features/characters`. **VERIFIED:** `npx tsc --noEmit` clean (0 errors). |
| 2026-02-27 | PRD-113 (Character Ingest Pipeline Frontend) | Frontend: `features/character-ingest/types.ts`, `features/character-ingest/hooks/use-character-ingest.ts`, `features/character-ingest/NameParserPreview.tsx`, `features/character-ingest/ImportPreviewTable.tsx`, `features/character-ingest/FolderImportWizard.tsx`, `features/character-ingest/ValidationDashboard.tsx`, `features/character-ingest/CharacterIngestPage.tsx`, `features/character-ingest/index.ts`, `app/router.tsx`, `app/navigation.ts` | 2 HIGH fixed (DRY-385, DRY-386), 1 MEDIUM fixed (DRY-387), 1 MEDIUM fixed (DRY-388), 3 WATCH items (DRY-389, DRY-390, DRY-391), rest CLEAN | **FIXED (HIGH DRY-385):** `CONFIDENCE_VARIANT` and `CONFIDENCE_LABEL` badge variant maps defined locally in `NameParserPreview.tsx` (lines 19-29) instead of in the shared `types.ts` module. Any future component needing confidence-to-badge mapping would have to reduplicate. Moved both maps to `types.ts`. `NameParserPreview.tsx` now imports from `types.ts`. ~12 lines of local definitions eliminated. **FIXED (HIGH DRY-386):** Three separate badge variant maps (`METADATA_STATUS_VARIANT`, `VALIDATION_STATUS_VARIANT`, and `SessionStatusBadge` inline ternary) defined locally in `ImportPreviewTable.tsx` and `ValidationDashboard.tsx` instead of in `types.ts`. The same `VALIDATION_STATUS_VARIANT` was also duplicated as an inline ternary in `ConfirmPreview` inside `FolderImportWizard.tsx` (`pass -> success, warning -> warning, else -> default`). Consolidated all 3 maps + `ingestSessionBadgeVariant()` function into `types.ts`. All 3 consumer files now import from the shared source. `SessionStatusBadge` in `ValidationDashboard.tsx` reduced from 12 lines to 3. Inline ternary in `ConfirmPreview` replaced with `VALIDATION_STATUS_VARIANT[status] ?? "default"`. ~25 lines of duplicate mapping logic eliminated. **FIXED (MEDIUM DRY-387):** `new Date(session.created_at).toLocaleDateString()` in `ValidationDashboard.tsx` (line 118) instead of using `formatDate()` from `@/lib/format.ts`. This produces inconsistent date formatting compared to the rest of the app. Replaced with `formatDate(session.created_at)`. **FIXED (MEDIUM DRY-388):** Wizard navigation footer (Back \| Cancel + Primary button bar) duplicated 3x within `FolderImportWizard.tsx` (steps 2, 3, and 4) with identical `flex justify-between > Back + (Cancel + Primary)` structure. Extracted `WizardNavFooter({ onBack, onCancel, children })` sub-component. 3 call sites reduced from ~10 lines each to 3 lines each. ~21 lines of intra-file duplication eliminated. **WATCH (LOW DRY-389):** `SummaryCard` component in `ValidationDashboard.tsx` is the **4th consumer** of the stat card pattern (alongside `StatItem` in `ProjectOverviewTab.tsx`, `StatCard` in `ProductionProgress.tsx`, and `StatCard` in `ImportProgress.tsx`). Per DRY-382, extraction threshold was set at 4th consumer. This is now met. Extract shared `StatCard` component to `components/domain/StatCard.tsx` at next design-system pass. **WATCH (LOW DRY-390):** Loading spinner pattern in `ValidationDashboard.tsx` (`flex justify-center p-8 > Spinner`). **25th consumer**. Updates DRY-339/377/383 count. Well past extraction threshold -- `<CenteredSpinner />` component should be created at next design-system pass. **WATCH (LOW DRY-391):** `StepIndicator` in `FolderImportWizard.tsx` is the **4th** step indicator implementation (alongside `onboarding-wizard/StepIndicator.tsx`, `legacy-import/LegacyImportWizard.tsx` inline step nav, and `workflow-import/ImportWizard.tsx` inline step breadcrumb). Each has different UI treatment (numbered pills vs buttons vs breadcrumbs) and different interaction models (read-only vs clickable). Conceptual duplication but NOT exact. Monitor -- if a 5th wizard appears, extract a generic `<WizardStepBar steps={[]} currentIndex={N} />` component. **NOT DUPLICATED:** TanStack Query hooks follow the established key factory pattern consistently (matches onboarding-wizard, naming-rules, settings hooks). `ingestKeys` / `templateKeys` / `videoSpecKeys` key factories are correctly scoped. All mutations properly invalidate related queries. `api.get`/`api.post`/`api.put`/`api.delete` from `@/lib/api` used throughout (no raw `fetch`). `CharacterIngestPage.tsx` page structure matches established patterns (Tabs from composite, Stack from layout, Button from primitives). `WIZARD_STEPS` array with `as const` is the correct pattern for step definitions. `INGEST_STATUS_LABELS` maps numeric IDs to labels -- same pattern as other status label maps in the codebase. `NameParserPreview` inline edit pattern (isEditing/editValue state + Enter/Escape/blur handlers) is structurally similar to `SettingRow.tsx` editing pattern but with simpler needs (no save mutation, just callback) -- acceptable at 2 consumers. `DEFAULT_PROJECT_ID = 1` in `CharacterIngestPage.tsx` is a temporary placeholder (noted with comment), not a reusable constant. `router.tsx` and `navigation.ts` changes are purely additive (new route + nav item). All barrel exports in `index.ts` include shared types and constants for downstream consumers. All file sizes well within limits: `types.ts` 234 lines, `use-character-ingest.ts` 279 lines, `FolderImportWizard.tsx` 372 lines, `ValidationDashboard.tsx` ~170 lines, all others under 100 lines. **VERIFIED:** `npx tsc --noEmit` clean (0 errors). |
| 2026-02-28 | PRD-100 (Scene Type Inheritance & Composition) | Backend: `core/src/scene_type_inheritance.rs`, `api/src/handlers/scene_type_inheritance.rs`, `api/src/routes/scene_type_inheritance.rs`, `db/src/models/mixin.rs`, `db/src/models/scene_type_override.rs`, `db/src/repositories/mixin_repo.rs`, `db/src/repositories/scene_type_override_repo.rs`, `db/src/models/scene_type.rs`, `db/src/repositories/scene_type_repo.rs`, `api/src/handlers/scene_type.rs`. Frontend: `features/scene-types/types.ts`, `features/scene-types/hooks/use-scene-type-inheritance.ts`, `features/scene-types/OverrideIndicator.tsx`, `features/scene-types/InheritanceTree.tsx`, `features/scene-types/index.ts`, tests. Migration: `20260228000001_add_scene_type_inheritance.sql`. | 4 HIGH fixed (DRY-392 to DRY-394, DRY-397), 2 WATCH new (DRY-398, DRY-399), 1 LOW fixed (biome lint), rest CLEAN | **FIXED (HIGH DRY-392):** `list_children` handler at line 100 had inline 16-line `sqlx::query_as` with hardcoded 27-column SELECT duplicating `COLUMNS` constant from `scene_type_repo.rs`. Extracted `SceneTypeRepo::list_children(pool, parent_id)` repository method. Handler reduced to 1-line delegation. **FIXED (HIGH DRY-393):** `cascade_preview` handler had 2 inline SQL queries: `SELECT id FROM scene_types WHERE parent_scene_type_id = $1` and `SELECT field_name FROM scene_type_overrides WHERE scene_type_id = $1`. Extracted `SceneTypeRepo::list_children_ids()` and `SceneTypeOverrideRepo::list_field_names()`. Handler now uses 2 clean repo calls. **FIXED (HIGH DRY-394):** `SceneTypeRepo::find_by_id(&state.pool, id).await?.ok_or(AppError::Core(CoreError::NotFound { entity: "SceneType", id }))` duplicated 5x across `scene_type_inheritance.rs` (3x: create_child parent, create_child reload, build_inheritance_chain) and `scene_type.rs` (2x: get_by_id_inner, preview_prompt). Extracted `ensure_scene_type_exists(pool, id) -> AppResult<SceneType>` as `pub(crate)` helper. All 5 consumers refactored. ~25 lines eliminated. Following established pattern (DRY-097 find_and_authorize, DRY-121 ensure_asset_exists, DRY-180 ensure_segment_exists, DRY-361 ensure_rule_exists). **FIXED (HIGH DRY-397):** `sqlx::query("UPDATE scene_types SET depth = $1 WHERE id = $2")` inline SQL in `create_child` handler. Extracted `SceneTypeRepo::update_depth(pool, id, depth)` repository method. Handler now calls `SceneTypeRepo::update_depth()`. **FIXED (LOW):** Biome lint `noNonNullAssertion` in `InheritanceTree.tsx:48` -- `nodeMap.get(st.id)!` replaced with guard `if (!node) continue`. **WATCH (LOW DRY-398):** `scene_type_to_field_map()` lives in handler layer but logically belongs in core/db. 1 consumer. Move when architecture allows. **WATCH (LOW DRY-399):** `Mixin` -> `MixinEntry` manual conversion in handler. 1 consumer. Extract `From<Mixin>` impl at 2nd consumer. **NOT DUPLICATED:** `resolve_effective_config()` in core is pure logic with no DB dependency -- correctly placed. `FieldSource` enum in core uses `#[serde(rename_all = "snake_case")]` while frontend `FieldSource` uses `type: "own" \| "inherited" \| "mixin"` -- these are equivalent (Rust `Own` -> JSON `"own"`, etc.). Frontend types correctly mirror backend serialization shapes. `MixinRepo` COLUMNS pattern follows established codebase convention. `SceneTypeOverrideRepo` upsert uses `ON CONFLICT DO UPDATE` correctly (not DO NOTHING -- per coding rules). `buildTree()` in `InheritanceTree.tsx` is unique (no other tree-from-flat-list utility in the codebase). Query key factories in `use-scene-type-inheritance.ts` follow established TanStack Query conventions. Mutation `onSuccess` invalidation patterns are standard (not duplicated -- each mutation invalidates different query keys). Frontend tests use `renderWithProviders` from `@/lib/test-utils` (correct). `InheritanceTree.test.tsx` `makeSceneType()` fixture builder is test-local (acceptable). **CLEAN:** Migration has proper triggers, UNIQUE indexes, FK indexes, CASCADE rules. All models use `DbId`/`Timestamp` types. Core module has comprehensive unit tests (9 tests covering 3-level chains, mixins, overrides, cascade, depth validation, edge cases). Frontend components use design system primitives (`Badge`, `Button`, `cn()`). No `serde_json::json!` in handler responses. No magic strings. No hardcoded status IDs. Route file is a thin router. **VERIFIED:** `cargo check` clean (0 warnings), `npx tsc --noEmit` clean (0 errors). |
| 2026-02-28 | PRD-115 (Prompt Management & Generation Strategy) | Backend: `core/src/prompt_resolution.rs`, `api/src/handlers/prompt_management.rs`, `api/src/routes/prompt_management.rs`, `db/src/models/workflow_prompt_slot.rs`, `db/src/models/scene_type_prompt_default.rs`, `db/src/models/character_scene_prompt_override.rs`, `db/src/models/prompt_fragment.rs`, `db/src/models/scene_artifact.rs`, `db/src/repositories/workflow_prompt_slot_repo.rs`, `db/src/repositories/scene_type_prompt_default_repo.rs`, `db/src/repositories/character_scene_prompt_override_repo.rs`, `db/src/repositories/prompt_fragment_repo.rs`, `db/src/repositories/scene_artifact_repo.rs`. Frontend: `features/prompt-management/types.ts`, `features/prompt-management/hooks/use-prompt-management.ts`, `features/prompt-management/PromptSlotsPanel.tsx`, `features/prompt-management/SlotCard.tsx`, `features/prompt-management/FragmentDropdown.tsx`, `features/prompt-management/CharacterSceneOverrideEditor.tsx`, `features/prompt-management/SlotOverrideSection.tsx`, `features/prompt-management/GenerationStrategySelector.tsx`, `features/prompt-management/index.ts`. | 2 HIGH fixed (DRY-400, DRY-401), 2 MEDIUM fixed (DRY-402, DRY-403), 3 WATCH (DRY-339 worsened, DRY-404, DRY-405) | **HIGH (DRY-400):** `prompt_management.rs` uses fully-qualified `x121_core::error::CoreError::NotFound` 4x (lines 98, 111, 370, 388) instead of `use x121_core::error::CoreError;` import. Every other handler file in the codebase uses the imported alias. Needs fix: add import, replace all 4 occurrences. **HIGH (DRY-401):** `SlotCard.tsx:18` defines `const PLACEHOLDER_REGEX = /\{(\w+)\}/g;` locally. `features/prompt-editor/types.ts:11` already exports canonical `PLACEHOLDER_REGEX = /\{[a-zA-Z_][a-zA-Z0-9_.]*\}/g;` (synced with backend `PLACEHOLDER_PATTERN`). The PRD-115 version is looser (matches digits as first char). Needs fix: import from `@/features/prompt-editor` instead of redefining. **MEDIUM (DRY-402):** `SlotDraft` interface defined identically in both `CharacterSceneOverrideEditor.tsx:43-46` and `SlotOverrideSection.tsx:19-22` (`{ fragments: FragmentEntry[]; notes: string; }`). Should be exported from `types.ts` and imported in both. **MEDIUM (DRY-403):** `find_by_id + ok_or(NotFound)` pattern for `WorkflowPromptSlot` 2x (lines 96-101, 111-114) and `PromptFragment` 2x (lines 370-373, 388-392) in same handler file. Extract `ensure_slot_exists()` and `ensure_fragment_exists()` helpers following established `ensure_scene_type_exists` pattern. **WATCH (DRY-339 +3):** Loading spinner `<div className="flex items-center justify-center py-{4|8}"><Spinner /></div>` in 3 more files (PromptSlotsPanel.tsx:88, CharacterSceneOverrideEditor.tsx:157, FragmentDropdown.tsx:70). Total now ~28 consumers. Past extraction threshold -- deferred to design-system pass. **WATCH (DRY-404):** `renderPromptText(text)` in `SlotCard.tsx:25-37` (splits on regex, renders `<strong>` for placeholders) is behavioral duplicate of `highlightPlaceholders(text)` in `PromptEditor.tsx:54-74` (splits on regex, returns `{text, isPlaceholder}[]`). Different return shapes. 2 consumers -- extract shared utility at 3rd. **WATCH (DRY-405):** Card container styling `cn("rounded-[var(--radius-md)] border border-[var(--color-border-default)]", "bg-[var(--color-surface-primary)] p-4")` repeated in `SlotCard.tsx:61-63` and `SlotOverrideSection.tsx:49-51`. 2 consumers within same feature -- extract at 3rd. **NOT DUPLICATED:** `prompt_resolution.rs` correctly reuses `resolve_prompt_template` from `scene_type_config.rs` (no regex reimplementation). Repos follow established COLUMNS + format! pattern. Models use `DbId`/`Timestamp`. TanStack Query hooks follow established key factory convention. Route files follow established merge pattern. `FragmentListParams` combines pagination with domain filters correctly (not duplicating `PaginationParams` -- different struct by design for Axum query extraction). `CharacterScenePromptOverrideRepo` (PRD-115) vs `CharacterSceneOverrideRepo` (PRD-111) are genuinely different tables/concepts (prompt overrides vs scene setting overrides). **CLEAN:** `prompt_resolution.rs` has comprehensive unit tests (13 tests). All repos use COLUMNS constant. All frontend components use design system primitives. No `serde_json::json!` in handler responses. No magic strings. No hardcoded status IDs. Route file is well-structured with 5 sub-routers. Core module is pure logic with no DB dependency. |
| 2026-02-23 | AppShell, Router, Navigation (Frontend Layout Infrastructure) | 36 files (10 new: useSidebar.ts, navigation.ts, NavItem.tsx, NavGroup.tsx, Sidebar.tsx, Header.tsx, UserMenu.tsx, AppShell.tsx, AdminGuard.tsx, PageSkeleton.tsx; 25 page wrappers in app/pages/; 1 router.tsx rewrite) + cross-ref (ProtectedRoute.tsx, ThemeProvider.tsx, Button.tsx, Drawer.tsx, auth-store.ts, Badge.tsx) | 1 CRITICAL fixed (DRY-330), 2 HIGH fixed (DRY-331, DRY-332), 1 MEDIUM fixed (DRY-333), rest CLEAN | **FIXED (CRITICAL DRY-330):** 23 placeholder page files (ScenesPage, CharactersPage, AnnotationsPage, ReviewNotesPage, ProductionNotesPage, GenerationPage, TestShotsPage, ImagesPage, StoryboardPage, BatchProductionPage, WorkflowsPage, SceneTypesPage, CharacterDashboardPage, DeliveryPage, CheckpointsPage, QaGatesPage, CinemaPage, PromptsPage, BranchingPage, IntegrityPage, StoragePage, OnboardingWizardPage, LegacyImportPage, ReadinessPage, WikiPage) each had identical 9-line structure (`div.p-6 > h1.mb-4 + p`). Created `PlaceholderPage` shared component in `app/pages/PlaceholderPage.tsx`. All 23 consumers refactored to 3-line delegations. ~160 lines of duplicate JSX eliminated. **FIXED (HIGH DRY-331):** `AdminGuard.tsx` re-implemented access-denied UI (identical text, similar layout to `ProtectedRoute.tsx` lines 47-57) and duplicated role checking logic (inline `user.role !== "admin"` instead of reusing `hasAccess()` from ProtectedRoute). Rewritten to wrap `<ProtectedRoute requiredRole="admin">` -- delegates all auth/role logic and denied UI to canonical component. **FIXED (HIGH DRY-332):** `Sidebar.tsx` SidebarContent manually re-implemented `hasAccess()` role checking logic (3-line if-chain: no requiredRole -> true, no user -> false, admin bypass -> true, exact match) which was identical to `ProtectedRoute.tsx:hasAccess()`. Exported `hasAccess` from ProtectedRoute, imported in Sidebar. Role filtering now single expression: `hasAccess(user.role, group.requiredRole)`. **FIXED (MEDIUM DRY-333):** Icon button styling (3-line class block: `rounded-[var(--radius-md)] p-1.5` + muted/tertiary hover + transition) duplicated 3x in `Header.tsx` (mobile hamburger, desktop collapse toggle, theme toggle). Extracted `ICON_BTN` constant. All 3 buttons now reference single constant with only visibility modifiers differing. ~18 lines reduced to 3. **NOT DUPLICATED:** `useSidebar` Zustand store is unique -- no existing sidebar store. `useTheme` properly imported from theme context (not re-implemented). `navigation.ts` nav group definitions are unique data (not logic duplication). `UserMenu.tsx` `Dropdown` usage follows established composite component pattern. `AppShell.tsx` composition of `ProtectedRoute + Sidebar + Header + Outlet` is unique layout orchestration. `Sidebar.tsx` correctly uses `Drawer` composite for mobile (not hand-rolled). `PageSkeleton.tsx` uses `Spinner` primitive (not duplicated). Theme toggle `setColorScheme(dark ? light : dark)` is the only correct way to call `useTheme()` -- not behavioral duplication. localStorage persistence in `useSidebar` follows same try/catch pattern as `ThemeProvider` but different key and simpler data (boolean vs JSON object) -- acceptable structural similarity at 2 consumers. **CLEAN:** `NavItem.tsx` uses `Tooltip` from primitives, `useMatchRoute` from TanStack Router, design token CSS variables. `NavGroup.tsx` uses `ChevronDown` from icons barrel. `navigation.ts` imports `LucideIcon` type and `UserRole` type. `router.tsx` uses `lazyRouteComponent` for all feature pages. DashboardPage and SearchPage correctly left as custom (they render feature components, not just title+description). NotFoundPage correctly left as custom (has Link + Button). All design token CSS variables used consistently. No magic strings. No raw fetch. No inline SVGs. **VERIFIED:** `npx tsc --noEmit` clean (0 errors), `npx biome check` clean (auto-fixed 10 import orderings). |

| 2026-02-28 | PRD-71 (Smart Auto-Retry) | Backend: `core/src/auto_retry.rs`, `db/src/models/retry_attempt.rs`, `db/src/repositories/retry_attempt_repo.rs`, `api/src/handlers/auto_retry.rs`, `api/src/routes/auto_retry.rs`, `db/src/models/scene_type.rs` (modified), `db/src/repositories/scene_type_repo.rs` (modified). Frontend: `features/auto-retry/types.ts`, `features/auto-retry/hooks/use-auto-retry.ts`, `features/auto-retry/AttemptRow.tsx`, `features/auto-retry/RetryHistoryPanel.tsx`, `features/auto-retry/RetryPolicyEditor.tsx`, `features/auto-retry/index.ts`, 2 test files. Migration: 1 SQL file. | 8 FIXED (DRY-409 to DRY-416), 3 WATCH new (DRY-406 to DRY-408), DRY-339 worsened (+2), DRY-301 worsened (+1), rest CLEAN | **FIXED (HIGH DRY-409):** `get_retry_policy` handler used inline `SceneTypeRepo::find_by_id + ok_or(NotFound)` instead of `ensure_scene_type_exists()` from `scene_type_inheritance.rs`. Replaced with shared helper. **FIXED (HIGH DRY-410):** `RetryPolicyResponse { enabled: st.auto_retry_enabled, ... }` construction duplicated 2x in handler (get + update). Extracted `build_retry_policy_response(st)` helper. **FIXED (HIGH DRY-411):** `RetryAttemptRepo::find_by_id + ok_or(NotFound)` 3x in handler. Extracted `ensure_retry_attempt_exists(pool, id)` helper. 1 of 3 refactored (get_retry_attempt); update/select retain inline pattern since they return `Option` from different repo methods. **FIXED (HIGH DRY-412):** `x121_db::models::retry_attempt::RetryAttempt` fully-qualified 5x in handler. Added `RetryAttempt` to import statement. **FIXED (HIGH DRY-413):** Magic numbers `1..=10` for max_attempts validation. Extracted `MIN_RETRY_ATTEMPTS`/`MAX_RETRY_ATTEMPTS` constants to `core/src/auto_retry.rs`. Frontend `types.ts` has sync comment referencing canonical Rust source. **FIXED (HIGH DRY-414):** `UpdateSceneType` construction with 25 explicit `None` fields. Added `#[derive(Default)]` to `UpdateSceneType`. Handler now uses `..Default::default()`. **FIXED (MEDIUM DRY-415):** `a.status == "passed"` magic string in `select_best_attempt`. Replaced with `RetryAttemptStatus::Passed.as_str()`. **FIXED (LOW DRY-416):** `!draft.enabled && "opacity-50 pointer-events-none"` repeated 4x in RetryPolicyEditor. Extracted to local `disabledClass` const. **WATCH (LOW DRY-406):** Handler skips `DataResponse<T>` envelope (returns raw `Json<T>`). 2 handler modules skip it (auto_retry + scene_type_inheritance). Adopt when frontend expects envelope. **WATCH (LOW DRY-407):** `RetryAttemptScore.status` is `String` not `RetryAttemptStatus` enum. 1 consumer. **WATCH (LOW DRY-408):** `rand::random::<u32>() as i64` seed pattern 2x. Extract `new_seed()` at 3rd consumer. **DRY-339 WORSENED (+2):** Loading spinner in RetryHistoryPanel + RetryPolicyEditor. ~30 consumers total. **DRY-301 WORSENED (+1):** `RetryAttemptStatus` enum with `as_str()`/`from_str()` boilerplate. 9th enum. **NOT DUPLICATED:** Core `auto_retry.rs` is pure logic (no I/O deps) -- correctly placed. `JitterConfig`/`RetryPolicy`/`RetryDecision` are domain-unique. `jitter_parameters()` and `select_best_attempt()` are novel algorithms (no prior art in codebase). `should_retry()` combines policy eval + jitter -- unique. Tests are comprehensive (11 tests). Frontend types correctly mirror backend serialization. `ATTEMPT_STATUS_BADGE_VARIANT` follows established `badgeVariant` mapper pattern (DRY-282). TanStack Query hooks follow key factory pattern. Frontend barrel export is clean. Both test files use `renderWithProviders` from `@/lib/test-utils` and proper mock patterns. `AttemptRow.tsx` uses `Badge`/`Button` from design system. `Checkbox`/`Toggle`/`Input` all imported from primitives (not reimplemented). No `serde_json::json!` for handler success responses. No hardcoded `"admin"`. No inline SVGs. No raw `fetch`. All files under 500 lines. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core -- auto_retry` 11/11 passed, `npx tsc --noEmit` clean, `npx vitest run auto-retry` 15/15 passed, `npx biome check` clean. |

| 2026-02-28 | PRD-65 (Workflow Regression Testing) | Backend: `core/src/regression.rs`, `core/src/segment_comparison.rs` (modified), `db/src/models/regression.rs`, `db/src/repositories/regression_repo.rs`, `api/src/handlers/regression.rs`, `api/src/routes/regression.rs`. Frontend: `features/regression/types.ts`, `hooks/use-regression.ts`, `VerdictBadge.tsx`, `ScoreDiffDisplay.tsx`, `ReferenceManager.tsx`, `RunHistoryPanel.tsx`, `RegressionReport.tsx`, `index.ts`. Shared: `lib/qa-constants.ts` (modified). Migration: 1 SQL file. Tests: 4 FE files (17 tests), 19 core tests. | 3 CRITICAL/HIGH fixed (DRY-460, DRY-462, DRY-463), 2 MEDIUM fixed (DRY-465, DRY-466), 1 LOW fixed (DRY-467), 3 WATCH (DRY-461, DRY-464, DRY-468), DRY-339 worsened (+4), rest CLEAN | **FIXED (CRITICAL DRY-460):** `formatScore`/`formatDiff` duplicated in `ScoreDiffDisplay.tsx` and `QAScoreComparison.tsx`. Extracted to `@/lib/qa-constants.ts`. **FIXED (HIGH DRY-462):** `classify_from_diffs` reimplemented fold/sum/count/average from `overall_quality_improved`. Extracted `average_score_diffs()` to `segment_comparison.rs`. Both callers use shared fn. **FIXED (HIGH DRY-463):** `RunReportSummary` duplicate of `RunSummary`. Unified to `RunSummary` with `i32 + Serialize`, deleted redundant type. **FIXED (MEDIUM DRY-465/466):** Badge variant typing corrected, inline maps moved to types.ts. **FIXED (LOW DRY-467):** Dead `VERDICT_COLORS` export removed. **WATCH (DRY-461):** `ScoreDiffDisplay` vs `QAScoreComparison` — different layouts, shared helpers extracted. **WATCH (DRY-464):** `RUN_STATUS_*` name collision with `batch_production.rs` — intentional, different domains. **WATCH (DRY-468):** Loading spinner +4 (~39 total). **VERIFIED:** `cargo check` clean, `cargo test -p x121-core -- regression` 19/19, `segment_comparison` 12/12, `npx tsc --noEmit` clean, `npx vitest run regression` 17/17. |
| 2026-02-28 | PRD-82 (Content Sensitivity Controls) | Backend: `core/src/sensitivity.rs`, `db/src/models/sensitivity.rs`, `db/src/repositories/sensitivity_repo.rs`, `api/src/handlers/sensitivity.rs`, `api/src/routes/sensitivity.rs`. Frontend: `features/sensitivity/types.ts`, `features/sensitivity/hooks/use-sensitivity-settings.ts`, `features/sensitivity/SensitivityProvider.tsx`, `features/sensitivity/BlurredMedia.tsx`, `features/sensitivity/WatermarkOverlay.tsx`, `features/sensitivity/ScreenShareIndicator.tsx`, `features/sensitivity/hooks/useViewOverride.ts`, `features/sensitivity/hooks/useScreenShareMode.ts`, `features/sensitivity/index.ts`, `features/admin/SensitivityDefaults.tsx`. Migration: `20260228000007_create_sensitivity_settings.sql`. Tests: 4 frontend test files. | 1 FLAGGED (DRY-455), 6 WATCH new (DRY-453 to DRY-459), DRY-160 worsened (+1), DRY-339 worsened (+1), rest CLEAN | **FLAGGED (MEDIUM DRY-455):** `match option { Some => DataResponse, None => NO_CONTENT }` pattern at 6 call sites in 4 handler files (`sensitivity.rs` 2x, `themes.rs` 1x, `keymaps.rs` 1x, `proficiency.rs` 1x). PRD-82 added 2 consumers. Extract `optional_data_response<T>` to `api/src/response.rs`. **WATCH (HIGH DRY-453):** `SensitivityProvider.tsx` inlines ~70 lines of API sync logic structurally identical to `useThemePersistence.ts`: `lastSavedRef` dirty check, cancelled load effect, 500ms debounce save, silent catch fallback. 2 consumers. Extract shared `useApiPersistence` at 3rd provider. Also adds 7th consumer to DRY-160 debounce timer pattern. **WATCH (MEDIUM DRY-454):** `BlurPreviewCard` in `SensitivityDefaults.tsx` duplicates placeholder/blur rendering from `BlurredMedia.tsx` (same `isPlaceholder ? User icon : img+filter` conditional). Static admin preview vs context-aware component. 2 consumers. Extract `BlurPreview` at 3rd. **WATCH (LOW DRY-456):** `SensitivityDefaults.tsx` uses `<p>Loading...</p>` instead of `<Spinner />`. 35th consumer of loading pattern (DRY-339). **WATCH (LOW DRY-457):** `POSITION_CLASSES` constant name in 4 files -- module-scoped, different semantics. No action. **WATCH (LOW DRY-458):** Blur levels in 3 tiers (SQL/Rust/TS) -- expected cross-boundary pattern, values consistent. **WATCH (LOW DRY-459):** `maxBlurLevel` (FE) mirrors `enforce_minimum_level` (BE) -- intentional cross-boundary behavioral duplicate. **DRY-160 WORSENED (+1):** 7th consumer of debounce timer pattern (SensitivityProvider inline). **DRY-339 WORSENED (+1):** SensitivityDefaults uses text instead of Spinner (conceptually same pattern). **NOT DUPLICATED:** Handler uses `DataResponse<T>` correctly for all non-204 responses. TanStack Query hooks follow established key factory + invalidation pattern. Repository uses `ON CONFLICT DO UPDATE` correctly (same as `theme_repo.rs`). `WatermarkOverlay` is entirely distinct from `GhostingOverlay` (text overlay vs canvas compositing). `core/src/sensitivity.rs` is clean, focused, well-tested (7 unit tests). Route wiring is properly modular. Tests use `vi.mock` correctly with custom wrappers (not `renderWithProviders` -- appropriate for context testing). All file sizes under 300 lines. **VERIFIED:** All files read in full, patterns searched against codebase. |
| 2026-02-28 | PRD-68 (Cross-Character Scene Comparison) | 12 files (backend: `comparison.rs` handler, `comparison.rs` route, `handlers/mod.rs`, `routes/mod.rs`; frontend: `types.ts`, `use-comparison.ts`, `useGalleryState.ts`, `GalleryCell.tsx`, `GalleryControls.tsx`, `SceneGallery.tsx`, `CharacterAllScenes.tsx`, `index.ts`) | 1 CRITICAL + 6 HIGH + 4 MEDIUM + 2 LOW = 13 total; 7 FIXED, 5 WATCH, 1 REDUCED | **FIXED (CRITICAL DRY-437):** Extracted `GalleryLayout` shared component from `SceneGallery` and `CharacterAllScenes` (85% identical ~120 lines). Both components now ~30 lines, ~150 lines eliminated. Created `useGalleryActions()` hook (DRY-439) and moved constants to `types.ts` (DRY-438) as part of extraction. **FIXED (HIGH DRY-434):** Deleted local `ensure_scene_type_exists` (raw SQL), imported shared from `scene_type_inheritance.rs`. **FIXED (HIGH DRY-435):** Rewrote `ensure_project_exists` to use `ProjectRepo::find_by_id`. **FIXED (HIGH DRY-436):** Rewrote `ensure_character_in_project` to use `CharacterRepo::find_by_id` + project_id check. **FIXED (HIGH DRY-441):** Consolidated `fetch_comparison_rows` two 35-line SQL branches into single query with conditional `format!`. Eliminated 35 duplicate lines. **REDUCED (DRY-443):** Spinner now 1 instance in `GalleryLayout` (was 2). DRY-339 at 31. **WATCH:** DRY-440 (APPROVAL_BADGE_VARIANT overlap, 2 consumers), DRY-442 (SQL LATERAL join pattern, 3 queries), DRY-444 (action button array, internal), DRY-445 (status visual mapping, 2 consumers). **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean, 14/14 tests pass. |
| 2026-02-28 | PRD-96 (Poster Frame & Thumbnail Selection) | Backend: `core/src/poster_frame.rs`, `db/src/models/poster_frame.rs`, `db/src/repositories/poster_frame_repo.rs`, `api/src/handlers/poster_frame.rs`, `api/src/routes/poster_frame.rs`. Frontend: `features/poster-frame/types.ts`, `features/poster-frame/hooks/use-poster-frame.ts`, `features/poster-frame/EntityPoster.tsx` (new), `features/poster-frame/CharacterPoster.tsx`, `features/poster-frame/ScenePoster.tsx`, `features/poster-frame/SetPosterButton.tsx`, `features/poster-frame/PosterGallery.tsx`, `features/poster-frame/CropAdjust.tsx`, `features/poster-frame/index.ts`. Migration: 1 SQL file. Tests: 3 frontend files (16 tests), 6 core unit tests. | 7 FIXED (DRY-442 to DRY-445, DRY-447, DRY-449, DRY-450), 4 WATCH (DRY-446 entity type constants in frontend, DRY-448 CropAdjust slider pattern, DRY-451 poster query key shape, DRY-452 frame number hardcoded 0), rest CLEAN | **FIXED (HIGH DRY-442):** Removed duplicate `ENTITY_TYPE_CHARACTER`/`ENTITY_TYPE_SCENE` from `core/poster_frame.rs`. Now re-exports from `metadata.rs` via `pub use`. Single source of truth. **FIXED (HIGH DRY-443):** Changed `ensure_scene_exists`/`ensure_character_exists`/`ensure_project_exists` signatures from `&AppState` to `&PgPool`, matching codebase convention (embedding.rs, comparison.rs, resolution.rs). **FIXED (HIGH DRY-444):** Extracted shared `EntityPoster` component from `CharacterPoster` and `ScenePoster` (85% identical). Both now thin wrappers (~40 lines each, down from ~110). ~70 lines eliminated. **FIXED (MEDIUM DRY-445):** Replaced `"face_confidence"` magic string with `CHECK_FACE_CONFIDENCE` from `quality_gate.rs`. **FIXED (MEDIUM DRY-447):** Aligned frontend `AutoSelectResult` with backend — removed `character_name`/`poster_frame` phantom fields, added `segment_id: number | null`. **FIXED (MEDIUM DRY-449):** Extracted `set_entity_poster()` and `get_entity_poster()` shared helpers. 4 public handlers reduced from ~15 lines each to ~3. **FIXED (LOW DRY-450):** Changed `segment_id: 0` sentinel to `Option<DbId>` (`Some(id)` / `None`). **WATCH:** DRY-446 (ENTITY_TYPE constants in frontend types.ts — acceptable cross-boundary pattern like DRY-079), DRY-448 (brightness/contrast slider pattern — 1 consumer, extract at 2nd), DRY-451 (poster query key shape — follows established factory), DRY-452 (`frame_number: 0` hardcoded in auto-select — acceptable default). **VERIFIED:** `cargo check -p x121-core -p x121-api` clean, `cargo test -p x121-core` 1375 tests passed, `npx tsc --noEmit` clean, `npx vitest run poster-frame` 16/16 passed. |
| 2026-02-28 | PRD-101 (Segment Regeneration Comparison) | 19 files (backend: `core/src/segment_comparison.rs`, `api/src/handlers/segment_comparison.rs`, `api/src/routes/segment_comparison.rs`, `db/src/models/segment_version.rs`, `db/src/repositories/segment_version_repo.rs`, `db/migrations/20260228000006_create_segment_versions.sql`, `core/src/lib.rs`, `api/src/handlers/mod.rs`, `api/src/routes/mod.rs`; frontend: `types.ts`, `hooks/use-segment-versions.ts`, `hooks/useDualSync.ts`, `RegenerationComparison.tsx`, `DiffOverlay.tsx`, `QAScoreComparison.tsx`, `ComparisonActions.tsx`, `VersionFilmstrip.tsx`, `BatchComparison.tsx`, `index.ts`, 3 test files) | 1 CRITICAL fixed (DRY-451), 5 WATCH new (DRY-446 to DRY-450), DRY-339 worsened (+2), rest CLEAN | **FIXED (CRITICAL DRY-451):** Diff overlay JSX (absolute-positioned `div` with SSIM placeholder) duplicated byte-for-byte in both `RegenerationComparison.tsx` (inline, lines 117-130) AND `DiffOverlay.tsx` (component, lines 41-54). The `DiffOverlay` component was placed outside the video container (in the header flex row), so its absolute overlay wouldn't position over the video. Developer added a second inline copy inside the video container to make it work. Fix: split `DiffOverlay` into `DiffOverlayToggle` (button) + `DiffOverlayPanel` (overlay). `RegenerationComparison.tsx` now places toggle in header, panel inside video container. Original `DiffOverlay` composite preserved with `@deprecated` tag. Barrel exports updated. ~14 duplicate lines eliminated. **WATCH (MEDIUM DRY-446):** `useDualSync` (PRD-101) vs `useSyncPlay` (PRD-036) -- two video sync hooks with different paradigms. `useSyncPlay`: N-player, raw refs, drift correction. `useDualSync`: 2-player, `useVideoPlayer`-based, frame-accurate. Different use cases. Extract shared abstraction at 3rd sync pattern. **WATCH (LOW DRY-447):** Loading spinner pattern: +2 consumers (`RegenerationComparison.tsx` py-12 md, `VersionFilmstrip.tsx` py-4 sm). DRY-339 total ~34. `<CenteredSpinner />` extraction overdue. **WATCH (LOW DRY-448):** `formatScore`/`formatDiff` local helpers in `QAScoreComparison.tsx`. 1 consumer. Extract `formatDiff` to `@/lib/format.ts` at 2nd consumer. **WATCH (LOW DRY-449):** `computeOverallTrend` (frontend ternary) vs `overall_quality_improved` (backend binary) -- same concept, acceptable granularity divergence. **WATCH (LOW DRY-450):** Keyboard shortcut pattern with INPUT/TEXTAREA guard in `ComparisonActions.tsx`. 1 consumer. Extract `useKeyboardShortcut(key, handler, opts?)` at 3rd consumer. **NOT DUPLICATED:** `ensure_segment_exists` correctly imported from `handlers/segment.rs` (not redefined). `compute_score_diffs` (core) differs from `extract_qa_score` (comparison.rs) -- different operations (per-metric diffs vs single-number extraction). `DataResponse<T>` used for all handler responses. `qaMetricLabel` imported from shared `@/lib/qa-constants.ts`. `formatDate` imported from `@/lib/format.ts`. `renderWithProviders` used in all tests. `useVideoPlayer` reused from video-player feature. `Badge` imported from primitives. Query key factory follows established pattern. All DTOs properly typed with `FromRow`/`Serialize`/`Deserialize`. No `serde_json::json!` in handlers. No magic strings. No hardcoded status IDs. All files under 500 lines. **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean, `npx vitest run segment-comparison` 18/18 passed, `npx biome check` clean. |
| 2026-02-28 | PRD-91 (Custom QA Rulesets per Scene Type) | Backend: `core/src/qa_ruleset.rs`, `db/src/models/qa_profile.rs`, `db/src/models/scene_type_qa_override.rs`, `db/src/repositories/qa_profile_repo.rs`, `db/src/repositories/scene_type_qa_override_repo.rs`, `api/src/handlers/qa_rulesets.rs`, `api/src/routes/qa_rulesets.rs`. Frontend: `features/qa-rulesets/types.ts`, `features/qa-rulesets/hooks/use-qa-rulesets.ts`, `features/qa-rulesets/ThresholdSlider.tsx`, `features/qa-rulesets/ThresholdEditor.tsx`, `features/qa-rulesets/ProfileCard.tsx`, `features/qa-rulesets/QaProfileListPanel.tsx`, `features/qa-rulesets/AbTestPanel.tsx`, `features/qa-rulesets/index.ts`. Shared: `lib/qa-constants.ts` (new). Migration: 1 SQL file. | 9 FIXED (DRY-420 to DRY-424, DRY-427, DRY-428, DRY-430, DRY-431), 2 WATCH (DRY-425/426 metricNames/handleThresholdChange at 2 consumers), 2 LOW (DRY-432 MetricThreshold name collision, DRY-433 table cell classes), DRY-339 worsened (+3 spinners, ~33 consumers), rest CLEAN | **FIXED (HIGH DRY-420):** `classify_score()` was behavioral duplicate of `evaluate_score()` from `quality_gate.rs` + used magic strings `"pass"`/`"warn"`/`"fail"` 12x. Refactored to delegate to `evaluate_score()`. Replaced all magic strings with `QA_PASS`/`QA_WARN` constants. **FIXED (HIGH DRY-421):** `QA_METRIC_LABELS` in `qa-rulesets/types.ts` duplicated `CHECK_TYPE_LABELS` from `quality-gates/types.ts` (4 overlapping keys, divergent labels for `motion`). Created canonical `@/lib/qa-constants.ts` with `QA_CHECK_TYPE_LABELS` + `qaMetricLabel()`. Both features re-export from shared source. Also related: `TRIGGER_CHECK_OPTIONS` in auto-retry (DRY-408 pattern, uses `{value, label}` tuples -- different shape, monitor only). **FIXED (HIGH DRY-422):** `QaProfileRepo::find_by_id + ok_or(NotFound)` 4x in handler. Extracted `ensure_qa_profile_exists(pool, id)`. 3 of 4 consumers refactored. **FIXED (HIGH DRY-423):** 8 fully-qualified type paths in handler. Added `QaProfile`, `SceneTypeQaOverride`, `QaThreshold` imports. **FIXED (HIGH DRY-424):** Threshold resolution logic (~20 lines) duplicated between `resolve_effective_thresholds` and `ab_test_thresholds`. Extracted `load_effective_thresholds()` helper. **FIXED (MEDIUM DRY-427):** `{ warn: 0, fail: 0 }` magic object in 2 components. Extracted `EMPTY_THRESHOLD` to `types.ts`. **FIXED (MEDIUM DRY-428):** Inline `{ warn: number; fail: number }` instead of `MetricThreshold` in hook. Replaced with type import. **FIXED (MEDIUM DRY-430):** `handleWarnChange`/`handleFailChange` structural duplicates. Extracted `handleFieldChange("warn" | "fail")` factory. **FIXED (MEDIUM DRY-431):** Section heading class string 3x. Extracted `SECTION_HEADING_CLASSES` constant. **WATCH (MEDIUM DRY-425/426):** `metricNames` useMemo + `handleThresholdChange` callback duplicated in ThresholdEditor and AbTestPanel. 2 consumers each -- extract `useThresholdDraft(effective)` hook at 3rd consumer. **LOW DRY-432:** `MetricThreshold` name exists in both `core::qa_ruleset` and `db::models::hardware`. Different domains, different types. Monitor for confusion. **LOW DRY-433:** Table cell classes repeated heavily in AbTestPanel (internal to 1 component). **DRY-339 WORSENED (+3):** Loading spinner in ThresholdEditor, AbTestPanel, QaProfileListPanel. ~33 consumers total. **DRY-282 WORSENED (+0):** No new badge variant mappers. **NOT DUPLICATED:** `resolve_thresholds()` in core is unique 4-layer merge logic (no prior art). `ResolvedThresholds` (`HashMap<String, MetricThreshold>`) is domain-specific (distinct from `DefaultThreshold` in `quality_gate.rs` which adds `check_type` field). `run_ab_test()` is novel comparison algorithm. Frontend `ResultsTable` sub-component is unique (no other A/B result table exists). `ThresholdSlider` is unique (no other dual-value number input for warn/fail). `ProfileCard` uses `Badge`/`Button` from design system correctly. TanStack Query hooks follow key factory pattern. All handlers use `DataResponse<T>`. `QaProfileRepo` has proper builtin protection in `delete`. Core module has 5 unit tests. Frontend has 17 tests across 3 files. **VERIFIED:** `cargo check` clean (0 PRD-91 warnings), `cargo test -p x121-core -- qa_ruleset` 5/5 passed, `npx tsc --noEmit` clean, `npx vitest run qa-rulesets` 17/17 passed. |

| 2026-02-28 | PRD-82 (Content Sensitivity Controls) | Backend: `core/src/sensitivity.rs`, `db/src/models/sensitivity.rs`, `db/src/repositories/sensitivity_repo.rs`, `api/src/handlers/sensitivity.rs`, `api/src/routes/sensitivity.rs`. Frontend: `features/sensitivity/types.ts`, `hooks/use-sensitivity-settings.ts`, `SensitivityProvider.tsx`, `BlurredMedia.tsx`, `WatermarkOverlay.tsx`, `ScreenShareIndicator.tsx`, `hooks/useViewOverride.ts`, `hooks/useScreenShareMode.ts`, `index.ts`, `features/admin/SensitivityDefaults.tsx`. Migration: 1 SQL file. Tests: 4 files (18 tests). | 0 CRITICAL, 1 FLAGGED (DRY-455), 6 WATCH (DRY-453 to DRY-459), DRY-160 worsened (+1), DRY-339 worsened (+1), rest CLEAN | **FLAGGED (DRY-455):** `match option { Some => DataResponse, None => NO_CONTENT }` 4-line block repeated 6x across 4 handler files (sensitivity 2x, themes, keymaps, proficiency). Exceeds 3-instance threshold — extract `optional_data_response<T>` to `api/src/response.rs` at next opportunity. **WATCH (HIGH DRY-453):** SensitivityProvider inlines ~70 lines of API persistence logic structurally identical to `useThemePersistence` (lastSavedRef dirty check, cancelled load effect, 500ms debounce save). Extract `useSettingsPersistence<T>` at 3rd provider. **WATCH (MEDIUM DRY-454):** BlurPreviewCard in SensitivityDefaults duplicates placeholder/blur rendering pattern from BlurredMedia. 2 consumers — extract at 3rd. **WATCH (LOW DRY-456):** SensitivityDefaults uses raw `<p>Loading...</p>` instead of `<Spinner>`. DRY-339 at ~35. **WATCH (LOW DRY-457):** POSITION_CLASSES constant name in 4 files — module-scoped, different semantics, harmless. **WATCH (LOW DRY-458):** Blur level values in 3 tiers (SQL CHECK/Rust const/TS const) — expected cross-boundary, values consistent. **WATCH (LOW DRY-459):** `maxBlurLevel` (FE) mirrors `enforce_minimum_level` (BE) — intentional cross-boundary behavioral duplicate. **VERIFIED:** `cargo check` clean, `cargo test` 1448 passed, `npx tsc --noEmit` clean, `npx vitest run sensitivity` 18/18 passed. |
| 2026-02-28 | PRD-73 (Production Reporting & Data Export) | 12 files (backend: `core/src/production_report.rs`, `db/src/models/production_report.rs`, `db/src/repositories/production_report_repo.rs`, `api/src/handlers/production_report.rs`, `api/src/routes/production_report.rs`; frontend: `features/reports/types.ts`, `hooks/use-reports.ts`, `ReportGenerator.tsx`, `ReportList.tsx`, `ScheduleManager.tsx`, `CreateScheduleForm.tsx`, `ReportViewer.tsx`) | 2 CRITICAL + 5 HIGH + 3 MEDIUM + 2 LOW = 12 total; 0 FIXED (audit-only run) | **CRITICAL (DRY-470):** `resolveStatus()` exact 8-line duplicate in `ReportList.tsx` and `ReportViewer.tsx` (identical STATUS_MAP + fallback). Extract to `types.ts`. **CRITICAL (DRY-471):** `ListReportsParams { limit, offset }` in handler is structurally identical to shared `PaginationParams` from `api/src/query.rs` (used by 4+ handlers). Delete local, import shared. **HIGH (DRY-472):** `BASE_URL` constant duplicated in `ReportList.tsx` and `ReportViewer.tsx` (also 2 more codebase-wide). Export from `lib/api.ts`. **HIGH (DRY-473):** Raw `new Date().toLocaleString()` 3x instead of shared `formatDateTime()` from `@/lib/format.ts` (used by 20+ components). Same pattern as DRY-224/DRY-257. **HIGH (DRY-474):** Inline `SELECT id FROM report_types WHERE id = $1` raw SQL in handler. Violates `ensure_X_exists` pattern (15+ handlers). Add repo method + helper. **HIGH (DRY-475):** Validators return `&'static str` instead of convention `String` (15+ validators use `String`). Forces verbose `.map_err(\|e\| AppError::BadRequest(e.to_string()))` 5x. **HIGH (DRY-476):** Magic `1_i64` for pending status_id in repo. Need `REPORT_STATUS_ID_*` constants (DRY-249/265 pattern). **MEDIUM (DRY-477):** Report type select options mapping duplicated in 2 components. Only 2 consumers -- watch. **MEDIUM (DRY-478):** Date From/To state+grid duplicated in 2 components. Only 2 consumers -- watch. **MEDIUM (DRY-479):** Duplicate icon imports (2 lines instead of 1) in 3 files. **LOW (DRY-480):** Download URL+link rendering structural duplicate in 2 components. Different visual contexts. **LOW (DRY-481):** Loading/empty state class pattern 4x in PRD-73 (DRY-468 continuation). **CLEAN:** Format validation (unique), cron validation (unique), status badge variant mapping (follows convention), TanStack Query hooks (correct patterns), COLUMNS constants (consistent), route structure (standard), model/DTO structure (standard), core constants (consistent). **PENDING FIX:** All 7 CRITICAL+HIGH items flagged for resolution before commit. |
| 2026-03-01 | PRD-65 (Workflow Regression Testing) | Backend: `core/src/regression.rs`, `core/src/segment_comparison.rs` (modified), `db/src/models/regression.rs`, `db/src/repositories/regression_repo.rs`, `api/src/handlers/regression.rs`, `api/src/routes/regression.rs`. Frontend: `features/regression/types.ts`, `features/regression/hooks/use-regression.ts`, `features/regression/VerdictBadge.tsx`, `features/regression/ScoreDiffDisplay.tsx`, `features/regression/ReferenceManager.tsx`, `features/regression/RunHistoryPanel.tsx`, `features/regression/RegressionReport.tsx`, `features/regression/index.ts`, `features/segment-comparison/QAScoreComparison.tsx` (modified), `lib/qa-constants.ts` (modified). Tests: 4 frontend test files. Migration: 1 SQL file. | 6 FIXED (DRY-460, DRY-462, DRY-463, DRY-465, DRY-466, DRY-467), 3 WATCH (DRY-461, DRY-464, DRY-468), rest CLEAN | **FIXED (CRITICAL DRY-460):** `formatScore`/`formatDiff` exact duplicates in `ScoreDiffDisplay.tsx` (PRD-65) and `QAScoreComparison.tsx` (PRD-101) -- identical functions, copy-paste clone. Extracted to `@/lib/qa-constants.ts`. Both consumers updated to import shared versions. **FIXED (HIGH DRY-462):** `classify_from_diffs` (regression.rs) structurally duplicated `overall_quality_improved` (segment_comparison.rs) -- identical fold/sum/count/average pattern over JSON diffs. Extracted `average_score_diffs(diffs: &Value) -> Option<f64>` to `segment_comparison.rs`. Both consumers refactored: `overall_quality_improved` is now a 1-liner, `classify_from_diffs` uses shared average then applies threshold. ~15 lines of duplicate iteration eliminated. **FIXED (HIGH DRY-463):** `RunReportSummary` (db/models) duplicated `RunSummary` (core) -- same fields, different int types (i32 vs usize). Handler had 6-line manual conversion with `as i32` casts. Fixed: changed `RunSummary` to `i32` fields + added `Serialize`, deleted `RunReportSummary`, handler now passes `summary` directly. ~10 lines eliminated. **FIXED (MEDIUM DRY-465):** `VERDICT_BADGE_VARIANTS` typed as `Record<Verdict, string>` instead of `Record<Verdict, BadgeVariant>`. Fixed type, removed `as BadgeVariant` cast from VerdictBadge.tsx. **FIXED (MEDIUM DRY-466):** `STATUS_BADGE_VARIANT`/`STATUS_LABELS` defined inline in `RunHistoryPanel.tsx` instead of `types.ts`. Moved to types.ts as `RUN_STATUS_BADGE_VARIANT`/`RUN_STATUS_LABELS`, exported from index.ts. **FIXED (LOW DRY-467):** `VERDICT_COLORS` dead code (defined and exported but never imported). Removed from types.ts and index.ts. **WATCH (MEDIUM DRY-461):** `ScoreDiffDisplay` (PRD-65, table layout) is structural duplicate of `QAScoreComparison` (PRD-101, flex layout). Both iterate metric keys with colored diffs. Different visual layouts justify separate components. Shared `formatScore`/`formatDiff` extracted (DRY-460). Monitor for 3rd consumer. **WATCH (LOW DRY-464):** `RUN_STATUS_COMPLETED`/`RUN_STATUS_FAILED` name collision between `regression.rs` and `batch_production.rs`. Same values, different domains. No action needed. **WATCH (LOW DRY-468):** Loading text pattern adds 4 instances (ReferenceManager, RunHistoryPanel, RegressionReport x2). DRY-339 total now ~39. `<CenteredSpinner />` extraction overdue. **NOT DUPLICATED:** `classify_verdict` correctly reuses `compute_score_diffs` from `segment_comparison` (not reimplemented). `validate_trigger_type` follows established validation pattern. `ensure_reference_exists`/`ensure_run_exists` follow established `ensure_X_exists` pattern. All handlers use `DataResponse<T>` (not `serde_json::json!`). TanStack Query hooks follow key factory pattern. `VerdictBadge` uses `Badge` from primitives. `ReferenceManager`/`RunHistoryPanel` use `Card`/`CardBody`/`CardHeader` composites. Frontend tests use `renderWithProviders` from `@/lib/test-utils`. Verdict constants in Rust and TS are consistent cross-boundary. No handler uses optional response pattern (DRY-455 not worsened). No hardcoded status IDs. No inline SVGs. No raw fetch. All files under 300 lines. **VERIFIED:** `cargo check` clean (0 PRD-65 warnings), `cargo test -p x121-core -- regression` 19/19 passed, `cargo test -p x121-core -- segment_comparison` 12/12 passed, `npx tsc --noEmit` clean. |

| 2026-02-28 | PRD-94 (Character Consistency Report) | Backend: `core/src/consistency_report.rs`, `db/src/models/consistency_report.rs`, `db/src/repositories/consistency_report_repo.rs`, `api/src/handlers/consistency_report.rs`, `api/src/routes/consistency_report.rs`. Frontend: `features/consistency/types.ts`, `features/consistency/hooks/use-consistency.ts`, `features/consistency/ConsistencyHeatmap.tsx`, `features/consistency/OutlierPanel.tsx`, `features/consistency/ConsistencyOverview.tsx`, `features/consistency/ConsistencyReportCard.tsx`, `features/consistency/index.ts`. | 4 FIXED (DRY-482 to DRY-485), 2 WATCH (DRY-486, DRY-487), 5 CLEAN (DRY-488 to DRY-492) | **FIXED (HIGH DRY-482):** `ensure_character_exists` was inline in `consistency_report.rs` (CharacterRepo::find_by_id + ok_or_else NotFound), same pattern already extracted in `embedding.rs` and `poster_frame.rs`. Extracted as `pub(crate)` helper in `consistency_report.rs`. **FIXED (HIGH DRY-483):** Report-building logic (compute_overall_consistency + identify_outliers + outlier_ids conversion + CreateConsistencyReport DTO construction) copy-pasted between `generate_report` (lines 105-121) and `batch_generate` (lines 210-235). Extracted `build_report_input(character_id, project_id, scores, report_type)` shared helper. ~20 lines eliminated. **FIXED (MEDIUM DRY-484):** `cellBg()` in `ConsistencyHeatmap.tsx` (threshold-based green/yellow/red bg with /40 opacity) was structural duplicate of `consistencyBg()` in `types.ts` (same thresholds, /30 opacity). Extracted `consistencyCellBg()` to `types.ts`. ConsistencyHeatmap now imports from types. **FIXED (MEDIUM DRY-485):** `(score * 100).toFixed(1)%` percentage formatting repeated 3x in ConsistencyHeatmap + 1x in ConsistencyReportCard. Added `formatPercent(value, decimals?)` to `@/lib/format.ts`. Updated all 3 full-percent instances (one cell-only integer kept as `Math.round()`). Pattern exists 15+ times across other features -- `formatPercent` available for future migration. **WATCH (MEDIUM DRY-486):** `validate_report_type` returns `Result<(), String>` -- minority pattern among 20 validators (5 use String, 14 use CoreError). Not unique to PRD-94. **WATCH (LOW DRY-487):** Score-to-color threshold pattern in 3 features with different threshold values. No extraction needed. **CLEAN (LOW DRY-488):** `REPORT_TYPE_BADGE_VARIANT` follows established `Record<Union, BadgeVariant>` pattern (10+ mappers). **CLEAN (LOW DRY-489):** `COLUMNS` constant follows established repo pattern (90+ repos). **CLEAN (LOW DRY-490):** `default_report_type()` serde default fn. Standard pattern, no extraction needed. **CLEAN (LOW DRY-491):** `compute_overall_consistency` is plain arithmetic mean -- semantically distinct from `incremental_mean` in estimation.rs. **CLEAN (LOW DRY-492):** `compute_pairwise_matrix` is novel to PRD-94. No similar matrix computation in codebase. **NOT DUPLICATED:** Core module (`consistency_report.rs`) is pure logic with no DB deps -- correctly placed. `identify_outliers` is novel (threshold-based index filtering). `ConsistencyOverview` composition is unique (card list + summary badge). `OutlierPanel` is unique (outlier-specific action panel). TanStack Query hooks follow key factory pattern. Route file follows established merge pattern. `ConsistencyHeatmap` renders unique pairwise matrix table -- no prior heatmap component in codebase. Barrel export follows convention. All handlers use `DataResponse<T>`. `validate_report_type` uses `.map_err(AppError::BadRequest)` method reference (not closure). No `serde_json::json!` in handler responses. No magic strings. No hardcoded status IDs. No inline SVGs. No raw fetch. All files under 300 lines. **VERIFIED:** `cargo check` clean (0 PRD-94 warnings), `npx tsc --noEmit` clean (0 errors). |
| 2026-02-28 | PRD-103 (Character Face Contact Sheet) | Backend: `core/src/contact_sheet.rs`, `db/src/models/contact_sheet.rs`, `db/src/repositories/contact_sheet_repo.rs`, `api/src/handlers/contact_sheet.rs`, `api/src/routes/contact_sheet.rs`. Frontend: `features/contact-sheet/types.ts`, `features/contact-sheet/hooks/use-contact-sheet.ts`, `features/contact-sheet/FaceCropGrid.tsx`, `features/contact-sheet/ContactSheetControls.tsx`, `features/contact-sheet/ContactSheetPage.tsx`. | 4 FIXED (DRY-493, DRY-495, DRY-496, DRY-498), rest CLEAN | **FIXED (CRITICAL DRY-493):** `ensure_character_exists` identically defined in 4 handler files (`contact_sheet.rs`, `poster_frame.rs`, `embedding.rs`, `consistency_report.rs`). All 3 duplicates removed. All consumers now import `pub(crate)` version from `consistency_report.rs`. ~30 lines eliminated. **FIXED (HIGH DRY-495):** `confidenceBadgeVariant(score)` was an inline function in `FaceCropGrid.tsx` with hardcoded 0.9/0.7 thresholds. Extracted `CONFIDENCE_THRESHOLDS` const and `confidenceBadgeVariant()` function to `features/contact-sheet/types.ts`, following the same pattern as `consistencyBadgeVariant` in `features/consistency/types.ts`. **FIXED (HIGH DRY-496):** `formatConfidence(score)` in `FaceCropGrid.tsx` (`Math.round(score * 100) + "%"`) was a behavioral duplicate of `formatPercent(value, 0)` from `@/lib/format.ts`. Replaced with shared import. ~3 lines eliminated. **FIXED (HIGH DRY-498):** `validate_grid_size` and `validate_export_format` returned `Result<(), String>` instead of `Result<(), CoreError>`, inconsistent with the established pattern (14+ core validators return `CoreError::Validation`). Changed both to return `CoreError`. Handler now uses `?` instead of `.map_err(AppError::BadRequest)`. Tests updated. **NOT DUPLICATED:** `validate_export_format` pattern (check-in-list, error-with-list) is structurally similar to `production_report::validate_format` and `poster_frame::validate_entity_type`, but different valid values per domain -- only 2-3 instances, no extraction justified. `select_best_frames` (N-best indices from f64 slice) vs `select_best_frame` (single best DbId from (DbId,f64) pairs) -- different return types and input shapes. `FaceCropGrid` grid pattern is unique CSS grid component (image-based, not data table). Repo CRUD follows established COLUMNS constant pattern. TanStack Query hooks follow key factory pattern. Route file follows merge-into-character pattern. All handlers use `DataResponse<T>`. All core logic in leaf crate with unit tests. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core -- contact_sheet` 15/15 passed, `npx tsc --noEmit` clean. |
| 2026-03-01 | PRD-40 (VFX Sidecar & Dataset Export) | Backend: `core/src/sidecar.rs`, `core/src/job_status.rs` (NEW), `db/src/models/sidecar.rs`, `db/src/repositories/sidecar_repo.rs`, `api/src/handlers/sidecar.rs`, `api/src/routes/sidecar.rs`. Frontend: `features/sidecar/types.ts`, `features/sidecar/hooks/use-sidecar.ts`, `features/sidecar/TemplateManager.tsx`, `features/sidecar/CreateTemplateForm.tsx`, `features/sidecar/DatasetExportPanel.tsx`, `features/sidecar/CreateExportForm.tsx`, `features/sidecar/SplitConfigurator.tsx`, `features/sidecar/index.ts`, `lib/job-status.ts` (NEW), `lib/validation.ts` (NEW). Migration: 1 SQL file. Tests: 3 frontend test files (11 tests), 16 backend unit tests. | 4 FIXED (DRY-505, DRY-506, DRY-507, DRY-508), 2 WATCH (DRY-509, DRY-504 worsened), rest CLEAN | **FIXED (CRITICAL DRY-505):** `JOB_STATUS_ID_PENDING/RUNNING/COMPLETED/FAILED` (1/2/3/4) defined identically in `core/src/sidecar.rs` AND `core/src/production_report.rs` (as `REPORT_STATUS_ID_*`). Both reference the same `job_statuses` seed data. Created shared `core/src/job_status.rs` with canonical constants. `sidecar.rs` now re-exports via `pub use`. `production_report.rs` now uses `const` aliases pointing to shared module. ~8 lines of duplicate constant definitions eliminated. **FIXED (CRITICAL DRY-506):** `resolveExportStatus()` in `sidecar/types.ts` and `resolveReportStatus()` in `reports/types.ts` were IDENTICAL -- same type union, same `Record<number, Status>` map, same badge variant map, same label map, same fallback. Total ~40 lines of exact duplication. Created shared `@/lib/job-status.ts` with `resolveJobStatus()`, `JOB_STATUS_BADGE_VARIANT`, `JOB_STATUS_LABELS`, `JobStatusLabel` type. Both feature type files now delegate to shared module via re-exports (deprecated aliases for backward compatibility). **FIXED (HIGH DRY-507):** `SidecarTemplate` NotFound pattern repeated 3x in `sidecar.rs` handler (get_template line 60, update_template line 97, delete_template line 124). Extracted `ensure_template_exists(pool, id)` helper. `get_template` and `delete_template` now use helper. `update_template` retained inline because its NotFound is on the repo update return value, not a separate lookup. ~12 lines of duplicate ok_or_else eliminated. **FIXED (MEDIUM DRY-508):** `isValidJson(str)` defined as local function inside `CreateTemplateForm.tsx`. JSON validation is a general utility. Extracted to shared `@/lib/validation.ts`. Component now imports from shared module. **WATCH (LOW DRY-509):** `border-b border-[var(--color-border-default)] last:border-b-0` row-separator pattern appears 2x in PRD-40 (TemplateManager, DatasetExportPanel) and 18+ times codebase-wide. Same issue as DRY-339. Too many consumers to justify extraction now; would need design system ListRow component. **WATCH (LOW DRY-504):** `StatusBadge<S>` generic component need now at 3 instances (VerdictBadge, ComplianceBadge, and now sidecar status badge rendering). Still below action threshold -- uses inline Badge with lookup maps which is the convention. **NOT DUPLICATED:** `validate_sidecar_format()` follows established validation pattern (20+ validators). `sidecar_filename()` is unique (extension-swap utility). `validate_splits()` is unique (sum-to-1.0 check). `assign_split()` is unique (index-to-split assignment). `SplitConfigurator` is unique form widget. `CreateTemplateForm` textarea is inline (only 1 consumer, unlike DEBUGGER_TEXTAREA_BASE which has 3). `CreateExportForm` quality slider is unique. Download URL construction uses shared `API_BASE_URL` from `@/lib/api` (correct). File sizes use shared `formatBytes` (correct). Dates use shared `formatDateTime` (correct). Tests use shared `renderWithProviders` (correct). Hooks follow key factory pattern. All handlers use `DataResponse<T>`. No `serde_json::json!` in handlers. No magic status IDs in handlers (uses `JOB_STATUS_ID_PENDING` from core). Repo uses `COLUMNS` constants and `clamp_limit`/`clamp_offset` from `core/src/search.rs`. All files under 250 lines. **VERIFIED:** `cargo check` clean (0 PRD-40 warnings), `cargo test -p x121-core -- sidecar` 16/16 passed, `npx tsc --noEmit` clean (0 errors), `npx vitest run sidecar` 11/11 passed. |
| 2026-02-28 | PRD-102 (Video Compliance Checker) | Backend: `core/src/compliance.rs`, `db/src/models/compliance.rs`, `db/src/repositories/compliance_repo.rs`, `api/src/handlers/compliance.rs`, `api/src/routes/compliance.rs`. Frontend: `features/compliance/types.ts`, `features/compliance/hooks/use-compliance.ts`, `features/compliance/ComplianceBadge.tsx`, `features/compliance/CreateRuleForm.tsx`, `features/compliance/RuleManager.tsx`, `features/compliance/ComplianceCheckList.tsx`, `features/compliance/index.ts`. Tests: `ComplianceBadge.test.tsx`, `ComplianceCheckList.test.tsx`. | 4 FIXED (DRY-499, DRY-500, DRY-501, DRY-502), 1 FIXED (DRY-503), 1 WATCH (DRY-504) | **FIXED (CRITICAL DRY-499):** `serde_json::json!` used 2x in `run_compliance_check` handler for check-run response DTO. Violates `DataResponse<T>` rule (never use ad-hoc JSON in handlers). Created typed `ComplianceCheckRunResponse` in `db/src/models/compliance.rs`. Handler now uses typed struct. ~8 lines of untyped JSON eliminated. **FIXED (HIGH DRY-500):** `ComplianceRule` NotFound pattern repeated 3x in handler (get_rule, update_rule, delete_rule -- lines 72-77, 110-115, 136-139). Extracted `ensure_rule_exists(pool, id)` helper in `compliance.rs`. `get_rule` handler now uses helper. `update_rule`/`delete_rule` retained inline because their repo methods return `Option`/`bool` from update/delete, not get. **FIXED (HIGH DRY-501):** `validate_rule_type` error mapping used `.map_err(\|msg\| AppError::Core(CoreError::Validation(msg)))` (2x) instead of established `.map_err(AppError::BadRequest)` pattern (used by 8+ handlers with `Result<(), String>` validators). Fixed both call sites to use method reference. **FIXED (HIGH DRY-502):** Frontend `ComplianceSummary` type included `pass_rate: number` but backend `ComplianceCheckSummary` returns only `{ total, passed, failed }` -- no `pass_rate` field. Frontend would read `undefined` at runtime. Removed `pass_rate` from interface, added `compliancePassRate(summary)` helper. `ComplianceCheckList` updated. Test fixture fixed. **FIXED (MEDIUM DRY-503):** `ComplianceState` type + `STATE_LABELS` + `STATE_VARIANT` badge mapping defined locally in `ComplianceBadge.tsx` instead of `types.ts`. Follows antipattern flagged in DRY-466. Moved to `types.ts` as `COMPLIANCE_STATE_BADGE_VARIANT`/`COMPLIANCE_STATE_LABELS`. Component imports from types. Index barrel updated. **WATCH (LOW DRY-504):** `ComplianceBadge` is structurally identical to `VerdictBadge` (lookup label+variant from map, wrap in Badge). Only 2 instances; monitor for 3rd consumer that would justify a generic `StatusBadge<S>` component. **NOT DUPLICATED:** `validate_rule_type` (VALID_list + contains check + error message) follows established pattern (20+ validators in core). `ComplianceResult`/`ComplianceSummary` in core are pure-logic types distinct from DB models. `summarize_checks` vs `summarize_verdicts` -- different semantics (bool pass/fail vs string verdict classification). `check_naming` regex compilation is dynamic (user-supplied pattern) -- cannot be static. Rule CRUD handler structure follows established CRUD patterns. TanStack Query hooks follow key factory pattern. `RULE_TYPE_BADGE_VARIANT` follows established `Record<Union, BadgeVariant>` convention. Repo uses `COLUMNS` constant. Route file follows standard structure. `CreateRuleForm` is unique form composition. All files under 200 lines. **VERIFIED:** `cargo check` clean (0 PRD-102 warnings), `npx tsc --noEmit` clean. |
| 2026-03-01 | PRD-72 (Project Lifecycle & Archival) | Backend: `core/src/project_lifecycle.rs`, `db/src/models/project_lifecycle.rs`, `db/src/repositories/project_lifecycle_repo.rs`, `api/src/handlers/project_lifecycle.rs`, `api/src/routes/project_lifecycle.rs`. Frontend: `features/project-lifecycle/types.ts`, `features/project-lifecycle/hooks/use-project-lifecycle.ts`, `features/project-lifecycle/LifecycleStateBadge.tsx`, `features/project-lifecycle/TransitionControls.tsx`, `features/project-lifecycle/CompletionChecklist.tsx`, `features/project-lifecycle/SummaryReportCard.tsx`, `features/project-lifecycle/BulkArchivalPanel.tsx`, `features/project-lifecycle/index.ts`. Tests: 3 frontend test files, core unit tests. Migration: 1 SQL file. | 4 FIXED (DRY-521, DRY-522, DRY-524, DRY-530/531/532), 5 WATCH (DRY-520, DRY-523, DRY-525, DRY-526, DRY-527/528/529), rest CLEAN | **FIXED (CRITICAL DRY-521):** `const SCENE_STATUS_APPROVED: i16 = 4` hardcoded magic number in `project_lifecycle_repo.rs` instead of using `SceneStatus::Approved.id()` from `db/src/models/status.rs`. Replaced with typed enum import. **FIXED (HIGH DRY-522):** `checklist_to_json()` used `serde_json::json!` to manually serialize `ChecklistItem` and `ChecklistResult` (DRY-083 antipattern). Added `#[derive(serde::Serialize)]` to both structs in core. Deleted `checklist_to_json`. Changed `TransitionResponse.checklist` from `Option<serde_json::Value>` to `Option<ChecklistResult>`. Both handlers now pass typed structs directly. ~15 lines of manual JSON construction eliminated. **FIXED (HIGH DRY-524):** 4-query aggregate pattern (`count_scenes` + `count_approved_scenes` + `count_characters` + `count_characters_with_metadata`) called 3x in handler (checklist eval, summary generation, get_checklist). Created `ProjectAggregates` struct and `get_project_aggregates()` single-query method using subselects. Reduces 12+ sequential DB round-trips to 2 (one for checklist+summary, one for get_checklist). Handler restructured to eliminate redundant query block. **FIXED (LOW DRY-530):** Dead code in `bulk_archive` -- both `if/else` branches returned `Vec::new()`. Simplified to single `Vec::new()` with TODO. **FIXED (LOW DRY-531):** Unnecessary `StatusCode::OK` in `bulk_archive` return -- removed, consistent with other handlers. `StatusCode` import removed. **FIXED (LOW DRY-532):** Redundant test in `LifecycleStateBadge.test.tsx` -- "renders all 5 states correctly" duplicated the parametric `test.each(STATES)` block above it. Removed. **WATCH (CRITICAL DRY-520):** `ensure_project_exists(pool, id)` identically defined in 3 handler files (`project_lifecycle.rs`, `poster_frame.rs`, `comparison.rs`). Plus `project.rs` inlines the same pattern. Needs extraction to shared handler helpers. **WATCH (HIGH DRY-525):** `LifecycleState` enum in core (`Setup/Active/Delivered/Archived/Closed` with string-based `from_str/as_str`) overlaps with `ProjectStatus` enum in `db/models/status.rs` (`Draft=1/Active=2/Paused=3/Completed=4/Archived=5` with ID-based `.id()`). Both represent project statuses but different variants and representations. The new migration adds 3 new statuses to the DB. `ProjectStatus` needs extension to match. **WATCH (HIGH DRY-526):** Multi-select Set toggle pattern (`toggleId/toggleAll/setSelectedIds(new Set())`) duplicated identically in `BulkArchivalPanel.tsx`, `BranchCleanup.tsx`, `ContactSheetPage.tsx`. Candidate for `useMultiSelect` hook extraction. **WATCH (MEDIUM DRY-527):** `LIFECYCLE_STATE_LABELS` in `project-lifecycle/types.ts` and `PROJECT_STATUS_LABELS` in `projects/types.ts` overlap on `active` and `archived`. Candidate for unification. **WATCH (MEDIUM DRY-528):** Confirmation modal pattern (open/close state + Cancel/Confirm buttons in flex-end layout inside Modal) appears 8x across features. Candidate for `ConfirmModal` composite. **WATCH (MEDIUM DRY-529):** `project.rs:get_stats` handler inlines `COUNT(*) FROM characters WHERE project_id = $1 AND deleted_at IS NULL` -- same query as `ProjectLifecycleRepo::count_characters`. Should reuse repo. **WATCH (HIGH DRY-523):** `compute_qa_pass_rate` in `project_lifecycle.rs` follows the `(a as f64 / b as f64) * 100.0` with zero-guard pattern seen 6x across codebase. Keep local for now, extract if a 4th direct consumer appears. **NOT DUPLICATED:** `LifecycleState` enum `from_str/as_str` follows established pattern (20+ core enums). `validate_transition` is domain-specific. `evaluate_checklist` is unique logic. `ProjectSummaryData` is unique DTO. `compute_wall_clock_days` is unique. `LifecycleStateBadge` follows established `*BadgeVariant` map + Badge pattern. `TransitionControls` confirmation modal is domain-specific (different text per target). `CompletionChecklist` checklist UI is unique. `SummaryReportCard` stat grid is unique. Frontend hooks follow key factory pattern. Route file follows standard structure. All handlers use `DataResponse<T>`. All files under 300 lines. **VERIFIED:** `cargo check` clean (0 PRD-72 warnings). |
| 2026-03-01 | PRD-84 (External Review / Shareable Preview Links) | Backend: `core/src/shared_link.rs`, `db/src/models/shared_link.rs`, `db/src/repositories/shared_link_repo.rs`, `api/src/handlers/shared_link.rs`, `api/src/routes/shared_link.rs`. Frontend: `features/shared-links/types.ts`, `features/shared-links/hooks/use-shared-links.ts`, `features/shared-links/LinkStatusBadge.tsx`, `features/shared-links/SharedLinksPanel.tsx`, `features/shared-links/CreateLinkDialog.tsx`, `features/shared-links/LinkActivityPanel.tsx`, `features/shared-links/ExternalReviewPage.tsx`, `features/shared-links/FeedbackForm.tsx`, `features/shared-links/PasswordGate.tsx`, `features/shared-links/LinkError.tsx`, `features/shared-links/index.ts`. Tests: 3 test files. Migration: 1 SQL. Cross-feature: `provenance/ReceiptPanel.tsx`, `api-keys/ApiKeyManager.tsx`. | 4 CRITICAL + 1 HIGH + 1 MEDIUM = 6 total; ALL FIXED (DRY-509 to DRY-514) | **FIXED (CRITICAL DRY-509):** `serde_json::json!({ "verified": true })` in `verify_password` handler -- replaced with typed `PasswordVerifiedResponse` struct. **FIXED (CRITICAL DRY-510):** Token lookup + validity check pattern (hash_token -> find_by_token_hash -> ok_or_else NotFound -> check_link_validity -> map_err BadRequest) duplicated 3x in `validate_token`, `verify_password`, `submit_feedback`. Extracted `ensure_valid_link(pool, token) -> AppResult<SharedLink>` helper. ~45 lines eliminated. **FIXED (CRITICAL DRY-511):** Frontend API route mismatch -- hooks called `/public/shared-links/{token}` but backend routes are at `/review/{token}`. All 3 public hook endpoints corrected. Would have been runtime 404 errors. **FIXED (CRITICAL DRY-512):** Frontend `SharedLink` interface included `token_hash: string` and `password_hash: string | null` but backend model has `#[serde(skip_serializing)]` on both -- they are NEVER sent to frontend. Removed both fields from TS type. Fixed `SharedLinksPanel.tsx:194` which was copying `link.token_hash` (always `undefined`). Removed broken Copy button from link rows. Updated test fixtures. **FIXED (HIGH DRY-513):** `useRevokeLink` used `api.post(/shared-links/${id}/revoke)` but backend route is `DELETE /shared-links/{id}`. Fixed to `api.delete`. Updated test assertion from mockPost to mockDelete. **FIXED (MEDIUM DRY-514):** Copy-to-clipboard pattern (`useState(false) + navigator.clipboard.writeText + setCopied(true) + setTimeout(setCopied(false), 2000)`) duplicated 4x across `SharedLinksPanel.tsx`, `CreateLinkDialog.tsx`, `provenance/ReceiptPanel.tsx`, `api-keys/ApiKeyManager.tsx`. Extracted `useCopyToClipboard(resetMs?)` hook to `@/hooks/useCopyToClipboard.ts`. All 3 remaining consumers (SharedLinksPanel copy removed) refactored to use shared hook. **NOT DUPLICATED:** Token generation in core uses shared `sha256_hex` from `core/src/hashing.rs` (correct). Password hashing also uses shared `sha256_hex` (correct). `PaginationParams` imported from `api/src/query.rs` (correct). `clamp_limit`/`clamp_offset` from `core/src/search.rs` (correct). `DataResponse<T>` used throughout (correct). `formatDateTime`/`formatCountdown` from `@/lib/format.ts` (correct). `Badge`/`BadgeVariant` from primitives (correct). `Modal` from composite (correct). TanStack Query hooks follow key factory pattern. Constants properly extracted to `core/src/shared_link.rs`. SQL has proper CHECK constraints matching Rust constants. Migration follows established pattern (trigger, indexes). Route file cleanly separates authenticated/public routers. `LinkStatusBadge` follows established badge resolver pattern. All files under 255 lines. **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean. |

| 2026-03-01 | PRD-92 (Batch Review & Approval Workflows) | Backend: `core/src/batch_review.rs`, `db/src/models/batch_review.rs`, `db/src/repositories/batch_review_repo.rs`, `api/src/handlers/batch_review.rs`, `api/src/routes/batch_review.rs`. Frontend: `features/batch-review/types.ts`, `features/batch-review/hooks/use-batch-review.ts`, `features/batch-review/BatchActionBar.tsx`, `features/batch-review/AutoApproveAction.tsx`, `features/batch-review/ReviewProgressBar.tsx`, `features/batch-review/QuickReviewMode.tsx`, `features/batch-review/AssignmentManager.tsx`, `features/batch-review/DeadlineTracker.tsx`, `features/batch-review/index.ts`. Tests: 3 files. Migration: 1 SQL. | 5 FIXED (DRY-530, DRY-532, DRY-533, DRY-534, DRY-535), 4 WATCH (DRY-531, DRY-536, DRY-537, DRY-538) | **FIXED (CRITICAL DRY-530):** `validate_qa_threshold` in `core/src/batch_review.rs` reimplemented [0.0,1.0] range validation already in `core/src/threshold_validation.rs::validate_unit_range`. Replaced with delegation. Return type changed `Result<(),String>` -> `Result<(),CoreError>`. Handler `.map_err(AppError::BadRequest)` changed to `.map_err(AppError::Core)`. Tests updated. **FIXED (HIGH DRY-532):** `AssignmentListParams` defined local `limit: Option<i64>` and `offset: Option<i64>` fields -- exact duplicate of `PaginationParams` from `api/src/query.rs`. Replaced with `#[serde(flatten)] pub pagination: PaginationParams`. **FIXED (HIGH DRY-533):** `ASSIGNMENT_STATUS_BADGE_VARIANT` in `features/batch-review/types.ts` typed as `Record<AssignmentStatus, string>` instead of `Record<AssignmentStatus, BadgeVariant>`, forcing `as BadgeVariant` casts at 2 consumer sites (`AssignmentManager.tsx:118`, `DeadlineTracker.tsx:76`). Changed to proper `BadgeVariant` type, removed both casts and orphaned `import type { BadgeVariant }` in consumers. **FIXED (MEDIUM DRY-534):** `"segment_ids must not be empty"` validation block (4 lines) duplicated identically in `batch_approve` and `batch_reject` handlers. Extracted `require_non_empty_segment_ids(ids)` helper. Both handlers now call shared function. **FIXED (MEDIUM DRY-535):** `count_approved_segments` and `count_rejected_segments` in `batch_review_repo.rs` had identical 16-line SQL queries differing only by `WHERE latest.decision = 'approved'` vs `'rejected'`. Extracted `count_segments_by_decision(pool, project_id, decision)` private method. Both public methods now delegate. ~16 lines of duplicate SQL eliminated. **WATCH (MEDIUM DRY-531):** `parse_deadline` structurally similar to `parse_timestamp` from `api/src/query.rs` -- different return type semantics prevent merging. **WATCH (LOW DRY-536):** `formatEstimatedTime` in `types.ts` -- thin wrapper over `formatDuration`-like logic with " remaining" suffix. Domain-specific. **WATCH (LOW DRY-537):** `createWrapper()` in hook test -- diverges from `renderWithProviders` (raw wrapper + mutation retry disabled). **WATCH (LOW DRY-538):** `'approved'`/`'rejected'` strings in repo SQL (3 instances, single file). **NOT DUPLICATED:** `ensure_assignment_exists` helper follows established pattern (40+ in codebase). `BatchActionResponse` is unique DTO (processed_count + segment_ids). `ReviewProgressResponse` is unique DTO (total/reviewed/approved/rejected/pending/pace/estimate). Core functions (`compute_avg_pace`, `estimate_remaining_seconds`, `is_overdue`, `count_above_threshold`, `filter_above_threshold`) are domain-specific, no duplicates in codebase. `validate_assignment_status`/`validate_sort_mode` follow established pattern but return `Result<(), String>` (minority pattern, DRY-486). `formatPace` is domain-specific formatter. `QUICK_REVIEW_KEYS` is unique key mapping. Repo column constants follow established pattern. Session CRUD is unique. Batch INSERT with UNNEST is unique. Progress count queries are unique. TanStack Query hooks follow key factory pattern. Route file follows standard structure. All handlers use `DataResponse<T>`. `AutoApproveAction` correctly uses `Modal` from composites and `formatPercent` from `@/lib/format.ts`. `ReviewProgressBar` correctly uses `formatPercent`. `AssignmentManager` correctly uses `formatDateTime`. `DeadlineTracker` correctly uses `formatDateTime` and `formatCountdown`. Tests use `renderWithProviders` for component tests. No `serde_json::json!` in handler responses (except `serde_json::json!({})` default for filter_criteria_json -- acceptable, used once in create_assignment). No inline SVGs. No raw fetch. All files under 270 lines. **VERIFIED:** `cargo check` clean (0 PRD-92 warnings), `cargo test -p x121-core -- batch_review` 17/17 passed, `npx biome check types.ts` clean. |

| 2026-03-01 | PRD-87 (GPU Power Management) | Backend: `core/src/gpu_power.rs`, `db/src/models/gpu_power.rs`, `db/src/repositories/gpu_power_repo.rs`, `api/src/handlers/gpu_power.rs`, `api/src/routes/gpu_power.rs`. Frontend: `features/gpu-power/types.ts`, `features/gpu-power/hooks/use-gpu-power.ts`, `features/gpu-power/PowerStateBadge.tsx`, `features/gpu-power/ConsumptionSummary.tsx`, `features/gpu-power/FleetSettingsPanel.tsx`, `features/gpu-power/PowerDashboard.tsx`, `features/gpu-power/ScheduleEditor.tsx`, `features/gpu-power/WorkerPowerCard.tsx`. Tests: 3 files. Migration: 1 SQL. Cross-feature: `workers/WorkerDashboard.tsx`, `dashboard/hooks/use-performance.ts`. | 5 CRITICAL + 4 HIGH = 9 fixed; 2 MEDIUM + 1 LOW watch | **FIXED (CRITICAL):** All frontend API paths used `/admin/gpu-power/` instead of backend's `/admin/power/` -- 10+ endpoints would 404. **FIXED (CRITICAL):** `FleetPowerSettings` frontend type had `min_fleet_size`/`default_schedule` fields that don't exist on backend; missing `default_wake_method`/`fleet_schedules` -- complete type mismatch. **FIXED (CRITICAL):** `ConsumptionSummaryData` frontend used `total_kwh`/`savings_percent` but backend sends `total_estimated_kwh`/`savings_pct`. **FIXED (CRITICAL):** Consumption query params `start_date`/`end_date` vs backend `from`/`to`. **FIXED (CRITICAL):** Missing fleet-wide worker status list endpoint (`GET /admin/power/workers/status`) -- added handler, repo method, route. **FIXED (HIGH):** `canShutdown` allowed "on" state but backend only allows `idle -> shutting_down`. **FIXED (HIGH):** `ConsumptionEntry.estimated_kwh` typed as `number` but backend sends `Option<f32>` (nullable). **FIXED (HIGH):** `parse_date` local function duplicated `parse_timestamp` pattern -- extracted to shared `api/src/query.rs`. **FIXED (HIGH):** `StatBadge`/`StatItem` duplicated 4+ times -- extracted to `components/primitives/StatBadge.tsx`. **FIXED (HIGH):** `daysAgo`/`daysAgoIso` duplicated -- extracted to `@/lib/format.ts`. **WATCH:** DRY-540 (string-list validator pattern, project convention), DRY-541 (fleet settings not persisted), DRY-543 (ConsumptionEntry omits some fields). **VERIFIED:** `cargo check` clean, `cargo test -p x121-core` 1621 tests passed, `npx tsc --noEmit` clean. |
| 2026-03-01 | PRD-19 (Disk Space Visualizer / Treemap) | Backend: `core/src/storage_visualizer.rs`, `db/src/models/storage_visualizer.rs`, `db/src/repositories/storage_visualizer_repo.rs`, `api/src/handlers/storage_visualizer.rs`, `api/src/routes/storage_visualizer.rs`. Frontend: `features/storage-visualizer/types.ts`, `features/storage-visualizer/hooks/use-storage-visualizer.ts`, `features/storage-visualizer/StorageSummaryCard.tsx`, `features/storage-visualizer/FileTypeBreakdownChart.tsx`, `features/storage-visualizer/StorageTreemap.tsx`, `features/storage-visualizer/TreemapBreadcrumbs.tsx`, `features/storage-visualizer/TreemapActions.tsx`, `features/storage-visualizer/treemap-layout.ts`, `features/storage-visualizer/StorageVisualizerPage.tsx`, `features/storage-visualizer/index.ts`. Tests: 3 files (16 tests). Migration: 1 SQL. | 3 CRITICAL + 2 HIGH + 1 MEDIUM = 6 fixed; 1 LOW watch | **FIXED (CRITICAL DRY-540):** Backend `StorageSummary` struct missing `snapshot_at` field that frontend reads in `StorageSummaryCard.tsx:61`. Would produce `undefined` at runtime. Added `snapshot_at: Option<Timestamp>` to model, `MAX(snapshot_at) AS latest_snapshot_at` to repo summary query, and wired through handler. Frontend type updated to `string \| null`, component now null-guards. **FIXED (CRITICAL DRY-541):** `reclaimable_percentage` value range mismatch -- backend `compute_reclaimable_percentage()` returned 0-100 range but frontend `formatPercent(summary.reclaimable_percentage)` expects 0-1 (multiplies by 100 internally). 25% reclaimable would display as "2500.0%". Renamed to `compute_reclaimable_fraction()`, changed to return 0.0-1.0 ratio. All 4 core unit tests updated. **FIXED (CRITICAL DRY-542):** Backend treemap handler returns `DataResponse<Vec<TreemapNode>>` (array) but frontend hook typed as `api.get<TreemapNode>` (singular). `computeTreemapLayout` takes a single root. Hook now fetches `TreemapNode[]` and wraps in synthetic root node with aggregated totals. Test mocks updated to return arrays. **FIXED (HIGH DRY-543):** `FileTypeBreakdown.percentage` backend `compute_breakdown()` returned 0-100 range (`* 100.0`) but frontend type comment says "Fraction of total storage (0-1)" and test mocks use 0.5. Changed backend to return 0.0-1.0 fraction. Core unit tests updated (50.0->0.5, 20.0->0.2). **FIXED (HIGH DRY-544):** `FileTypeBreakdownChart.tsx:87-91` inlined Recharts `contentStyle` identical to `TOOLTIP_CONTENT_STYLE` from `@/features/dashboard/performance/chartStyles.ts`. 4 properties duplicated verbatim. Replaced with shared import. **FIXED (MEDIUM DRY-545):** `safetensors` extension listed in BOTH `intermediate` and `model` category seed data in migration, and classified as `CATEGORY_INTERMEDIATE` in `classify_extension()`. Inconsistent -- safetensors is a model weight format. Moved to `model` category only (removed from intermediate seed array, changed core classification). Tests updated. **WATCH (LOW DRY-546):** `/admin/storage` route prefix mounted 2x in `routes/mod.rs` (line 757 for PRD-19, line 938 for PRD-48). No path collisions (different sub-paths) but architecturally confusing. Consider renaming PRD-19 to `/admin/storage-visualizer` if it causes confusion. |
| 2026-03-01 | PRD-119 (Time-Based Job Scheduling) | Backend: `core/src/job_scheduling.rs`, `db/src/models/job_scheduling.rs`, `db/src/repositories/job_scheduling_repo.rs`, `api/src/handlers/job_scheduling.rs`, `api/src/routes/job_scheduling.rs`. Frontend: `features/job-scheduling/types.ts`, `features/job-scheduling/hooks/use-job-scheduling.ts`, `features/job-scheduling/cron-utils.ts`, `features/job-scheduling/JobSchedulingPage.tsx`, `features/job-scheduling/ScheduleForm.tsx`, `features/job-scheduling/ScheduleList.tsx`, `features/job-scheduling/ScheduleRow.tsx`, `features/job-scheduling/ScheduleHistoryPanel.tsx`, `features/job-scheduling/ScheduleStatusBadge.tsx`, `features/job-scheduling/OffPeakConfigEditor.tsx`, `features/job-scheduling/OffPeakGrid.tsx`, `features/job-scheduling/CronPreview.tsx`, `features/job-scheduling/JsonTextarea.tsx`, `features/job-scheduling/index.ts`. Tests: 3 files. Migration: 1 SQL. | 4 CRITICAL + 4 HIGH + 1 LOW = 9 fixed; 3 MEDIUM watch | **FIXED (CRITICAL DRY-550):** Frontend `UpdateOffPeakConfig.windows` vs backend `UpdateOffPeakConfigBulk.entries` field name mismatch -- serde would reject every PUT to `/schedules/off-peak`. Renamed frontend field to `entries`. Updated `gridToWindows` -> `gridToEntries` and all callers. **FIXED (CRITICAL DRY-551):** Frontend `Schedule` type missing `updated_at` field from backend Serialize struct. Added field + updated test fixture. **FIXED (CRITICAL DRY-552):** Frontend `ScheduleHistory` type missing `created_at` field. Added. **FIXED (CRITICAL DRY-553):** Frontend `OffPeakConfig` type missing `created_at` and `updated_at` fields. Added both. **FIXED (HIGH DRY-554):** `useSchedules` hook accepted `params` but never appended them to API URL -- all query filters (schedule_type, is_active, limit, offset) silently lost. Built URLSearchParams query string. **FIXED (HIGH DRY-556):** 6x repeated `ScheduleRepo::find_by_id + ok_or(NotFound) + owner_id != auth.user_id + Forbidden` pattern in handler (get, update, delete, pause, resume, history). Extracted `ensure_schedule_owned(pool, id, user_id)` helper. ~60 lines eliminated. **FIXED (HIGH DRY-557):** `TIMEZONE_SELECT_OPTIONS` identical mapping defined in both `ScheduleForm.tsx:29-31` and `OffPeakConfigEditor.tsx:27-29`. Moved to single definition in `types.ts`, both consumers import. **FIXED (HIGH DRY-558):** `CRON_DAY_NAMES` in `cron-utils.ts` identical to `DAY_NAMES` in `types.ts`. Removed duplicate, `cron-utils.ts` now imports from `types.ts`. **FIXED (LOW DRY-562):** Magic strings `"recurring"` and `"one_time"` in `compute_next_run_for_schedule` helper -- replaced with `SCHEDULE_RECURRING`/`SCHEDULE_ONE_TIME` constants. **WATCH (MEDIUM DRY-555):** `ScheduleListParams` and `ScheduleHistoryParams` define inline `limit`/`offset` instead of using `PaginationParams` from `api/src/query.rs`. Should flatten. Left as watch -- functional. **WATCH (MEDIUM DRY-559):** Inline `JSON.parse` try/catch in `ScheduleForm.tsx` instead of `isValidJson` from `@/lib/validation.ts` -- acceptable since form needs parsed value. **WATCH (MEDIUM DRY-561):** Off-peak hour range validation inline in handler duplicates DB CHECK constraints. Defense-in-depth acceptable. **NOT DUPLICATED:** `DataResponse<T>` used correctly throughout all handlers. `clamp_limit`/`clamp_offset` imported from `core/src/search.rs`. `formatDateTime`/`formatDuration` imported from `@/lib/format.ts`. `Badge`/`BadgeVariant` from primitives. `Modal` from composite. Route file cleanly separates auth/admin routes. Core module has comprehensive unit tests (32 tests). Cron parsing logic is frontend-only (display preview, not shared with backend). Off-peak logic is backend-only (scheduling decisions). Handler properly uses `SCHEDULE_RECURRING`/`SCHEDULE_ONE_TIME` constants. `ScheduleStatusBadge` follows established badge pattern. `ScheduleRow` follows table row pattern. `JsonTextarea` is unique to this feature. `OffPeakGrid` is unique grid widget. All files under 200 lines. **VERIFIED:** `cargo check` clean. |
| 2026-03-01 | PRD-80 (System Health Page) | Backend: `core/src/system_health.rs`, `db/src/models/system_health.rs`, `db/src/repositories/system_health_repo.rs`, `api/src/handlers/system_health.rs`, `api/src/routes/system_health.rs`, `api/src/engine/health_aggregator.rs`. Frontend: `features/system-health/types.ts`, `features/system-health/hooks/use-system-health.ts`, `features/system-health/HealthStatusBadge.tsx`, `features/system-health/ServiceCard.tsx`, `features/system-health/ServiceStatusGrid.tsx`, `features/system-health/UptimeBar.tsx`, `features/system-health/StartupChecklist.tsx`, `features/system-health/AlertConfigPanel.tsx`. Tests: 3 files. Migration: 1 SQL. | 3 CRITICAL + 3 HIGH = 6 fixed; 2 MEDIUM + 1 LOW watch | **FIXED (CRITICAL DRY-530):** Frontend `ServiceStatusResponse.last_checked` did not match backend `checked_at`. **FIXED (CRITICAL DRY-531):** Frontend `UptimeResponse` had `uptime_7d`/`uptime_30d` but backend sends `uptime_percent_24h` + `down_seconds`. **FIXED (CRITICAL DRY-532):** 4 frontend API paths/methods wrong: missing `/statuses` suffix, missing `/services/` prefix, reversed path segments, PATCH vs PUT method + numeric ID vs service name. **FIXED (HIGH DRY-534):** `StartupCheckResponse`/`StartupCheckItem` duplicated core `StartupCheck`/`StartupCheckResult` -- added `Serialize` to core, removed handler duplicates. **FIXED (HIGH DRY-535):** `health_aggregator.rs` used 8 string literals instead of `STATUS_HEALTHY`/`STATUS_DEGRADED`/`STATUS_DOWN` constants. **SKIPPED (HIGH DRY-533):** `probe_service` vs health_aggregator probes -- different return types and persistence patterns, structural difference not pure duplication. **WATCH:** DRY-542 (fmtPct semantic difference), DRY-543 (JSONB type assumption), DRY-544 (magic number 50). **VERIFIED:** `cargo check` clean, `cargo test -p x121-core` 1621 tests passed, `npx tsc --noEmit` clean. |
| 2026-02-28 | PRD-99 (Webhook Integration Testing Console) | Backend: `core/src/webhook_testing.rs`, `db/src/models/webhook_testing.rs`, `db/src/repositories/webhook_testing_repo.rs`, `api/src/handlers/webhook_testing.rs`, `api/src/routes/webhook_testing.rs`, `api/src/request.rs` (NEW). Frontend: `features/webhook-testing/types.ts`, `features/webhook-testing/hooks/use-webhook-testing.ts`, `features/webhook-testing/WebhookTestingPage.tsx`, `features/webhook-testing/TestPayloadSender.tsx`, `features/webhook-testing/TestResultDisplay.tsx`, `features/webhook-testing/DeliveryLogViewer.tsx`, `features/webhook-testing/DeliveryRow.tsx`, `features/webhook-testing/FailedDeliveryInspector.tsx`, `features/webhook-testing/EndpointHealthDashboard.tsx`, `features/webhook-testing/MockEndpointManager.tsx`, `features/webhook-testing/MockRow.tsx`, `features/webhook-testing/MockCapturesList.tsx`, `features/webhook-testing/index.ts`. Tests: 6 files. Migration: 1 SQL. | 7 CRITICAL + 3 HIGH = 10 fixed; 2 MEDIUM watch | **FIXED (CRITICAL DRY-570):** ALL 10 frontend API paths wrong -- missing `/admin` prefix, used `/mocks` instead of `/mock-endpoints`, used `/endpoints/` instead of `/webhooks/`, health summary at `/health` instead of `/health/summary`, health per-endpoint at `/health/{type}/{id}` instead of `/webhooks/{id}/health`. Every hook call would have 404'd at runtime. Extracted `const BASE = "/admin/webhook-testing"` and corrected all paths. **FIXED (CRITICAL DRY-571):** Frontend `MockEndpointCapture.captured_at` does not exist on backend -- backend serializes `received_at`. Renamed field and updated `MockCapturesList.tsx`. **FIXED (CRITICAL DRY-572):** Frontend `EndpointHealth` type was flat (`endpoint_id`, `endpoint_type`, `success_rate_pct`, etc.) but backend `HealthSummary` nests health metrics inside `health: EndpointHealth`. Split into `EndpointHealthMetrics` (inner) and `EndpointHealth` (outer wrapper with `endpoint_id`, `endpoint_type`, `health`). Updated `EndpointHealthDashboard.tsx` to access `health.health.success_rate_pct`. **FIXED (CRITICAL DRY-573):** `useMockEndpoints` returned `MockEndpoint[]` but backend returns `MockEndpointPage { items, total }` (paginated). Would produce runtime error when iterating. Changed to `MockEndpointPage`, updated `MockEndpointManager.tsx` to use `data?.items ?? []`. Same for `useMockCaptures` -- changed to `CapturePage`, updated `MockCapturesList.tsx`. Added `MockEndpointPage` and `CapturePage` types to frontend. **FIXED (CRITICAL DRY-574):** Frontend `DeliveryLogFilters` used `page`/`per_page` but backend `DeliveryFilterParams` expects `limit`/`offset`. Frontend would silently send parameters backend ignores. Changed to `limit`/`offset`, added `is_replay` filter support, updated `DeliveryLogViewer.tsx` pagination logic. **FIXED (CRITICAL DRY-575):** Frontend `WebhookDeliveryLog.duration_ms` typed as `number | null` but backend `duration_ms: i32` with `DEFAULT 0` is non-null. Frontend `MockEndpoint.created_by` typed as `number | null` but backend `created_by: DbId` is non-null (`NOT NULL` constraint). Fixed both to non-nullable. Removed unnecessary null guards in `TestResultDisplay.tsx`, `DeliveryRow.tsx`, `FailedDeliveryInspector.tsx`. **FIXED (CRITICAL DRY-576):** Mock endpoint URL in `MockRow.tsx` used `/webhook-testing/mocks/${mock.token}/receive` but backend route is `POST /mock/{token}` (mounted at `/mock` not `/webhook-testing/mocks`). Fixed to `${API_BASE_URL}/mock/${mock.token}`. **FIXED (HIGH DRY-577):** `extract_ip(headers)` function duplicated identically (12 lines) in `webhook_testing.rs` and `shared_link.rs`. Created `api/src/request.rs` shared module with `pub fn extract_ip` and `pub fn headers_to_json`. Both consumers now import from shared module. Added `pub mod request` to `lib.rs`. ~24 lines eliminated. **FIXED (HIGH DRY-578):** Copy-to-clipboard pattern (`useState(false) + navigator.clipboard.writeText + setTimeout(setCopied(false), 2000)`) inline in `FailedDeliveryInspector.tsx` and `MockRow.tsx` -- shared `useCopyToClipboard` hook exists at `@/hooks/useCopyToClipboard.ts` but was not used. Refactored both to use shared hook. Removed `useCallback` + `useState` boilerplate. **FIXED (HIGH DRY-579):** Frontend `CreateMockEndpoint` request type missing `capture_enabled?: boolean` field that backend `CreateMockEndpointRequest` accepts. Added field. **WATCH (MEDIUM DRY-580):** `list_filtered` and `count_filtered` in `webhook_testing_repo.rs` share identical 30-line dynamic WHERE clause building + parameter binding logic. Structural duplication within single file. Acceptable for now -- extraction would require generics over query type (`query_as` vs `query_scalar`). Monitor if pattern spreads. **WATCH (MEDIUM DRY-581):** 5x `find_by_id + ok_or(AppError::Core(CoreError::NotFound))` in handler file -- 2x for `WebhookDeliveryLog`, 2x for `Webhook`, 1x for `MockEndpoint`. Below 3x threshold per entity. Monitor if more handlers are added. **NOT DUPLICATED:** `DataResponse<T>` used correctly throughout all handlers. `PaginationParams` imported from `api/src/query.rs` for mock endpoint listing. `clamp_limit`/`clamp_offset` from `core/src/search.rs` used in all repos. `formatDateTime` from `@/lib/format.ts` used in `DeliveryRow.tsx` and `MockCapturesList.tsx`. `formatPercent` from `@/lib/format.ts` used in `EndpointHealthDashboard.tsx`. `Badge`/`BadgeVariant` from primitives used throughout. `Card`/`Modal` from composite used correctly. `renderWithProviders` from test-utils used in all component tests. `Tabs` from composite used in page. Constants properly extracted to `core/src/webhook_testing.rs`. `serde_json::json!` used only for constructing test payloads in `test_webhook` handler (appropriate, not for responses). Core module has comprehensive unit tests (19 tests). TanStack Query hooks follow key factory pattern. Route file cleanly separates admin/public routers. All files under 200 lines. **VERIFIED:** `cargo check` clean (0 PRD-99 warnings), `npx tsc --noEmit` 0 webhook-testing errors. |
| 2026-02-28 | PRD-97 (Job Dependency Chains & Triggered Workflows) | Backend: `core/src/trigger_workflow.rs`, `db/src/models/trigger_workflow.rs`, `db/src/repositories/trigger_workflow_repo.rs`, `api/src/handlers/trigger_workflow.rs`, `api/src/routes/trigger_workflow.rs`. Frontend: `features/trigger-workflows/types.ts`, `features/trigger-workflows/hooks/use-trigger-workflows.ts`, `features/trigger-workflows/TriggerWorkflowPage.tsx`, `features/trigger-workflows/TriggerList.tsx`, `features/trigger-workflows/TriggerRow.tsx`, `features/trigger-workflows/TriggerForm.tsx`, `features/trigger-workflows/ConditionBuilder.tsx`, `features/trigger-workflows/ExecutionSettings.tsx`, `features/trigger-workflows/ChainGraph.tsx`, `features/trigger-workflows/chain-layout.ts`, `features/trigger-workflows/TriggerLogTable.tsx`, `features/trigger-workflows/LogRow.tsx`, `features/trigger-workflows/DryRunPanel.tsx`, `features/trigger-workflows/JsonTextarea.tsx`, `features/trigger-workflows/index.ts`. Tests: 4 files. Migration: 1 SQL. | 7 CRITICAL + 3 HIGH = 10 fixed; 2 MEDIUM watch | **FIXED (CRITICAL DRY-590):** ALL 10 frontend API paths missing `/admin` prefix -- backend mounts at `/admin/triggers` but frontend called `/triggers`. Every hook would 404 at runtime. Added `/admin/triggers` prefix to all 10 API calls. **FIXED (CRITICAL DRY-591):** `useTriggers` typed return as `TriggerWithStats[]` but backend `list_triggers` handler returns `Vec<Trigger>` (no fire_count/last_fired_at). Changed to `Trigger[]`, removed fire_count/last_fired_at columns from TriggerList and TriggerRow. **FIXED (CRITICAL DRY-592):** `useDryRun` typed return as `DryRunResult[]` (array) but backend returns single `DryRunResult`. Changed to `DryRunResult`, updated TriggerRow to wrap in array for DryRunPanel. **FIXED (CRITICAL DRY-593):** Frontend `ChainGraphNode` had `downstream_triggers: number[]` field that backend `ChainGraphNode` does not serialize. Split into `ChainGraphNodeRaw` (matches backend) and `ChainGraphNode` (extends with computed `downstream_triggers`). Hook now maps raw to enriched. **FIXED (CRITICAL DRY-594):** Frontend `ChainGraphNode` missing `actions` and `is_enabled` fields that backend serializes. Added to `ChainGraphNodeRaw`. Test fixtures updated. **FIXED (CRITICAL DRY-595):** Frontend `Trigger.created_by_id` typed as `number` (non-null) but backend `created_by_id: Option<DbId>` is nullable (`ON DELETE SET NULL`). Fixed to `number | null`. **FIXED (CRITICAL DRY-596):** Frontend `Trigger.project_id` typed as `number | null` but backend `project_id: DbId` with `NOT NULL REFERENCES projects(id)` is required. Fixed to `number`. **FIXED (HIGH DRY-597):** `tryParseJson` local function in `TriggerForm.tsx` duplicated `isValidJson` from `@/lib/validation.ts`. Added `parseJsonOrNull<T>()` to shared `validation.ts` (returns parsed value or null). Replaced local function in TriggerForm. Also fixes PRD-119's DRY-559 watch item. **FIXED (HIGH DRY-598):** `TriggerListQuery` in handler duplicated `PaginationParams` fields (limit, offset) -- used `#[serde(flatten)]` to compose `PaginationParams` instead of redefining. **FIXED (HIGH DRY-599):** `evaluate_conditions` and `evaluate_scope` in `core/src/trigger_workflow.rs` were structurally identical (15-line exact clones with different parameter names). Extracted shared `json_kv_match()` helper, both functions now delegate to it. ~20 lines eliminated. **WATCH (MEDIUM DRY-600):** `TriggerLog.actions_taken` backend type is `serde_json::Value` (JSON array default `[]`). Frontend typed as `Record<string, unknown>`. Corrected to `TriggerAction[]` to match semantic intent, but backend serializes raw Value -- watch for mismatch if non-array data is inserted. **WATCH (MEDIUM DRY-601):** `TableHead` pattern (columns array + thead map) duplicated in `TriggerList.tsx`, `TriggerLogTable.tsx`, and `job-scheduling/ScheduleList.tsx`. Below extraction threshold (each has different columns and conditional styling). Monitor if it spreads further. **ALSO FIXED:** Handler `list_trigger_logs` double-clamped pagination (handler + repo both called `clamp_limit`/`clamp_offset`). Removed redundant handler-level clamping. Frontend `CreateTrigger.project_id` made required to match backend. `TriggerForm` now accepts `projectId` prop for new triggers. `Trigger.execution_mode` changed from `ExecutionMode` union to `string` to match backend `String`. `usePauseAll`/`useResumeAll` return types corrected from `void` to `{ affected: number }` matching backend `BulkToggleResponse`. **NOT DUPLICATED:** `DataResponse<T>` used correctly throughout all handlers. `clamp_limit`/`clamp_offset` from `core/src/search.rs` used in repo. `PaginationParams` from `api/src/query.rs` used for log listing. Constants properly extracted to `core/src/trigger_workflow.rs`. `ensure_trigger_exists` helper follows established pattern. `formatDateTime` from `@/lib/format.ts` in LogRow. `Badge`/`BadgeVariant` from primitives. `Modal`/`Card`/`Tabs` from composite. `renderWithProviders` from test-utils. `cn` from `@/lib/cn`. Core module has 35 unit tests (all pass). Route file clean. All files under 200 lines. **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` 0 trigger-workflows errors. |
| 2026-02-28 | PRD-106 (API Usage & Observability Dashboard) | Backend: `core/src/api_observability.rs`, `db/src/models/api_observability.rs`, `db/src/repositories/api_observability_repo.rs`, `api/src/handlers/api_observability.rs`, `api/src/routes/api_observability.rs`. Frontend: `features/api-observability/types.ts`, `features/api-observability/hooks/use-api-observability.ts`, `features/api-observability/ApiObservabilityPage.tsx`, `features/api-observability/RequestVolumeChart.tsx`, `features/api-observability/ResponseTimeChart.tsx`, `features/api-observability/ErrorRateChart.tsx`, `features/api-observability/RateLimitPanel.tsx`, `features/api-observability/TopConsumersTable.tsx`, `features/api-observability/EndpointHeatmap.tsx`, `features/api-observability/AlertConfigPanel.tsx`, `features/api-observability/CreateAlertForm.tsx`, `features/api-observability/index.ts`. Tests: 6 files. Migration: 1 SQL. | 5 CRITICAL + 4 HIGH = 9 fixed; 2 MEDIUM watch | **FIXED (CRITICAL DRY-610):** ALL 4 frontend alert API paths wrong -- used `/admin/api-metrics/alerts` but backend mounts at `/admin/api-alerts`. All CRUD hooks (list, create, update, delete) would 404 at runtime. Corrected all paths. **FIXED (CRITICAL DRY-611):** `EndpointBreakdown` field name mismatch -- frontend used `p50_ms`/`p95_ms`/`p99_ms` but backend serializes `p50`/`p95`/`p99`. Frontend types corrected to match backend. **FIXED (CRITICAL DRY-612):** `TopConsumer.api_key_id` typed as `number` (non-null) on frontend but backend has `Option<DbId>`. Fixed to `number | null`, added null handling in `TopConsumersTable.tsx` display and key prop. **FIXED (CRITICAL DRY-613):** `HeatmapData` frontend type was `{ cells, max_count, time_granularity }` wrapper but backend returns `Vec<HeatmapCell>` directly as `data`. Changed `HeatmapData` to type alias for `HeatmapCell[]`, updated hook, component, and all test fixtures. **FIXED (CRITICAL DRY-614):** `formatPercent()` double-scaling -- backend `error_rate` returned as 0-100 percentage (handler multiplies by 100, SQL `* 100.0`), but `formatPercent()` from `@/lib/format.ts` multiplies by 100 again. Removed `formatPercent` import from `ApiObservabilityPage.tsx` and `TopConsumersTable.tsx`, replaced with inline `toFixed(1)%`. Same pattern as DRY-544 in PRD-93. **FIXED (HIGH DRY-615):** `formatTime(iso)` identically defined 3x in `RequestVolumeChart.tsx`, `ResponseTimeChart.tsx`, `ErrorRateChart.tsx` + functionally identical `formatBucketTime(iso)` in `EndpointHeatmap.tsx`. Extracted `formatChartTime()` to `types.ts`, all 4 files now import from shared source. ~28 lines eliminated. **FIXED (HIGH DRY-616):** Local `parse_ts()` function in handler (line 90) duplicated `parse_timestamp()` from `api/src/query.rs` (DRY-367 pattern). Removed local, imported shared function. All 8 call sites updated. **FIXED (HIGH DRY-617):** `serde_json::json!([...])` in `list_sample_payloads` handler instead of typed `DataResponse<T>`. Replaced with typed `SamplePayloadDescriptor` struct + `SAMPLE_PAYLOADS` static array. DRY-083 reduced. **FIXED (HIGH DRY-618):** `AppError::BadRequest("Alert config with id {id} not found")` in `update_alert` and `delete_alert` returns HTTP 400 instead of 404. Replaced with `CoreError::NotFound { entity: "alert_config", id }` (proper 404). Extracted `ensure_alert_exists(pool, id)` helper for `delete_alert`. **WATCH (MEDIUM DRY-619):** `useMetricsSummary` and `useEndpointBreakdown` send `?period=24h` query parameter but backend `PeriodParams` expects `?start=`/`?end=`. Backend silently ignores unknown params and uses 24h default. Functional mismatch -- frontend time period selector has no effect. Needs frontend hooks to convert period to start/end timestamps. **WATCH (MEDIUM DRY-620):** Duplicate `MOCK_METRICS` test fixtures across `RequestVolumeChart.test.tsx` (2 metrics) and `ResponseTimeChart.test.tsx` (1 metric) -- identical `ApiMetric` shape. 2 consumers, extract to shared fixture at 3rd. **NOT DUPLICATED:** `DataResponse<T>` used correctly for all other handler endpoints. `clamp_limit`/`clamp_offset` from `core/src/search.rs` used correctly. `PaginationParams` from `api/src/query.rs` imported. `formatBytes` from `@/lib/format.ts` used in `TopConsumersTable.tsx`. `chartStyles.ts` NOT imported (PRD-106 charts use custom Recharts with no Tooltip contentStyle overlap -- heatmap is pure CSS, charts use minimal Recharts config). `renderWithProviders` from `@/lib/test-utils` used in all 6 test files. `BadgeVariant` imported from primitives in `AlertConfigPanel` and `CreateAlertForm`. Constants properly extracted to `core/src/api_observability.rs`. Core module has unit tests. All files under 200 lines. **VERIFIED:** `cargo check` clean (0 PRD-106 warnings), `npx tsc --noEmit` 0 errors. |
| 2026-02-28 | PRD-81 (Backup & Disaster Recovery) | Backend: `core/src/backup_recovery.rs`, `db/src/models/backup_recovery.rs`, `db/src/repositories/backup_recovery_repo.rs`, `api/src/handlers/backup_recovery.rs`, `api/src/routes/backup_recovery.rs`. Frontend: `features/backup-recovery/types.ts`, `features/backup-recovery/hooks/use-backup-recovery.ts`, `features/backup-recovery/BackupDashboard.tsx`, `features/backup-recovery/BackupList.tsx`, `features/backup-recovery/BackupRow.tsx`, `features/backup-recovery/TriggerBackupDialog.tsx`, `features/backup-recovery/VerificationPanel.tsx`, `features/backup-recovery/RecoveryRunbookDownload.tsx`, `features/backup-recovery/ScheduleForm.tsx`, `features/backup-recovery/ScheduleRow.tsx`, `features/backup-recovery/ScheduleManager.tsx`, `features/backup-recovery/index.ts`. Tests: 4 files. Migration: 1 SQL file. | 3 CRITICAL + 2 HIGH = 5 fixed; 1 HIGH watch (backend) | **FIXED (CRITICAL DRY-621):** `useTriggerBackup` hook called `POST /admin/backups/trigger` but backend route mounts trigger at `POST /admin/backups` (root). All trigger requests would 404 at runtime. Fixed hook URL, updated comment, and fixed test assertion in `TriggerBackupDialog.test.tsx`. **FIXED (CRITICAL DRY-622):** `RecoveryRunbookDownload` called `api.raw("/admin/backups/runbook")` but backend route is `GET /admin/backups/recovery-runbook`. Download would 404. Fixed path. **FIXED (CRITICAL DRY-623):** Frontend `CreateBackupSchedule.retention_days` typed as `number` (required) but backend DTO has `Option<i32>` with default 30. Submissions with missing `retention_days` would fail if frontend omitted it. Fixed to `number?` (optional). **FIXED (HIGH DRY-624):** `BACKUP_TYPE_OPTIONS` identically defined in both `TriggerBackupDialog.tsx` and `ScheduleForm.tsx` -- same 3-line derivation from `BACKUP_TYPE_LABEL`. Extracted to `types.ts`, both consumers now import shared constant. ~6 lines eliminated. **FIXED (HIGH DRY-625):** Comment in `types.ts` referenced wrong URL `POST /admin/backups/trigger` for `CreateBackup`. Corrected to `POST /admin/backups`. **WATCH (HIGH DRY-626):** `validate_cron_expression` + `is_valid_cron_field` in `core/src/backup_recovery.rs` duplicates nearly identical logic from `core/src/job_scheduling.rs` and `core/src/production_report.rs`. Three implementations of cron field validation across the codebase. `backup_recovery.rs` also accepts 6-field (extended) cron while `job_scheduling.rs` only accepts 5-field -- signatures differ enough to warrant refactoring with care. Candidate for extraction to shared `core/src/cron.rs` module. **NOT DUPLICATED:** `DataResponse<T>` used correctly for all handler endpoints. `clamp_limit`/`clamp_offset` from `core/src/search.rs` used correctly. `PaginationParams` from `api/src/query.rs` imported (not redefined). `formatBytes`/`formatDateTime` from `@/lib/format.ts` used. `Badge`/`BadgeVariant` from primitives used. `Modal` from composites used. `renderWithProviders` from `@/lib/test-utils` used in all 4 test files. `cn` from `@/lib/cn` used. `ensure_backup_exists`/`ensure_schedule_exists` follow established pattern. `BackupType::parse`/`TriggeredBy::parse` follow enum parse pattern. TanStack Query hooks follow key factory pattern. Constants in `types.ts` follow naming convention (`*_LABEL`, `*_BADGE_VARIANT`). `TableHead` duplicated structurally between `BackupList` and `ScheduleManager` but with different columns -- acceptable (2 consumers). All files under 200 lines. No inline SVGs. No raw fetch except runbook download (correct usage of `api.raw`). **VERIFIED:** `npx tsc --noEmit` clean (0 errors). |
| 2026-02-28 | PRD-93 (Generation Budget & Quota Management) | Backend: `core/src/budget_quota.rs`, `db/src/models/budget_quota.rs`, `db/src/repositories/budget_quota_repo.rs`, `api/src/handlers/budget_quota.rs`, `api/src/routes/budget_quota.rs`. Frontend: `features/budget-quota/types.ts`, `features/budget-quota/hooks/use-budget-quota.ts`, `features/budget-quota/BudgetDashboard.tsx`, `features/budget-quota/BudgetAdminPanel.tsx`, `features/budget-quota/AdminListItems.tsx`, `features/budget-quota/SubmissionBudgetCheck.tsx`, `features/budget-quota/QuotaStatusWidget.tsx`, `features/budget-quota/index.ts`. Tests: 4 files. Migration: 1 SQL. | 11 CRITICAL + 3 HIGH = 14 fixed; 1 MEDIUM watch | **FIXED (CRITICAL DRY-539):** Frontend exemption routes used `/admin/budgets/exemptions` but backend mounts at `/admin/budget-exemptions` -- all 4 exemption hooks (list, create, update, delete) would 404. **FIXED (CRITICAL DRY-540):** Frontend user budget route used `/budgets/my/${projectId}` but backend has `/budgets/my-project/{project_id}`. **FIXED (CRITICAL DRY-541):** Frontend quota route used `/quotas/my` but backend has `/budgets/my-quota`. **FIXED (CRITICAL DRY-542):** Frontend budget check used path param + `?hours=N` but backend expects `?project_id=N&estimated_hours=N` on flat `/budgets/check`. **FIXED (CRITICAL DRY-543):** Frontend called non-existent `/budgets/history/${type}/${id}?period=P` -- backend has separate admin endpoints at `/{project_id}/history` and `/{user_id}/history`. Replaced with `useBudgetHistory` and `useQuotaHistory` hooks targeting correct routes. **FIXED (CRITICAL DRY-530):** `BudgetExemption` type had `job_type_filter`/`resolution_tier_filter`/`enabled` but backend serializes `job_type`/`resolution_tier`/`is_enabled`. All response and mutation types corrected. **FIXED (CRITICAL DRY-531):** `TrendProjection` had `daily_avg_consumption`/`consumption_trend`/`projected_end_date` but backend sends `daily_avg`/`trend_direction` (no `projected_end_date`). Corrected. **FIXED (CRITICAL DRY-532/533):** `BudgetStatus.consumed/remaining` and `QuotaStatus.consumed/remaining` don't exist -- backend sends `consumed_gpu_hours`/`remaining_gpu_hours`. Corrected. **FIXED (CRITICAL DRY-534):** `DailyConsumption` had `date`/`gpu_hours`/`exempt_gpu_hours` but backend sends `day`/`total_gpu_hours` (no exempt breakdown). **FIXED (CRITICAL DRY-536):** Frontend `ProjectBudget` had phantom `consumed_gpu_hours` field not on backend struct. Removed. **FIXED (HIGH DRY-544):** consumed_pct range mismatch -- backend sends 0-100 (e.g. 75.0), frontend compared against 0-1 thresholds (0.75, 0.9) and used `formatPercent()` which multiplies by 100 again. Aligned all frontend to 0-100 range: extracted `WARNING_THRESHOLD_PCT=75`, `CRITICAL_THRESHOLD_PCT=90`, `budgetBarColor()`, `budgetBadgeVariant()` to shared `types.ts`. All components use shared helpers. Test data updated. **FIXED (HIGH DRY-545):** `WARNING_THRESHOLD`/`CRITICAL_THRESHOLD` constants duplicated in `BudgetDashboard.tsx` and `QuotaStatusWidget.tsx` -- unified in `types.ts`. **FIXED (HIGH DRY-547/548):** Consumed_pct computation duplicated 4x in handler, budget status assembly duplicated 2x -- extracted `consumed_pct()`, `build_budget_status()`, `build_quota_status()`, `ensure_budget_exists()`, `ensure_quota_exists()`, `ensure_exemption_exists()` helpers. ~60 lines of duplicate backend code eliminated. **FIXED (HIGH DRY-549):** `user_project_budget` and `user_my_quota` returned `DataResponse<serde_json::Value>` with `serde_json::to_value().map_err()` pattern (3x) and `serde_json::json!({ "status": "no_quota" })` -- replaced with typed `DataResponse<BudgetStatus>`/`DataResponse<QuotaStatus>` and 204 NO_CONTENT for missing budget/quota. Eliminated all `serde_json` usage from handler. **WATCH (MEDIUM):** `CreateProjectBudget` frontend included `project_id` field not present in backend struct (project_id comes from URL path). Corrected but no shared validation -- watch for similar input DTO mismatches. **NOT DUPLICATED:** `PaginationParams` imported from `api/src/query.rs` (correct). `clamp_limit`/`clamp_offset` from `core/src/search.rs` (correct). `DataResponse<T>` used throughout (correct). `formatPercent`/`formatCountdown` from `@/lib/format.ts` (correct). `Badge`/`BadgeVariant` from primitives (correct). `Card`/`CardBody`/`CardHeader` from composite (correct). `renderWithProviders` from `@/lib/test-utils` (correct). `cn` from `@/lib/cn` (correct). TanStack Query hooks follow key factory pattern. Constants properly in `core/src/budget_quota.rs`. Core module has comprehensive unit tests (33 tests, all pass). Repos use `COLUMNS` constant pattern. Migration follows conventions (triggers, indexes, CHECK constraints). All files under 300 lines. **VERIFIED:** `cargo check` clean, `cargo test -p x121-core -- budget_quota` 33/33 passed, `npx tsc --noEmit` 0 budget-quota errors. |

| 2026-02-28 | PRD-55 (Director's View - Mobile/Tablet Review) | Backend: `core/src/directors_view.rs`, `db/src/models/directors_view.rs`, `db/src/repositories/directors_view_repo.rs`, `api/src/handlers/directors_view.rs`, `api/src/routes/directors_view.rs`. Frontend: `features/directors-view/types.ts`, `features/directors-view/hooks/use-directors-view.ts`, `features/directors-view/hooks/use-swipe-gesture.ts`, `features/directors-view/hooks/use-breakpoint.ts`, `features/directors-view/OfflineIndicator.tsx`, `features/directors-view/SwipeOverlay.tsx`, `features/directors-view/SegmentCard.tsx`, `features/directors-view/MobilePlayer.tsx`, `features/directors-view/MobileVoiceNote.tsx`, `features/directors-view/ActivityFeed.tsx`, `features/directors-view/DirectorsViewNav.tsx`, `features/directors-view/DirectorsViewLayout.tsx`, `features/directors-view/ReviewQueue.tsx`, `features/directors-view/index.ts`. Tests: 5 files. Migration: 1 SQL. | 5 CRITICAL + 3 HIGH = 8 fixed; 0 watch | **FIXED (CRITICAL DRY-630):** `ActivityFeedItem` frontend interface had completely wrong fields: `event_type`, `description`, `timestamp`, `segment_id|null`. Backend `ActivityFeedEntry` serializes `action_type`, `target_id`, `synced`, `client_timestamp`, `created_at`. Every field except `id` was wrong. Fixed interface and updated `ActivityFeed.tsx` to use correct field names. **FIXED (CRITICAL DRY-631):** `PushSubscription` frontend interface missing 4 backend fields: `p256dh_key`, `auth_key`, `user_agent` (nullable), `updated_at`. Added all fields. **FIXED (CRITICAL DRY-632):** `useRegisterPushSubscription` sent `{ endpoint, keys: Record<string,string> }` but backend `CreatePushSubscription` expects `{ endpoint, p256dh_key, auth_key, user_agent? }`. Created `CreatePushSubscriptionInput` typed DTO matching backend. **FIXED (CRITICAL DRY-633):** `ReviewQueueItem.submitted_by` typed as `string | null` but backend uses `COALESCE(u.name, 'unknown')` making it always non-null `String`. Fixed to `string`. Updated test fixtures. **FIXED (CRITICAL DRY-634):** `api.delete()` only accepts path (no body), but backend `delete_push_subscription` handler expects `Json<DeletePushSubscriptionRequest>` body `{ endpoint: string }`. Added `useDeletePushSubscription` hook using `api.raw()` with DELETE method and JSON body. **FIXED (HIGH DRY-635):** `formatTime(seconds)` local function in `MobilePlayer.tsx` was behavioral duplicate of `formatDuration(seconds)` from `@/features/video-player/frame-utils.ts` (same M:SS/H:MM:SS output). Removed local, imported shared utility. **FIXED (HIGH DRY-636):** `submit_review_action` handler used `serde_json::json!({ segment_id, action, status: "recorded" })` instead of typed struct. Created `ReviewActionResponse` typed DTO. DRY-083 pattern. **FIXED (HIGH DRY-637):** `ActivityFeedParams { limit: Option<i64>, offset: Option<i64> }` was exact duplicate of `PaginationParams` from `api/src/query.rs`. Removed local struct, imported shared `PaginationParams`. **NOT DUPLICATED:** API paths all correct (`/user/review-queue`, `/user/push-subscription`, `/user/sync`, `/user/activity-feed`) -- routes mounted at `/user` in `routes/mod.rs`. `DataResponse<T>` used correctly (except for the one serde_json::json fixed above). `clamp_limit`/`clamp_offset` from `core/src/search.rs` used correctly. `ReviewQueueParams` is unique (status+project_id+sort_by+limit+offset). `SyncResponse` is unique typed struct. Frontend `ReviewAction` matches backend `ReviewActionRequest` field names (`action`, `notes?`). `OfflineSyncAction` matches backend `OfflineSyncItem` (`target_id`, `action_type`, `client_timestamp`). `SyncConflict` matches backend `SyncConflict` exactly. `useBreakpoint` hook is unique (matchMedia-based). `useSwipeGesture` hook is unique (touch gesture detection). `OfflineIndicator` uses unique `navigator.onLine` pattern. All frontend components under 166 lines. All test files use `renderWithProviders` from `@/lib/test-utils`. `formatDateTime` from `@/lib/format.ts` used in `SegmentCard.tsx` and `ActivityFeed.tsx`. `Badge`/`BadgeVariant` from primitives used correctly. `Spinner` from primitives used in `ActivityFeed.tsx`. `cn` from `@/lib/cn` used throughout. `MIN_TOUCH_TARGET` constant properly centralized in `types.ts`. Barrel export clean. **VERIFIED:** `cargo check` clean (0 PRD-55 warnings), `npx tsc --noEmit` clean (0 errors). |

| 2026-02-28 | PRD-105 (Platform Setup Wizard) | Backend: `core/src/setup_wizard.rs`, `db/src/models/setup_wizard.rs`, `db/src/repositories/setup_wizard_repo.rs`, `api/src/handlers/setup_wizard.rs`, `api/src/routes/setup_wizard.rs`. Frontend: `features/setup-wizard/types.ts`, `features/setup-wizard/hooks/use-setup-wizard.ts`, `features/setup-wizard/SetupWizardPage.tsx`, `features/setup-wizard/StepProgress.tsx`, `features/setup-wizard/StepFeedback.tsx`, `features/setup-wizard/DatabaseStep.tsx`, `features/setup-wizard/StorageStep.tsx`, `features/setup-wizard/ComfyUiStep.tsx`, `features/setup-wizard/AdminAccountStep.tsx`, `features/setup-wizard/WorkerStep.tsx`, `features/setup-wizard/IntegrationsStep.tsx`, `features/setup-wizard/HealthCheckStep.tsx`, `features/setup-wizard/WizardCompletePanel.tsx`, `features/setup-wizard/index.ts`. Tests: 4 files. Migration: 1 SQL. | 6 CRITICAL + 2 HIGH = 8 fixed; 0 watch | **FIXED (CRITICAL-1):** `useExecuteStep` hook typed return as `StepValidationResult` but backend `execute_step` handler returns `DataResponse<StepStatus>`. Would produce runtime field access errors (`data.success` and `data.message` undefined on `StepStatus`). Changed to `StepStatus`. Also fixed request body: frontend sent raw config object but backend `ExecuteStepBody` expects `{ config: {...} }` wrapper. **FIXED (CRITICAL-2):** `useStepConfig` hook typed return as `PlatformSetup` (raw DB row with `id`, `config_json`, `configured_by`, `created_at`, `updated_at`) but backend `get_step_config` handler returns `DataResponse<StepStatus>` (projected DTO with `name`, `completed`, `validated_at`, `error_message`, `has_config`). Changed to `StepStatus`. **FIXED (CRITICAL-3):** Frontend `IntegrationsStepConfig.email` included `from: string` field that does not exist on backend `SmtpConfig` struct (only `host` and `port`). Backend deserialization would either fail or silently ignore the extra field. Removed `from` field from TS type, removed `emailFrom` state variable and `From Address` input from IntegrationsStep.tsx, removed `from` from test-email config payload. **FIXED (CRITICAL-4):** `WorkerStep` called `testConnection.mutate({ service_type: "worker", ... })` but backend `test_connection` handler only accepts `"database"`, `"comfyui"`, or `"smtp"` -- `"worker"` returns `Unknown service type` error. Removed broken test-connection from WorkerStep, simplified to direct execute-step submission with frontend-only validation. **FIXED (CRITICAL-5):** `useSkipWizard` typed return as `void` but backend `skip_wizard` handler returns `DataResponse<WizardState>`. Changed to `WizardState`. **FIXED (CRITICAL-6):** `useResetStep` typed return as `void` but backend `reset_step` handler returns `DataResponse<StepStatus>`. Changed to `StepStatus`. **FIXED (HIGH-1):** Inline feedback banner pattern (success/error div with identical CSS classes) duplicated 5x across `StorageStep.tsx`, `ComfyUiStep.tsx` (1x), `WorkerStep.tsx` (2x), `AdminAccountStep.tsx` (1x). `StepFeedback` component already existed but was not used. Refactored all to use `StepFeedback`. Created `stepStatusToFeedback()` adapter in `types.ts` to convert `StepStatus` to `StepValidationResult` shape for display. ~35 lines of duplicated feedback JSX eliminated. **FIXED (HIGH-2):** Frontend `AdminAccountStep` used `MIN_LENGTH = 8` for password strength assessment but backend `validate_password_strength` uses `MIN_PASSWORD_LENGTH = 12`. Passwords between 8-11 chars would show as "fair" on frontend but be rejected by backend. Changed frontend `MIN_LENGTH` to 12 to match backend. **ALSO FIXED:** IntegrationsStep email test-connection called `handleTest("email", ...)` but backend only handles `"smtp"` service type. Changed to `handleTest("smtp", ...)`. HealthCheckStep removed unused imports (`Badge`, `Card`, `CardBody`, `AlertTriangle`, `XCircle`, `ServiceCheck` interface, `STATUS_ICON`, `STATUS_BADGE_VARIANT`) and simplified to display `StepStatus.completed`/`StepStatus.error_message` instead of non-existent `StepValidationResult.details.checks` array. WorkerStep removed unused `Badge` import. **NOT DUPLICATED:** API paths all correct (`/admin/setup/status`, `/admin/setup/step/:step_name`, `/admin/setup/test-connection`, `/admin/setup/skip`, `/admin/setup/step/:step_name/reset`). `SetupStepName` union matches backend `SetupStepName::as_str()` values exactly. `WizardState` fields match backend struct (`steps`, `completed`, `current_step_index`). `StepValidationResult` fields match backend struct (`success`, `message`, `details`). `TestConnectionRequest` fields match backend `TestConnectionBody` (`service_type`, `config`). All step config types (`DatabaseStepConfig`, `StorageStepConfig`, `ComfyUiStepConfig`, `AdminAccountStepConfig`, `WorkerStepConfig`) match backend struct fields exactly. `REQUIRED_STEPS` and `STEP_ORDER` arrays match backend `SetupStepName::all()` ordering. `DataResponse` envelope correctly unwrapped by `api.ts` (returns `body.data`). `renderWithProviders` from `@/lib/test-utils` used in all 4 test files. Design token CSS variables used throughout. Frontend components all under 200 lines. Backend handler uses `DataResponse<T>` throughout (no `serde_json::json!`). Backend uses `ensure_step_exists()` helper. Backend core module has comprehensive unit tests (32 tests). **VERIFIED:** `cargo check` clean, `npx tsc --noEmit` clean, `npx vitest run setup-wizard` 27/27 tests passed, `npx biome check` clean (1 pre-existing key={index} warning in ComfyUiStep, not introduced by this audit). |

| 2026-02-28 | PRD-114 (Cloud GPU Provider Integration) | Backend: 6 migrations, `db/src/models/cloud_provider.rs`, 5 repos (`cloud_provider_repo.rs`, `cloud_gpu_type_repo.rs`, `cloud_instance_repo.rs`, `cloud_scaling_rule_repo.rs`, `cloud_cost_event_repo.rs`), `core/src/cloud.rs`, `core/src/crypto.rs`, `core/src/cloud_scaling.rs`, `cloud/src/` (8 files: registry, runpod GraphQL+serverless+types, 3 services, storage), `api/src/handlers/cloud_providers.rs`, `api/src/routes/cloud_providers.rs`, `api/src/error.rs`, `api/src/state.rs`, `api/src/main.rs`. Frontend: `cloud-gpus/CloudGpuDashboard.tsx`, 4 components, `hooks/use-cloud-providers.ts`, `lib/format.ts`. | 16 findings: 2 CRITICAL fixed, 6 HIGH fixed, 5 MEDIUM fixed (3 watch), 3 LOW fixed | **FIXED (CRITICAL DRY-539):** `terminate_and_record()` loop duplicated 3x (emergency_stop_provider, emergency_stop_all, scaling service ScaleDown). Extracted to `cloud/src/services/mod.rs`. **FIXED (CRITICAL DRY-540):** 13 `.map_err(CloudProviderError)` + 2 `.map_err(CryptoError)` boilerplate calls. Added `From<CloudProviderError>` and `From<CryptoError>` impls in `api/src/error.rs`. All 15 calls eliminated with `?`. **FIXED (HIGH DRY-541):** Background service spawn boilerplate duplicated 3x. Extracted `spawn_periodic_service()` in `cloud/src/services/mod.rs`. **FIXED (HIGH DRY-542):** `InstanceStatus` -> DB status_id manual 8-arm match. Added `InstanceStatus::to_db_status_id()` in `core/src/cloud.rs`. **FIXED (HIGH DRY-543):** `formatCents()` duplicated 5x in frontend. Exported from `@/lib/format.ts`, imported in all components. **FIXED (HIGH DRY-544):** `serde_json::json!` response in serverless handler. Created typed `ServerlessJobResponse` struct. **FIXED (HIGH DRY-545):** Page header duplicated 2x in dashboard. Extracted `PageHeader` component. **FIXED (HIGH DRY-546):** Presigned URL generation duplicated 2x. Extracted shared `generate_presigned_url()` private fn. **FIXED (MEDIUM DRY-547):** Serverless REST boilerplate 3x. Extracted `serverless_request()` helper. **FIXED (MEDIUM DRY-549):** Magic number status IDs in frontend. Created named `INSTANCE_STATUS`/`PROVIDER_STATUS` constants with sync comments. **FIXED (MEDIUM DRY-551):** Redundant Content-Type headers removed from graphql.rs and serverless.rs. **FIXED (MEDIUM DRY-552):** `ensure_provider`/`ensure_instance` renamed to `ensure_provider_exists`/`ensure_instance_exists`. **FIXED (LOW DRY-553):** `toLocaleString()` replaced with `formatDateTime()`. **FIXED (LOW DRY-554):** Duplicate `"Encryption failed"` strings eliminated by `From<CryptoError>`. **WATCH (DRY-548):** Handler file at ~620 lines (above 500 threshold but cohesive). **WATCH (DRY-550):** Provider iterator pattern 3x across services. **VERIFIED:** `cargo check` clean (0 new warnings), 2053 core tests passed, 15 status tests passed. |
| 2026-03-01 | Deferred Work Queue (PRD-29 Phase 7 + Primer reskin) | Frontend: `app/router.tsx`, `app/navigation.ts`, `tokens/colors.css`, `tokens/spacing.css`, `tokens/typography.css`, `features/admin/token-editor/TokenSections.tsx`, `features/admin/TokenEditor.tsx`, `features/admin/components/MetricsChart.tsx` | 3 HIGH fixed, 2 MEDIUM fixed; 2 MEDIUM watch | **FIXED (HIGH):** 9 `??` fallback colors in `TokenSections.tsx` section components were stale pre-Primer values (Indigo `#6366f1`, Tailwind red `#ef4444`, etc.) -- updated all to Primer palette values matching `EMPTY_TOKENS`. **FIXED (HIGH):** 6 `getCssVar()` fallback colors in `MetricsChart.tsx` were pre-Primer values (`#e94560`, `#4a6cf7`, `#2ecc71`, etc.) -- updated all to Primer dark-obsidian palette. **FIXED (HIGH):** `EMPTY_TOKENS.font.family` was truncated (missing emoji fallback fonts) and `sizeBase` was `"14px"` instead of actual `"13px"` body font-size -- corrected both, added sync comments. **FIXED (MEDIUM):** `FontSection` fallback `"16px"` for base size mismatched actual `13px` -- corrected. **FIXED (MEDIUM):** No sync comments between `@theme` block and `EMPTY_TOKENS` -- added comments in both `colors.css` and `TokenSections.tsx`. **WATCH (MEDIUM):** Hardcoded chart colors (`#6366f1`, `#ef4444`, `#f59e0b`, `#10b981`, `#8b5cf6`) duplicated across 7 chart files without shared palette constant. Candidate for `CHART_PALETTE` extraction in `chartStyles.ts`. **WATCH (MEDIUM):** `@theme` block in `colors.css` is verbatim copy of `dark-obsidian` block (structurally required by Tailwind v4, documented with sync comment). **VERIFIED:** `npx tsc --noEmit` clean, `npx vitest run TokenEditor` 5/5 passed. |
| 2026-03-01 | Page wiring + backend fixes (12 page wrappers, router, navigation, palette, delivery, workflow_canvas, test_shot, scaling, prompt_management) | Frontend: 12 page wrappers in `app/pages/`, `app/router.tsx`, `app/navigation.ts`, `tokens/icons.ts`. Backend: `handlers/palette.rs`, `handlers/delivery.rs`, `handlers/workflow_canvas.rs`, `handlers/test_shot.rs`, `cloud/src/services/scaling.rs`, `handlers/prompt_management.rs`. | 1 HIGH fixed, 3 MEDIUM fixed, 2 MEDIUM watch, 1 LOW watch | **FIXED (HIGH DRY-638):** `ListExportsParams { limit: Option<i64>, offset: Option<i64> }` in `delivery.rs` was exact duplicate of `PaginationParams` from `api/src/query.rs`. Removed local struct, imported shared `PaginationParams`. Removed now-unused `serde::Deserialize` import. **FIXED (MEDIUM DRY-641):** `PipelineHooksPage.tsx` `HookIdInput` sub-component used raw HTML `<input>` with hand-rolled CSS (`w-full rounded border border-[var(--color-border-default)] bg-[var(--color-surface-primary)] px-2 py-1.5 text-sm text-[var(--color-text-primary)]`) instead of design system `Input` component. Replaced with `<Input>`. **FIXED (MEDIUM DRY-642):** `UndoPage.tsx` used raw HTML `<select>` with hand-rolled CSS instead of design system `Select` component from `@/components/primitives`. Replaced with `<Select>` with `options` array. **FIXED (MEDIUM DRY-643):** 8 page wrappers (BatchMetadata, Budget, ContactSheet, Debugger, Importer, PipelineHooks, Temporal, Undo) manually rendered inline `<label className="block text-sm font-medium text-[var(--color-text-secondary)] mb-1">` above `<Input>` components. The `Input` component already has a `label` prop. Removed 10 redundant `<label>` elements, replaced with `<Input label="...">`. Also fixed `parseInt` -> `Number.parseInt` in all 8 files (biome lint). **WATCH (MEDIUM DRY-639):** `handleLoad` function (parse input string -> positive integer -> set state) is copy-pasted in 8 page wrappers (3 lines each). Candidate for `useEntityIdInput(label)` custom hook or `parsePositiveInt(value)` utility if more pages adopt this pattern. Currently 8 consumers. **WATCH (LOW DRY-645):** Page header pattern (`<h1 className="text-xl font-semibold text-[var(--color-text-primary)]">Title</h1><p className="mt-1 text-sm text-[var(--color-text-muted)]">Desc</p>`) appears in 43+ files across pages and features. Candidate for shared `PageHeader` component (exists locally in CloudGpuDashboard but not shared). Deferring -- 43-consumer extraction is a larger refactor. **NOT DUPLICATED:** `app/router.tsx` route definitions follow established lazy-import pattern consistently. `app/navigation.ts` entries follow established nav-item pattern. `palette.rs` search handler is clean (uses `DataResponse`, no `json!`). `test_shot.rs` uses `ensure_test_shot_exists` helper, `clamp_limit`/`clamp_offset`, `DataResponse<T>` throughout -- clean. `scaling.rs` properly uses `spawn_periodic_service` and `terminate_and_record` shared helpers. `prompt_management.rs` uses `ensure_slot_exists` helper, typed returns throughout. `workflow_canvas.rs` telemetry endpoint uses typed `WorkflowTelemetry`/`NodeTiming` structs (good). `import_comfyui` still uses 5x `serde_json::json!` for building ComfyUI nodes/edges -- this is the known DRY-083 pattern (json! in handlers), but the function is complex parser logic where typed structs would require significant additional types for an import-only flow. Acceptable trade-off. ExternalReviewPage.tsx is a thin wrapper (16 lines) -- no duplication. DuplicatesPage.tsx and FailureAnalyticsPage.tsx have unique feature-specific logic -- no extractable duplication beyond the page header pattern. WorkflowImportPage.tsx is minimal (57 lines) -- clean. **VERIFIED:** `cargo check --package x121-api` clean, `npx tsc --noEmit` clean (1 pre-existing CloudGpuDashboard error unrelated), `npx biome check` clean on all 8 modified files. |
| 2026-03-01 | PRD-123 (Scene Catalog & Scene Types Unification) | Backend: `db/src/models/scene_type.rs`, `db/src/models/project_scene_setting.rs`, `db/src/models/character_scene_override.rs`, `db/src/repositories/scene_type_repo.rs`, `db/src/repositories/project_scene_setting_repo.rs`, `db/src/repositories/character_scene_override_repo.rs`, `api/src/handlers/scene_type.rs`, `api/src/routes/scene_type.rs`. Frontend: `features/scene-types/types.ts`, `features/scene-types/SceneTypeEditor.tsx`, `features/scene-types/__tests__/InheritanceTree.test.tsx`, `features/scene-types/__tests__/SceneMatrixView.test.tsx`, `features/scene-catalog/types.ts`, `features/scene-catalog/SourceBadge.tsx`, `features/scene-catalog/hooks/use-scene-catalog.ts`, `features/scene-catalog/hooks/use-project-scene-settings.ts`, `features/scene-catalog/hooks/use-character-scene-settings.ts`, `features/scene-catalog/ProjectSceneSettings.tsx`, `features/scene-catalog/CharacterSceneOverrides.tsx`. 5 new migrations: `000031-000035`. | 1 CRITICAL fixed, 2 HIGH fixed, 4 flagged/watch | **FIXED (CRITICAL DRY-650):** Frontend `EffectiveSceneSetting.source` type in `scene-catalog/types.ts` had `"catalog"` as a source value, but backend SQL returns `"scene_type"` (never `"catalog"`). `SourceBadge.tsx` mapped `catalog` -> "Default" label, so the badge would break at runtime. Fixed type union to `"scene_type" | "project" | "character"`, updated `SourceBadge.tsx` `SOURCE_CONFIG` mapping. **FIXED (HIGH DRY-651):** `SceneType` interface in `scene-types/types.ts` was missing 10 fields added by PRD-123 unification (`slug`, `has_clothes_off_transition`, `generation_strategy`, `expected_chunks`, `chunk_output_pattern`, `auto_retry_enabled`, `auto_retry_max_attempts`, `auto_retry_trigger_checks`, `auto_retry_seed_variation`, `auto_retry_cfg_jitter`). Frontend consumers of `SceneType` (SceneMatrixView, InheritanceTree, SceneTypeEditor) would receive data with these fields but TS would not expose them. Updated all 3 TS interfaces (`SceneType`, `CreateSceneType`, `UpdateSceneType`) to match backend model. **FIXED (HIGH DRY-652):** `SceneTypeEditor` did not include a `slug` field, making `CreateSceneType` submission fail at the backend (`slug` is `NOT NULL`). Added slug `Input` field with auto-generation from name using shared `generateSnakeSlug` from `@/lib/format.ts` (reusing existing utility). Updated test fixtures to include all new required fields. **FLAGGED (DRY-646):** `SceneCatalogEntry` in `scene-catalog/types.ts` and `SceneType` in `scene-types/types.ts` are now parallel types for the same DB entity. Should be unified. Deferred: requires coordinated import refactor. **FLAGGED (DRY-647):** `use-scene-catalog.ts` and `use-scene-types.ts` hooks both hit `/scene-types` but use separate query key factories (`["scene-catalog"]` vs `["scene-types"]`), causing disconnected cache invalidation. Should be merged. Deferred: requires coordinated feature module refactor. **WATCH (DRY-648):** `SettingRow` component structurally duplicated in `ProjectSceneSettings.tsx` and `CharacterSceneOverrides.tsx` (4 identical columns, character adds 5th). 2 consumers. **WATCH (DRY-649):** `"not_started"` magic string in `scene_type.rs` handler line 306 -- should use `STATE_NOT_STARTED` from `core/src/readiness.rs`. **UPDATED:** DRY-334 notes corrected (struct moved from deleted `scene_catalog.rs` to `scene_type.rs`). DRY-335 notes corrected (FK renamed to `scene_type_id`). **NOT DUPLICATED:** Backend track management methods (get_tracks, set_tracks, add_track, remove_track) in `SceneTypeRepo` are properly extracted with no remaining duplication from deleted `SceneCatalogRepo`. `ensure_scene_type_exists` is properly shared from `scene_type_inheritance.rs`. `SceneSettingUpdate` and `EffectiveSceneSetting` properly shared via re-exports. Backend SQL in repos is clean and uses `COLUMNS` constant pattern. All backend handlers use `DataResponse<T>` (no `json!`). Migrations properly cascade data from old tables. **VERIFIED:** `npx tsc --noEmit` clean (0 errors), `npx biome check` shows only pre-existing formatting issues (import sort order, JSX wrapping). |
| 2026-03-01 | PRD-121 (SVI Clip Management) + PRD-122 (Storage Configuration) | Backend: `db/migrations/20260301000029_add_qa_columns_to_scene_video_versions.sql`, `db/src/models/scene_video_version.rs`, `db/src/repositories/scene_video_version_repo.rs`, `db/src/repositories/segment_repo.rs`, `api/src/handlers/scene_video_version.rs`, `api/src/routes/scene.rs`, `core/src/storage/mod.rs`, `core/src/storage/local.rs`, `core/src/storage/factory.rs`, `core/src/error.rs`, `core/src/settings.rs`, `cloud/src/storage_provider.rs`, `cloud/src/storage.rs`, `api/src/handlers/storage.rs`, `api/src/routes/storage.rs`, `api/src/state.rs`, `api/src/error.rs`. Frontend: `features/scenes/types.ts`, `features/scenes/hooks/useClipManagement.ts`, `features/scenes/ClipGallery.tsx`, `features/scenes/ClipCard.tsx`, `features/scenes/ClipQAActions.tsx`, `features/scenes/ClipRejectionDialog.tsx`, `features/scenes/ResumeFromDialog.tsx`, `features/scenes/ImportClipDialog.tsx`, `features/storage/hooks/use-storage.ts`, `features/storage/types.ts`, `features/storage/BackendConfigPanel.tsx`, `features/settings/SettingsPanel.tsx`. | 2 CRITICAL fixed, 3 HIGH fixed, 2 MEDIUM fixed, 1 LOW fixed | **FIXED (CRITICAL DRY-627):** `formatFileSize(bytes)` in `features/scenes/types.ts` was exact behavioral duplicate of `formatBytes(bytes)` from `@/lib/format.ts` -- both convert bytes to human-readable strings with KB/MB/GB suffixes. Removed local function, all consumers now import `formatBytes` from shared location. **FIXED (CRITICAL DRY-628):** `formatDuration(seconds)` in `features/scenes/types.ts` was exact behavioral duplicate of `formatDuration(seconds)` from `@/features/video-player/frame-utils.ts` -- both produce M:SS / H:MM:SS timecodes from seconds. Removed local function, `ClipCard.tsx` already imported from the correct shared source. **FIXED (HIGH DRY-629):** 9x inline `find_by_id + ok_or(NotFound)` pattern in `scene_video_version.rs` handler -- every handler function repeated the same 5-line lookup+error. Extracted `ensure_version_exists(pool, id) -> AppResult<SceneVideoVersion>` helper at top of handler file, following established `ensure_X_exists` convention. ~40 lines of duplicated lookup code eliminated. **FIXED (HIGH DRY-630):** Magic strings `"approved"`, `"rejected"`, `"pending"`, `"imported"`, `"generated"`, `"ready_to_generate"` scattered across handler logic without constants. Created `core/src/clip_qa.rs` with `CLIP_QA_PENDING`, `CLIP_QA_APPROVED`, `CLIP_QA_REJECTED`, `CLIP_SOURCE_GENERATED`, `CLIP_SOURCE_IMPORTED`, `RESUME_STATUS_READY`, `VALID_CLIP_QA_STATUSES`, and `validate_clip_qa_status()`. Registered in `core/src/lib.rs`. Handler now uses these constants throughout. 2 unit tests added and passing. **FIXED (HIGH DRY-631):** 3 handler functions (`get_version`, `import_video`, `set_final`) returned bare `Json(entity)` responses instead of `DataResponse<T>` envelope. All 3 wrapped in `DataResponse::new()` to follow established convention. **FIXED (MEDIUM DRY-632):** `BackendConfigPanel.tsx` used raw `<button>` element with hand-rolled CSS for the play/select area on each backend card, despite having `Button` imported from `@/components/primitives`. Replaced with `<Button variant="ghost">` component. **FIXED (MEDIUM DRY-633):** `import_video` handler in `scene_video_version.rs` wrote uploaded files directly to filesystem via `tokio::fs::write()` instead of using the `StorageProvider` trait from PRD-122. Refactored to use `state.storage_provider().await` with `provider.upload(&storage_key, &data)`. This ensures imported clips use the configured storage backend (local or S3) rather than hardcoded filesystem paths. **FIXED (LOW DRY-634):** `ACCEPTED_FORMATS` constant in `ImportClipDialog.tsx` and `SUPPORTED_VIDEO_EXTENSIONS` in `scene_video_version.rs` handler define the same video format list (`.mp4`, `.webm`, `.mov`) without cross-boundary sync comments. Added sync comment to frontend pointing to backend canonical source. Also replaced inline `(file.size / (1024 * 1024)).toFixed(1) MB` with `formatBytes(file.size)` from `@/lib/format.ts` in ImportClipDialog.tsx. **NOT DUPLICATED:** SQL migration follows conventions (FK indexes, NOT NULL constraints, DEFAULT values). Model struct uses `DbId` consistently. Repo methods use `sqlx::query_as!` and `sqlx::query!` macros correctly. Frontend TanStack Query hooks follow key factory pattern (`sceneVideoVersionKeys`). `ClipQAActions`, `ClipRejectionDialog`, `ResumeFromDialog` are all single-purpose components with no structural duplication. `ClipGallery` orchestrates subcomponents cleanly. Storage trait (`StorageProvider`) properly uses `async_trait`. `LocalStorageProvider` implements the trait correctly. `StorageFactory` uses `SettingsService` for configuration. PRD-122 handler uses `DataResponse<T>` throughout, `ensure_backend_exists` helper present, `PaginationParams` from shared query module. PRD-122 frontend hooks follow established patterns. Raw `<textarea>` in `ClipRejectionDialog` and `ImportClipDialog` is a codebase-wide pattern (31 files) -- no Textarea design system component exists. **VERIFIED:** `cargo check` clean, `cargo test --lib -p x121-core clip_qa` 2/2 passed, `npx tsc --noEmit` clean (0 errors), `npx biome check --fix` applied to 8 files (4 remaining lint items are pre-existing `noLabelWithoutControl` and non-null assertion patterns). |

---

## How to Use This File

### When Starting a New PRD Implementation
1. Read the "Active Watch List" section
2. Check if your PRD is mentioned in any existing DRY entry
3. If yes, check whether a shared utility already exists (see "Resolved" section)
4. If a shared utility exists, use it. If not, build one and resolve the DRY entry.

### After Implementing Code
1. Run `dry-guy` agent on all changed files
2. If new patterns are flagged, add them to the "Active Watch List"
3. If existing patterns are resolved, move them to "Resolved" with the shared location

### When Reviewing PRs
1. Check if the PR introduces code that matches any `watch` or `flagged` entries
2. If so, request that the author use the shared utility or extract one
3. Block merge until DRY-GUY audit passes

---

## Version History

- **v1.0** (2026-02-18): Initial creation with pre-identified watch patterns from PRD analysis
